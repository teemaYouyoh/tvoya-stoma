/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
(self["webpackChunkpersona_patman"] = self["webpackChunkpersona_patman"] || []).push([["vendors"],{

/***/ "./node_modules/detect-browser/es/index.js":
/*!*************************************************!*\
  !*** ./node_modules/detect-browser/es/index.js ***!
  \*************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   BotInfo: () => (/* binding */ BotInfo),\n/* harmony export */   BrowserInfo: () => (/* binding */ BrowserInfo),\n/* harmony export */   NodeInfo: () => (/* binding */ NodeInfo),\n/* harmony export */   ReactNativeInfo: () => (/* binding */ ReactNativeInfo),\n/* harmony export */   SearchBotDeviceInfo: () => (/* binding */ SearchBotDeviceInfo),\n/* harmony export */   browserName: () => (/* binding */ browserName),\n/* harmony export */   detect: () => (/* binding */ detect),\n/* harmony export */   detectOS: () => (/* binding */ detectOS),\n/* harmony export */   getNodeVersion: () => (/* binding */ getNodeVersion),\n/* harmony export */   parseUserAgent: () => (/* binding */ parseUserAgent)\n/* harmony export */ });\nvar __spreadArray = (undefined && undefined.__spreadArray) || function (to, from, pack) {\n    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {\n        if (ar || !(i in from)) {\n            if (!ar) ar = Array.prototype.slice.call(from, 0, i);\n            ar[i] = from[i];\n        }\n    }\n    return to.concat(ar || Array.prototype.slice.call(from));\n};\nvar BrowserInfo = /** @class */ (function () {\n    function BrowserInfo(name, version, os) {\n        this.name = name;\n        this.version = version;\n        this.os = os;\n        this.type = 'browser';\n    }\n    return BrowserInfo;\n}());\n\nvar NodeInfo = /** @class */ (function () {\n    function NodeInfo(version) {\n        this.version = version;\n        this.type = 'node';\n        this.name = 'node';\n        this.os = process.platform;\n    }\n    return NodeInfo;\n}());\n\nvar SearchBotDeviceInfo = /** @class */ (function () {\n    function SearchBotDeviceInfo(name, version, os, bot) {\n        this.name = name;\n        this.version = version;\n        this.os = os;\n        this.bot = bot;\n        this.type = 'bot-device';\n    }\n    return SearchBotDeviceInfo;\n}());\n\nvar BotInfo = /** @class */ (function () {\n    function BotInfo() {\n        this.type = 'bot';\n        this.bot = true; // NOTE: deprecated test name instead\n        this.name = 'bot';\n        this.version = null;\n        this.os = null;\n    }\n    return BotInfo;\n}());\n\nvar ReactNativeInfo = /** @class */ (function () {\n    function ReactNativeInfo() {\n        this.type = 'react-native';\n        this.name = 'react-native';\n        this.version = null;\n        this.os = null;\n    }\n    return ReactNativeInfo;\n}());\n\n// tslint:disable-next-line:max-line-length\nvar SEARCHBOX_UA_REGEX = /alexa|bot|crawl(er|ing)|facebookexternalhit|feedburner|google web preview|nagios|postrank|pingdom|slurp|spider|yahoo!|yandex/;\nvar SEARCHBOT_OS_REGEX = /(nuhk|curl|Googlebot|Yammybot|Openbot|Slurp|MSNBot|Ask\\ Jeeves\\/Teoma|ia_archiver)/;\nvar REQUIRED_VERSION_PARTS = 3;\nvar userAgentRules = [\n    ['aol', /AOLShield\\/([0-9\\._]+)/],\n    ['edge', /Edge\\/([0-9\\._]+)/],\n    ['edge-ios', /EdgiOS\\/([0-9\\._]+)/],\n    ['yandexbrowser', /YaBrowser\\/([0-9\\._]+)/],\n    ['kakaotalk', /KAKAOTALK\\s([0-9\\.]+)/],\n    ['samsung', /SamsungBrowser\\/([0-9\\.]+)/],\n    ['silk', /\\bSilk\\/([0-9._-]+)\\b/],\n    ['miui', /MiuiBrowser\\/([0-9\\.]+)$/],\n    ['beaker', /BeakerBrowser\\/([0-9\\.]+)/],\n    ['edge-chromium', /EdgA?\\/([0-9\\.]+)/],\n    [\n        'chromium-webview',\n        /(?!Chrom.*OPR)wv\\).*Chrom(?:e|ium)\\/([0-9\\.]+)(:?\\s|$)/,\n    ],\n    ['chrome', /(?!Chrom.*OPR)Chrom(?:e|ium)\\/([0-9\\.]+)(:?\\s|$)/],\n    ['phantomjs', /PhantomJS\\/([0-9\\.]+)(:?\\s|$)/],\n    ['crios', /CriOS\\/([0-9\\.]+)(:?\\s|$)/],\n    ['firefox', /Firefox\\/([0-9\\.]+)(?:\\s|$)/],\n    ['fxios', /FxiOS\\/([0-9\\.]+)/],\n    ['opera-mini', /Opera Mini.*Version\\/([0-9\\.]+)/],\n    ['opera', /Opera\\/([0-9\\.]+)(?:\\s|$)/],\n    ['opera', /OPR\\/([0-9\\.]+)(:?\\s|$)/],\n    ['pie', /^Microsoft Pocket Internet Explorer\\/(\\d+\\.\\d+)$/],\n    ['pie', /^Mozilla\\/\\d\\.\\d+\\s\\(compatible;\\s(?:MSP?IE|MSInternet Explorer) (\\d+\\.\\d+);.*Windows CE.*\\)$/],\n    ['netfront', /^Mozilla\\/\\d\\.\\d+.*NetFront\\/(\\d.\\d)/],\n    ['ie', /Trident\\/7\\.0.*rv\\:([0-9\\.]+).*\\).*Gecko$/],\n    ['ie', /MSIE\\s([0-9\\.]+);.*Trident\\/[4-7].0/],\n    ['ie', /MSIE\\s(7\\.0)/],\n    ['bb10', /BB10;\\sTouch.*Version\\/([0-9\\.]+)/],\n    ['android', /Android\\s([0-9\\.]+)/],\n    ['ios', /Version\\/([0-9\\._]+).*Mobile.*Safari.*/],\n    ['safari', /Version\\/([0-9\\._]+).*Safari/],\n    ['facebook', /FB[AS]V\\/([0-9\\.]+)/],\n    ['instagram', /Instagram\\s([0-9\\.]+)/],\n    ['ios-webview', /AppleWebKit\\/([0-9\\.]+).*Mobile/],\n    ['ios-webview', /AppleWebKit\\/([0-9\\.]+).*Gecko\\)$/],\n    ['curl', /^curl\\/([0-9\\.]+)$/],\n    ['searchbot', SEARCHBOX_UA_REGEX],\n];\nvar operatingSystemRules = [\n    ['iOS', /iP(hone|od|ad)/],\n    ['Android OS', /Android/],\n    ['BlackBerry OS', /BlackBerry|BB10/],\n    ['Windows Mobile', /IEMobile/],\n    ['Amazon OS', /Kindle/],\n    ['Windows 3.11', /Win16/],\n    ['Windows 95', /(Windows 95)|(Win95)|(Windows_95)/],\n    ['Windows 98', /(Windows 98)|(Win98)/],\n    ['Windows 2000', /(Windows NT 5.0)|(Windows 2000)/],\n    ['Windows XP', /(Windows NT 5.1)|(Windows XP)/],\n    ['Windows Server 2003', /(Windows NT 5.2)/],\n    ['Windows Vista', /(Windows NT 6.0)/],\n    ['Windows 7', /(Windows NT 6.1)/],\n    ['Windows 8', /(Windows NT 6.2)/],\n    ['Windows 8.1', /(Windows NT 6.3)/],\n    ['Windows 10', /(Windows NT 10.0)/],\n    ['Windows ME', /Windows ME/],\n    ['Windows CE', /Windows CE|WinCE|Microsoft Pocket Internet Explorer/],\n    ['Open BSD', /OpenBSD/],\n    ['Sun OS', /SunOS/],\n    ['Chrome OS', /CrOS/],\n    ['Linux', /(Linux)|(X11)/],\n    ['Mac OS', /(Mac_PowerPC)|(Macintosh)/],\n    ['QNX', /QNX/],\n    ['BeOS', /BeOS/],\n    ['OS/2', /OS\\/2/],\n];\nfunction detect(userAgent) {\n    if (!!userAgent) {\n        return parseUserAgent(userAgent);\n    }\n    if (typeof document === 'undefined' &&\n        typeof navigator !== 'undefined' &&\n        navigator.product === 'ReactNative') {\n        return new ReactNativeInfo();\n    }\n    if (typeof navigator !== 'undefined') {\n        return parseUserAgent(navigator.userAgent);\n    }\n    return getNodeVersion();\n}\nfunction matchUserAgent(ua) {\n    // opted for using reduce here rather than Array#first with a regex.test call\n    // this is primarily because using the reduce we only perform the regex\n    // execution once rather than once for the test and for the exec again below\n    // probably something that needs to be benchmarked though\n    return (ua !== '' &&\n        userAgentRules.reduce(function (matched, _a) {\n            var browser = _a[0], regex = _a[1];\n            if (matched) {\n                return matched;\n            }\n            var uaMatch = regex.exec(ua);\n            return !!uaMatch && [browser, uaMatch];\n        }, false));\n}\nfunction browserName(ua) {\n    var data = matchUserAgent(ua);\n    return data ? data[0] : null;\n}\nfunction parseUserAgent(ua) {\n    var matchedRule = matchUserAgent(ua);\n    if (!matchedRule) {\n        return null;\n    }\n    var name = matchedRule[0], match = matchedRule[1];\n    if (name === 'searchbot') {\n        return new BotInfo();\n    }\n    // Do not use RegExp for split operation as some browser do not support it (See: http://blog.stevenlevithan.com/archives/cross-browser-split)\n    var versionParts = match[1] && match[1].split('.').join('_').split('_').slice(0, 3);\n    if (versionParts) {\n        if (versionParts.length < REQUIRED_VERSION_PARTS) {\n            versionParts = __spreadArray(__spreadArray([], versionParts, true), createVersionParts(REQUIRED_VERSION_PARTS - versionParts.length), true);\n        }\n    }\n    else {\n        versionParts = [];\n    }\n    var version = versionParts.join('.');\n    var os = detectOS(ua);\n    var searchBotMatch = SEARCHBOT_OS_REGEX.exec(ua);\n    if (searchBotMatch && searchBotMatch[1]) {\n        return new SearchBotDeviceInfo(name, version, os, searchBotMatch[1]);\n    }\n    return new BrowserInfo(name, version, os);\n}\nfunction detectOS(ua) {\n    for (var ii = 0, count = operatingSystemRules.length; ii < count; ii++) {\n        var _a = operatingSystemRules[ii], os = _a[0], regex = _a[1];\n        var match = regex.exec(ua);\n        if (match) {\n            return os;\n        }\n    }\n    return null;\n}\nfunction getNodeVersion() {\n    var isNode = typeof process !== 'undefined' && process.version;\n    return isNode ? new NodeInfo(process.version.slice(1)) : null;\n}\nfunction createVersionParts(count) {\n    var output = [];\n    for (var ii = 0; ii < count; ii++) {\n        output.push('0');\n    }\n    return output;\n}\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/detect-browser/es/index.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeInBack.js":
/*!***********************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeInBack.js ***!
  \***********************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeInBack: () => (/* binding */ easeInBack)\n/* harmony export */ });\nfunction easeInBack(x) {\r\n    const c1 = 1.70158;\r\n    const c3 = c1 + 1;\r\n    return c3 * x * x * x - c1 * x * x;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeInBack.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeInBounce.js":
/*!*************************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeInBounce.js ***!
  \*************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeInBounce: () => (/* binding */ easeInBounce)\n/* harmony export */ });\n/* harmony import */ var _easeOutBounce__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./easeOutBounce */ \"./node_modules/easing-progress/dist/es/easing.net/easeOutBounce.js\");\n\r\nfunction easeInBounce(x) {\r\n    return 1 - (0,_easeOutBounce__WEBPACK_IMPORTED_MODULE_0__.easeOutBounce)(1 - x);\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeInBounce.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeInCirc.js":
/*!***********************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeInCirc.js ***!
  \***********************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeInCirc: () => (/* binding */ easeInCirc)\n/* harmony export */ });\nfunction easeInCirc(x) {\r\n    return 1 - Math.sqrt(1 - Math.pow(x, 2));\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeInCirc.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeInCubic.js":
/*!************************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeInCubic.js ***!
  \************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeInCubic: () => (/* binding */ easeInCubic)\n/* harmony export */ });\nfunction easeInCubic(x) {\r\n    return Math.pow(x, 3);\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeInCubic.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeInElastic.js":
/*!**************************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeInElastic.js ***!
  \**************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeInElastic: () => (/* binding */ easeInElastic)\n/* harmony export */ });\nfunction easeInElastic(x) {\r\n    const c4 = (2 * Math.PI) / 3;\r\n    return x === 0\r\n        ? 0\r\n        : x === 1\r\n            ? 1\r\n            : -(Math.pow(2, (10 * x - 10))) * Math.sin((x * 10 - 10.75) * c4);\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeInElastic.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeInExpo.js":
/*!***********************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeInExpo.js ***!
  \***********************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeInExpo: () => (/* binding */ easeInExpo)\n/* harmony export */ });\nfunction easeInExpo(x) {\r\n    return x === 0 ? 0 : (Math.pow(2, (10 * x - 10)));\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeInExpo.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeInOutBack.js":
/*!**************************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeInOutBack.js ***!
  \**************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeInOutBack: () => (/* binding */ easeInOutBack)\n/* harmony export */ });\nfunction easeInOutBack(x) {\r\n    const c1 = 1.70158;\r\n    const c2 = c1 * 1.525;\r\n    return x < 0.5\r\n        ? ((Math.pow((2 * x), 2)) * ((c2 + 1) * 2 * x - c2)) / 2\r\n        : ((Math.pow((2 * x - 2), 2)) * ((c2 + 1) * (x * 2 - 2) + c2) + 2) / 2;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeInOutBack.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeInOutBounce.js":
/*!****************************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeInOutBounce.js ***!
  \****************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeInOutBounce: () => (/* binding */ easeInOutBounce)\n/* harmony export */ });\n/* harmony import */ var _easeOutBounce__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./easeOutBounce */ \"./node_modules/easing-progress/dist/es/easing.net/easeOutBounce.js\");\n\r\nfunction easeInOutBounce(x) {\r\n    return x < 0.5\r\n        ? (1 - (0,_easeOutBounce__WEBPACK_IMPORTED_MODULE_0__.easeOutBounce)(1 - 2 * x)) / 2\r\n        : (1 + (0,_easeOutBounce__WEBPACK_IMPORTED_MODULE_0__.easeOutBounce)(2 * x - 1)) / 2;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeInOutBounce.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeInOutCirc.js":
/*!**************************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeInOutCirc.js ***!
  \**************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeInOutCirc: () => (/* binding */ easeInOutCirc)\n/* harmony export */ });\nfunction easeInOutCirc(x) {\r\n    return x < 0.5\r\n        ? (1 - Math.sqrt(1 - (Math.pow((2 * x), 2)))) / 2\r\n        : (Math.sqrt(1 - (Math.pow((-2 * x + 2), 2))) + 1) / 2;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeInOutCirc.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeInOutCubic.js":
/*!***************************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeInOutCubic.js ***!
  \***************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeInOutCubic: () => (/* binding */ easeInOutCubic)\n/* harmony export */ });\nfunction easeInOutCubic(x) {\r\n    return x < 0.5 ? 4 * x * x * x : 1 - (Math.pow((-2 * x + 2), 3)) / 2;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeInOutCubic.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeInOutElastic.js":
/*!*****************************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeInOutElastic.js ***!
  \*****************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeInOutElastic: () => (/* binding */ easeInOutElastic)\n/* harmony export */ });\nfunction easeInOutElastic(x) {\r\n    const c5 = (2 * Math.PI) / 4.5;\r\n    return x === 0\r\n        ? 0\r\n        : x === 1\r\n            ? 1\r\n            : x < 0.5\r\n                ? -((Math.pow(2, (20 * x - 10))) * Math.sin((20 * x - 11.125) * c5)) / 2\r\n                : ((Math.pow(2, (-20 * x + 10))) * Math.sin((20 * x - 11.125) * c5)) / 2 + 1;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeInOutElastic.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeInOutExpo.js":
/*!**************************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeInOutExpo.js ***!
  \**************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeInOutExpo: () => (/* binding */ easeInOutExpo)\n/* harmony export */ });\nfunction easeInOutExpo(x) {\r\n    return x === 0\r\n        ? 0\r\n        : x === 1\r\n            ? 1\r\n            : x < 0.5 ? (Math.pow(2, (20 * x - 10))) / 2\r\n                : (2 - (Math.pow(2, (-20 * x + 10)))) / 2;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeInOutExpo.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeInOutQuad.js":
/*!**************************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeInOutQuad.js ***!
  \**************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeInOutQuad: () => (/* binding */ easeInOutQuad)\n/* harmony export */ });\nfunction easeInOutQuad(x) {\r\n    return x < 0.5 ? 2 * x * x : 1 - (Math.pow((-2 * x + 2), 2)) / 2;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeInOutQuad.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeInOutQuart.js":
/*!***************************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeInOutQuart.js ***!
  \***************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeInOutQuart: () => (/* binding */ easeInOutQuart)\n/* harmony export */ });\nfunction easeInOutQuart(x) {\r\n    return x < 0.5 ? 8 * x * x * x * x : 1 - (Math.pow((-2 * x + 2), 4)) / 2;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeInOutQuart.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeInOutQuint.js":
/*!***************************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeInOutQuint.js ***!
  \***************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeInOutQuint: () => (/* binding */ easeInOutQuint)\n/* harmony export */ });\nfunction easeInOutQuint(x) {\r\n    return x < 0.5 ? 16 * x * x * x * x * x : 1 - (Math.pow((-2 * x + 2), 5)) / 2;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeInOutQuint.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeInOutSine.js":
/*!**************************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeInOutSine.js ***!
  \**************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeInOutSine: () => (/* binding */ easeInOutSine)\n/* harmony export */ });\nfunction easeInOutSine(x) {\r\n    return -(Math.cos(Math.PI * x) - 1) / 2;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeInOutSine.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeInQuad.js":
/*!***********************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeInQuad.js ***!
  \***********************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeInQuad: () => (/* binding */ easeInQuad)\n/* harmony export */ });\nfunction easeInQuad(x) {\r\n    return Math.pow(x, 2);\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeInQuad.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeInQuart.js":
/*!************************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeInQuart.js ***!
  \************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeInQuart: () => (/* binding */ easeInQuart)\n/* harmony export */ });\nfunction easeInQuart(x) {\r\n    return Math.pow(x, 4);\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeInQuart.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeInQuint.js":
/*!************************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeInQuint.js ***!
  \************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeInQuint: () => (/* binding */ easeInQuint)\n/* harmony export */ });\nfunction easeInQuint(x) {\r\n    return Math.pow(x, 5);\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeInQuint.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeInSine.js":
/*!***********************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeInSine.js ***!
  \***********************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeInSine: () => (/* binding */ easeInSine)\n/* harmony export */ });\nfunction easeInSine(x) {\r\n    return 1 - Math.cos((x * Math.PI) / 2);\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeInSine.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeOutBack.js":
/*!************************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeOutBack.js ***!
  \************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeOutBack: () => (/* binding */ easeOutBack)\n/* harmony export */ });\nfunction easeOutBack(x) {\r\n    const c1 = 1.70158;\r\n    const c3 = c1 + 1;\r\n    return 1 + c3 * (Math.pow((x - 1), 3)) + c1 * (Math.pow((x - 1), 2));\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeOutBack.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeOutBounce.js":
/*!**************************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeOutBounce.js ***!
  \**************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeOutBounce: () => (/* binding */ easeOutBounce)\n/* harmony export */ });\nfunction easeOutBounce(x) {\r\n    const n1 = 7.5625;\r\n    const d1 = 2.75;\r\n    if (x < 1 / d1) {\r\n        return n1 * x * x;\r\n    }\r\n    if (x < 2 / d1) {\r\n        return n1 * (x -= 1.5 / d1) * x + 0.75;\r\n    }\r\n    if (x < 2.5 / d1) {\r\n        return n1 * (x -= 2.25 / d1) * x + 0.9375;\r\n    }\r\n    return n1 * (x -= 2.625 / d1) * x + 0.984375;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeOutBounce.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeOutCirc.js":
/*!************************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeOutCirc.js ***!
  \************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeOutCirc: () => (/* binding */ easeOutCirc)\n/* harmony export */ });\nfunction easeOutCirc(x) {\r\n    return Math.sqrt(1 - (Math.pow((x - 1), 2)));\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeOutCirc.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeOutCubic.js":
/*!*************************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeOutCubic.js ***!
  \*************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeOutCubic: () => (/* binding */ easeOutCubic)\n/* harmony export */ });\nfunction easeOutCubic(x) {\r\n    return 1 - (Math.pow((1 - x), 3));\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeOutCubic.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeOutElastic.js":
/*!***************************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeOutElastic.js ***!
  \***************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeOutElastic: () => (/* binding */ easeOutElastic)\n/* harmony export */ });\nfunction easeOutElastic(x) {\r\n    const c4 = (2 * Math.PI) / 3;\r\n    return x === 0\r\n        ? 0\r\n        : x === 1\r\n            ? 1\r\n            : (Math.pow(2, (-10 * x))) * Math.sin((x * 10 - 0.75) * c4) + 1;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeOutElastic.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeOutExpo.js":
/*!************************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeOutExpo.js ***!
  \************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeOutExpo: () => (/* binding */ easeOutExpo)\n/* harmony export */ });\nfunction easeOutExpo(x) {\r\n    return x === 1 ? 1 : 1 - (Math.pow(2, (-10 * x)));\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeOutExpo.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeOutQuad.js":
/*!************************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeOutQuad.js ***!
  \************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeOutQuad: () => (/* binding */ easeOutQuad)\n/* harmony export */ });\nfunction easeOutQuad(x) {\r\n    return 1 - Math.pow((1 - x), 2);\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeOutQuad.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeOutQuart.js":
/*!*************************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeOutQuart.js ***!
  \*************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeOutQuart: () => (/* binding */ easeOutQuart)\n/* harmony export */ });\nfunction easeOutQuart(x) {\r\n    return 1 - (Math.pow((1 - x), 4));\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeOutQuart.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeOutQuint.js":
/*!*************************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeOutQuint.js ***!
  \*************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeOutQuint: () => (/* binding */ easeOutQuint)\n/* harmony export */ });\nfunction easeOutQuint(x) {\r\n    return 1 - (Math.pow((1 - x), 5));\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeOutQuint.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easing.net/easeOutSine.js":
/*!************************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easing.net/easeOutSine.js ***!
  \************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easeOutSine: () => (/* binding */ easeOutSine)\n/* harmony export */ });\nfunction easeOutSine(x) {\r\n    return Math.sin((x * Math.PI) / 2);\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easing.net/easeOutSine.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easingByBezier.js":
/*!****************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easingByBezier.js ***!
  \****************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   easingByBezier: () => (/* binding */ easingByBezier)\n/* harmony export */ });\nfunction easingByBezier(progress, bezier) {\r\n    const [x1, y1, x2, y2] = bezier;\r\n    if (x1 === y1 && x2 === y2) {\r\n        return progress;\r\n    }\r\n    const val = [];\r\n    for (let i = 0; i < 11; ++i) {\r\n        val[i] = bezierCalc(i * 0.1, x1, x2);\r\n    }\r\n    if (progress === 0) {\r\n        return 0;\r\n    }\r\n    if (progress === 1) {\r\n        return 1;\r\n    }\r\n    return bezierCalc(bezierX(bezier, progress, val), y1, y2);\r\n}\r\nfunction bezierCalc(progress, x1, x2) {\r\n    return ((bezierA(x1, x2) * progress + bezierB(x1, x2)) * progress + bezierC(x1)) * progress;\r\n}\r\nfunction bezierA(x1, x2) {\r\n    return 1.0 - 3.0 * x2 + 3.0 * x1;\r\n}\r\nfunction bezierB(x1, x2) {\r\n    return 3.0 * x2 - 6.0 * x1;\r\n}\r\nfunction bezierC(x1) {\r\n    return 3.0 * x1;\r\n}\r\nfunction bezierX(bezier, progress, val) {\r\n    // eslint-disable-next-line no-unused-vars\r\n    const x1 = bezier[0];\r\n    const x2 = bezier[2];\r\n    let start = 0;\r\n    let current = 1;\r\n    for (; current !== 10 && val[current] <= progress; ++current) {\r\n        start += 0.1;\r\n    }\r\n    --current;\r\n    const dist = (progress - val[current]) / (val[current + 1] - val[current]);\r\n    const guessForT = start + dist * 0.1;\r\n    const initialSlope = bezierSlope(guessForT, x1, x2);\r\n    if (initialSlope >= 0.001) {\r\n        return bezierNewtonRaphsonIterate(progress, guessForT, x1, x2);\r\n    }\r\n    if (initialSlope === 0.0) {\r\n        return guessForT;\r\n    }\r\n    return bezierBinarySubdivide(progress, start, start + 0.1, x1, x2);\r\n}\r\nfunction bezierSlope(progress, x1, x2) {\r\n    return 3.0 * bezierA(x1, x2) * progress * progress + 2.0 * bezierB(x1, x2) * progress + bezierC(x1);\r\n}\r\nfunction bezierNewtonRaphsonIterate(progress, guessForT, x1, x2) {\r\n    for (let i = 0; i < 4; ++i) {\r\n        const currentSlope = bezierSlope(guessForT, x1, x2);\r\n        if (currentSlope === 0.0) {\r\n            return guessForT;\r\n        }\r\n        const currentX = bezierCalc(guessForT, x1, x2) - progress;\r\n        guessForT -= currentX / currentSlope;\r\n    }\r\n    return guessForT;\r\n}\r\nfunction bezierBinarySubdivide(progress, a, b, x1, x2) {\r\n    let currentX;\r\n    let currentT;\r\n    let i = 0;\r\n    do {\r\n        currentT = a + (b - a) / 2.0;\r\n        currentX = bezierCalc(currentT, x1, x2) - progress;\r\n        if (currentX > 0.0) {\r\n            b = currentT;\r\n        }\r\n        else {\r\n            a = currentT;\r\n        }\r\n    } while (Math.abs(currentX) > 0.0000001 && ++i < 10);\r\n    return currentT;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easingByBezier.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/easingByName.js":
/*!**************************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/easingByName.js ***!
  \**************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ easingByName)\n/* harmony export */ });\n/* harmony import */ var _easing_net_easeInOutQuad__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./easing.net/easeInOutQuad */ \"./node_modules/easing-progress/dist/es/easing.net/easeInOutQuad.js\");\n/* harmony import */ var _easing_net_easeInOutSine__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./easing.net/easeInOutSine */ \"./node_modules/easing-progress/dist/es/easing.net/easeInOutSine.js\");\n/* harmony import */ var _easing_net_easeInQuad__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./easing.net/easeInQuad */ \"./node_modules/easing-progress/dist/es/easing.net/easeInQuad.js\");\n/* harmony import */ var _easing_net_easeInSine__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./easing.net/easeInSine */ \"./node_modules/easing-progress/dist/es/easing.net/easeInSine.js\");\n/* harmony import */ var _easing_net_easeOutQuad__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./easing.net/easeOutQuad */ \"./node_modules/easing-progress/dist/es/easing.net/easeOutQuad.js\");\n/* harmony import */ var _easing_net_easeOutSine__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./easing.net/easeOutSine */ \"./node_modules/easing-progress/dist/es/easing.net/easeOutSine.js\");\n/* harmony import */ var _easing_net_easeInCubic__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./easing.net/easeInCubic */ \"./node_modules/easing-progress/dist/es/easing.net/easeInCubic.js\");\n/* harmony import */ var _types__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./types */ \"./node_modules/easing-progress/dist/es/types.js\");\n/* harmony import */ var _easing_net_easeOutCubic__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./easing.net/easeOutCubic */ \"./node_modules/easing-progress/dist/es/easing.net/easeOutCubic.js\");\n/* harmony import */ var _easing_net_easeInOutCubic__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./easing.net/easeInOutCubic */ \"./node_modules/easing-progress/dist/es/easing.net/easeInOutCubic.js\");\n/* harmony import */ var _easing_net_easeInQuart__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ./easing.net/easeInQuart */ \"./node_modules/easing-progress/dist/es/easing.net/easeInQuart.js\");\n/* harmony import */ var _easing_net_easeOutQuart__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ./easing.net/easeOutQuart */ \"./node_modules/easing-progress/dist/es/easing.net/easeOutQuart.js\");\n/* harmony import */ var _easing_net_easeInOutQuart__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ./easing.net/easeInOutQuart */ \"./node_modules/easing-progress/dist/es/easing.net/easeInOutQuart.js\");\n/* harmony import */ var _easing_net_easeInQuint__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! ./easing.net/easeInQuint */ \"./node_modules/easing-progress/dist/es/easing.net/easeInQuint.js\");\n/* harmony import */ var _easing_net_easeOutQuint__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! ./easing.net/easeOutQuint */ \"./node_modules/easing-progress/dist/es/easing.net/easeOutQuint.js\");\n/* harmony import */ var _easing_net_easeInOutQuint__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! ./easing.net/easeInOutQuint */ \"./node_modules/easing-progress/dist/es/easing.net/easeInOutQuint.js\");\n/* harmony import */ var _easing_net_easeInExpo__WEBPACK_IMPORTED_MODULE_16__ = __webpack_require__(/*! ./easing.net/easeInExpo */ \"./node_modules/easing-progress/dist/es/easing.net/easeInExpo.js\");\n/* harmony import */ var _easing_net_easeOutExpo__WEBPACK_IMPORTED_MODULE_17__ = __webpack_require__(/*! ./easing.net/easeOutExpo */ \"./node_modules/easing-progress/dist/es/easing.net/easeOutExpo.js\");\n/* harmony import */ var _easing_net_easeInOutExpo__WEBPACK_IMPORTED_MODULE_18__ = __webpack_require__(/*! ./easing.net/easeInOutExpo */ \"./node_modules/easing-progress/dist/es/easing.net/easeInOutExpo.js\");\n/* harmony import */ var _easing_net_easeInCirc__WEBPACK_IMPORTED_MODULE_19__ = __webpack_require__(/*! ./easing.net/easeInCirc */ \"./node_modules/easing-progress/dist/es/easing.net/easeInCirc.js\");\n/* harmony import */ var _easing_net_easeOutCirc__WEBPACK_IMPORTED_MODULE_20__ = __webpack_require__(/*! ./easing.net/easeOutCirc */ \"./node_modules/easing-progress/dist/es/easing.net/easeOutCirc.js\");\n/* harmony import */ var _easing_net_easeInOutCirc__WEBPACK_IMPORTED_MODULE_21__ = __webpack_require__(/*! ./easing.net/easeInOutCirc */ \"./node_modules/easing-progress/dist/es/easing.net/easeInOutCirc.js\");\n/* harmony import */ var _easing_net_easeInBack__WEBPACK_IMPORTED_MODULE_22__ = __webpack_require__(/*! ./easing.net/easeInBack */ \"./node_modules/easing-progress/dist/es/easing.net/easeInBack.js\");\n/* harmony import */ var _easing_net_easeOutBack__WEBPACK_IMPORTED_MODULE_23__ = __webpack_require__(/*! ./easing.net/easeOutBack */ \"./node_modules/easing-progress/dist/es/easing.net/easeOutBack.js\");\n/* harmony import */ var _easing_net_easeInOutBack__WEBPACK_IMPORTED_MODULE_24__ = __webpack_require__(/*! ./easing.net/easeInOutBack */ \"./node_modules/easing-progress/dist/es/easing.net/easeInOutBack.js\");\n/* harmony import */ var _easing_net_easeInElastic__WEBPACK_IMPORTED_MODULE_25__ = __webpack_require__(/*! ./easing.net/easeInElastic */ \"./node_modules/easing-progress/dist/es/easing.net/easeInElastic.js\");\n/* harmony import */ var _easing_net_easeOutElastic__WEBPACK_IMPORTED_MODULE_26__ = __webpack_require__(/*! ./easing.net/easeOutElastic */ \"./node_modules/easing-progress/dist/es/easing.net/easeOutElastic.js\");\n/* harmony import */ var _easing_net_easeInOutElastic__WEBPACK_IMPORTED_MODULE_27__ = __webpack_require__(/*! ./easing.net/easeInOutElastic */ \"./node_modules/easing-progress/dist/es/easing.net/easeInOutElastic.js\");\n/* harmony import */ var _easing_net_easeInBounce__WEBPACK_IMPORTED_MODULE_28__ = __webpack_require__(/*! ./easing.net/easeInBounce */ \"./node_modules/easing-progress/dist/es/easing.net/easeInBounce.js\");\n/* harmony import */ var _easing_net_easeOutBounce__WEBPACK_IMPORTED_MODULE_29__ = __webpack_require__(/*! ./easing.net/easeOutBounce */ \"./node_modules/easing-progress/dist/es/easing.net/easeOutBounce.js\");\n/* harmony import */ var _easing_net_easeInOutBounce__WEBPACK_IMPORTED_MODULE_30__ = __webpack_require__(/*! ./easing.net/easeInOutBounce */ \"./node_modules/easing-progress/dist/es/easing.net/easeInOutBounce.js\");\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\nfunction easingByName(progress, name) {\r\n    switch (name) {\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeInSine:\r\n            return (0,_easing_net_easeInSine__WEBPACK_IMPORTED_MODULE_3__.easeInSine)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeOutSine:\r\n            return (0,_easing_net_easeOutSine__WEBPACK_IMPORTED_MODULE_5__.easeOutSine)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeInOutSine:\r\n            return (0,_easing_net_easeInOutSine__WEBPACK_IMPORTED_MODULE_1__.easeInOutSine)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeInQuad:\r\n            return (0,_easing_net_easeInQuad__WEBPACK_IMPORTED_MODULE_2__.easeInQuad)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeOutQuad:\r\n            return (0,_easing_net_easeOutQuad__WEBPACK_IMPORTED_MODULE_4__.easeOutQuad)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeInOutQuad:\r\n            return (0,_easing_net_easeInOutQuad__WEBPACK_IMPORTED_MODULE_0__.easeInOutQuad)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeInCubic:\r\n            return (0,_easing_net_easeInCubic__WEBPACK_IMPORTED_MODULE_6__.easeInCubic)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeOutCubic:\r\n            return (0,_easing_net_easeOutCubic__WEBPACK_IMPORTED_MODULE_8__.easeOutCubic)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeInOutCubic:\r\n            return (0,_easing_net_easeInOutCubic__WEBPACK_IMPORTED_MODULE_9__.easeInOutCubic)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeInQuart:\r\n            return (0,_easing_net_easeInQuart__WEBPACK_IMPORTED_MODULE_10__.easeInQuart)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeOutQuart:\r\n            return (0,_easing_net_easeOutQuart__WEBPACK_IMPORTED_MODULE_11__.easeOutQuart)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeInOutQuart:\r\n            return (0,_easing_net_easeInOutQuart__WEBPACK_IMPORTED_MODULE_12__.easeInOutQuart)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeInQuint:\r\n            return (0,_easing_net_easeInQuint__WEBPACK_IMPORTED_MODULE_13__.easeInQuint)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeOutQuint:\r\n            return (0,_easing_net_easeOutQuint__WEBPACK_IMPORTED_MODULE_14__.easeOutQuint)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeInOutQuint:\r\n            return (0,_easing_net_easeInOutQuint__WEBPACK_IMPORTED_MODULE_15__.easeInOutQuint)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeInExpo:\r\n            return (0,_easing_net_easeInExpo__WEBPACK_IMPORTED_MODULE_16__.easeInExpo)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeOutExpo:\r\n            return (0,_easing_net_easeOutExpo__WEBPACK_IMPORTED_MODULE_17__.easeOutExpo)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeInOutExpo:\r\n            return (0,_easing_net_easeInOutExpo__WEBPACK_IMPORTED_MODULE_18__.easeInOutExpo)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeInCirc:\r\n            return (0,_easing_net_easeInCirc__WEBPACK_IMPORTED_MODULE_19__.easeInCirc)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeOutCirc:\r\n            return (0,_easing_net_easeOutCirc__WEBPACK_IMPORTED_MODULE_20__.easeOutCirc)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeInOutCirc:\r\n            return (0,_easing_net_easeInOutCirc__WEBPACK_IMPORTED_MODULE_21__.easeInOutCirc)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeInBack:\r\n            return (0,_easing_net_easeInBack__WEBPACK_IMPORTED_MODULE_22__.easeInBack)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeOutBack:\r\n            return (0,_easing_net_easeOutBack__WEBPACK_IMPORTED_MODULE_23__.easeOutBack)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeInOutBack:\r\n            return (0,_easing_net_easeInOutBack__WEBPACK_IMPORTED_MODULE_24__.easeInOutBack)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeInElastic:\r\n            return (0,_easing_net_easeInElastic__WEBPACK_IMPORTED_MODULE_25__.easeInElastic)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeOutElastic:\r\n            return (0,_easing_net_easeOutElastic__WEBPACK_IMPORTED_MODULE_26__.easeOutElastic)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeInOutElastic:\r\n            return (0,_easing_net_easeInOutElastic__WEBPACK_IMPORTED_MODULE_27__.easeInOutElastic)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeInBounce:\r\n            return (0,_easing_net_easeInBounce__WEBPACK_IMPORTED_MODULE_28__.easeInBounce)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeOutBounce:\r\n            return (0,_easing_net_easeOutBounce__WEBPACK_IMPORTED_MODULE_29__.easeOutBounce)(progress);\r\n        case _types__WEBPACK_IMPORTED_MODULE_7__.Easing.easeInOutBounce:\r\n            return (0,_easing_net_easeInOutBounce__WEBPACK_IMPORTED_MODULE_30__.easeInOutBounce)(progress);\r\n        default:\r\n            return progress;\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/easingByName.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/index.js":
/*!*******************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/index.js ***!
  \*******************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   Easing: () => (/* reexport safe */ _types__WEBPACK_IMPORTED_MODULE_2__.Easing),\n/* harmony export */   \"default\": () => (/* binding */ easingProgress)\n/* harmony export */ });\n/* harmony import */ var _easingByBezier__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./easingByBezier */ \"./node_modules/easing-progress/dist/es/easingByBezier.js\");\n/* harmony import */ var _easingByName__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./easingByName */ \"./node_modules/easing-progress/dist/es/easingByName.js\");\n/* harmony import */ var _types__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./types */ \"./node_modules/easing-progress/dist/es/types.js\");\n\r\n\r\n\r\n\r\n/**\r\n * Calculate easing progress\r\n */\r\nfunction easingProgress(progress, easing = false) {\r\n    if (!easing) {\r\n        return progress;\r\n    }\r\n    if (Array.isArray(easing)) {\r\n        return (0,_easingByBezier__WEBPACK_IMPORTED_MODULE_0__.easingByBezier)(progress, easing);\r\n    }\r\n    if (typeof easing === 'function') {\r\n        return easing(progress);\r\n    }\r\n    return (0,_easingByName__WEBPACK_IMPORTED_MODULE_1__[\"default\"])(progress, easing);\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/index.js?");

/***/ }),

/***/ "./node_modules/easing-progress/dist/es/types.js":
/*!*******************************************************!*\
  !*** ./node_modules/easing-progress/dist/es/types.js ***!
  \*******************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   Easing: () => (/* binding */ Easing)\n/* harmony export */ });\nvar Easing;\r\n(function (Easing) {\r\n    Easing[\"easeInSine\"] = \"easeInSine\";\r\n    Easing[\"easeOutSine\"] = \"easeOutSine\";\r\n    Easing[\"easeInOutSine\"] = \"easeInOutSine\";\r\n    Easing[\"easeInQuad\"] = \"easeInQuad\";\r\n    Easing[\"easeOutQuad\"] = \"easeOutQuad\";\r\n    Easing[\"easeInOutQuad\"] = \"easeInOutQuad\";\r\n    Easing[\"easeInCubic\"] = \"easeInCubic\";\r\n    Easing[\"easeOutCubic\"] = \"easeOutCubic\";\r\n    Easing[\"easeInOutCubic\"] = \"easeInOutCubic\";\r\n    Easing[\"easeInQuart\"] = \"easeInQuart\";\r\n    Easing[\"easeOutQuart\"] = \"easeOutQuart\";\r\n    Easing[\"easeInOutQuart\"] = \"easeInOutQuart\";\r\n    Easing[\"easeInQuint\"] = \"easeInQuint\";\r\n    Easing[\"easeOutQuint\"] = \"easeOutQuint\";\r\n    Easing[\"easeInOutQuint\"] = \"easeInOutQuint\";\r\n    Easing[\"easeInExpo\"] = \"easeInExpo\";\r\n    Easing[\"easeOutExpo\"] = \"easeOutExpo\";\r\n    Easing[\"easeInOutExpo\"] = \"easeInOutExpo\";\r\n    Easing[\"easeInCirc\"] = \"easeInCirc\";\r\n    Easing[\"easeOutCirc\"] = \"easeOutCirc\";\r\n    Easing[\"easeInOutCirc\"] = \"easeInOutCirc\";\r\n    Easing[\"easeInBack\"] = \"easeInBack\";\r\n    Easing[\"easeOutBack\"] = \"easeOutBack\";\r\n    Easing[\"easeInOutBack\"] = \"easeInOutBack\";\r\n    Easing[\"easeInElastic\"] = \"easeInElastic\";\r\n    Easing[\"easeOutElastic\"] = \"easeOutElastic\";\r\n    Easing[\"easeInOutElastic\"] = \"easeInOutElastic\";\r\n    Easing[\"easeInBounce\"] = \"easeInBounce\";\r\n    Easing[\"easeOutBounce\"] = \"easeOutBounce\";\r\n    Easing[\"easeInOutBounce\"] = \"easeInOutBounce\";\r\n})(Easing || (Easing = {}));\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/easing-progress/dist/es/types.js?");

/***/ }),

/***/ "./node_modules/get-image-pos/dist/es/applyHTMLPos.js":
/*!************************************************************!*\
  !*** ./node_modules/get-image-pos/dist/es/applyHTMLPos.js ***!
  \************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   applyHTMLPos: () => (/* binding */ applyHTMLPos)\n/* harmony export */ });\n/* harmony import */ var _getPos__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./getPos */ \"./node_modules/get-image-pos/dist/es/getPos.js\");\n\r\n/**\r\n * Apply position of an image.\r\n */\r\nfunction applyHTMLPos(data) {\r\n    const sizes = (0,_getPos__WEBPACK_IMPORTED_MODULE_0__.getPos)(data);\r\n    // apply sizes\r\n    const { source } = data;\r\n    source.style.position = 'absolute';\r\n    source.style.left = `${sizes.x}px`;\r\n    source.style.top = `${sizes.y}px`;\r\n    source.style.width = `${sizes.width}px`;\r\n    source.style.height = `${sizes.height}px`;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/get-image-pos/dist/es/applyHTMLPos.js?");

/***/ }),

/***/ "./node_modules/get-image-pos/dist/es/getPos.js":
/*!******************************************************!*\
  !*** ./node_modules/get-image-pos/dist/es/getPos.js ***!
  \******************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   getPos: () => (/* binding */ getPos)\n/* harmony export */ });\n/**\r\n * Get image size according to the selected rules.\r\n * Note that the resources must be loaded before applying sizes to them.\r\n */\r\nfunction getPos(data) {\r\n    // get container size\r\n    const containerSizes = getContainerSizes(data);\r\n    // get source size\r\n    const sourceSizes = getSourceSizes(data);\r\n    // source coordinates\r\n    let coords;\r\n    // calculate position\r\n    if (data.rule === 'cover') {\r\n        coords = getPosCover(data, containerSizes, sourceSizes);\r\n    }\r\n    else if (data.rule === 'contain') {\r\n        coords = getPosContain(data, containerSizes, sourceSizes);\r\n    }\r\n    else if (data.rule === 'top-left') {\r\n        coords = getPosTopLeft(data, sourceSizes);\r\n    }\r\n    else if (data.rule === 'top-right') {\r\n        coords = getPosTopRight(data, containerSizes, sourceSizes);\r\n    }\r\n    else if (data.rule === 'bottom-left') {\r\n        coords = getPosBottomLeft(data, containerSizes, sourceSizes);\r\n    }\r\n    else if (data.rule === 'bottom-right') {\r\n        coords = getPosBottomRight(data, containerSizes, sourceSizes);\r\n    }\r\n    else if (data.rule === 'center') {\r\n        coords = getPosCenter(data, containerSizes, sourceSizes);\r\n    }\r\n    // return position\r\n    return Object.assign(coords, {\r\n        sourceWidth: sourceSizes.width,\r\n        sourceHeight: sourceSizes.height,\r\n    });\r\n}\r\n/**\r\n * Get scale prop\r\n * @ignore\r\n */\r\nfunction getScale(data) {\r\n    if (typeof data.scale !== 'undefined') {\r\n        return data.scale;\r\n    }\r\n    return 1;\r\n}\r\n/**\r\n * Get source sizes\r\n * @ignore\r\n */\r\nfunction getSourceSizes(data) {\r\n    const { source } = data;\r\n    if (!!data.sourceWidth && !!data.sourceHeight) {\r\n        return {\r\n            width: data.sourceWidth,\r\n            height: data.sourceHeight,\r\n        };\r\n    }\r\n    if (source instanceof HTMLVideoElement) {\r\n        return {\r\n            width: source.videoWidth,\r\n            height: source.videoHeight,\r\n        };\r\n    }\r\n    if (source instanceof HTMLImageElement) {\r\n        return {\r\n            width: source.naturalWidth,\r\n            height: source.naturalHeight,\r\n        };\r\n    }\r\n    return {\r\n        width: source.width,\r\n        height: source.height,\r\n    };\r\n}\r\n/**\r\n * Get container sizes\r\n * @ignore\r\n */\r\nfunction getContainerSizes(data) {\r\n    // if has a container\r\n    if ('container' in data) {\r\n        return {\r\n            width: data.container.clientWidth,\r\n            height: data.container.clientHeight,\r\n        };\r\n    }\r\n    return {\r\n        width: data.width,\r\n        height: data.height,\r\n    };\r\n}\r\n/**\r\n * Get \"cover\" position\r\n * @ignore\r\n */\r\nfunction getPosCover(data, containerSizes, sourceSizes) {\r\n    let width = containerSizes.width * getScale(data);\r\n    let height = sourceSizes.height * width / sourceSizes.width;\r\n    if (height / getScale(data) < containerSizes.height) {\r\n        height = containerSizes.height * getScale(data);\r\n        width = sourceSizes.width * height / sourceSizes.height;\r\n    }\r\n    const x = (containerSizes.width - width) / 2;\r\n    const y = (containerSizes.height - height) / 2;\r\n    return {\r\n        width,\r\n        height,\r\n        x,\r\n        y,\r\n    };\r\n}\r\n/**\r\n * Get \"contain\" position\r\n * @ignore\r\n */\r\nfunction getPosContain(data, containerSizes, sourceSizes) {\r\n    let width = 0;\r\n    let height = 0;\r\n    const widthRatio = sourceSizes.width / sourceSizes.height;\r\n    if (containerSizes.width > containerSizes.height) {\r\n        height = containerSizes.height;\r\n        width = height * widthRatio;\r\n        if (width > containerSizes.width) {\r\n            width = containerSizes.width;\r\n            height = width / widthRatio;\r\n        }\r\n    }\r\n    else if (containerSizes.height >= containerSizes.width) {\r\n        width = containerSizes.width;\r\n        height = width / widthRatio;\r\n        if (height > containerSizes.height) {\r\n            height = containerSizes.height;\r\n            width = height * widthRatio;\r\n        }\r\n    }\r\n    width *= getScale(data);\r\n    height *= getScale(data);\r\n    const x = (containerSizes.width - width) / 2;\r\n    const y = (containerSizes.height - height) / 2;\r\n    return {\r\n        width,\r\n        height,\r\n        x,\r\n        y,\r\n    };\r\n}\r\n/**\r\n * Get \"top-left\" position\r\n * @ignore\r\n */\r\nfunction getPosTopLeft(data, sourceSizes) {\r\n    const width = sourceSizes.width * getScale(data);\r\n    const height = sourceSizes.height * getScale(data);\r\n    return {\r\n        width,\r\n        height,\r\n        x: 0,\r\n        y: 0,\r\n    };\r\n}\r\n/**\r\n * Get \"top-right\" position\r\n * @ignore\r\n */\r\nfunction getPosTopRight(data, containerSizes, sourceSizes) {\r\n    const width = sourceSizes.width * getScale(data);\r\n    const height = sourceSizes.height * getScale(data);\r\n    const x = containerSizes.width - sourceSizes.width;\r\n    return {\r\n        width,\r\n        height,\r\n        x,\r\n        y: 0,\r\n    };\r\n}\r\n/**\r\n * Get \"bottom-left\" position\r\n * @ignore\r\n */\r\nfunction getPosBottomLeft(data, containerSizes, sourceSizes) {\r\n    const width = sourceSizes.width * getScale(data);\r\n    const height = sourceSizes.height * getScale(data);\r\n    const y = containerSizes.height - sourceSizes.height;\r\n    return {\r\n        width,\r\n        height,\r\n        x: 0,\r\n        y,\r\n    };\r\n}\r\n/**\r\n * Get \"bottom-right\" position\r\n * @ignore\r\n */\r\nfunction getPosBottomRight(data, containerSizes, sourceSizes) {\r\n    const width = sourceSizes.width * getScale(data);\r\n    const height = sourceSizes.height * getScale(data);\r\n    const x = containerSizes.width - sourceSizes.width;\r\n    const y = containerSizes.height - sourceSizes.height;\r\n    return {\r\n        width,\r\n        height,\r\n        x,\r\n        y,\r\n    };\r\n}\r\n/**\r\n * Get \"center\" position\r\n * @ignore\r\n */\r\nfunction getPosCenter(data, containerSizes, sourceSizes) {\r\n    const width = sourceSizes.width * getScale(data);\r\n    const height = sourceSizes.height * getScale(data);\r\n    const x = (containerSizes.width - sourceSizes.width) / 2;\r\n    const y = (containerSizes.height - sourceSizes.height) / 2;\r\n    return {\r\n        width,\r\n        height,\r\n        x,\r\n        y,\r\n    };\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/get-image-pos/dist/es/getPos.js?");

/***/ }),

/***/ "./node_modules/get-image-pos/dist/es/index.js":
/*!*****************************************************!*\
  !*** ./node_modules/get-image-pos/dist/es/index.js ***!
  \*****************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   applyHTMLPos: () => (/* reexport safe */ _applyHTMLPos__WEBPACK_IMPORTED_MODULE_1__.applyHTMLPos),\n/* harmony export */   getPos: () => (/* reexport safe */ _getPos__WEBPACK_IMPORTED_MODULE_0__.getPos)\n/* harmony export */ });\n/* harmony import */ var _getPos__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./getPos */ \"./node_modules/get-image-pos/dist/es/getPos.js\");\n/* harmony import */ var _applyHTMLPos__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./applyHTMLPos */ \"./node_modules/get-image-pos/dist/es/applyHTMLPos.js\");\n\r\n\r\n\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/get-image-pos/dist/es/index.js?");

/***/ }),

/***/ "./node_modules/ismobilejs/esm/index.js":
/*!**********************************************!*\
  !*** ./node_modules/ismobilejs/esm/index.js ***!
  \**********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* reexport safe */ _isMobile__WEBPACK_IMPORTED_MODULE_0__[\"default\"])\n/* harmony export */ });\n/* harmony import */ var _isMobile__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./isMobile */ \"./node_modules/ismobilejs/esm/isMobile.js\");\n\n\n//# sourceMappingURL=index.js.map\n\n//# sourceURL=webpack://persona-patman/./node_modules/ismobilejs/esm/index.js?");

/***/ }),

/***/ "./node_modules/ismobilejs/esm/isMobile.js":
/*!*************************************************!*\
  !*** ./node_modules/ismobilejs/esm/isMobile.js ***!
  \*************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ isMobile)\n/* harmony export */ });\nvar appleIphone = /iPhone/i;\nvar appleIpod = /iPod/i;\nvar appleTablet = /iPad/i;\nvar appleUniversal = /\\biOS-universal(?:.+)Mac\\b/i;\nvar androidPhone = /\\bAndroid(?:.+)Mobile\\b/i;\nvar androidTablet = /Android/i;\nvar amazonPhone = /(?:SD4930UR|\\bSilk(?:.+)Mobile\\b)/i;\nvar amazonTablet = /Silk/i;\nvar windowsPhone = /Windows Phone/i;\nvar windowsTablet = /\\bWindows(?:.+)ARM\\b/i;\nvar otherBlackBerry = /BlackBerry/i;\nvar otherBlackBerry10 = /BB10/i;\nvar otherOpera = /Opera Mini/i;\nvar otherChrome = /\\b(CriOS|Chrome)(?:.+)Mobile/i;\nvar otherFirefox = /Mobile(?:.+)Firefox\\b/i;\nvar isAppleTabletOnIos13 = function (navigator) {\n    return (typeof navigator !== 'undefined' &&\n        navigator.platform === 'MacIntel' &&\n        typeof navigator.maxTouchPoints === 'number' &&\n        navigator.maxTouchPoints > 1 &&\n        typeof MSStream === 'undefined');\n};\nfunction createMatch(userAgent) {\n    return function (regex) { return regex.test(userAgent); };\n}\nfunction isMobile(param) {\n    var nav = {\n        userAgent: '',\n        platform: '',\n        maxTouchPoints: 0\n    };\n    if (!param && typeof navigator !== 'undefined') {\n        nav = {\n            userAgent: navigator.userAgent,\n            platform: navigator.platform,\n            maxTouchPoints: navigator.maxTouchPoints || 0\n        };\n    }\n    else if (typeof param === 'string') {\n        nav.userAgent = param;\n    }\n    else if (param && param.userAgent) {\n        nav = {\n            userAgent: param.userAgent,\n            platform: param.platform,\n            maxTouchPoints: param.maxTouchPoints || 0\n        };\n    }\n    var userAgent = nav.userAgent;\n    var tmp = userAgent.split('[FBAN');\n    if (typeof tmp[1] !== 'undefined') {\n        userAgent = tmp[0];\n    }\n    tmp = userAgent.split('Twitter');\n    if (typeof tmp[1] !== 'undefined') {\n        userAgent = tmp[0];\n    }\n    var match = createMatch(userAgent);\n    var result = {\n        apple: {\n            phone: match(appleIphone) && !match(windowsPhone),\n            ipod: match(appleIpod),\n            tablet: !match(appleIphone) &&\n                (match(appleTablet) || isAppleTabletOnIos13(nav)) &&\n                !match(windowsPhone),\n            universal: match(appleUniversal),\n            device: (match(appleIphone) ||\n                match(appleIpod) ||\n                match(appleTablet) ||\n                match(appleUniversal) ||\n                isAppleTabletOnIos13(nav)) &&\n                !match(windowsPhone)\n        },\n        amazon: {\n            phone: match(amazonPhone),\n            tablet: !match(amazonPhone) && match(amazonTablet),\n            device: match(amazonPhone) || match(amazonTablet)\n        },\n        android: {\n            phone: (!match(windowsPhone) && match(amazonPhone)) ||\n                (!match(windowsPhone) && match(androidPhone)),\n            tablet: !match(windowsPhone) &&\n                !match(amazonPhone) &&\n                !match(androidPhone) &&\n                (match(amazonTablet) || match(androidTablet)),\n            device: (!match(windowsPhone) &&\n                (match(amazonPhone) ||\n                    match(amazonTablet) ||\n                    match(androidPhone) ||\n                    match(androidTablet))) ||\n                match(/\\bokhttp\\b/i)\n        },\n        windows: {\n            phone: match(windowsPhone),\n            tablet: match(windowsTablet),\n            device: match(windowsPhone) || match(windowsTablet)\n        },\n        other: {\n            blackberry: match(otherBlackBerry),\n            blackberry10: match(otherBlackBerry10),\n            opera: match(otherOpera),\n            firefox: match(otherFirefox),\n            chrome: match(otherChrome),\n            device: match(otherBlackBerry) ||\n                match(otherBlackBerry10) ||\n                match(otherOpera) ||\n                match(otherFirefox) ||\n                match(otherChrome)\n        },\n        any: false,\n        phone: false,\n        tablet: false\n    };\n    result.any =\n        result.apple.device ||\n            result.android.device ||\n            result.windows.device ||\n            result.other.device;\n    result.phone =\n        result.apple.phone || result.android.phone || result.windows.phone;\n    result.tablet =\n        result.apple.tablet || result.android.tablet || result.windows.tablet;\n    return result;\n}\n//# sourceMappingURL=isMobile.js.map\n\n//# sourceURL=webpack://persona-patman/./node_modules/ismobilejs/esm/isMobile.js?");

/***/ }),

/***/ "./node_modules/lodash.mergewith/index.js":
/*!************************************************!*\
  !*** ./node_modules/lodash.mergewith/index.js ***!
  \************************************************/
/***/ ((module, exports, __webpack_require__) => {

eval("/* module decorator */ module = __webpack_require__.nmd(module);\n/**\n * Lodash (Custom Build) <https://lodash.com/>\n * Build: `lodash modularize exports=\"npm\" -o ./`\n * Copyright OpenJS Foundation and other contributors <https://openjsf.org/>\n * Released under MIT license <https://lodash.com/license>\n * Based on Underscore.js 1.8.3 <http://underscorejs.org/LICENSE>\n * Copyright Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors\n */\n\n/** Used as the size to enable large array optimizations. */\nvar LARGE_ARRAY_SIZE = 200;\n\n/** Used to stand-in for `undefined` hash values. */\nvar HASH_UNDEFINED = '__lodash_hash_undefined__';\n\n/** Used to detect hot functions by number of calls within a span of milliseconds. */\nvar HOT_COUNT = 800,\n    HOT_SPAN = 16;\n\n/** Used as references for various `Number` constants. */\nvar MAX_SAFE_INTEGER = 9007199254740991;\n\n/** `Object#toString` result references. */\nvar argsTag = '[object Arguments]',\n    arrayTag = '[object Array]',\n    asyncTag = '[object AsyncFunction]',\n    boolTag = '[object Boolean]',\n    dateTag = '[object Date]',\n    errorTag = '[object Error]',\n    funcTag = '[object Function]',\n    genTag = '[object GeneratorFunction]',\n    mapTag = '[object Map]',\n    numberTag = '[object Number]',\n    nullTag = '[object Null]',\n    objectTag = '[object Object]',\n    proxyTag = '[object Proxy]',\n    regexpTag = '[object RegExp]',\n    setTag = '[object Set]',\n    stringTag = '[object String]',\n    undefinedTag = '[object Undefined]',\n    weakMapTag = '[object WeakMap]';\n\nvar arrayBufferTag = '[object ArrayBuffer]',\n    dataViewTag = '[object DataView]',\n    float32Tag = '[object Float32Array]',\n    float64Tag = '[object Float64Array]',\n    int8Tag = '[object Int8Array]',\n    int16Tag = '[object Int16Array]',\n    int32Tag = '[object Int32Array]',\n    uint8Tag = '[object Uint8Array]',\n    uint8ClampedTag = '[object Uint8ClampedArray]',\n    uint16Tag = '[object Uint16Array]',\n    uint32Tag = '[object Uint32Array]';\n\n/**\n * Used to match `RegExp`\n * [syntax characters](http://ecma-international.org/ecma-262/7.0/#sec-patterns).\n */\nvar reRegExpChar = /[\\\\^$.*+?()[\\]{}|]/g;\n\n/** Used to detect host constructors (Safari). */\nvar reIsHostCtor = /^\\[object .+?Constructor\\]$/;\n\n/** Used to detect unsigned integer values. */\nvar reIsUint = /^(?:0|[1-9]\\d*)$/;\n\n/** Used to identify `toStringTag` values of typed arrays. */\nvar typedArrayTags = {};\ntypedArrayTags[float32Tag] = typedArrayTags[float64Tag] =\ntypedArrayTags[int8Tag] = typedArrayTags[int16Tag] =\ntypedArrayTags[int32Tag] = typedArrayTags[uint8Tag] =\ntypedArrayTags[uint8ClampedTag] = typedArrayTags[uint16Tag] =\ntypedArrayTags[uint32Tag] = true;\ntypedArrayTags[argsTag] = typedArrayTags[arrayTag] =\ntypedArrayTags[arrayBufferTag] = typedArrayTags[boolTag] =\ntypedArrayTags[dataViewTag] = typedArrayTags[dateTag] =\ntypedArrayTags[errorTag] = typedArrayTags[funcTag] =\ntypedArrayTags[mapTag] = typedArrayTags[numberTag] =\ntypedArrayTags[objectTag] = typedArrayTags[regexpTag] =\ntypedArrayTags[setTag] = typedArrayTags[stringTag] =\ntypedArrayTags[weakMapTag] = false;\n\n/** Detect free variable `global` from Node.js. */\nvar freeGlobal = typeof __webpack_require__.g == 'object' && __webpack_require__.g && __webpack_require__.g.Object === Object && __webpack_require__.g;\n\n/** Detect free variable `self`. */\nvar freeSelf = typeof self == 'object' && self && self.Object === Object && self;\n\n/** Used as a reference to the global object. */\nvar root = freeGlobal || freeSelf || Function('return this')();\n\n/** Detect free variable `exports`. */\nvar freeExports =  true && exports && !exports.nodeType && exports;\n\n/** Detect free variable `module`. */\nvar freeModule = freeExports && \"object\" == 'object' && module && !module.nodeType && module;\n\n/** Detect the popular CommonJS extension `module.exports`. */\nvar moduleExports = freeModule && freeModule.exports === freeExports;\n\n/** Detect free variable `process` from Node.js. */\nvar freeProcess = moduleExports && freeGlobal.process;\n\n/** Used to access faster Node.js helpers. */\nvar nodeUtil = (function() {\n  try {\n    // Use `util.types` for Node.js 10+.\n    var types = freeModule && freeModule.require && freeModule.require('util').types;\n\n    if (types) {\n      return types;\n    }\n\n    // Legacy `process.binding('util')` for Node.js < 10.\n    return freeProcess && freeProcess.binding && freeProcess.binding('util');\n  } catch (e) {}\n}());\n\n/* Node.js helper references. */\nvar nodeIsTypedArray = nodeUtil && nodeUtil.isTypedArray;\n\n/**\n * A faster alternative to `Function#apply`, this function invokes `func`\n * with the `this` binding of `thisArg` and the arguments of `args`.\n *\n * @private\n * @param {Function} func The function to invoke.\n * @param {*} thisArg The `this` binding of `func`.\n * @param {Array} args The arguments to invoke `func` with.\n * @returns {*} Returns the result of `func`.\n */\nfunction apply(func, thisArg, args) {\n  switch (args.length) {\n    case 0: return func.call(thisArg);\n    case 1: return func.call(thisArg, args[0]);\n    case 2: return func.call(thisArg, args[0], args[1]);\n    case 3: return func.call(thisArg, args[0], args[1], args[2]);\n  }\n  return func.apply(thisArg, args);\n}\n\n/**\n * The base implementation of `_.times` without support for iteratee shorthands\n * or max array length checks.\n *\n * @private\n * @param {number} n The number of times to invoke `iteratee`.\n * @param {Function} iteratee The function invoked per iteration.\n * @returns {Array} Returns the array of results.\n */\nfunction baseTimes(n, iteratee) {\n  var index = -1,\n      result = Array(n);\n\n  while (++index < n) {\n    result[index] = iteratee(index);\n  }\n  return result;\n}\n\n/**\n * The base implementation of `_.unary` without support for storing metadata.\n *\n * @private\n * @param {Function} func The function to cap arguments for.\n * @returns {Function} Returns the new capped function.\n */\nfunction baseUnary(func) {\n  return function(value) {\n    return func(value);\n  };\n}\n\n/**\n * Gets the value at `key` of `object`.\n *\n * @private\n * @param {Object} [object] The object to query.\n * @param {string} key The key of the property to get.\n * @returns {*} Returns the property value.\n */\nfunction getValue(object, key) {\n  return object == null ? undefined : object[key];\n}\n\n/**\n * Creates a unary function that invokes `func` with its argument transformed.\n *\n * @private\n * @param {Function} func The function to wrap.\n * @param {Function} transform The argument transform.\n * @returns {Function} Returns the new function.\n */\nfunction overArg(func, transform) {\n  return function(arg) {\n    return func(transform(arg));\n  };\n}\n\n/** Used for built-in method references. */\nvar arrayProto = Array.prototype,\n    funcProto = Function.prototype,\n    objectProto = Object.prototype;\n\n/** Used to detect overreaching core-js shims. */\nvar coreJsData = root['__core-js_shared__'];\n\n/** Used to resolve the decompiled source of functions. */\nvar funcToString = funcProto.toString;\n\n/** Used to check objects for own properties. */\nvar hasOwnProperty = objectProto.hasOwnProperty;\n\n/** Used to detect methods masquerading as native. */\nvar maskSrcKey = (function() {\n  var uid = /[^.]+$/.exec(coreJsData && coreJsData.keys && coreJsData.keys.IE_PROTO || '');\n  return uid ? ('Symbol(src)_1.' + uid) : '';\n}());\n\n/**\n * Used to resolve the\n * [`toStringTag`](http://ecma-international.org/ecma-262/7.0/#sec-object.prototype.tostring)\n * of values.\n */\nvar nativeObjectToString = objectProto.toString;\n\n/** Used to infer the `Object` constructor. */\nvar objectCtorString = funcToString.call(Object);\n\n/** Used to detect if a method is native. */\nvar reIsNative = RegExp('^' +\n  funcToString.call(hasOwnProperty).replace(reRegExpChar, '\\\\$&')\n  .replace(/hasOwnProperty|(function).*?(?=\\\\\\()| for .+?(?=\\\\\\])/g, '$1.*?') + '$'\n);\n\n/** Built-in value references. */\nvar Buffer = moduleExports ? root.Buffer : undefined,\n    Symbol = root.Symbol,\n    Uint8Array = root.Uint8Array,\n    allocUnsafe = Buffer ? Buffer.allocUnsafe : undefined,\n    getPrototype = overArg(Object.getPrototypeOf, Object),\n    objectCreate = Object.create,\n    propertyIsEnumerable = objectProto.propertyIsEnumerable,\n    splice = arrayProto.splice,\n    symToStringTag = Symbol ? Symbol.toStringTag : undefined;\n\nvar defineProperty = (function() {\n  try {\n    var func = getNative(Object, 'defineProperty');\n    func({}, '', {});\n    return func;\n  } catch (e) {}\n}());\n\n/* Built-in method references for those with the same name as other `lodash` methods. */\nvar nativeIsBuffer = Buffer ? Buffer.isBuffer : undefined,\n    nativeMax = Math.max,\n    nativeNow = Date.now;\n\n/* Built-in method references that are verified to be native. */\nvar Map = getNative(root, 'Map'),\n    nativeCreate = getNative(Object, 'create');\n\n/**\n * The base implementation of `_.create` without support for assigning\n * properties to the created object.\n *\n * @private\n * @param {Object} proto The object to inherit from.\n * @returns {Object} Returns the new object.\n */\nvar baseCreate = (function() {\n  function object() {}\n  return function(proto) {\n    if (!isObject(proto)) {\n      return {};\n    }\n    if (objectCreate) {\n      return objectCreate(proto);\n    }\n    object.prototype = proto;\n    var result = new object;\n    object.prototype = undefined;\n    return result;\n  };\n}());\n\n/**\n * Creates a hash object.\n *\n * @private\n * @constructor\n * @param {Array} [entries] The key-value pairs to cache.\n */\nfunction Hash(entries) {\n  var index = -1,\n      length = entries == null ? 0 : entries.length;\n\n  this.clear();\n  while (++index < length) {\n    var entry = entries[index];\n    this.set(entry[0], entry[1]);\n  }\n}\n\n/**\n * Removes all key-value entries from the hash.\n *\n * @private\n * @name clear\n * @memberOf Hash\n */\nfunction hashClear() {\n  this.__data__ = nativeCreate ? nativeCreate(null) : {};\n  this.size = 0;\n}\n\n/**\n * Removes `key` and its value from the hash.\n *\n * @private\n * @name delete\n * @memberOf Hash\n * @param {Object} hash The hash to modify.\n * @param {string} key The key of the value to remove.\n * @returns {boolean} Returns `true` if the entry was removed, else `false`.\n */\nfunction hashDelete(key) {\n  var result = this.has(key) && delete this.__data__[key];\n  this.size -= result ? 1 : 0;\n  return result;\n}\n\n/**\n * Gets the hash value for `key`.\n *\n * @private\n * @name get\n * @memberOf Hash\n * @param {string} key The key of the value to get.\n * @returns {*} Returns the entry value.\n */\nfunction hashGet(key) {\n  var data = this.__data__;\n  if (nativeCreate) {\n    var result = data[key];\n    return result === HASH_UNDEFINED ? undefined : result;\n  }\n  return hasOwnProperty.call(data, key) ? data[key] : undefined;\n}\n\n/**\n * Checks if a hash value for `key` exists.\n *\n * @private\n * @name has\n * @memberOf Hash\n * @param {string} key The key of the entry to check.\n * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.\n */\nfunction hashHas(key) {\n  var data = this.__data__;\n  return nativeCreate ? (data[key] !== undefined) : hasOwnProperty.call(data, key);\n}\n\n/**\n * Sets the hash `key` to `value`.\n *\n * @private\n * @name set\n * @memberOf Hash\n * @param {string} key The key of the value to set.\n * @param {*} value The value to set.\n * @returns {Object} Returns the hash instance.\n */\nfunction hashSet(key, value) {\n  var data = this.__data__;\n  this.size += this.has(key) ? 0 : 1;\n  data[key] = (nativeCreate && value === undefined) ? HASH_UNDEFINED : value;\n  return this;\n}\n\n// Add methods to `Hash`.\nHash.prototype.clear = hashClear;\nHash.prototype['delete'] = hashDelete;\nHash.prototype.get = hashGet;\nHash.prototype.has = hashHas;\nHash.prototype.set = hashSet;\n\n/**\n * Creates an list cache object.\n *\n * @private\n * @constructor\n * @param {Array} [entries] The key-value pairs to cache.\n */\nfunction ListCache(entries) {\n  var index = -1,\n      length = entries == null ? 0 : entries.length;\n\n  this.clear();\n  while (++index < length) {\n    var entry = entries[index];\n    this.set(entry[0], entry[1]);\n  }\n}\n\n/**\n * Removes all key-value entries from the list cache.\n *\n * @private\n * @name clear\n * @memberOf ListCache\n */\nfunction listCacheClear() {\n  this.__data__ = [];\n  this.size = 0;\n}\n\n/**\n * Removes `key` and its value from the list cache.\n *\n * @private\n * @name delete\n * @memberOf ListCache\n * @param {string} key The key of the value to remove.\n * @returns {boolean} Returns `true` if the entry was removed, else `false`.\n */\nfunction listCacheDelete(key) {\n  var data = this.__data__,\n      index = assocIndexOf(data, key);\n\n  if (index < 0) {\n    return false;\n  }\n  var lastIndex = data.length - 1;\n  if (index == lastIndex) {\n    data.pop();\n  } else {\n    splice.call(data, index, 1);\n  }\n  --this.size;\n  return true;\n}\n\n/**\n * Gets the list cache value for `key`.\n *\n * @private\n * @name get\n * @memberOf ListCache\n * @param {string} key The key of the value to get.\n * @returns {*} Returns the entry value.\n */\nfunction listCacheGet(key) {\n  var data = this.__data__,\n      index = assocIndexOf(data, key);\n\n  return index < 0 ? undefined : data[index][1];\n}\n\n/**\n * Checks if a list cache value for `key` exists.\n *\n * @private\n * @name has\n * @memberOf ListCache\n * @param {string} key The key of the entry to check.\n * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.\n */\nfunction listCacheHas(key) {\n  return assocIndexOf(this.__data__, key) > -1;\n}\n\n/**\n * Sets the list cache `key` to `value`.\n *\n * @private\n * @name set\n * @memberOf ListCache\n * @param {string} key The key of the value to set.\n * @param {*} value The value to set.\n * @returns {Object} Returns the list cache instance.\n */\nfunction listCacheSet(key, value) {\n  var data = this.__data__,\n      index = assocIndexOf(data, key);\n\n  if (index < 0) {\n    ++this.size;\n    data.push([key, value]);\n  } else {\n    data[index][1] = value;\n  }\n  return this;\n}\n\n// Add methods to `ListCache`.\nListCache.prototype.clear = listCacheClear;\nListCache.prototype['delete'] = listCacheDelete;\nListCache.prototype.get = listCacheGet;\nListCache.prototype.has = listCacheHas;\nListCache.prototype.set = listCacheSet;\n\n/**\n * Creates a map cache object to store key-value pairs.\n *\n * @private\n * @constructor\n * @param {Array} [entries] The key-value pairs to cache.\n */\nfunction MapCache(entries) {\n  var index = -1,\n      length = entries == null ? 0 : entries.length;\n\n  this.clear();\n  while (++index < length) {\n    var entry = entries[index];\n    this.set(entry[0], entry[1]);\n  }\n}\n\n/**\n * Removes all key-value entries from the map.\n *\n * @private\n * @name clear\n * @memberOf MapCache\n */\nfunction mapCacheClear() {\n  this.size = 0;\n  this.__data__ = {\n    'hash': new Hash,\n    'map': new (Map || ListCache),\n    'string': new Hash\n  };\n}\n\n/**\n * Removes `key` and its value from the map.\n *\n * @private\n * @name delete\n * @memberOf MapCache\n * @param {string} key The key of the value to remove.\n * @returns {boolean} Returns `true` if the entry was removed, else `false`.\n */\nfunction mapCacheDelete(key) {\n  var result = getMapData(this, key)['delete'](key);\n  this.size -= result ? 1 : 0;\n  return result;\n}\n\n/**\n * Gets the map value for `key`.\n *\n * @private\n * @name get\n * @memberOf MapCache\n * @param {string} key The key of the value to get.\n * @returns {*} Returns the entry value.\n */\nfunction mapCacheGet(key) {\n  return getMapData(this, key).get(key);\n}\n\n/**\n * Checks if a map value for `key` exists.\n *\n * @private\n * @name has\n * @memberOf MapCache\n * @param {string} key The key of the entry to check.\n * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.\n */\nfunction mapCacheHas(key) {\n  return getMapData(this, key).has(key);\n}\n\n/**\n * Sets the map `key` to `value`.\n *\n * @private\n * @name set\n * @memberOf MapCache\n * @param {string} key The key of the value to set.\n * @param {*} value The value to set.\n * @returns {Object} Returns the map cache instance.\n */\nfunction mapCacheSet(key, value) {\n  var data = getMapData(this, key),\n      size = data.size;\n\n  data.set(key, value);\n  this.size += data.size == size ? 0 : 1;\n  return this;\n}\n\n// Add methods to `MapCache`.\nMapCache.prototype.clear = mapCacheClear;\nMapCache.prototype['delete'] = mapCacheDelete;\nMapCache.prototype.get = mapCacheGet;\nMapCache.prototype.has = mapCacheHas;\nMapCache.prototype.set = mapCacheSet;\n\n/**\n * Creates a stack cache object to store key-value pairs.\n *\n * @private\n * @constructor\n * @param {Array} [entries] The key-value pairs to cache.\n */\nfunction Stack(entries) {\n  var data = this.__data__ = new ListCache(entries);\n  this.size = data.size;\n}\n\n/**\n * Removes all key-value entries from the stack.\n *\n * @private\n * @name clear\n * @memberOf Stack\n */\nfunction stackClear() {\n  this.__data__ = new ListCache;\n  this.size = 0;\n}\n\n/**\n * Removes `key` and its value from the stack.\n *\n * @private\n * @name delete\n * @memberOf Stack\n * @param {string} key The key of the value to remove.\n * @returns {boolean} Returns `true` if the entry was removed, else `false`.\n */\nfunction stackDelete(key) {\n  var data = this.__data__,\n      result = data['delete'](key);\n\n  this.size = data.size;\n  return result;\n}\n\n/**\n * Gets the stack value for `key`.\n *\n * @private\n * @name get\n * @memberOf Stack\n * @param {string} key The key of the value to get.\n * @returns {*} Returns the entry value.\n */\nfunction stackGet(key) {\n  return this.__data__.get(key);\n}\n\n/**\n * Checks if a stack value for `key` exists.\n *\n * @private\n * @name has\n * @memberOf Stack\n * @param {string} key The key of the entry to check.\n * @returns {boolean} Returns `true` if an entry for `key` exists, else `false`.\n */\nfunction stackHas(key) {\n  return this.__data__.has(key);\n}\n\n/**\n * Sets the stack `key` to `value`.\n *\n * @private\n * @name set\n * @memberOf Stack\n * @param {string} key The key of the value to set.\n * @param {*} value The value to set.\n * @returns {Object} Returns the stack cache instance.\n */\nfunction stackSet(key, value) {\n  var data = this.__data__;\n  if (data instanceof ListCache) {\n    var pairs = data.__data__;\n    if (!Map || (pairs.length < LARGE_ARRAY_SIZE - 1)) {\n      pairs.push([key, value]);\n      this.size = ++data.size;\n      return this;\n    }\n    data = this.__data__ = new MapCache(pairs);\n  }\n  data.set(key, value);\n  this.size = data.size;\n  return this;\n}\n\n// Add methods to `Stack`.\nStack.prototype.clear = stackClear;\nStack.prototype['delete'] = stackDelete;\nStack.prototype.get = stackGet;\nStack.prototype.has = stackHas;\nStack.prototype.set = stackSet;\n\n/**\n * Creates an array of the enumerable property names of the array-like `value`.\n *\n * @private\n * @param {*} value The value to query.\n * @param {boolean} inherited Specify returning inherited property names.\n * @returns {Array} Returns the array of property names.\n */\nfunction arrayLikeKeys(value, inherited) {\n  var isArr = isArray(value),\n      isArg = !isArr && isArguments(value),\n      isBuff = !isArr && !isArg && isBuffer(value),\n      isType = !isArr && !isArg && !isBuff && isTypedArray(value),\n      skipIndexes = isArr || isArg || isBuff || isType,\n      result = skipIndexes ? baseTimes(value.length, String) : [],\n      length = result.length;\n\n  for (var key in value) {\n    if ((inherited || hasOwnProperty.call(value, key)) &&\n        !(skipIndexes && (\n           // Safari 9 has enumerable `arguments.length` in strict mode.\n           key == 'length' ||\n           // Node.js 0.10 has enumerable non-index properties on buffers.\n           (isBuff && (key == 'offset' || key == 'parent')) ||\n           // PhantomJS 2 has enumerable non-index properties on typed arrays.\n           (isType && (key == 'buffer' || key == 'byteLength' || key == 'byteOffset')) ||\n           // Skip index properties.\n           isIndex(key, length)\n        ))) {\n      result.push(key);\n    }\n  }\n  return result;\n}\n\n/**\n * This function is like `assignValue` except that it doesn't assign\n * `undefined` values.\n *\n * @private\n * @param {Object} object The object to modify.\n * @param {string} key The key of the property to assign.\n * @param {*} value The value to assign.\n */\nfunction assignMergeValue(object, key, value) {\n  if ((value !== undefined && !eq(object[key], value)) ||\n      (value === undefined && !(key in object))) {\n    baseAssignValue(object, key, value);\n  }\n}\n\n/**\n * Assigns `value` to `key` of `object` if the existing value is not equivalent\n * using [`SameValueZero`](http://ecma-international.org/ecma-262/7.0/#sec-samevaluezero)\n * for equality comparisons.\n *\n * @private\n * @param {Object} object The object to modify.\n * @param {string} key The key of the property to assign.\n * @param {*} value The value to assign.\n */\nfunction assignValue(object, key, value) {\n  var objValue = object[key];\n  if (!(hasOwnProperty.call(object, key) && eq(objValue, value)) ||\n      (value === undefined && !(key in object))) {\n    baseAssignValue(object, key, value);\n  }\n}\n\n/**\n * Gets the index at which the `key` is found in `array` of key-value pairs.\n *\n * @private\n * @param {Array} array The array to inspect.\n * @param {*} key The key to search for.\n * @returns {number} Returns the index of the matched value, else `-1`.\n */\nfunction assocIndexOf(array, key) {\n  var length = array.length;\n  while (length--) {\n    if (eq(array[length][0], key)) {\n      return length;\n    }\n  }\n  return -1;\n}\n\n/**\n * The base implementation of `assignValue` and `assignMergeValue` without\n * value checks.\n *\n * @private\n * @param {Object} object The object to modify.\n * @param {string} key The key of the property to assign.\n * @param {*} value The value to assign.\n */\nfunction baseAssignValue(object, key, value) {\n  if (key == '__proto__' && defineProperty) {\n    defineProperty(object, key, {\n      'configurable': true,\n      'enumerable': true,\n      'value': value,\n      'writable': true\n    });\n  } else {\n    object[key] = value;\n  }\n}\n\n/**\n * The base implementation of `baseForOwn` which iterates over `object`\n * properties returned by `keysFunc` and invokes `iteratee` for each property.\n * Iteratee functions may exit iteration early by explicitly returning `false`.\n *\n * @private\n * @param {Object} object The object to iterate over.\n * @param {Function} iteratee The function invoked per iteration.\n * @param {Function} keysFunc The function to get the keys of `object`.\n * @returns {Object} Returns `object`.\n */\nvar baseFor = createBaseFor();\n\n/**\n * The base implementation of `getTag` without fallbacks for buggy environments.\n *\n * @private\n * @param {*} value The value to query.\n * @returns {string} Returns the `toStringTag`.\n */\nfunction baseGetTag(value) {\n  if (value == null) {\n    return value === undefined ? undefinedTag : nullTag;\n  }\n  return (symToStringTag && symToStringTag in Object(value))\n    ? getRawTag(value)\n    : objectToString(value);\n}\n\n/**\n * The base implementation of `_.isArguments`.\n *\n * @private\n * @param {*} value The value to check.\n * @returns {boolean} Returns `true` if `value` is an `arguments` object,\n */\nfunction baseIsArguments(value) {\n  return isObjectLike(value) && baseGetTag(value) == argsTag;\n}\n\n/**\n * The base implementation of `_.isNative` without bad shim checks.\n *\n * @private\n * @param {*} value The value to check.\n * @returns {boolean} Returns `true` if `value` is a native function,\n *  else `false`.\n */\nfunction baseIsNative(value) {\n  if (!isObject(value) || isMasked(value)) {\n    return false;\n  }\n  var pattern = isFunction(value) ? reIsNative : reIsHostCtor;\n  return pattern.test(toSource(value));\n}\n\n/**\n * The base implementation of `_.isTypedArray` without Node.js optimizations.\n *\n * @private\n * @param {*} value The value to check.\n * @returns {boolean} Returns `true` if `value` is a typed array, else `false`.\n */\nfunction baseIsTypedArray(value) {\n  return isObjectLike(value) &&\n    isLength(value.length) && !!typedArrayTags[baseGetTag(value)];\n}\n\n/**\n * The base implementation of `_.keysIn` which doesn't treat sparse arrays as dense.\n *\n * @private\n * @param {Object} object The object to query.\n * @returns {Array} Returns the array of property names.\n */\nfunction baseKeysIn(object) {\n  if (!isObject(object)) {\n    return nativeKeysIn(object);\n  }\n  var isProto = isPrototype(object),\n      result = [];\n\n  for (var key in object) {\n    if (!(key == 'constructor' && (isProto || !hasOwnProperty.call(object, key)))) {\n      result.push(key);\n    }\n  }\n  return result;\n}\n\n/**\n * The base implementation of `_.merge` without support for multiple sources.\n *\n * @private\n * @param {Object} object The destination object.\n * @param {Object} source The source object.\n * @param {number} srcIndex The index of `source`.\n * @param {Function} [customizer] The function to customize merged values.\n * @param {Object} [stack] Tracks traversed source values and their merged\n *  counterparts.\n */\nfunction baseMerge(object, source, srcIndex, customizer, stack) {\n  if (object === source) {\n    return;\n  }\n  baseFor(source, function(srcValue, key) {\n    stack || (stack = new Stack);\n    if (isObject(srcValue)) {\n      baseMergeDeep(object, source, key, srcIndex, baseMerge, customizer, stack);\n    }\n    else {\n      var newValue = customizer\n        ? customizer(safeGet(object, key), srcValue, (key + ''), object, source, stack)\n        : undefined;\n\n      if (newValue === undefined) {\n        newValue = srcValue;\n      }\n      assignMergeValue(object, key, newValue);\n    }\n  }, keysIn);\n}\n\n/**\n * A specialized version of `baseMerge` for arrays and objects which performs\n * deep merges and tracks traversed objects enabling objects with circular\n * references to be merged.\n *\n * @private\n * @param {Object} object The destination object.\n * @param {Object} source The source object.\n * @param {string} key The key of the value to merge.\n * @param {number} srcIndex The index of `source`.\n * @param {Function} mergeFunc The function to merge values.\n * @param {Function} [customizer] The function to customize assigned values.\n * @param {Object} [stack] Tracks traversed source values and their merged\n *  counterparts.\n */\nfunction baseMergeDeep(object, source, key, srcIndex, mergeFunc, customizer, stack) {\n  var objValue = safeGet(object, key),\n      srcValue = safeGet(source, key),\n      stacked = stack.get(srcValue);\n\n  if (stacked) {\n    assignMergeValue(object, key, stacked);\n    return;\n  }\n  var newValue = customizer\n    ? customizer(objValue, srcValue, (key + ''), object, source, stack)\n    : undefined;\n\n  var isCommon = newValue === undefined;\n\n  if (isCommon) {\n    var isArr = isArray(srcValue),\n        isBuff = !isArr && isBuffer(srcValue),\n        isTyped = !isArr && !isBuff && isTypedArray(srcValue);\n\n    newValue = srcValue;\n    if (isArr || isBuff || isTyped) {\n      if (isArray(objValue)) {\n        newValue = objValue;\n      }\n      else if (isArrayLikeObject(objValue)) {\n        newValue = copyArray(objValue);\n      }\n      else if (isBuff) {\n        isCommon = false;\n        newValue = cloneBuffer(srcValue, true);\n      }\n      else if (isTyped) {\n        isCommon = false;\n        newValue = cloneTypedArray(srcValue, true);\n      }\n      else {\n        newValue = [];\n      }\n    }\n    else if (isPlainObject(srcValue) || isArguments(srcValue)) {\n      newValue = objValue;\n      if (isArguments(objValue)) {\n        newValue = toPlainObject(objValue);\n      }\n      else if (!isObject(objValue) || isFunction(objValue)) {\n        newValue = initCloneObject(srcValue);\n      }\n    }\n    else {\n      isCommon = false;\n    }\n  }\n  if (isCommon) {\n    // Recursively merge objects and arrays (susceptible to call stack limits).\n    stack.set(srcValue, newValue);\n    mergeFunc(newValue, srcValue, srcIndex, customizer, stack);\n    stack['delete'](srcValue);\n  }\n  assignMergeValue(object, key, newValue);\n}\n\n/**\n * The base implementation of `_.rest` which doesn't validate or coerce arguments.\n *\n * @private\n * @param {Function} func The function to apply a rest parameter to.\n * @param {number} [start=func.length-1] The start position of the rest parameter.\n * @returns {Function} Returns the new function.\n */\nfunction baseRest(func, start) {\n  return setToString(overRest(func, start, identity), func + '');\n}\n\n/**\n * The base implementation of `setToString` without support for hot loop shorting.\n *\n * @private\n * @param {Function} func The function to modify.\n * @param {Function} string The `toString` result.\n * @returns {Function} Returns `func`.\n */\nvar baseSetToString = !defineProperty ? identity : function(func, string) {\n  return defineProperty(func, 'toString', {\n    'configurable': true,\n    'enumerable': false,\n    'value': constant(string),\n    'writable': true\n  });\n};\n\n/**\n * Creates a clone of  `buffer`.\n *\n * @private\n * @param {Buffer} buffer The buffer to clone.\n * @param {boolean} [isDeep] Specify a deep clone.\n * @returns {Buffer} Returns the cloned buffer.\n */\nfunction cloneBuffer(buffer, isDeep) {\n  if (isDeep) {\n    return buffer.slice();\n  }\n  var length = buffer.length,\n      result = allocUnsafe ? allocUnsafe(length) : new buffer.constructor(length);\n\n  buffer.copy(result);\n  return result;\n}\n\n/**\n * Creates a clone of `arrayBuffer`.\n *\n * @private\n * @param {ArrayBuffer} arrayBuffer The array buffer to clone.\n * @returns {ArrayBuffer} Returns the cloned array buffer.\n */\nfunction cloneArrayBuffer(arrayBuffer) {\n  var result = new arrayBuffer.constructor(arrayBuffer.byteLength);\n  new Uint8Array(result).set(new Uint8Array(arrayBuffer));\n  return result;\n}\n\n/**\n * Creates a clone of `typedArray`.\n *\n * @private\n * @param {Object} typedArray The typed array to clone.\n * @param {boolean} [isDeep] Specify a deep clone.\n * @returns {Object} Returns the cloned typed array.\n */\nfunction cloneTypedArray(typedArray, isDeep) {\n  var buffer = isDeep ? cloneArrayBuffer(typedArray.buffer) : typedArray.buffer;\n  return new typedArray.constructor(buffer, typedArray.byteOffset, typedArray.length);\n}\n\n/**\n * Copies the values of `source` to `array`.\n *\n * @private\n * @param {Array} source The array to copy values from.\n * @param {Array} [array=[]] The array to copy values to.\n * @returns {Array} Returns `array`.\n */\nfunction copyArray(source, array) {\n  var index = -1,\n      length = source.length;\n\n  array || (array = Array(length));\n  while (++index < length) {\n    array[index] = source[index];\n  }\n  return array;\n}\n\n/**\n * Copies properties of `source` to `object`.\n *\n * @private\n * @param {Object} source The object to copy properties from.\n * @param {Array} props The property identifiers to copy.\n * @param {Object} [object={}] The object to copy properties to.\n * @param {Function} [customizer] The function to customize copied values.\n * @returns {Object} Returns `object`.\n */\nfunction copyObject(source, props, object, customizer) {\n  var isNew = !object;\n  object || (object = {});\n\n  var index = -1,\n      length = props.length;\n\n  while (++index < length) {\n    var key = props[index];\n\n    var newValue = customizer\n      ? customizer(object[key], source[key], key, object, source)\n      : undefined;\n\n    if (newValue === undefined) {\n      newValue = source[key];\n    }\n    if (isNew) {\n      baseAssignValue(object, key, newValue);\n    } else {\n      assignValue(object, key, newValue);\n    }\n  }\n  return object;\n}\n\n/**\n * Creates a function like `_.assign`.\n *\n * @private\n * @param {Function} assigner The function to assign values.\n * @returns {Function} Returns the new assigner function.\n */\nfunction createAssigner(assigner) {\n  return baseRest(function(object, sources) {\n    var index = -1,\n        length = sources.length,\n        customizer = length > 1 ? sources[length - 1] : undefined,\n        guard = length > 2 ? sources[2] : undefined;\n\n    customizer = (assigner.length > 3 && typeof customizer == 'function')\n      ? (length--, customizer)\n      : undefined;\n\n    if (guard && isIterateeCall(sources[0], sources[1], guard)) {\n      customizer = length < 3 ? undefined : customizer;\n      length = 1;\n    }\n    object = Object(object);\n    while (++index < length) {\n      var source = sources[index];\n      if (source) {\n        assigner(object, source, index, customizer);\n      }\n    }\n    return object;\n  });\n}\n\n/**\n * Creates a base function for methods like `_.forIn` and `_.forOwn`.\n *\n * @private\n * @param {boolean} [fromRight] Specify iterating from right to left.\n * @returns {Function} Returns the new base function.\n */\nfunction createBaseFor(fromRight) {\n  return function(object, iteratee, keysFunc) {\n    var index = -1,\n        iterable = Object(object),\n        props = keysFunc(object),\n        length = props.length;\n\n    while (length--) {\n      var key = props[fromRight ? length : ++index];\n      if (iteratee(iterable[key], key, iterable) === false) {\n        break;\n      }\n    }\n    return object;\n  };\n}\n\n/**\n * Gets the data for `map`.\n *\n * @private\n * @param {Object} map The map to query.\n * @param {string} key The reference key.\n * @returns {*} Returns the map data.\n */\nfunction getMapData(map, key) {\n  var data = map.__data__;\n  return isKeyable(key)\n    ? data[typeof key == 'string' ? 'string' : 'hash']\n    : data.map;\n}\n\n/**\n * Gets the native function at `key` of `object`.\n *\n * @private\n * @param {Object} object The object to query.\n * @param {string} key The key of the method to get.\n * @returns {*} Returns the function if it's native, else `undefined`.\n */\nfunction getNative(object, key) {\n  var value = getValue(object, key);\n  return baseIsNative(value) ? value : undefined;\n}\n\n/**\n * A specialized version of `baseGetTag` which ignores `Symbol.toStringTag` values.\n *\n * @private\n * @param {*} value The value to query.\n * @returns {string} Returns the raw `toStringTag`.\n */\nfunction getRawTag(value) {\n  var isOwn = hasOwnProperty.call(value, symToStringTag),\n      tag = value[symToStringTag];\n\n  try {\n    value[symToStringTag] = undefined;\n    var unmasked = true;\n  } catch (e) {}\n\n  var result = nativeObjectToString.call(value);\n  if (unmasked) {\n    if (isOwn) {\n      value[symToStringTag] = tag;\n    } else {\n      delete value[symToStringTag];\n    }\n  }\n  return result;\n}\n\n/**\n * Initializes an object clone.\n *\n * @private\n * @param {Object} object The object to clone.\n * @returns {Object} Returns the initialized clone.\n */\nfunction initCloneObject(object) {\n  return (typeof object.constructor == 'function' && !isPrototype(object))\n    ? baseCreate(getPrototype(object))\n    : {};\n}\n\n/**\n * Checks if `value` is a valid array-like index.\n *\n * @private\n * @param {*} value The value to check.\n * @param {number} [length=MAX_SAFE_INTEGER] The upper bounds of a valid index.\n * @returns {boolean} Returns `true` if `value` is a valid index, else `false`.\n */\nfunction isIndex(value, length) {\n  var type = typeof value;\n  length = length == null ? MAX_SAFE_INTEGER : length;\n\n  return !!length &&\n    (type == 'number' ||\n      (type != 'symbol' && reIsUint.test(value))) &&\n        (value > -1 && value % 1 == 0 && value < length);\n}\n\n/**\n * Checks if the given arguments are from an iteratee call.\n *\n * @private\n * @param {*} value The potential iteratee value argument.\n * @param {*} index The potential iteratee index or key argument.\n * @param {*} object The potential iteratee object argument.\n * @returns {boolean} Returns `true` if the arguments are from an iteratee call,\n *  else `false`.\n */\nfunction isIterateeCall(value, index, object) {\n  if (!isObject(object)) {\n    return false;\n  }\n  var type = typeof index;\n  if (type == 'number'\n        ? (isArrayLike(object) && isIndex(index, object.length))\n        : (type == 'string' && index in object)\n      ) {\n    return eq(object[index], value);\n  }\n  return false;\n}\n\n/**\n * Checks if `value` is suitable for use as unique object key.\n *\n * @private\n * @param {*} value The value to check.\n * @returns {boolean} Returns `true` if `value` is suitable, else `false`.\n */\nfunction isKeyable(value) {\n  var type = typeof value;\n  return (type == 'string' || type == 'number' || type == 'symbol' || type == 'boolean')\n    ? (value !== '__proto__')\n    : (value === null);\n}\n\n/**\n * Checks if `func` has its source masked.\n *\n * @private\n * @param {Function} func The function to check.\n * @returns {boolean} Returns `true` if `func` is masked, else `false`.\n */\nfunction isMasked(func) {\n  return !!maskSrcKey && (maskSrcKey in func);\n}\n\n/**\n * Checks if `value` is likely a prototype object.\n *\n * @private\n * @param {*} value The value to check.\n * @returns {boolean} Returns `true` if `value` is a prototype, else `false`.\n */\nfunction isPrototype(value) {\n  var Ctor = value && value.constructor,\n      proto = (typeof Ctor == 'function' && Ctor.prototype) || objectProto;\n\n  return value === proto;\n}\n\n/**\n * This function is like\n * [`Object.keys`](http://ecma-international.org/ecma-262/7.0/#sec-object.keys)\n * except that it includes inherited enumerable properties.\n *\n * @private\n * @param {Object} object The object to query.\n * @returns {Array} Returns the array of property names.\n */\nfunction nativeKeysIn(object) {\n  var result = [];\n  if (object != null) {\n    for (var key in Object(object)) {\n      result.push(key);\n    }\n  }\n  return result;\n}\n\n/**\n * Converts `value` to a string using `Object.prototype.toString`.\n *\n * @private\n * @param {*} value The value to convert.\n * @returns {string} Returns the converted string.\n */\nfunction objectToString(value) {\n  return nativeObjectToString.call(value);\n}\n\n/**\n * A specialized version of `baseRest` which transforms the rest array.\n *\n * @private\n * @param {Function} func The function to apply a rest parameter to.\n * @param {number} [start=func.length-1] The start position of the rest parameter.\n * @param {Function} transform The rest array transform.\n * @returns {Function} Returns the new function.\n */\nfunction overRest(func, start, transform) {\n  start = nativeMax(start === undefined ? (func.length - 1) : start, 0);\n  return function() {\n    var args = arguments,\n        index = -1,\n        length = nativeMax(args.length - start, 0),\n        array = Array(length);\n\n    while (++index < length) {\n      array[index] = args[start + index];\n    }\n    index = -1;\n    var otherArgs = Array(start + 1);\n    while (++index < start) {\n      otherArgs[index] = args[index];\n    }\n    otherArgs[start] = transform(array);\n    return apply(func, this, otherArgs);\n  };\n}\n\n/**\n * Gets the value at `key`, unless `key` is \"__proto__\" or \"constructor\".\n *\n * @private\n * @param {Object} object The object to query.\n * @param {string} key The key of the property to get.\n * @returns {*} Returns the property value.\n */\nfunction safeGet(object, key) {\n  if (key === 'constructor' && typeof object[key] === 'function') {\n    return;\n  }\n\n  if (key == '__proto__') {\n    return;\n  }\n\n  return object[key];\n}\n\n/**\n * Sets the `toString` method of `func` to return `string`.\n *\n * @private\n * @param {Function} func The function to modify.\n * @param {Function} string The `toString` result.\n * @returns {Function} Returns `func`.\n */\nvar setToString = shortOut(baseSetToString);\n\n/**\n * Creates a function that'll short out and invoke `identity` instead\n * of `func` when it's called `HOT_COUNT` or more times in `HOT_SPAN`\n * milliseconds.\n *\n * @private\n * @param {Function} func The function to restrict.\n * @returns {Function} Returns the new shortable function.\n */\nfunction shortOut(func) {\n  var count = 0,\n      lastCalled = 0;\n\n  return function() {\n    var stamp = nativeNow(),\n        remaining = HOT_SPAN - (stamp - lastCalled);\n\n    lastCalled = stamp;\n    if (remaining > 0) {\n      if (++count >= HOT_COUNT) {\n        return arguments[0];\n      }\n    } else {\n      count = 0;\n    }\n    return func.apply(undefined, arguments);\n  };\n}\n\n/**\n * Converts `func` to its source code.\n *\n * @private\n * @param {Function} func The function to convert.\n * @returns {string} Returns the source code.\n */\nfunction toSource(func) {\n  if (func != null) {\n    try {\n      return funcToString.call(func);\n    } catch (e) {}\n    try {\n      return (func + '');\n    } catch (e) {}\n  }\n  return '';\n}\n\n/**\n * Performs a\n * [`SameValueZero`](http://ecma-international.org/ecma-262/7.0/#sec-samevaluezero)\n * comparison between two values to determine if they are equivalent.\n *\n * @static\n * @memberOf _\n * @since 4.0.0\n * @category Lang\n * @param {*} value The value to compare.\n * @param {*} other The other value to compare.\n * @returns {boolean} Returns `true` if the values are equivalent, else `false`.\n * @example\n *\n * var object = { 'a': 1 };\n * var other = { 'a': 1 };\n *\n * _.eq(object, object);\n * // => true\n *\n * _.eq(object, other);\n * // => false\n *\n * _.eq('a', 'a');\n * // => true\n *\n * _.eq('a', Object('a'));\n * // => false\n *\n * _.eq(NaN, NaN);\n * // => true\n */\nfunction eq(value, other) {\n  return value === other || (value !== value && other !== other);\n}\n\n/**\n * Checks if `value` is likely an `arguments` object.\n *\n * @static\n * @memberOf _\n * @since 0.1.0\n * @category Lang\n * @param {*} value The value to check.\n * @returns {boolean} Returns `true` if `value` is an `arguments` object,\n *  else `false`.\n * @example\n *\n * _.isArguments(function() { return arguments; }());\n * // => true\n *\n * _.isArguments([1, 2, 3]);\n * // => false\n */\nvar isArguments = baseIsArguments(function() { return arguments; }()) ? baseIsArguments : function(value) {\n  return isObjectLike(value) && hasOwnProperty.call(value, 'callee') &&\n    !propertyIsEnumerable.call(value, 'callee');\n};\n\n/**\n * Checks if `value` is classified as an `Array` object.\n *\n * @static\n * @memberOf _\n * @since 0.1.0\n * @category Lang\n * @param {*} value The value to check.\n * @returns {boolean} Returns `true` if `value` is an array, else `false`.\n * @example\n *\n * _.isArray([1, 2, 3]);\n * // => true\n *\n * _.isArray(document.body.children);\n * // => false\n *\n * _.isArray('abc');\n * // => false\n *\n * _.isArray(_.noop);\n * // => false\n */\nvar isArray = Array.isArray;\n\n/**\n * Checks if `value` is array-like. A value is considered array-like if it's\n * not a function and has a `value.length` that's an integer greater than or\n * equal to `0` and less than or equal to `Number.MAX_SAFE_INTEGER`.\n *\n * @static\n * @memberOf _\n * @since 4.0.0\n * @category Lang\n * @param {*} value The value to check.\n * @returns {boolean} Returns `true` if `value` is array-like, else `false`.\n * @example\n *\n * _.isArrayLike([1, 2, 3]);\n * // => true\n *\n * _.isArrayLike(document.body.children);\n * // => true\n *\n * _.isArrayLike('abc');\n * // => true\n *\n * _.isArrayLike(_.noop);\n * // => false\n */\nfunction isArrayLike(value) {\n  return value != null && isLength(value.length) && !isFunction(value);\n}\n\n/**\n * This method is like `_.isArrayLike` except that it also checks if `value`\n * is an object.\n *\n * @static\n * @memberOf _\n * @since 4.0.0\n * @category Lang\n * @param {*} value The value to check.\n * @returns {boolean} Returns `true` if `value` is an array-like object,\n *  else `false`.\n * @example\n *\n * _.isArrayLikeObject([1, 2, 3]);\n * // => true\n *\n * _.isArrayLikeObject(document.body.children);\n * // => true\n *\n * _.isArrayLikeObject('abc');\n * // => false\n *\n * _.isArrayLikeObject(_.noop);\n * // => false\n */\nfunction isArrayLikeObject(value) {\n  return isObjectLike(value) && isArrayLike(value);\n}\n\n/**\n * Checks if `value` is a buffer.\n *\n * @static\n * @memberOf _\n * @since 4.3.0\n * @category Lang\n * @param {*} value The value to check.\n * @returns {boolean} Returns `true` if `value` is a buffer, else `false`.\n * @example\n *\n * _.isBuffer(new Buffer(2));\n * // => true\n *\n * _.isBuffer(new Uint8Array(2));\n * // => false\n */\nvar isBuffer = nativeIsBuffer || stubFalse;\n\n/**\n * Checks if `value` is classified as a `Function` object.\n *\n * @static\n * @memberOf _\n * @since 0.1.0\n * @category Lang\n * @param {*} value The value to check.\n * @returns {boolean} Returns `true` if `value` is a function, else `false`.\n * @example\n *\n * _.isFunction(_);\n * // => true\n *\n * _.isFunction(/abc/);\n * // => false\n */\nfunction isFunction(value) {\n  if (!isObject(value)) {\n    return false;\n  }\n  // The use of `Object#toString` avoids issues with the `typeof` operator\n  // in Safari 9 which returns 'object' for typed arrays and other constructors.\n  var tag = baseGetTag(value);\n  return tag == funcTag || tag == genTag || tag == asyncTag || tag == proxyTag;\n}\n\n/**\n * Checks if `value` is a valid array-like length.\n *\n * **Note:** This method is loosely based on\n * [`ToLength`](http://ecma-international.org/ecma-262/7.0/#sec-tolength).\n *\n * @static\n * @memberOf _\n * @since 4.0.0\n * @category Lang\n * @param {*} value The value to check.\n * @returns {boolean} Returns `true` if `value` is a valid length, else `false`.\n * @example\n *\n * _.isLength(3);\n * // => true\n *\n * _.isLength(Number.MIN_VALUE);\n * // => false\n *\n * _.isLength(Infinity);\n * // => false\n *\n * _.isLength('3');\n * // => false\n */\nfunction isLength(value) {\n  return typeof value == 'number' &&\n    value > -1 && value % 1 == 0 && value <= MAX_SAFE_INTEGER;\n}\n\n/**\n * Checks if `value` is the\n * [language type](http://www.ecma-international.org/ecma-262/7.0/#sec-ecmascript-language-types)\n * of `Object`. (e.g. arrays, functions, objects, regexes, `new Number(0)`, and `new String('')`)\n *\n * @static\n * @memberOf _\n * @since 0.1.0\n * @category Lang\n * @param {*} value The value to check.\n * @returns {boolean} Returns `true` if `value` is an object, else `false`.\n * @example\n *\n * _.isObject({});\n * // => true\n *\n * _.isObject([1, 2, 3]);\n * // => true\n *\n * _.isObject(_.noop);\n * // => true\n *\n * _.isObject(null);\n * // => false\n */\nfunction isObject(value) {\n  var type = typeof value;\n  return value != null && (type == 'object' || type == 'function');\n}\n\n/**\n * Checks if `value` is object-like. A value is object-like if it's not `null`\n * and has a `typeof` result of \"object\".\n *\n * @static\n * @memberOf _\n * @since 4.0.0\n * @category Lang\n * @param {*} value The value to check.\n * @returns {boolean} Returns `true` if `value` is object-like, else `false`.\n * @example\n *\n * _.isObjectLike({});\n * // => true\n *\n * _.isObjectLike([1, 2, 3]);\n * // => true\n *\n * _.isObjectLike(_.noop);\n * // => false\n *\n * _.isObjectLike(null);\n * // => false\n */\nfunction isObjectLike(value) {\n  return value != null && typeof value == 'object';\n}\n\n/**\n * Checks if `value` is a plain object, that is, an object created by the\n * `Object` constructor or one with a `[[Prototype]]` of `null`.\n *\n * @static\n * @memberOf _\n * @since 0.8.0\n * @category Lang\n * @param {*} value The value to check.\n * @returns {boolean} Returns `true` if `value` is a plain object, else `false`.\n * @example\n *\n * function Foo() {\n *   this.a = 1;\n * }\n *\n * _.isPlainObject(new Foo);\n * // => false\n *\n * _.isPlainObject([1, 2, 3]);\n * // => false\n *\n * _.isPlainObject({ 'x': 0, 'y': 0 });\n * // => true\n *\n * _.isPlainObject(Object.create(null));\n * // => true\n */\nfunction isPlainObject(value) {\n  if (!isObjectLike(value) || baseGetTag(value) != objectTag) {\n    return false;\n  }\n  var proto = getPrototype(value);\n  if (proto === null) {\n    return true;\n  }\n  var Ctor = hasOwnProperty.call(proto, 'constructor') && proto.constructor;\n  return typeof Ctor == 'function' && Ctor instanceof Ctor &&\n    funcToString.call(Ctor) == objectCtorString;\n}\n\n/**\n * Checks if `value` is classified as a typed array.\n *\n * @static\n * @memberOf _\n * @since 3.0.0\n * @category Lang\n * @param {*} value The value to check.\n * @returns {boolean} Returns `true` if `value` is a typed array, else `false`.\n * @example\n *\n * _.isTypedArray(new Uint8Array);\n * // => true\n *\n * _.isTypedArray([]);\n * // => false\n */\nvar isTypedArray = nodeIsTypedArray ? baseUnary(nodeIsTypedArray) : baseIsTypedArray;\n\n/**\n * Converts `value` to a plain object flattening inherited enumerable string\n * keyed properties of `value` to own properties of the plain object.\n *\n * @static\n * @memberOf _\n * @since 3.0.0\n * @category Lang\n * @param {*} value The value to convert.\n * @returns {Object} Returns the converted plain object.\n * @example\n *\n * function Foo() {\n *   this.b = 2;\n * }\n *\n * Foo.prototype.c = 3;\n *\n * _.assign({ 'a': 1 }, new Foo);\n * // => { 'a': 1, 'b': 2 }\n *\n * _.assign({ 'a': 1 }, _.toPlainObject(new Foo));\n * // => { 'a': 1, 'b': 2, 'c': 3 }\n */\nfunction toPlainObject(value) {\n  return copyObject(value, keysIn(value));\n}\n\n/**\n * Creates an array of the own and inherited enumerable property names of `object`.\n *\n * **Note:** Non-object values are coerced to objects.\n *\n * @static\n * @memberOf _\n * @since 3.0.0\n * @category Object\n * @param {Object} object The object to query.\n * @returns {Array} Returns the array of property names.\n * @example\n *\n * function Foo() {\n *   this.a = 1;\n *   this.b = 2;\n * }\n *\n * Foo.prototype.c = 3;\n *\n * _.keysIn(new Foo);\n * // => ['a', 'b', 'c'] (iteration order is not guaranteed)\n */\nfunction keysIn(object) {\n  return isArrayLike(object) ? arrayLikeKeys(object, true) : baseKeysIn(object);\n}\n\n/**\n * This method is like `_.merge` except that it accepts `customizer` which\n * is invoked to produce the merged values of the destination and source\n * properties. If `customizer` returns `undefined`, merging is handled by the\n * method instead. The `customizer` is invoked with six arguments:\n * (objValue, srcValue, key, object, source, stack).\n *\n * **Note:** This method mutates `object`.\n *\n * @static\n * @memberOf _\n * @since 4.0.0\n * @category Object\n * @param {Object} object The destination object.\n * @param {...Object} sources The source objects.\n * @param {Function} customizer The function to customize assigned values.\n * @returns {Object} Returns `object`.\n * @example\n *\n * function customizer(objValue, srcValue) {\n *   if (_.isArray(objValue)) {\n *     return objValue.concat(srcValue);\n *   }\n * }\n *\n * var object = { 'a': [1], 'b': [2] };\n * var other = { 'a': [3], 'b': [4] };\n *\n * _.mergeWith(object, other, customizer);\n * // => { 'a': [1, 3], 'b': [2, 4] }\n */\nvar mergeWith = createAssigner(function(object, source, srcIndex, customizer) {\n  baseMerge(object, source, srcIndex, customizer);\n});\n\n/**\n * Creates a function that returns `value`.\n *\n * @static\n * @memberOf _\n * @since 2.4.0\n * @category Util\n * @param {*} value The value to return from the new function.\n * @returns {Function} Returns the new constant function.\n * @example\n *\n * var objects = _.times(2, _.constant({ 'a': 1 }));\n *\n * console.log(objects);\n * // => [{ 'a': 1 }, { 'a': 1 }]\n *\n * console.log(objects[0] === objects[1]);\n * // => true\n */\nfunction constant(value) {\n  return function() {\n    return value;\n  };\n}\n\n/**\n * This method returns the first argument it receives.\n *\n * @static\n * @since 0.1.0\n * @memberOf _\n * @category Util\n * @param {*} value Any value.\n * @returns {*} Returns `value`.\n * @example\n *\n * var object = { 'a': 1 };\n *\n * console.log(_.identity(object) === object);\n * // => true\n */\nfunction identity(value) {\n  return value;\n}\n\n/**\n * This method returns `false`.\n *\n * @static\n * @memberOf _\n * @since 4.13.0\n * @category Util\n * @returns {boolean} Returns `false`.\n * @example\n *\n * _.times(2, _.stubFalse);\n * // => [false, false]\n */\nfunction stubFalse() {\n  return false;\n}\n\nmodule.exports = mergeWith;\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/lodash.mergewith/index.js?");

/***/ }),

/***/ "./node_modules/normalize-wheel/index.js":
/*!***********************************************!*\
  !*** ./node_modules/normalize-wheel/index.js ***!
  \***********************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

eval("module.exports = __webpack_require__(/*! ./src/normalizeWheel.js */ \"./node_modules/normalize-wheel/src/normalizeWheel.js\");\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/normalize-wheel/index.js?");

/***/ }),

/***/ "./node_modules/normalize-wheel/src/ExecutionEnvironment.js":
/*!******************************************************************!*\
  !*** ./node_modules/normalize-wheel/src/ExecutionEnvironment.js ***!
  \******************************************************************/
/***/ ((module) => {

"use strict";
eval("/**\n * Copyright (c) 2015, Facebook, Inc.\n * All rights reserved.\n *\n * This source code is licensed under the BSD-style license found in the\n * LICENSE file in the root directory of this source tree. An additional grant\n * of patent rights can be found in the PATENTS file in the same directory.\n *\n * @providesModule ExecutionEnvironment\n */\n\n/*jslint evil: true */\n\n\n\nvar canUseDOM = !!(\n  typeof window !== 'undefined' &&\n  window.document &&\n  window.document.createElement\n);\n\n/**\n * Simple, lightweight module assisting with the detection and context of\n * Worker. Helps avoid circular dependencies and allows code to reason about\n * whether or not they are in a Worker, even if they never include the main\n * `ReactWorker` dependency.\n */\nvar ExecutionEnvironment = {\n\n  canUseDOM: canUseDOM,\n\n  canUseWorkers: typeof Worker !== 'undefined',\n\n  canUseEventListeners:\n    canUseDOM && !!(window.addEventListener || window.attachEvent),\n\n  canUseViewport: canUseDOM && !!window.screen,\n\n  isInWorker: !canUseDOM // For now, this is true - might change in the future.\n\n};\n\nmodule.exports = ExecutionEnvironment;\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/normalize-wheel/src/ExecutionEnvironment.js?");

/***/ }),

/***/ "./node_modules/normalize-wheel/src/UserAgent_DEPRECATED.js":
/*!******************************************************************!*\
  !*** ./node_modules/normalize-wheel/src/UserAgent_DEPRECATED.js ***!
  \******************************************************************/
/***/ ((module) => {

eval("/**\n * Copyright 2004-present Facebook. All Rights Reserved.\n *\n * @providesModule UserAgent_DEPRECATED\n */\n\n/**\n *  Provides entirely client-side User Agent and OS detection. You should prefer\n *  the non-deprecated UserAgent module when possible, which exposes our\n *  authoritative server-side PHP-based detection to the client.\n *\n *  Usage is straightforward:\n *\n *    if (UserAgent_DEPRECATED.ie()) {\n *      //  IE\n *    }\n *\n *  You can also do version checks:\n *\n *    if (UserAgent_DEPRECATED.ie() >= 7) {\n *      //  IE7 or better\n *    }\n *\n *  The browser functions will return NaN if the browser does not match, so\n *  you can also do version compares the other way:\n *\n *    if (UserAgent_DEPRECATED.ie() < 7) {\n *      //  IE6 or worse\n *    }\n *\n *  Note that the version is a float and may include a minor version number,\n *  so you should always use range operators to perform comparisons, not\n *  strict equality.\n *\n *  **Note:** You should **strongly** prefer capability detection to browser\n *  version detection where it's reasonable:\n *\n *    http://www.quirksmode.org/js/support.html\n *\n *  Further, we have a large number of mature wrapper functions and classes\n *  which abstract away many browser irregularities. Check the documentation,\n *  grep for things, or ask on javascript@lists.facebook.com before writing yet\n *  another copy of \"event || window.event\".\n *\n */\n\nvar _populated = false;\n\n// Browsers\nvar _ie, _firefox, _opera, _webkit, _chrome;\n\n// Actual IE browser for compatibility mode\nvar _ie_real_version;\n\n// Platforms\nvar _osx, _windows, _linux, _android;\n\n// Architectures\nvar _win64;\n\n// Devices\nvar _iphone, _ipad, _native;\n\nvar _mobile;\n\nfunction _populate() {\n  if (_populated) {\n    return;\n  }\n\n  _populated = true;\n\n  // To work around buggy JS libraries that can't handle multi-digit\n  // version numbers, Opera 10's user agent string claims it's Opera\n  // 9, then later includes a Version/X.Y field:\n  //\n  // Opera/9.80 (foo) Presto/2.2.15 Version/10.10\n  var uas = navigator.userAgent;\n  var agent = /(?:MSIE.(\\d+\\.\\d+))|(?:(?:Firefox|GranParadiso|Iceweasel).(\\d+\\.\\d+))|(?:Opera(?:.+Version.|.)(\\d+\\.\\d+))|(?:AppleWebKit.(\\d+(?:\\.\\d+)?))|(?:Trident\\/\\d+\\.\\d+.*rv:(\\d+\\.\\d+))/.exec(uas);\n  var os    = /(Mac OS X)|(Windows)|(Linux)/.exec(uas);\n\n  _iphone = /\\b(iPhone|iP[ao]d)/.exec(uas);\n  _ipad = /\\b(iP[ao]d)/.exec(uas);\n  _android = /Android/i.exec(uas);\n  _native = /FBAN\\/\\w+;/i.exec(uas);\n  _mobile = /Mobile/i.exec(uas);\n\n  // Note that the IE team blog would have you believe you should be checking\n  // for 'Win64; x64'.  But MSDN then reveals that you can actually be coming\n  // from either x64 or ia64;  so ultimately, you should just check for Win64\n  // as in indicator of whether you're in 64-bit IE.  32-bit IE on 64-bit\n  // Windows will send 'WOW64' instead.\n  _win64 = !!(/Win64/.exec(uas));\n\n  if (agent) {\n    _ie = agent[1] ? parseFloat(agent[1]) : (\n          agent[5] ? parseFloat(agent[5]) : NaN);\n    // IE compatibility mode\n    if (_ie && document && document.documentMode) {\n      _ie = document.documentMode;\n    }\n    // grab the \"true\" ie version from the trident token if available\n    var trident = /(?:Trident\\/(\\d+.\\d+))/.exec(uas);\n    _ie_real_version = trident ? parseFloat(trident[1]) + 4 : _ie;\n\n    _firefox = agent[2] ? parseFloat(agent[2]) : NaN;\n    _opera   = agent[3] ? parseFloat(agent[3]) : NaN;\n    _webkit  = agent[4] ? parseFloat(agent[4]) : NaN;\n    if (_webkit) {\n      // We do not add the regexp to the above test, because it will always\n      // match 'safari' only since 'AppleWebKit' appears before 'Chrome' in\n      // the userAgent string.\n      agent = /(?:Chrome\\/(\\d+\\.\\d+))/.exec(uas);\n      _chrome = agent && agent[1] ? parseFloat(agent[1]) : NaN;\n    } else {\n      _chrome = NaN;\n    }\n  } else {\n    _ie = _firefox = _opera = _chrome = _webkit = NaN;\n  }\n\n  if (os) {\n    if (os[1]) {\n      // Detect OS X version.  If no version number matches, set _osx to true.\n      // Version examples:  10, 10_6_1, 10.7\n      // Parses version number as a float, taking only first two sets of\n      // digits.  If only one set of digits is found, returns just the major\n      // version number.\n      var ver = /(?:Mac OS X (\\d+(?:[._]\\d+)?))/.exec(uas);\n\n      _osx = ver ? parseFloat(ver[1].replace('_', '.')) : true;\n    } else {\n      _osx = false;\n    }\n    _windows = !!os[2];\n    _linux   = !!os[3];\n  } else {\n    _osx = _windows = _linux = false;\n  }\n}\n\nvar UserAgent_DEPRECATED = {\n\n  /**\n   *  Check if the UA is Internet Explorer.\n   *\n   *\n   *  @return float|NaN Version number (if match) or NaN.\n   */\n  ie: function() {\n    return _populate() || _ie;\n  },\n\n  /**\n   * Check if we're in Internet Explorer compatibility mode.\n   *\n   * @return bool true if in compatibility mode, false if\n   * not compatibility mode or not ie\n   */\n  ieCompatibilityMode: function() {\n    return _populate() || (_ie_real_version > _ie);\n  },\n\n\n  /**\n   * Whether the browser is 64-bit IE.  Really, this is kind of weak sauce;  we\n   * only need this because Skype can't handle 64-bit IE yet.  We need to remove\n   * this when we don't need it -- tracked by #601957.\n   */\n  ie64: function() {\n    return UserAgent_DEPRECATED.ie() && _win64;\n  },\n\n  /**\n   *  Check if the UA is Firefox.\n   *\n   *\n   *  @return float|NaN Version number (if match) or NaN.\n   */\n  firefox: function() {\n    return _populate() || _firefox;\n  },\n\n\n  /**\n   *  Check if the UA is Opera.\n   *\n   *\n   *  @return float|NaN Version number (if match) or NaN.\n   */\n  opera: function() {\n    return _populate() || _opera;\n  },\n\n\n  /**\n   *  Check if the UA is WebKit.\n   *\n   *\n   *  @return float|NaN Version number (if match) or NaN.\n   */\n  webkit: function() {\n    return _populate() || _webkit;\n  },\n\n  /**\n   *  For Push\n   *  WILL BE REMOVED VERY SOON. Use UserAgent_DEPRECATED.webkit\n   */\n  safari: function() {\n    return UserAgent_DEPRECATED.webkit();\n  },\n\n  /**\n   *  Check if the UA is a Chrome browser.\n   *\n   *\n   *  @return float|NaN Version number (if match) or NaN.\n   */\n  chrome : function() {\n    return _populate() || _chrome;\n  },\n\n\n  /**\n   *  Check if the user is running Windows.\n   *\n   *  @return bool `true' if the user's OS is Windows.\n   */\n  windows: function() {\n    return _populate() || _windows;\n  },\n\n\n  /**\n   *  Check if the user is running Mac OS X.\n   *\n   *  @return float|bool   Returns a float if a version number is detected,\n   *                       otherwise true/false.\n   */\n  osx: function() {\n    return _populate() || _osx;\n  },\n\n  /**\n   * Check if the user is running Linux.\n   *\n   * @return bool `true' if the user's OS is some flavor of Linux.\n   */\n  linux: function() {\n    return _populate() || _linux;\n  },\n\n  /**\n   * Check if the user is running on an iPhone or iPod platform.\n   *\n   * @return bool `true' if the user is running some flavor of the\n   *    iPhone OS.\n   */\n  iphone: function() {\n    return _populate() || _iphone;\n  },\n\n  mobile: function() {\n    return _populate() || (_iphone || _ipad || _android || _mobile);\n  },\n\n  nativeApp: function() {\n    // webviews inside of the native apps\n    return _populate() || _native;\n  },\n\n  android: function() {\n    return _populate() || _android;\n  },\n\n  ipad: function() {\n    return _populate() || _ipad;\n  }\n};\n\nmodule.exports = UserAgent_DEPRECATED;\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/normalize-wheel/src/UserAgent_DEPRECATED.js?");

/***/ }),

/***/ "./node_modules/normalize-wheel/src/isEventSupported.js":
/*!**************************************************************!*\
  !*** ./node_modules/normalize-wheel/src/isEventSupported.js ***!
  \**************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";
eval("/**\n * Copyright 2013-2015, Facebook, Inc.\n * All rights reserved.\n *\n * This source code is licensed under the BSD-style license found in the\n * LICENSE file in the root directory of this source tree. An additional grant\n * of patent rights can be found in the PATENTS file in the same directory.\n *\n * @providesModule isEventSupported\n */\n\n\n\nvar ExecutionEnvironment = __webpack_require__(/*! ./ExecutionEnvironment */ \"./node_modules/normalize-wheel/src/ExecutionEnvironment.js\");\n\nvar useHasFeature;\nif (ExecutionEnvironment.canUseDOM) {\n  useHasFeature =\n    document.implementation &&\n    document.implementation.hasFeature &&\n    // always returns true in newer browsers as per the standard.\n    // @see http://dom.spec.whatwg.org/#dom-domimplementation-hasfeature\n    document.implementation.hasFeature('', '') !== true;\n}\n\n/**\n * Checks if an event is supported in the current execution environment.\n *\n * NOTE: This will not work correctly for non-generic events such as `change`,\n * `reset`, `load`, `error`, and `select`.\n *\n * Borrows from Modernizr.\n *\n * @param {string} eventNameSuffix Event name, e.g. \"click\".\n * @param {?boolean} capture Check if the capture phase is supported.\n * @return {boolean} True if the event is supported.\n * @internal\n * @license Modernizr 3.0.0pre (Custom Build) | MIT\n */\nfunction isEventSupported(eventNameSuffix, capture) {\n  if (!ExecutionEnvironment.canUseDOM ||\n      capture && !('addEventListener' in document)) {\n    return false;\n  }\n\n  var eventName = 'on' + eventNameSuffix;\n  var isSupported = eventName in document;\n\n  if (!isSupported) {\n    var element = document.createElement('div');\n    element.setAttribute(eventName, 'return;');\n    isSupported = typeof element[eventName] === 'function';\n  }\n\n  if (!isSupported && useHasFeature && eventNameSuffix === 'wheel') {\n    // This is the only way to test support for the `wheel` event in IE9+.\n    isSupported = document.implementation.hasFeature('Events.wheel', '3.0');\n  }\n\n  return isSupported;\n}\n\nmodule.exports = isEventSupported;\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/normalize-wheel/src/isEventSupported.js?");

/***/ }),

/***/ "./node_modules/normalize-wheel/src/normalizeWheel.js":
/*!************************************************************!*\
  !*** ./node_modules/normalize-wheel/src/normalizeWheel.js ***!
  \************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {

"use strict";
eval("/**\n * Copyright (c) 2015, Facebook, Inc.\n * All rights reserved.\n *\n * This source code is licensed under the BSD-style license found in the\n * LICENSE file in the root directory of this source tree. An additional grant\n * of patent rights can be found in the PATENTS file in the same directory.\n *\n * @providesModule normalizeWheel\n * @typechecks\n */\n\n\n\nvar UserAgent_DEPRECATED = __webpack_require__(/*! ./UserAgent_DEPRECATED */ \"./node_modules/normalize-wheel/src/UserAgent_DEPRECATED.js\");\n\nvar isEventSupported = __webpack_require__(/*! ./isEventSupported */ \"./node_modules/normalize-wheel/src/isEventSupported.js\");\n\n\n// Reasonable defaults\nvar PIXEL_STEP  = 10;\nvar LINE_HEIGHT = 40;\nvar PAGE_HEIGHT = 800;\n\n/**\n * Mouse wheel (and 2-finger trackpad) support on the web sucks.  It is\n * complicated, thus this doc is long and (hopefully) detailed enough to answer\n * your questions.\n *\n * If you need to react to the mouse wheel in a predictable way, this code is\n * like your bestest friend. * hugs *\n *\n * As of today, there are 4 DOM event types you can listen to:\n *\n *   'wheel'                -- Chrome(31+), FF(17+), IE(9+)\n *   'mousewheel'           -- Chrome, IE(6+), Opera, Safari\n *   'MozMousePixelScroll'  -- FF(3.5 only!) (2010-2013) -- don't bother!\n *   'DOMMouseScroll'       -- FF(0.9.7+) since 2003\n *\n * So what to do?  The is the best:\n *\n *   normalizeWheel.getEventType();\n *\n * In your event callback, use this code to get sane interpretation of the\n * deltas.  This code will return an object with properties:\n *\n *   spinX   -- normalized spin speed (use for zoom) - x plane\n *   spinY   -- \" - y plane\n *   pixelX  -- normalized distance (to pixels) - x plane\n *   pixelY  -- \" - y plane\n *\n * Wheel values are provided by the browser assuming you are using the wheel to\n * scroll a web page by a number of lines or pixels (or pages).  Values can vary\n * significantly on different platforms and browsers, forgetting that you can\n * scroll at different speeds.  Some devices (like trackpads) emit more events\n * at smaller increments with fine granularity, and some emit massive jumps with\n * linear speed or acceleration.\n *\n * This code does its best to normalize the deltas for you:\n *\n *   - spin is trying to normalize how far the wheel was spun (or trackpad\n *     dragged).  This is super useful for zoom support where you want to\n *     throw away the chunky scroll steps on the PC and make those equal to\n *     the slow and smooth tiny steps on the Mac. Key data: This code tries to\n *     resolve a single slow step on a wheel to 1.\n *\n *   - pixel is normalizing the desired scroll delta in pixel units.  You'll\n *     get the crazy differences between browsers, but at least it'll be in\n *     pixels!\n *\n *   - positive value indicates scrolling DOWN/RIGHT, negative UP/LEFT.  This\n *     should translate to positive value zooming IN, negative zooming OUT.\n *     This matches the newer 'wheel' event.\n *\n * Why are there spinX, spinY (or pixels)?\n *\n *   - spinX is a 2-finger side drag on the trackpad, and a shift + wheel turn\n *     with a mouse.  It results in side-scrolling in the browser by default.\n *\n *   - spinY is what you expect -- it's the classic axis of a mouse wheel.\n *\n *   - I dropped spinZ/pixelZ.  It is supported by the DOM 3 'wheel' event and\n *     probably is by browsers in conjunction with fancy 3D controllers .. but\n *     you know.\n *\n * Implementation info:\n *\n * Examples of 'wheel' event if you scroll slowly (down) by one step with an\n * average mouse:\n *\n *   OS X + Chrome  (mouse)     -    4   pixel delta  (wheelDelta -120)\n *   OS X + Safari  (mouse)     -  N/A   pixel delta  (wheelDelta  -12)\n *   OS X + Firefox (mouse)     -    0.1 line  delta  (wheelDelta  N/A)\n *   Win8 + Chrome  (mouse)     -  100   pixel delta  (wheelDelta -120)\n *   Win8 + Firefox (mouse)     -    3   line  delta  (wheelDelta -120)\n *\n * On the trackpad:\n *\n *   OS X + Chrome  (trackpad)  -    2   pixel delta  (wheelDelta   -6)\n *   OS X + Firefox (trackpad)  -    1   pixel delta  (wheelDelta  N/A)\n *\n * On other/older browsers.. it's more complicated as there can be multiple and\n * also missing delta values.\n *\n * The 'wheel' event is more standard:\n *\n * http://www.w3.org/TR/DOM-Level-3-Events/#events-wheelevents\n *\n * The basics is that it includes a unit, deltaMode (pixels, lines, pages), and\n * deltaX, deltaY and deltaZ.  Some browsers provide other values to maintain\n * backward compatibility with older events.  Those other values help us\n * better normalize spin speed.  Example of what the browsers provide:\n *\n *                          | event.wheelDelta | event.detail\n *        ------------------+------------------+--------------\n *          Safari v5/OS X  |       -120       |       0\n *          Safari v5/Win7  |       -120       |       0\n *         Chrome v17/OS X  |       -120       |       0\n *         Chrome v17/Win7  |       -120       |       0\n *                IE9/Win7  |       -120       |   undefined\n *         Firefox v4/OS X  |     undefined    |       1\n *         Firefox v4/Win7  |     undefined    |       3\n *\n */\nfunction normalizeWheel(/*object*/ event) /*object*/ {\n  var sX = 0, sY = 0,       // spinX, spinY\n      pX = 0, pY = 0;       // pixelX, pixelY\n\n  // Legacy\n  if ('detail'      in event) { sY = event.detail; }\n  if ('wheelDelta'  in event) { sY = -event.wheelDelta / 120; }\n  if ('wheelDeltaY' in event) { sY = -event.wheelDeltaY / 120; }\n  if ('wheelDeltaX' in event) { sX = -event.wheelDeltaX / 120; }\n\n  // side scrolling on FF with DOMMouseScroll\n  if ( 'axis' in event && event.axis === event.HORIZONTAL_AXIS ) {\n    sX = sY;\n    sY = 0;\n  }\n\n  pX = sX * PIXEL_STEP;\n  pY = sY * PIXEL_STEP;\n\n  if ('deltaY' in event) { pY = event.deltaY; }\n  if ('deltaX' in event) { pX = event.deltaX; }\n\n  if ((pX || pY) && event.deltaMode) {\n    if (event.deltaMode == 1) {          // delta in LINE units\n      pX *= LINE_HEIGHT;\n      pY *= LINE_HEIGHT;\n    } else {                             // delta in PAGE units\n      pX *= PAGE_HEIGHT;\n      pY *= PAGE_HEIGHT;\n    }\n  }\n\n  // Fall-back if spin cannot be determined\n  if (pX && !sX) { sX = (pX < 1) ? -1 : 1; }\n  if (pY && !sY) { sY = (pY < 1) ? -1 : 1; }\n\n  return { spinX  : sX,\n           spinY  : sY,\n           pixelX : pX,\n           pixelY : pY };\n}\n\n\n/**\n * The best combination if you prefer spinX + spinY normalization.  It favors\n * the older DOMMouseScroll for Firefox, as FF does not include wheelDelta with\n * 'wheel' event, making spin speed determination impossible.\n */\nnormalizeWheel.getEventType = function() /*string*/ {\n  return (UserAgent_DEPRECATED.firefox())\n           ? 'DOMMouseScroll'\n           : (isEventSupported('wheel'))\n               ? 'wheel'\n               : 'mousewheel';\n};\n\nmodule.exports = normalizeWheel;\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/normalize-wheel/src/normalizeWheel.js?");

/***/ }),

/***/ "./node_modules/vevet-dom/dist/es/childOf.js":
/*!***************************************************!*\
  !*** ./node_modules/vevet-dom/dist/es/childOf.js ***!
  \***************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   childOf: () => (/* binding */ childOf)\n/* harmony export */ });\n/**\r\n * Check if an element is a child of another element.\r\n */\r\nfunction childOf(\r\n/**\r\n * The element to be checked\r\n */\r\nelement, \r\n/**\r\n * Parent element\r\n */\r\nparent) {\r\n    return childOfCheck(element, parent);\r\n}\r\n/**\r\n * @ignore\r\n */\r\nfunction childOfCheck(el, parent) {\r\n    if (el === parent) {\r\n        return true;\r\n    }\r\n    if (el !== null) {\r\n        return childOfCheck(el.parentNode, parent);\r\n    }\r\n    return false;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet-dom/dist/es/childOf.js?");

/***/ }),

/***/ "./node_modules/vevet-dom/dist/es/classes.js":
/*!***************************************************!*\
  !*** ./node_modules/vevet-dom/dist/es/classes.js ***!
  \***************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   addClass: () => (/* binding */ addClass),\n/* harmony export */   removeClass: () => (/* binding */ removeClass),\n/* harmony export */   toggleClass: () => (/* binding */ toggleClass)\n/* harmony export */ });\n/**\r\n * Add a class name or names to an element or a collection of elements\r\n */\r\nfunction addClass(\r\n/**\r\n * The target element (elements)\r\n */\r\nel, \r\n/**\r\n * ClassName or names split by whitespace\r\n */\r\nclassNames) {\r\n    if (el instanceof Element) {\r\n        setElClass(el, classNames, true);\r\n    }\r\n    else {\r\n        for (let i = 0; i < el.length; i++) {\r\n            setElClass(el[i], classNames, true);\r\n        }\r\n    }\r\n}\r\n/**\r\n * Remove a class name or names from an element or a collection of elements\r\n */\r\nfunction removeClass(\r\n/**\r\n * The target element (elements)\r\n */\r\nel, \r\n/**\r\n * ClassName or names split by whitespace\r\n */\r\nclassNames) {\r\n    if (el instanceof Element) {\r\n        setElClass(el, classNames, false);\r\n    }\r\n    else {\r\n        for (let i = 0; i < el.length; i++) {\r\n            setElClass(el[i], classNames, false);\r\n        }\r\n    }\r\n}\r\n/**\r\n * Toggle a class name or names from an element or a collection of elements\r\n */\r\nfunction toggleClass(\r\n/**\r\n * The target element (elements)\r\n */\r\nel, \r\n/**\r\n * ClassName or names split by whitespace\r\n */\r\nclassNames) {\r\n    if (el instanceof Element) {\r\n        setElClass(el, classNames);\r\n    }\r\n    else {\r\n        for (let i = 0; i < el.length; i++) {\r\n            setElClass(el[i], classNames);\r\n        }\r\n    }\r\n}\r\n///\r\nfunction setElClass(\r\n/**\r\n * The element\r\n */\r\ne, \r\n/**\r\n * Class names\r\n */\r\nclassNames, \r\n/**\r\n * Undefined - toggle, true - add, false - remove\r\n */\r\naction) {\r\n    const names = classNames.split(' ');\r\n    for (let i = 0; i < names.length; i++) {\r\n        if (typeof action === 'undefined') {\r\n            e.classList.toggle(names[i]);\r\n        }\r\n        else if (action) {\r\n            e.classList.add(names[i]);\r\n        }\r\n        else {\r\n            e.classList.remove(names[i]);\r\n        }\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet-dom/dist/es/classes.js?");

/***/ }),

/***/ "./node_modules/vevet-dom/dist/es/createElement.js":
/*!*********************************************************!*\
  !*** ./node_modules/vevet-dom/dist/es/createElement.js ***!
  \*********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   createElement: () => (/* binding */ createElement)\n/* harmony export */ });\n/* harmony import */ var _classes__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./classes */ \"./node_modules/vevet-dom/dist/es/classes.js\");\n\r\n/**\r\n * Create a DOM element\r\n */\r\nfunction createElement(\r\n/**\r\n * TagName\r\n */\r\nselector, \r\n/**\r\n * Element properties\r\n */\r\nprop = {}) {\r\n    // create an element\r\n    const el = document.createElement(selector);\r\n    // add classes\r\n    if (prop.class) {\r\n        (0,_classes__WEBPACK_IMPORTED_MODULE_0__.addClass)(el, prop.class);\r\n    }\r\n    // add id\r\n    if (prop.id) {\r\n        el.setAttribute('id', prop.id);\r\n    }\r\n    // add attributes\r\n    if (prop.attr) {\r\n        for (let i = 0, l = prop.attr.length; i < l; i++) {\r\n            const attrInfo = prop.attr[i];\r\n            el.setAttribute(attrInfo[0], attrInfo[1]);\r\n        }\r\n    }\r\n    // append to parent\r\n    if (prop.parent) {\r\n        prop.parent.appendChild(el);\r\n    }\r\n    // add html\r\n    if (prop.html) {\r\n        el.innerHTML = prop.html;\r\n    }\r\n    // add children\r\n    if (prop.children) {\r\n        for (let i = 0, l = prop.children.length; i < l; i++) {\r\n            el.appendChild(prop.children[i]);\r\n        }\r\n    }\r\n    // return the element\r\n    return el;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet-dom/dist/es/createElement.js?");

/***/ }),

/***/ "./node_modules/vevet-dom/dist/es/inDOM.js":
/*!*************************************************!*\
  !*** ./node_modules/vevet-dom/dist/es/inDOM.js ***!
  \*************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   inDOM: () => (/* binding */ inDOM)\n/* harmony export */ });\n/* harmony import */ var _childOf__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./childOf */ \"./node_modules/vevet-dom/dist/es/childOf.js\");\n\r\n/**\r\n * Check if an element is in the DOM\r\n */\r\nfunction inDOM(\r\n/**\r\n * The element to be checked.\r\n */\r\nel) {\r\n    return (0,_childOf__WEBPACK_IMPORTED_MODULE_0__.childOf)(el, document.documentElement);\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet-dom/dist/es/inDOM.js?");

/***/ }),

/***/ "./node_modules/vevet-dom/dist/es/index.js":
/*!*************************************************!*\
  !*** ./node_modules/vevet-dom/dist/es/index.js ***!
  \*************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   addClass: () => (/* reexport safe */ _classes__WEBPACK_IMPORTED_MODULE_9__.addClass),\n/* harmony export */   addEventListener: () => (/* reexport safe */ _listeners__WEBPACK_IMPORTED_MODULE_10__.addEventListener),\n/* harmony export */   childOf: () => (/* reexport safe */ _childOf__WEBPACK_IMPORTED_MODULE_1__.childOf),\n/* harmony export */   createElement: () => (/* reexport safe */ _createElement__WEBPACK_IMPORTED_MODULE_8__.createElement),\n/* harmony export */   inDOM: () => (/* reexport safe */ _inDOM__WEBPACK_IMPORTED_MODULE_7__.inDOM),\n/* harmony export */   insertAfter: () => (/* reexport safe */ _insertAfter__WEBPACK_IMPORTED_MODULE_0__.insertAfter),\n/* harmony export */   isElement: () => (/* reexport safe */ _isElement__WEBPACK_IMPORTED_MODULE_3__.isElement),\n/* harmony export */   isWindow: () => (/* reexport safe */ _isWindow__WEBPACK_IMPORTED_MODULE_4__.isWindow),\n/* harmony export */   parentByClassName: () => (/* reexport safe */ _parentByClassName__WEBPACK_IMPORTED_MODULE_13__[\"default\"]),\n/* harmony export */   parentByInstance: () => (/* reexport safe */ _parentByInstance__WEBPACK_IMPORTED_MODULE_11__[\"default\"]),\n/* harmony export */   parentByTagName: () => (/* reexport safe */ _parentByTagName__WEBPACK_IMPORTED_MODULE_12__[\"default\"]),\n/* harmony export */   removeChildren: () => (/* reexport safe */ _removeChildren__WEBPACK_IMPORTED_MODULE_2__.removeChildren),\n/* harmony export */   removeClass: () => (/* reexport safe */ _classes__WEBPACK_IMPORTED_MODULE_9__.removeClass),\n/* harmony export */   removeEventListener: () => (/* reexport safe */ _listeners__WEBPACK_IMPORTED_MODULE_10__.removeEventListener),\n/* harmony export */   selectAll: () => (/* reexport safe */ _selectAll__WEBPACK_IMPORTED_MODULE_6__.selectAll),\n/* harmony export */   selectOne: () => (/* reexport safe */ _selectOne__WEBPACK_IMPORTED_MODULE_5__.selectOne),\n/* harmony export */   toggleClass: () => (/* reexport safe */ _classes__WEBPACK_IMPORTED_MODULE_9__.toggleClass)\n/* harmony export */ });\n/* harmony import */ var _insertAfter__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./insertAfter */ \"./node_modules/vevet-dom/dist/es/insertAfter.js\");\n/* harmony import */ var _childOf__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./childOf */ \"./node_modules/vevet-dom/dist/es/childOf.js\");\n/* harmony import */ var _removeChildren__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./removeChildren */ \"./node_modules/vevet-dom/dist/es/removeChildren.js\");\n/* harmony import */ var _isElement__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./isElement */ \"./node_modules/vevet-dom/dist/es/isElement.js\");\n/* harmony import */ var _isWindow__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./isWindow */ \"./node_modules/vevet-dom/dist/es/isWindow.js\");\n/* harmony import */ var _selectOne__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./selectOne */ \"./node_modules/vevet-dom/dist/es/selectOne.js\");\n/* harmony import */ var _selectAll__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./selectAll */ \"./node_modules/vevet-dom/dist/es/selectAll.js\");\n/* harmony import */ var _inDOM__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./inDOM */ \"./node_modules/vevet-dom/dist/es/inDOM.js\");\n/* harmony import */ var _createElement__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./createElement */ \"./node_modules/vevet-dom/dist/es/createElement.js\");\n/* harmony import */ var _classes__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./classes */ \"./node_modules/vevet-dom/dist/es/classes.js\");\n/* harmony import */ var _listeners__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ./listeners */ \"./node_modules/vevet-dom/dist/es/listeners.js\");\n/* harmony import */ var _parentByInstance__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ./parentByInstance */ \"./node_modules/vevet-dom/dist/es/parentByInstance.js\");\n/* harmony import */ var _parentByTagName__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ./parentByTagName */ \"./node_modules/vevet-dom/dist/es/parentByTagName.js\");\n/* harmony import */ var _parentByClassName__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! ./parentByClassName */ \"./node_modules/vevet-dom/dist/es/parentByClassName.js\");\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet-dom/dist/es/index.js?");

/***/ }),

/***/ "./node_modules/vevet-dom/dist/es/insertAfter.js":
/*!*******************************************************!*\
  !*** ./node_modules/vevet-dom/dist/es/insertAfter.js ***!
  \*******************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   insertAfter: () => (/* binding */ insertAfter)\n/* harmony export */ });\n/**\r\n * Insert an element after another one.\r\n */\r\nfunction insertAfter(\r\n/**\r\n * The element to be inserted\r\n */\r\nelement, \r\n/**\r\n * The reference element after which the first element will be appended.\r\n */\r\nreferenceElement) {\r\n    const parent = referenceElement.parentNode;\r\n    const next = referenceElement.nextSibling;\r\n    if (next) {\r\n        parent.insertBefore(element, next);\r\n    }\r\n    else {\r\n        parent.appendChild(element);\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet-dom/dist/es/insertAfter.js?");

/***/ }),

/***/ "./node_modules/vevet-dom/dist/es/isElement.js":
/*!*****************************************************!*\
  !*** ./node_modules/vevet-dom/dist/es/isElement.js ***!
  \*****************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   isElement: () => (/* binding */ isElement)\n/* harmony export */ });\n/**\r\n * Check if the variable is an HTML Element.\r\n */\r\nfunction isElement(el) {\r\n    return (el instanceof HTMLElement) || (el instanceof Element);\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet-dom/dist/es/isElement.js?");

/***/ }),

/***/ "./node_modules/vevet-dom/dist/es/isWindow.js":
/*!****************************************************!*\
  !*** ./node_modules/vevet-dom/dist/es/isWindow.js ***!
  \****************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   isWindow: () => (/* binding */ isWindow)\n/* harmony export */ });\n/**\r\n * Check if the variable is the Window instance.\r\n */\r\nfunction isWindow(el) {\r\n    return el instanceof Window;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet-dom/dist/es/isWindow.js?");

/***/ }),

/***/ "./node_modules/vevet-dom/dist/es/listeners.js":
/*!*****************************************************!*\
  !*** ./node_modules/vevet-dom/dist/es/listeners.js ***!
  \*****************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   addEventListener: () => (/* binding */ addEventListener),\n/* harmony export */   removeEventListener: () => (/* binding */ removeEventListener)\n/* harmony export */ });\n// event listeners\r\nconst listeners = [];\r\n/**\r\n * Add an event listener to an element.\r\n */\r\nfunction addEventListener(el, target, callback, options) {\r\n    // check if options exist\r\n    if (typeof options !== 'undefined') {\r\n        const listenerOptions = {\r\n            passive: false,\r\n            once: false,\r\n        };\r\n        // check if once\r\n        if (options.once) {\r\n            listenerOptions.once = true;\r\n        }\r\n        // check if passive\r\n        if (options.passive) {\r\n            listenerOptions.passive = true;\r\n        }\r\n        // add an event listener\r\n        el.addEventListener(target, callback, listenerOptions);\r\n    }\r\n    else {\r\n        // add an event listener\r\n        el.addEventListener(target, callback);\r\n    }\r\n    // generate an id for the listener\r\n    const id = `${Math.random()}-${+new Date()}`;\r\n    // add the listener to the stack\r\n    listeners.push({\r\n        id,\r\n        el,\r\n        target,\r\n        callback,\r\n    });\r\n    // return data\r\n    return {\r\n        id,\r\n        remove: removeEventListener.bind(this, id),\r\n    };\r\n}\r\n/**\r\n * Remove an event listener from an element.\r\n */\r\nfunction removeEventListener(\r\n/**\r\n * Id of the listener\r\n */\r\nid) {\r\n    // form a new array of listeners\r\n    const newListeners = [];\r\n    // go through all callbacks and search for the required one\r\n    for (let i = 0, l = listeners.length; i < l; i++) {\r\n        const data = listeners[i];\r\n        // remove the callback\r\n        if (data.id === id) {\r\n            data.el.removeEventListener(data.target, data.callback);\r\n        }\r\n        else {\r\n            // add the listener to the new array\r\n            newListeners.push(data);\r\n            continue;\r\n        }\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet-dom/dist/es/listeners.js?");

/***/ }),

/***/ "./node_modules/vevet-dom/dist/es/parentByClassName.js":
/*!*************************************************************!*\
  !*** ./node_modules/vevet-dom/dist/es/parentByClassName.js ***!
  \*************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ parentByClassName)\n/* harmony export */ });\n/**\r\n * Get the nearest parent element by tagName.\r\n */\r\nfunction parentByClassName(\r\n/**\r\n * The reference element (child)\r\n */\r\nchild, \r\n/**\r\n * Classname of the parent\r\n */\r\nclassName, \r\n/**\r\n * Maximum nesting level\r\n */\r\nmaxLevel = Infinity, \r\n/**\r\n * Current level\r\n */\r\ncurrentLevel = 0) {\r\n    // check nesting level\r\n    if (currentLevel >= maxLevel) {\r\n        return false;\r\n    }\r\n    // and iterate\r\n    currentLevel++;\r\n    // if overlap, return the parent\r\n    if (child.parentElement instanceof Element) {\r\n        if (child.parentElement.classList.contains(className)) {\r\n            return child.parentElement;\r\n        }\r\n        return parentByClassName(child.parentElement, className, maxLevel, currentLevel);\r\n    }\r\n    return false;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet-dom/dist/es/parentByClassName.js?");

/***/ }),

/***/ "./node_modules/vevet-dom/dist/es/parentByInstance.js":
/*!************************************************************!*\
  !*** ./node_modules/vevet-dom/dist/es/parentByInstance.js ***!
  \************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ parentByInstance)\n/* harmony export */ });\n/**\r\n * Get the nearest parent element by instance.\r\n */\r\nfunction parentByInstance(\r\n/**\r\n * The reference element (child)\r\n */\r\nchild, \r\n/**\r\n * The instance\r\n */\r\ninstance, \r\n/**\r\n * Maximum nesting level\r\n */\r\nmaxLevel = Infinity, \r\n/**\r\n * Current level\r\n */\r\ncurrentLevel = 0) {\r\n    // check nesting level\r\n    if (currentLevel >= maxLevel) {\r\n        return false;\r\n    }\r\n    // and iterate\r\n    currentLevel++;\r\n    // if overlap, return the parent\r\n    if (child.parentElement instanceof Element) {\r\n        if (child.parentElement instanceof fixInstance(instance)) {\r\n            return child.parentElement;\r\n        }\r\n        return parentByInstance(child.parentElement, instance, maxLevel, currentLevel);\r\n    }\r\n    return false;\r\n}\r\n/**\r\n * @ignore\r\n */\r\nfunction fixInstance(instance) {\r\n    return instance;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet-dom/dist/es/parentByInstance.js?");

/***/ }),

/***/ "./node_modules/vevet-dom/dist/es/parentByTagName.js":
/*!***********************************************************!*\
  !*** ./node_modules/vevet-dom/dist/es/parentByTagName.js ***!
  \***********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ parentByTagName)\n/* harmony export */ });\n/**\r\n * Get the nearest parent element by tagName.\r\n */\r\nfunction parentByTagName(\r\n/**\r\n * The reference element (child)\r\n */\r\nchild, \r\n/**\r\n * Tagname of the parent\r\n */\r\ntagName, \r\n/**\r\n * Maximum nesting level\r\n */\r\nmaxLevel = Infinity, \r\n/**\r\n * Current level\r\n */\r\ncurrentLevel = 0) {\r\n    // check nesting level\r\n    if (currentLevel >= maxLevel) {\r\n        return false;\r\n    }\r\n    // and iterate\r\n    currentLevel++;\r\n    // if overlap, return the parent\r\n    if (child.parentElement instanceof Element) {\r\n        if (child.parentElement.tagName.toLowerCase() === tagName.toLowerCase()) {\r\n            return child.parentElement;\r\n        }\r\n        return parentByTagName(child.parentElement, tagName, maxLevel, currentLevel);\r\n    }\r\n    return false;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet-dom/dist/es/parentByTagName.js?");

/***/ }),

/***/ "./node_modules/vevet-dom/dist/es/removeChildren.js":
/*!**********************************************************!*\
  !*** ./node_modules/vevet-dom/dist/es/removeChildren.js ***!
  \**********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   removeChildren: () => (/* binding */ removeChildren)\n/* harmony export */ });\n/**\r\n * Remove children of an element\r\n */\r\nfunction removeChildren(parent) {\r\n    while (parent.firstChild) {\r\n        parent.removeChild(parent.firstChild);\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet-dom/dist/es/removeChildren.js?");

/***/ }),

/***/ "./node_modules/vevet-dom/dist/es/selectAll.js":
/*!*****************************************************!*\
  !*** ./node_modules/vevet-dom/dist/es/selectAll.js ***!
  \*****************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   selectAll: () => (/* binding */ selectAll)\n/* harmony export */ });\n/* harmony import */ var _isElement__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./isElement */ \"./node_modules/vevet-dom/dist/es/isElement.js\");\n/* harmony import */ var _selectOne__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./selectOne */ \"./node_modules/vevet-dom/dist/es/selectOne.js\");\n\r\n\r\n/**\r\n * Select all DOM elements that match a query selector\r\n */\r\nfunction selectAll(\r\n/**\r\n * Query selector\r\n */\r\nselector, \r\n/**\r\n * A parent for the needed elements\r\n */\r\nparent) {\r\n    if (selector instanceof NodeList) {\r\n        return selector;\r\n    }\r\n    if ((0,_isElement__WEBPACK_IMPORTED_MODULE_0__.isElement)(selector)) {\r\n        return [selector];\r\n    }\r\n    if (Array.isArray(selector)) {\r\n        return selector;\r\n    }\r\n    // if string\r\n    if (typeof parent !== 'undefined') {\r\n        const parenEl = (0,_selectOne__WEBPACK_IMPORTED_MODULE_1__.selectOne)(parent);\r\n        if (parenEl) {\r\n            return parenEl.querySelectorAll(selector);\r\n        }\r\n    }\r\n    return document.querySelectorAll(selector);\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet-dom/dist/es/selectAll.js?");

/***/ }),

/***/ "./node_modules/vevet-dom/dist/es/selectOne.js":
/*!*****************************************************!*\
  !*** ./node_modules/vevet-dom/dist/es/selectOne.js ***!
  \*****************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   selectOne: () => (/* binding */ selectOne)\n/* harmony export */ });\n/* harmony import */ var _isElement__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./isElement */ \"./node_modules/vevet-dom/dist/es/isElement.js\");\n/* harmony import */ var _isWindow__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./isWindow */ \"./node_modules/vevet-dom/dist/es/isWindow.js\");\n\r\n\r\n/**\r\n * Select only one DOM element.\r\n */\r\nfunction selectOne(\r\n/**\r\n * Query selector\r\n */\r\nselector, \r\n/**\r\n * A parent for the needed element\r\n */\r\nparent) {\r\n    if ((0,_isWindow__WEBPACK_IMPORTED_MODULE_1__.isWindow)(selector)) {\r\n        return selector;\r\n    }\r\n    if ((0,_isElement__WEBPACK_IMPORTED_MODULE_0__.isElement)(selector)) {\r\n        return selector;\r\n    }\r\n    // if string\r\n    if (typeof parent !== 'undefined') {\r\n        const parenEl = selectOne(parent);\r\n        if (parenEl) {\r\n            return parenEl.querySelector(selector);\r\n        }\r\n    }\r\n    return document.querySelector(selector);\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet-dom/dist/es/selectOne.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/app/Application.js":
/*!********************************************************!*\
  !*** ./node_modules/vevet/build/es/app/Application.js ***!
  \********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   Application: () => (/* binding */ Application)\n/* harmony export */ });\n/* harmony import */ var ismobilejs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ismobilejs */ \"./node_modules/ismobilejs/esm/index.js\");\n/* harmony import */ var detect_browser__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! detect-browser */ \"./node_modules/detect-browser/es/index.js\");\n/* harmony import */ var p_cancelable__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! p-cancelable */ \"./node_modules/p-cancelable/index.js\");\n/* harmony import */ var _events_Viewport__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./events/Viewport */ \"./node_modules/vevet/build/es/app/events/Viewport.js\");\n/* harmony import */ var _events_PageLoad__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./events/PageLoad */ \"./node_modules/vevet/build/es/app/events/PageLoad.js\");\n/* harmony import */ var _version__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../version */ \"./node_modules/vevet/build/es/version.js\");\n\r\n\r\n\r\n\r\n\r\n\r\n/**\r\n * Vevet Application\r\n */\r\nclass Application {\r\n    /**\r\n     * @example\r\n     * const app = Application({\r\n     *     page: 'home'\r\n     * });\r\n     */\r\n    constructor(data = {}) {\r\n        /**\r\n         * Pages (instances)\r\n         */\r\n        this._pages = [];\r\n        /**\r\n         * Current Page (instance).\r\n         */\r\n        this._page = false;\r\n        // check if the application already exists\r\n        if (window.vevetApp) {\r\n            throw new Error('Vevet Application already exists!');\r\n        }\r\n        // set defaults\r\n        this._prop = Object.assign(Object.assign({}, this.defaultProp), data);\r\n        this._prefix = this.prop.prefix;\r\n        // initialize the application\r\n        // Define that you're using Vevet\r\n        this._sayHi();\r\n        // add the application to the window\r\n        window.vevetApp = this;\r\n        // get type of device\r\n        const ifmobile = (0,ismobilejs__WEBPACK_IMPORTED_MODULE_0__[\"default\"])();\r\n        this._isPhone = ifmobile.phone;\r\n        this.html.classList.toggle(`${this.prefix}phone`, this._isPhone);\r\n        this._isTablet = ifmobile.tablet;\r\n        this.html.classList.toggle(`${this.prefix}tablet`, this._isTablet);\r\n        this._isMobile = ifmobile.phone || ifmobile.tablet;\r\n        this.html.classList.toggle(`${this.prefix}mobile`, this._isMobile);\r\n        this._isDesktop = !this._isMobile;\r\n        this.html.classList.toggle(`${this.prefix}desktop`, this._isDesktop);\r\n        // get browser info\r\n        const browserData = (0,detect_browser__WEBPACK_IMPORTED_MODULE_2__.detect)();\r\n        // get OS name\r\n        if (browserData === null || browserData === void 0 ? void 0 : browserData.os) {\r\n            const osName = browserData.os.split(' ')[0].toLowerCase();\r\n            this.html.classList.add(`${this.prefix}os-${osName}`);\r\n            this._osName = osName;\r\n        }\r\n        else {\r\n            this._osName = '';\r\n        }\r\n        // get browser name\r\n        if (browserData === null || browserData === void 0 ? void 0 : browserData.name) {\r\n            const browserName = browserData.name.toLowerCase();\r\n            this.html.classList.add(`${this.prefix}browser-${browserName}`);\r\n            this._browserName = browserName;\r\n        }\r\n        else {\r\n            this._browserName = '';\r\n        }\r\n        // browserData\r\n        // create default helpers\r\n        this._pageLoad = new _events_PageLoad__WEBPACK_IMPORTED_MODULE_3__.PageLoad();\r\n        this._viewport = new _events_Viewport__WEBPACK_IMPORTED_MODULE_4__.Viewport();\r\n        // check webp support\r\n        if (this.prop.webpSupport) {\r\n            const testWebP = new Image();\r\n            testWebP.onload = () => {\r\n                this._supportsWebp = testWebP.width === 1;\r\n            };\r\n            testWebP.onerror = () => {\r\n                this._supportsWebp = false;\r\n            };\r\n            testWebP.src = 'data:image/webp;base64,UklGRiQAAABXRUJQVlA4IBgAAAAwAQCdASoBAAEAAwA0JaQAA3AA/vuUAAA=';\r\n        }\r\n        else {\r\n            this._supportsWebp = false;\r\n        }\r\n    }\r\n    get version() {\r\n        return _version__WEBPACK_IMPORTED_MODULE_5__[\"default\"];\r\n    }\r\n    /**\r\n     * Application properties.\r\n     */\r\n    get prop() {\r\n        return this._prop;\r\n    }\r\n    /**\r\n     * Default properties.\r\n     */\r\n    get defaultProp() {\r\n        return {\r\n            sayHi: true,\r\n            tablet: 1199,\r\n            phone: 899,\r\n            prefix: 'v-',\r\n            easing: [0.25, 0.1, 0.25, 1],\r\n            viewportResizeTimeout: 0,\r\n            webpSupport: false,\r\n        };\r\n    }\r\n    get prefix() {\r\n        return this._prefix;\r\n    }\r\n    get isPhone() {\r\n        return this._isPhone;\r\n    }\r\n    get isTablet() {\r\n        return this._isTablet;\r\n    }\r\n    get isMobile() {\r\n        return this._isMobile;\r\n    }\r\n    get isDesktop() {\r\n        return this._isDesktop;\r\n    }\r\n    get osName() {\r\n        return this._osName;\r\n    }\r\n    get browserName() {\r\n        return this._browserName;\r\n    }\r\n    get supportsWebp() {\r\n        return this._supportsWebp;\r\n    }\r\n    /**\r\n     * Get document element.\r\n     */\r\n    get doc() {\r\n        return document;\r\n    }\r\n    /**\r\n     * Get HTML element\r\n     */\r\n    get html() {\r\n        return document.documentElement;\r\n    }\r\n    /**\r\n     * Get body element.\r\n     */\r\n    get body() {\r\n        return document.body;\r\n    }\r\n    /**\r\n     * Get an array of existing pages.\r\n     * A new element is added to the array when {@linkcode Vevet.Page.create} is called.\r\n     */\r\n    get pages() {\r\n        return this._pages;\r\n    }\r\n    /**\r\n     * Get the current page instance.\r\n     */\r\n    get page() {\r\n        return this._page;\r\n    }\r\n    /**\r\n     * Set the current page instance.\r\n     */\r\n    set page(val) {\r\n        this._page = val;\r\n    }\r\n    /**\r\n     * Action on page created\r\n     */\r\n    onPageCreated() {\r\n        return new p_cancelable__WEBPACK_IMPORTED_MODULE_1__[\"default\"]((resolve) => {\r\n            if (this._page) {\r\n                this._page.onCreate().then(() => {\r\n                    resolve(this.page);\r\n                });\r\n                return;\r\n            }\r\n            setTimeout(() => {\r\n                this.onPageCreated().then(() => {\r\n                    resolve(this.page);\r\n                });\r\n            }, 30);\r\n        });\r\n    }\r\n    /**\r\n     * Action on page shown\r\n     */\r\n    onPageShown() {\r\n        return new p_cancelable__WEBPACK_IMPORTED_MODULE_1__[\"default\"]((resolve) => {\r\n            this.onPageCreated().then(() => {\r\n                if (this._page) {\r\n                    this._page.onShow().then(() => {\r\n                        resolve(this.page);\r\n                    });\r\n                }\r\n            });\r\n        });\r\n    }\r\n    get pageLoad() {\r\n        return this._pageLoad;\r\n    }\r\n    /**\r\n     * Action on page laoded\r\n     */\r\n    onPageLoaded() {\r\n        return new p_cancelable__WEBPACK_IMPORTED_MODULE_1__[\"default\"]((resolve) => {\r\n            this._pageLoad.onLoaded(() => {\r\n                resolve();\r\n            });\r\n        });\r\n    }\r\n    get viewport() {\r\n        return this._viewport;\r\n    }\r\n    /**\r\n     * Defines that you're using Vevet.\r\n     */\r\n    _sayHi() {\r\n        if (!this.prop.sayHi) {\r\n            return;\r\n        }\r\n        const msg = `Vevet ${this.version}`;\r\n        const style = [\r\n            'padding: 1rem 1.5rem;',\r\n            'background: #5F2580;',\r\n            'font: 1rem/1 Arial;',\r\n            'color: #ffffff;',\r\n        ].join('');\r\n        // eslint-disable-next-line no-console\r\n        console.log('%c%s', style, msg);\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/app/Application.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/app/events/PageLoad.js":
/*!************************************************************!*\
  !*** ./node_modules/vevet/build/es/app/events/PageLoad.js ***!
  \************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   PageLoad: () => (/* binding */ PageLoad)\n/* harmony export */ });\n/* harmony import */ var _base_Callbacks__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../base/Callbacks */ \"./node_modules/vevet/build/es/base/Callbacks.js\");\n\r\n/**\r\n * Callbacks on page loaded.\r\n */\r\nclass PageLoad extends _base_Callbacks__WEBPACK_IMPORTED_MODULE_0__.Callbacks {\r\n    constructor() {\r\n        super(false);\r\n        this._loaded = false;\r\n        this._init();\r\n    }\r\n    get loaded() {\r\n        return this._loaded;\r\n    }\r\n    _setEvents() {\r\n        if (document.readyState === 'complete') {\r\n            this._handleLoaded();\r\n        }\r\n        else {\r\n            window.addEventListener('load', () => {\r\n                this._handleLoaded();\r\n            });\r\n        }\r\n    }\r\n    /**\r\n     * When the page is loaded.\r\n     */\r\n    _handleLoaded() {\r\n        const app = this._app;\r\n        const { prefix } = app;\r\n        this._loaded = true;\r\n        app.html.classList.remove(`${prefix}loading`);\r\n        app.body.classList.remove(`${prefix}loading`);\r\n        app.html.classList.add(`${prefix}loaded`);\r\n        this.tbt('', false);\r\n    }\r\n    /**\r\n     * Add a callback on page load.\r\n     * If the page is already loaded, the callback will be immediately triggered.\r\n     */\r\n    onLoaded(callback) {\r\n        if (this.loaded) {\r\n            callback();\r\n            return undefined;\r\n        }\r\n        return this.add('', callback.bind(this));\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/app/events/PageLoad.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/app/events/Viewport.js":
/*!************************************************************!*\
  !*** ./node_modules/vevet/build/es/app/events/Viewport.js ***!
  \************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   Viewport: () => (/* binding */ Viewport)\n/* harmony export */ });\n/* harmony import */ var _base_Callbacks__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../base/Callbacks */ \"./node_modules/vevet/build/es/base/Callbacks.js\");\n/* harmony import */ var _utils_common__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../utils/common */ \"./node_modules/vevet/build/es/utils/common/timeoutCallback.js\");\n\r\n\r\n/**\r\n * Viewport size types\r\n */\r\nvar SizeTypes;\r\n(function (SizeTypes) {\r\n    SizeTypes[\"Desktop\"] = \"desktop\";\r\n    SizeTypes[\"Tablet\"] = \"tablet\";\r\n    SizeTypes[\"Phone\"] = \"phone\";\r\n})(SizeTypes || (SizeTypes = {}));\r\n/**\r\n * Orientation types\r\n */\r\nvar OrientationTypes;\r\n(function (OrientationTypes) {\r\n    OrientationTypes[\"Landscape\"] = \"landscape\";\r\n    OrientationTypes[\"Portrait\"] = \"portrait\";\r\n})(OrientationTypes || (OrientationTypes = {}));\r\n/**\r\n * Callbacks on window resize.\r\n * Here the names of the OS, Browser, and Device are also available. <br>\r\n */\r\nclass Viewport extends _base_Callbacks__WEBPACK_IMPORTED_MODULE_0__.Callbacks {\r\n    constructor() {\r\n        super(false);\r\n        this._width = 0;\r\n        this._height = 0;\r\n        this._radius = 0;\r\n        this._prevSize = { w: 0, h: 0 };\r\n        this._isDesktop = false;\r\n        this._isTablet = false;\r\n        this._isPhone = false;\r\n        this._init();\r\n    }\r\n    get width() {\r\n        return this._width;\r\n    }\r\n    get height() {\r\n        return this._height;\r\n    }\r\n    get radius() {\r\n        return Math.sqrt(Math.pow(this.width, 2) + Math.pow(this.height, 2)) / 2;\r\n    }\r\n    /**\r\n     * Get VW value\r\n     */\r\n    get vw() {\r\n        return this.width / 100;\r\n    }\r\n    /**\r\n     * Get VH value\r\n     */\r\n    get vh() {\r\n        return this.height / 100;\r\n    }\r\n    /**\r\n     * Get VR value (viewport radius / 100)\r\n     */\r\n    get vr() {\r\n        return this.radius / 100;\r\n    }\r\n    /**\r\n     * Get previous Viewport size\r\n     */\r\n    get prevSize() {\r\n        return this._prevSize;\r\n    }\r\n    /**\r\n     * If width greater than height.\r\n     */\r\n    get isLandscape() {\r\n        return this.width > this.height;\r\n    }\r\n    /**\r\n     * If width less than height.\r\n     */\r\n    get isPortrait() {\r\n        return this.width < this.height;\r\n    }\r\n    get isDesktop() {\r\n        return this._isDesktop;\r\n    }\r\n    get isTablet() {\r\n        return this._isTablet;\r\n    }\r\n    get isPhone() {\r\n        return this._isPhone;\r\n    }\r\n    /**\r\n     * Device pixel ratio\r\n     */\r\n    get dpr() {\r\n        if (typeof window.devicePixelRatio !== 'undefined') {\r\n            return window.devicePixelRatio;\r\n        }\r\n        return 1;\r\n    }\r\n    /**\r\n     * Device pixel ratio. For non-mobile devices it is always 1.\r\n     */\r\n    get lowerDesktopDPR() {\r\n        if (this._app.isDesktop) {\r\n            return 1;\r\n        }\r\n        return this.dpr;\r\n    }\r\n    // Extra constructor\r\n    _constructor() {\r\n        super._constructor();\r\n        this._setValues();\r\n    }\r\n    // Set events on resize\r\n    _setEvents() {\r\n        window.addEventListener('resize', () => {\r\n            if (this._resizeTimeout) {\r\n                this._resizeTimeout.clear();\r\n            }\r\n            this._resizeTimeout = (0,_utils_common__WEBPACK_IMPORTED_MODULE_1__[\"default\"])(() => {\r\n                this._onResize();\r\n            }, this._app.prop.viewportResizeTimeout);\r\n        });\r\n    }\r\n    /**\r\n     * Set viewport values\r\n     */\r\n    _setValues() {\r\n        const app = this._app;\r\n        const { html } = app;\r\n        const appProp = app.prop;\r\n        // set sizes\r\n        this._width = html.clientWidth;\r\n        this._height = html.clientHeight;\r\n        this._prevSize = {\r\n            w: this._width,\r\n            h: this._height,\r\n        };\r\n        // size values\r\n        const { width } = this;\r\n        this._isDesktop = width >= appProp.tablet;\r\n        this._isTablet = width <= appProp.tablet && width > appProp.phone;\r\n        this._isPhone = width <= appProp.phone;\r\n        // update other values\r\n        this._updateClasses();\r\n        this._updateCSSVars();\r\n    }\r\n    /**\r\n     * Change classes of the document element.\r\n     */\r\n    _updateClasses() {\r\n        // set viewport type\r\n        const viewportSizeTypes = [\r\n            SizeTypes.Desktop,\r\n            SizeTypes.Tablet,\r\n            SizeTypes.Phone,\r\n        ];\r\n        if (this.isDesktop) {\r\n            this._updateBreakpointClasses(SizeTypes.Desktop, viewportSizeTypes);\r\n        }\r\n        else if (this.isTablet) {\r\n            this._updateBreakpointClasses(SizeTypes.Tablet, viewportSizeTypes);\r\n        }\r\n        else {\r\n            this._updateBreakpointClasses(SizeTypes.Phone, viewportSizeTypes);\r\n        }\r\n        // set orientation type\r\n        const orientationTypes = [\r\n            OrientationTypes.Landscape,\r\n            OrientationTypes.Portrait,\r\n        ];\r\n        if (this.isLandscape) {\r\n            this._updateBreakpointClasses(OrientationTypes.Landscape, orientationTypes);\r\n        }\r\n        else if (this.isPortrait) {\r\n            this._updateBreakpointClasses(OrientationTypes.Portrait, orientationTypes);\r\n        }\r\n        else {\r\n            this._updateBreakpointClasses('', orientationTypes);\r\n        }\r\n    }\r\n    /**\r\n     *  Change breakpoint classes of the document element.\r\n     */\r\n    _updateBreakpointClasses(activeType, types) {\r\n        const { html } = this._app;\r\n        const { prefix } = this._app;\r\n        types.forEach((type) => {\r\n            if (type === activeType) {\r\n                html.classList.add(`${prefix}viewport-${type}`);\r\n            }\r\n            else {\r\n                html.classList.remove(`${prefix}viewport-${type}`);\r\n            }\r\n        });\r\n    }\r\n    /**\r\n     * Update CSS vars\r\n     */\r\n    _updateCSSVars() {\r\n        const { html } = this._app;\r\n        html.style.setProperty('--vw', `${this.vw}px`);\r\n        html.style.setProperty('--vh', `${this.vh}px`);\r\n        html.style.setProperty('--vr', `${this.vr}px`);\r\n    }\r\n    /**\r\n     * Launch callbacks on resize.\r\n     */\r\n    _onResize(\r\n    /**\r\n     * force all callbacks\r\n     */\r\n    force = false) {\r\n        // copy previous values\r\n        const prevWidth = this._prevSize.w;\r\n        const prevHeight = this._prevSize.h;\r\n        // set viewport values\r\n        this._setValues();\r\n        // copy size\r\n        const { width, height } = this;\r\n        // get changes in viewport\r\n        const changes = {\r\n            widthChanged: width !== prevWidth,\r\n            heightChanged: height !== prevHeight,\r\n            orientationChanged: (width > height) !== (prevWidth > prevHeight),\r\n        };\r\n        // only when width is changed\r\n        if (force || (width !== prevWidth && height === prevHeight)) {\r\n            this.tbt('w_', changes);\r\n        }\r\n        // only when height is changed\r\n        if (force || (height !== prevHeight && width === prevWidth)) {\r\n            this.tbt('h_', changes);\r\n        }\r\n        // when height & width are changed\r\n        if (force || (width !== prevWidth && height !== prevHeight)) {\r\n            this.tbt('wh', changes);\r\n            this.tbt('hw', changes);\r\n        }\r\n        // when width is changed\r\n        if (force || width !== prevWidth) {\r\n            this.tbt('w', changes);\r\n        }\r\n        // when height changed\r\n        if (force || height !== prevHeight) {\r\n            this.tbt('h', changes);\r\n        }\r\n        // on any change\r\n        this.tbt('', changes);\r\n    }\r\n    /**\r\n     * Force launching all callbacks\r\n     */\r\n    forceResize() {\r\n        this._onResize(true);\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/app/events/Viewport.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/base/Callbacks.js":
/*!*******************************************************!*\
  !*** ./node_modules/vevet/build/es/base/Callbacks.js ***!
  \*******************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   Callbacks: () => (/* binding */ Callbacks)\n/* harmony export */ });\n/* harmony import */ var _utils_common__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../utils/common */ \"./node_modules/vevet/build/es/utils/common/id.js\");\n/* harmony import */ var _utils_common__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../utils/common */ \"./node_modules/vevet/build/es/utils/common/timeoutCallback.js\");\n\r\n/**\r\n * A class for callbacks' manipulation.\r\n */\r\nclass Callbacks {\r\n    /**\r\n     * @example\r\n     * const callback = new Callbacks();\r\n     */\r\n    constructor(callInit = true) {\r\n        this._app = window.vevetApp;\r\n        this._callbacks = [];\r\n        // initialize callbacks\r\n        if (callInit) {\r\n            this._init();\r\n        }\r\n    }\r\n    /**\r\n     * Get all callbacks\r\n     */\r\n    get callbacks() {\r\n        return this._callbacks;\r\n    }\r\n    /**\r\n     * Initialize the class.\r\n     */\r\n    _init() {\r\n        this._constructor();\r\n        this._setEvents();\r\n    }\r\n    /**\r\n     * An empty method that is called in {@linkcode Callbacks._init}.\r\n     */\r\n    _constructor() {\r\n        // code\r\n    }\r\n    /**\r\n     * An empty method that is called in {@linkcode Callbacks._init}.\r\n     */\r\n    _setEvents() {\r\n        // code\r\n    }\r\n    /**\r\n     * Adds a callback\r\n     * @param target - Callback target name\r\n     * @param func - Callback function\r\n     * @param data - Callback data\r\n     *\r\n     * @example\r\n     * const onTarget = callback.add(\"target-name\", () => {\r\n     *     alert(\"callback\");\r\n     * });\r\n     */\r\n    add(target, func, data = {}) {\r\n        const id = (0,_utils_common__WEBPACK_IMPORTED_MODULE_0__[\"default\"])('callback');\r\n        const obj = {\r\n            id,\r\n            on: true,\r\n            data: Object.assign({ target, \r\n                // @ts-ignore\r\n                do: func }, data),\r\n        };\r\n        this._callbacks.push(obj);\r\n        this._onAdd(id);\r\n        return {\r\n            id,\r\n            remove: this.remove.bind(this, id),\r\n        };\r\n    }\r\n    /**\r\n     * Use it to implement some actions after adding a callback.\r\n     */\r\n    // eslint-disable-next-line @typescript-eslint/no-unused-vars\r\n    _onAdd(id) {\r\n        // code\r\n    }\r\n    /**\r\n     * Remove a callback.\r\n     */\r\n    remove(id, removeProtected = false) {\r\n        // remove the callback and get new ones\r\n        let removed = false;\r\n        const newCallbacks = this._callbacks.filter((callback) => {\r\n            // if not ID overlap\r\n            if (callback.id !== id) {\r\n                return true;\r\n            }\r\n            // to be removed\r\n            const { data } = callback;\r\n            if (data.protected && !removeProtected) {\r\n                return true;\r\n            }\r\n            this._onRemove(id);\r\n            removed = true;\r\n            return false;\r\n        });\r\n        // replace the callbacks' array\r\n        this._callbacks = newCallbacks;\r\n        // return results\r\n        return removed;\r\n    }\r\n    /**\r\n     * Use it to implement some actions after removing a callback.\r\n     */\r\n    // eslint-disable-next-line @typescript-eslint/no-unused-vars\r\n    _onRemove(id) {\r\n        // code\r\n    }\r\n    /**\r\n     * Remove all callbacks.\r\n     */\r\n    removeAll(removeProtected = false) {\r\n        while (this._callbacks.length > 0) {\r\n            this.remove(this._callbacks[0].id, removeProtected);\r\n        }\r\n    }\r\n    /**\r\n     * Enable/disable a callback.\r\n     * @param id - ID of the callback\r\n     * @param enabled - True to enable, false to disable.\r\n     */\r\n    turn(id, enabled = true) {\r\n        const callback = this.get(id);\r\n        if (callback) {\r\n            callback.on = enabled;\r\n            this._onTurn(id);\r\n            return true;\r\n        }\r\n        return false;\r\n    }\r\n    /**\r\n     * Use it to implement some actions after enabling or disabling a callback.\r\n     */\r\n    // eslint-disable-next-line @typescript-eslint/no-unused-vars\r\n    _onTurn(id) {\r\n        // code\r\n    }\r\n    /**\r\n     * Get a callback by id\r\n     */\r\n    get(id) {\r\n        const callbacks = this._callbacks.filter((callback) => callback.id === id);\r\n        if (callbacks.length > 0) {\r\n            return callbacks[0];\r\n        }\r\n        return false;\r\n    }\r\n    /**\r\n     * Trigger a callback. It will work only if the callback is enabled.\r\n     */\r\n    _trigger(callback, arg) {\r\n        // check if enabled\r\n        if (!callback.on) {\r\n            return;\r\n        }\r\n        const { timeout, once } = callback.data;\r\n        const func = callback.data.do;\r\n        // launch\r\n        if (timeout) {\r\n            if (arg) {\r\n                (0,_utils_common__WEBPACK_IMPORTED_MODULE_1__[\"default\"])(this._triggerFunc.bind(this, func, arg), timeout);\r\n            }\r\n            else {\r\n                (0,_utils_common__WEBPACK_IMPORTED_MODULE_1__[\"default\"])(this._triggerFunc.bind(this, func, false), timeout);\r\n            }\r\n        }\r\n        else if (arg) {\r\n            this._triggerFunc(func, arg);\r\n        }\r\n        else {\r\n            this._triggerFunc(func, false);\r\n        }\r\n        // remove once-callback\r\n        if (once) {\r\n            this.remove(callback.id);\r\n        }\r\n    }\r\n    /**\r\n     * Launch a callback's function\r\n     */\r\n    _triggerFunc(func, arg) {\r\n        if (arg) {\r\n            func(arg);\r\n        }\r\n        else {\r\n            func();\r\n        }\r\n    }\r\n    /**\r\n     * Trigger all enabled callbacks under a certain target name. (TBT: Trigger by target).\r\n     */\r\n    tbt(target, arg) {\r\n        this._callbacks.forEach((callback) => {\r\n            if (callback.data.target === target) {\r\n                this._trigger(callback, arg);\r\n            }\r\n        });\r\n    }\r\n    /**\r\n     * Destroy the callbacks\r\n     */\r\n    destroy() {\r\n        this.removeAll(true);\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/base/Callbacks.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/base/Component.js":
/*!*******************************************************!*\
  !*** ./node_modules/vevet/build/es/base/Component.js ***!
  \*******************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   Component: () => (/* binding */ Component)\n/* harmony export */ });\n/* harmony import */ var _Module__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./Module */ \"./node_modules/vevet/build/es/base/Module.js\");\n\r\n/**\r\n * A class for Components.\r\n */\r\nclass Component extends _Module__WEBPACK_IMPORTED_MODULE_0__.Module {\r\n    /**\r\n     * Add a single plugin\r\n     */\r\n    addPlugin(plugin) {\r\n        if (typeof this._plugins === 'undefined') {\r\n            this._plugins = [];\r\n        }\r\n        this._plugins.push(plugin);\r\n        if (!plugin.inited) {\r\n            plugin.initPlugin(this);\r\n        }\r\n    }\r\n    /**\r\n     * Destroy all plugins\r\n     */\r\n    _destroyPlugins() {\r\n        if (this._plugins) {\r\n            this._plugins.forEach((plugin) => {\r\n                plugin.destroy();\r\n            });\r\n        }\r\n    }\r\n    _destroy() {\r\n        super._destroy();\r\n        this._destroyPlugins();\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/base/Component.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/base/Module.js":
/*!****************************************************!*\
  !*** ./node_modules/vevet/build/es/base/Module.js ***!
  \****************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   Module: () => (/* binding */ Module)\n/* harmony export */ });\n/* harmony import */ var vevet_dom__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! vevet-dom */ \"./node_modules/vevet-dom/dist/es/index.js\");\n/* harmony import */ var _Callbacks__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./Callbacks */ \"./node_modules/vevet/build/es/base/Callbacks.js\");\n/* harmony import */ var _MutableProp__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./MutableProp */ \"./node_modules/vevet/build/es/base/MutableProp.js\");\n/* harmony import */ var _utils_common_mergeWithoutArrays__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../utils/common/mergeWithoutArrays */ \"./node_modules/vevet/build/es/utils/common/mergeWithoutArrays.js\");\n/* harmony import */ var _utils_errors__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../utils/errors */ \"./node_modules/vevet/build/es/utils/errors.js\");\n\r\n\r\n\r\n\r\n\r\n/**\r\n * A class for modules.\r\n */\r\nclass Module {\r\n    /**\r\n     * @example\r\n     * const mod = new Module();\r\n     */\r\n    constructor(\r\n    /**\r\n     * Properties on script start\r\n     */\r\n    initialProp, \r\n    /**\r\n     * Defines if you need to call {@linkcode Module.init} at the constructor's end.\r\n     * If you want to add responsive properties, set this argument to FALSE.\r\n     */\r\n    init = true) {\r\n        /**\r\n         * If the module is initialized\r\n         */\r\n        this._inited = false;\r\n        /**\r\n         * Viewport callbacks\r\n         */\r\n        this._viewportCallbacks = [];\r\n        // set vars\r\n        if (window.vevetApp) {\r\n            this._app = window.vevetApp;\r\n        }\r\n        else {\r\n            (0,_utils_errors__WEBPACK_IMPORTED_MODULE_1__.throwVevetAppError)();\r\n        }\r\n        // set default vars\r\n        this._destroyed = false;\r\n        this._listeners = [];\r\n        // create callbacks\r\n        this._callbacks = new _Callbacks__WEBPACK_IMPORTED_MODULE_2__.Callbacks();\r\n        // create mutable properties\r\n        const prop = (0,_utils_common_mergeWithoutArrays__WEBPACK_IMPORTED_MODULE_3__[\"default\"])(this._getDefaultProp(), initialProp || {});\r\n        this._mutableProp = new _MutableProp__WEBPACK_IMPORTED_MODULE_4__.MutableProp(prop, this._onPropResponsive.bind(this), this._onPropChange.bind(this), this.name);\r\n        if (init) {\r\n            this.init();\r\n        }\r\n    }\r\n    /**\r\n     * Get Default properties (should be extended)\r\n     */\r\n    _getDefaultProp() {\r\n        return {\r\n            parent: false,\r\n        };\r\n    }\r\n    /**\r\n     * Current properties\r\n     */\r\n    get prop() {\r\n        return this._mutableProp.prop;\r\n    }\r\n    /**\r\n     * Module Callbacks\r\n     */\r\n    get callbacks() {\r\n        return this._callbacks;\r\n    }\r\n    /**\r\n     * Module prefix\r\n     */\r\n    get prefix() {\r\n        return '';\r\n    }\r\n    /**\r\n     * Get module name\r\n     */\r\n    get name() {\r\n        return this.constructor.name;\r\n    }\r\n    get inited() {\r\n        return this._inited;\r\n    }\r\n    get destroyed() {\r\n        return this._destroyed;\r\n    }\r\n    /**\r\n     * Add responsive rules\r\n     */\r\n    addResponsiveProp(rules) {\r\n        if (this._inited) {\r\n            throw new Error('Responsive properties cannot be added because the class instance is already initialized');\r\n        }\r\n        else {\r\n            this._mutableProp.addResponsiveProp(rules);\r\n        }\r\n    }\r\n    /**\r\n     * Change module properties.\r\n     * @example\r\n     *\r\n     * // changing properties\r\n     * // let's imagine that the module has the following properties:\r\n     * prop = {\r\n     *     name: 'module',\r\n     *     cute: true\r\n     * };\r\n     * // we can change some properties in it: whether one or several properties\r\n     * // after the properties are changed, the method _onPropChange is called.\r\n     * module.changeProp({\r\n     *     cute: false\r\n     * });\r\n     */\r\n    changeProp(prop = {}) {\r\n        this._mutableProp.changeProp(prop);\r\n        this._callbacks.tbt('changeProp', false);\r\n    }\r\n    /**\r\n     * The method that is called on window resize and properties change.\r\n     */\r\n    _onPropResponsive() {\r\n        this._onPropMutate();\r\n    }\r\n    /**\r\n     * The method that is called on properties change.\r\n     */\r\n    _onPropChange(\r\n    // eslint-disable-next-line @typescript-eslint/no-unused-vars\r\n    changedProp) {\r\n        this._onPropMutate();\r\n    }\r\n    /**\r\n     * The method that is called on properties change.\r\n     */\r\n    _onPropMutate() { }\r\n    /**\r\n     * Initializes the class.\r\n     */\r\n    init() {\r\n        // return if the module is already initialized\r\n        if (this._inited) {\r\n            return;\r\n        }\r\n        this._inited = true;\r\n        // continue initializing\r\n        this._constructor();\r\n        this._setEvents();\r\n        // destroy the current module on parent destroy\r\n        if (this.prop.parent) {\r\n            this.prop.parent.addCallback('destroy', () => {\r\n                this.destroy();\r\n            });\r\n        }\r\n    }\r\n    /**\r\n     * Extra constructor\r\n     */\r\n    _constructor() {\r\n        // code\r\n    }\r\n    /**\r\n     * Set events\r\n     */\r\n    _setEvents() {\r\n        // code\r\n    }\r\n    /**\r\n     * Add a viewport callback that will be removed on class destroy\r\n     * {@see Viewport}\r\n     */\r\n    addViewportCallback(target, func, data = {}) {\r\n        const callback = this._app.viewport.add(target, func, Object.assign(Object.assign({}, data), { name: this.constructor.name }));\r\n        this._viewportCallbacks.push(callback);\r\n        return callback;\r\n    }\r\n    /**\r\n     * Add a module callback\r\n     * {@see Callbacks}\r\n     */\r\n    addCallback(target, func, data = {}) {\r\n        const callback = this.callbacks.add(target, func, data);\r\n        return callback;\r\n    }\r\n    /**\r\n     * Add a DOM event listeners\r\n     */\r\n    addEventListeners(el, target, callback, options) {\r\n        const listener = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.addEventListener)(el, target, callback, options);\r\n        this._listeners.push(listener);\r\n        return listener;\r\n    }\r\n    /**\r\n     * Destroy the module\r\n     */\r\n    destroy() {\r\n        if (this.destroyed) {\r\n            return;\r\n        }\r\n        this._destroy();\r\n    }\r\n    /**\r\n     * Destroy the module\r\n     */\r\n    _destroy() {\r\n        // destroy callbacks\r\n        this._callbacks.tbt('destroy', false);\r\n        this._callbacks.destroy();\r\n        // destroy mutable properties\r\n        this._mutableProp.destroy();\r\n        // destroy viewport callbacks\r\n        this._viewportCallbacks.forEach((callback) => {\r\n            callback.remove();\r\n        });\r\n        // destroy all listeners\r\n        this._listeners.forEach((listener) => {\r\n            listener.remove();\r\n        });\r\n        // events\r\n        this._destroyed = true;\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/base/Module.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/base/MutableProp.js":
/*!*********************************************************!*\
  !*** ./node_modules/vevet/build/es/base/MutableProp.js ***!
  \*********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   MutableProp: () => (/* binding */ MutableProp)\n/* harmony export */ });\n/* harmony import */ var _utils_common__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../utils/common */ \"./node_modules/vevet/build/es/utils/common/mergeWithoutArrays.js\");\n\r\n/**\r\n * A class for creating mutable properties that can change on window resize. <br><br>\r\n *\r\n * There are two ways to change properties:\r\n * <ul>\r\n *     <li>\r\n *         To set a resize-listener on window (or use {@linkcode Viewport}).\r\n *         When the window is resized, change the properties with the help of\r\n *         {@linkcode MutableProp.changeProp}</li>\r\n *     <li>\r\n *         The second way is to use the MutableProp and add responsive properties\r\n *         with help of {@linkcode MutableProp.addResponsiveProp}.</li>\r\n * </ul>\r\n */\r\nclass MutableProp {\r\n    /**\r\n     * @example\r\n     * const static = {\r\n     *      myProp: true,\r\n     * };\r\n     * const responsive = [\r\n     *      {\r\n     *          breakpoint: 'm',\r\n     *          settings: {\r\n     *              myProp: false\r\n     *          }\r\n     *      }\r\n     * ];\r\n     * const prop = new MutableProp(static, responsive);\r\n     */\r\n    constructor(\r\n    /**\r\n     * The properties that were set while initialization.\r\n     * These properties do not change throughout time.\r\n     */\r\n    _initProp, \r\n    /**\r\n     * A callback that is launched when properties are changed on window resize\r\n     */\r\n    _onResponsive = () => { }, \r\n    /**\r\n     * A callback that is launched when properties are changed\r\n     * through {@linkcode MutableProp.changeProp}\r\n     */\r\n    _onChange = () => { }, \r\n    /**\r\n     * Name of the responsive properties.\r\n     */\r\n    _name = 'Responsive Prop') {\r\n        this._initProp = _initProp;\r\n        this._onResponsive = _onResponsive;\r\n        this._onChange = _onChange;\r\n        this._name = _name;\r\n        /**\r\n         * A set of responsive rules\r\n         */\r\n        this._responsiveRules = [];\r\n        this._app = window.vevetApp;\r\n        this._refProp = (0,_utils_common__WEBPACK_IMPORTED_MODULE_0__[\"default\"])({}, _initProp);\r\n        this._prop = (0,_utils_common__WEBPACK_IMPORTED_MODULE_0__[\"default\"])({}, _initProp);\r\n    }\r\n    /**\r\n     * Get current properties\r\n     */\r\n    get prop() {\r\n        return this._prop;\r\n    }\r\n    /**\r\n     * Add responsive rules\r\n     */\r\n    addResponsiveProp(rules) {\r\n        this._responsiveRules.push(rules);\r\n        // add event on resize\r\n        if (typeof this._viewportCallback === 'undefined') {\r\n            this._viewportCallback = this._app.viewport.add('w', this._responseProp.bind(this, true), {\r\n                name: this._name,\r\n            });\r\n        }\r\n        // change properties according to the responsive prop\r\n        this._responseProp();\r\n    }\r\n    /**\r\n     * Change properties according to the \"responsive\" settings\r\n     * @param onResize - If the method was called on window resize.\r\n     */\r\n    _responseProp(onResize = false) {\r\n        const responsiveProp = this._responsiveRules;\r\n        // get sizes\r\n        const app = this._app;\r\n        const { viewport } = app;\r\n        const { width } = viewport;\r\n        let newProp = false;\r\n        const statProp = (0,_utils_common__WEBPACK_IMPORTED_MODULE_0__[\"default\"])({}, this._refProp);\r\n        // go through all breakpoints\r\n        // and check if a proper breakpoint exists\r\n        responsiveProp.forEach((prop) => {\r\n            // copy settings\r\n            const { settings, breakpoint } = prop;\r\n            // if the breakpoint is a number\r\n            if (typeof breakpoint === 'number') {\r\n                if (width <= prop.breakpoint) {\r\n                    newProp = (0,_utils_common__WEBPACK_IMPORTED_MODULE_0__[\"default\"])(statProp, settings);\r\n                }\r\n            }\r\n            else if (typeof breakpoint === 'string') {\r\n                const string = breakpoint.toLowerCase();\r\n                // viewport size\r\n                if ((string === 'd' && viewport.isDesktop)\r\n                    || (string === 't' && viewport.isTablet)\r\n                    || (string === 'p' && viewport.isPhone)) {\r\n                    newProp = (0,_utils_common__WEBPACK_IMPORTED_MODULE_0__[\"default\"])(statProp, settings);\r\n                }\r\n                // device type\r\n                if ((string === 'phone' && app.isPhone)\r\n                    || (string === 'tablet' && app.isTablet)\r\n                    || (string === 'mobile' && app.isMobile)) {\r\n                    newProp = (0,_utils_common__WEBPACK_IMPORTED_MODULE_0__[\"default\"])(statProp, settings);\r\n                }\r\n            }\r\n        });\r\n        // if there's no breakpoint, restore the props\r\n        if (!newProp) {\r\n            this._prop = (0,_utils_common__WEBPACK_IMPORTED_MODULE_0__[\"default\"])(this._prop, this._refProp);\r\n        }\r\n        else {\r\n            // otherwise, change the properties\r\n            this._prop = (0,_utils_common__WEBPACK_IMPORTED_MODULE_0__[\"default\"])(this._prop, newProp);\r\n        }\r\n        // responsive callback\r\n        if (onResize) {\r\n            this._onResponsive();\r\n        }\r\n    }\r\n    /**\r\n     * This method allows you to change the properties manually.\r\n     */\r\n    changeProp(prop) {\r\n        this._prop = (0,_utils_common__WEBPACK_IMPORTED_MODULE_0__[\"default\"])(this._prop, prop);\r\n        this._refProp = (0,_utils_common__WEBPACK_IMPORTED_MODULE_0__[\"default\"])(this._refProp, prop);\r\n        // change prop callback\r\n        this._onChange(prop);\r\n    }\r\n    /**\r\n     * Destroy the responsive properties.\r\n     */\r\n    destroy() {\r\n        if (this._viewportCallback) {\r\n            this._viewportCallback.remove();\r\n        }\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/base/MutableProp.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/base/Plugin.js":
/*!****************************************************!*\
  !*** ./node_modules/vevet/build/es/base/Plugin.js ***!
  \****************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   Plugin: () => (/* binding */ Plugin)\n/* harmony export */ });\n/* harmony import */ var _Module__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./Module */ \"./node_modules/vevet/build/es/base/Module.js\");\n\r\n/**\r\n * A class for Plugins.\r\n */\r\nclass Plugin extends _Module__WEBPACK_IMPORTED_MODULE_0__.Module {\r\n    constructor(initialProp) {\r\n        super(initialProp, false);\r\n    }\r\n    get component() {\r\n        return this._component;\r\n    }\r\n    /**\r\n     * Initializes the class\r\n     */\r\n    init() {\r\n        if (!this._component) {\r\n            throw new Error('Component is not set. Be sure that initlugin is called before.');\r\n        }\r\n        super.init();\r\n    }\r\n    /**\r\n     * Initialize the plugin\r\n     */\r\n    initPlugin(parent) {\r\n        if (this._inited) {\r\n            return;\r\n        }\r\n        this._component = parent;\r\n        this.init();\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/base/Plugin.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/components/animation-frame/AnimationFrame.js":
/*!**********************************************************************************!*\
  !*** ./node_modules/vevet/build/es/components/animation-frame/AnimationFrame.js ***!
  \**********************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   AnimationFrame: () => (/* binding */ AnimationFrame)\n/* harmony export */ });\n/* harmony import */ var _base_Component__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../base/Component */ \"./node_modules/vevet/build/es/base/Component.js\");\n/* harmony import */ var _utils_math__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../utils/math */ \"./node_modules/vevet/build/es/utils/math/clamp.js\");\n\r\n\r\n/**\r\n * Launch an animation frame with a certain FPS\r\n */\r\nclass AnimationFrame extends _base_Component__WEBPACK_IMPORTED_MODULE_0__.Component {\r\n    constructor(initialProp, init = true) {\r\n        super(initialProp, false);\r\n        this._isPlaying = false;\r\n        this._frame = null;\r\n        this._frameIndex = -1;\r\n        this._timeStamp = null;\r\n        this._prevFrameTime = null;\r\n        if (init) {\r\n            this.init();\r\n        }\r\n    }\r\n    get isPlaying() {\r\n        return this._isPlaying;\r\n    }\r\n    /**\r\n     * Get default properties\r\n     */\r\n    _getDefaultProp() {\r\n        return Object.assign(Object.assign({}, super._getDefaultProp()), { fps: 140, run: false });\r\n    }\r\n    // Extra constructor\r\n    _constructor() {\r\n        super._constructor();\r\n        this._create();\r\n    }\r\n    /**\r\n     * Create the frame\r\n     */\r\n    _create() {\r\n        if (this.prop.run) {\r\n            this.play();\r\n        }\r\n    }\r\n    _onPropMutate() {\r\n        this._frameIndex = -1;\r\n        this._timeStamp = null;\r\n        this._prevFrameTime = null;\r\n        if (this.prop.run) {\r\n            this._play();\r\n        }\r\n        else {\r\n            this._pause();\r\n        }\r\n    }\r\n    /**\r\n     * Launch the animation frame.\r\n     */\r\n    play() {\r\n        if (this.destroyed) {\r\n            return;\r\n        }\r\n        if (this.prop.run) {\r\n            return;\r\n        }\r\n        this.changeProp({\r\n            run: true,\r\n        });\r\n    }\r\n    /**\r\n     * Launch the animation frame.\r\n     */\r\n    _play() {\r\n        // check if already playing\r\n        if (this.isPlaying) {\r\n            return;\r\n        }\r\n        this._isPlaying = true;\r\n        // launch animation\r\n        this._frame = window.requestAnimationFrame(this._animate.bind(this));\r\n    }\r\n    /**\r\n     * Stop the animation frame\r\n     */\r\n    pause() {\r\n        if (!this.prop.run) {\r\n            return;\r\n        }\r\n        this.changeProp({\r\n            run: false,\r\n        });\r\n    }\r\n    /**\r\n     * Stop the animation frame\r\n     */\r\n    _pause() {\r\n        if (!this.isPlaying) {\r\n            return;\r\n        }\r\n        // cancel animation frame\r\n        if (this._frame) {\r\n            window.cancelAnimationFrame(this._frame);\r\n            this._frame = null;\r\n        }\r\n        // bool\r\n        this._isPlaying = false;\r\n    }\r\n    /**\r\n     * Launch the animation frame.\r\n     */\r\n    _animate(timestamp) {\r\n        if (!this._isPlaying) {\r\n            return;\r\n        }\r\n        // launch animation\r\n        this._frame = window.requestAnimationFrame(this._animate.bind(this));\r\n        // update variables\r\n        const currentTime = +new Date();\r\n        if (this._timeStamp == null) {\r\n            this._timeStamp = timestamp;\r\n        }\r\n        if (this._prevFrameTime == null) {\r\n            this._prevFrameTime = currentTime;\r\n        }\r\n        // calculate frame index\r\n        const newFrameIndex = Math.floor((timestamp - this._timeStamp) / (1000 / this.prop.fps));\r\n        if (newFrameIndex <= this._frameIndex) {\r\n            return;\r\n        }\r\n        // update frame index\r\n        this._frameIndex = newFrameIndex;\r\n        // calculate real fps\r\n        const timeDiff = currentTime - this._prevFrameTime;\r\n        const realFPS = (0,_utils_math__WEBPACK_IMPORTED_MODULE_1__[\"default\"])(timeDiff === 0 ? 1000 / 60 : Math.floor(1000 / timeDiff), [1, Infinity]);\r\n        // launch callbacks\r\n        this.callbacks.tbt('frame', {\r\n            fps: this.prop.fps,\r\n            realFPS,\r\n            prevFrameDuration: currentTime - this._prevFrameTime,\r\n        });\r\n        this._prevFrameTime = +new Date();\r\n    }\r\n    /**\r\n     * Destroy the animation frame\r\n     */\r\n    _destroy() {\r\n        this.pause();\r\n        super._destroy();\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/components/animation-frame/AnimationFrame.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/components/canvas/Ctx2D.js":
/*!****************************************************************!*\
  !*** ./node_modules/vevet/build/es/components/canvas/Ctx2D.js ***!
  \****************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   Ctx2D: () => (/* binding */ Ctx2D)\n/* harmony export */ });\n/* harmony import */ var vevet_dom__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! vevet-dom */ \"./node_modules/vevet-dom/dist/es/index.js\");\n/* harmony import */ var _base_Component__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../base/Component */ \"./node_modules/vevet/build/es/base/Component.js\");\n\r\n\r\n/**\r\n * Makes working with canvas easier.\r\n * It creates an HTML5 Canvas element and its 2d context and can\r\n * implement automatic resize.\r\n */\r\nclass Ctx2D extends _base_Component__WEBPACK_IMPORTED_MODULE_1__.Component {\r\n    constructor(initialProp, init = true) {\r\n        super(initialProp, false);\r\n        // set default values\r\n        this._width = 0;\r\n        this._height = 0;\r\n        this._dpr = 1;\r\n        // create canvas\r\n        const { append, container } = this.prop;\r\n        const parent = (append && container) ? container : undefined;\r\n        this._canvas = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.createElement)('canvas', {\r\n            parent,\r\n        });\r\n        this._ctx = this._canvas.getContext('2d');\r\n        // and resize it for the first time\r\n        if (this.prop.initialResize) {\r\n            this.resize();\r\n        }\r\n        // initialize the class\r\n        if (init) {\r\n            this.init();\r\n        }\r\n    }\r\n    /**\r\n     * The parent container of the canvas\r\n     */\r\n    get container() {\r\n        return this.prop.container;\r\n    }\r\n    get canvas() {\r\n        return this._canvas;\r\n    }\r\n    get ctx() {\r\n        return this._ctx;\r\n    }\r\n    get width() {\r\n        return this._width;\r\n    }\r\n    /**\r\n     * Canvas width without DPR\r\n     */\r\n    get nonDPRWidth() {\r\n        return this.width / this.dpr;\r\n    }\r\n    /**\r\n     * Canvas width without DPR\r\n     */\r\n    get clientWidth() {\r\n        return this.width / this.dpr;\r\n    }\r\n    get height() {\r\n        return this._height;\r\n    }\r\n    /**\r\n     * Canvas height without DPR\r\n     */\r\n    get nonDPRHeight() {\r\n        return this.height / this.dpr;\r\n    }\r\n    /**\r\n     * Canvas height without DPR\r\n     */\r\n    get clientHeight() {\r\n        return this.height / this.dpr;\r\n    }\r\n    get dpr() {\r\n        return this._dpr;\r\n    }\r\n    /**\r\n     * Check if width and height are not zero for content to be rendered\r\n     */\r\n    get canRender() {\r\n        return this.width > 0 && this.height > 0;\r\n    }\r\n    _getDefaultProp() {\r\n        return Object.assign(Object.assign({}, super._getDefaultProp()), { container: false, append: true, updateOnResize: false, initialResize: true, resizeTimeout: 0, width: false, height: false, dpr: false });\r\n    }\r\n    // Set Module Events\r\n    _setEvents() {\r\n        super._setEvents();\r\n        if (this.prop.updateOnResize !== false) {\r\n            const viewportTarget = typeof this.prop.updateOnResize === 'boolean' ? '' : this.prop.updateOnResize;\r\n            this.addViewportCallback(viewportTarget, () => {\r\n                this.resize();\r\n            }, {\r\n                timeout: this.prop.resizeTimeout,\r\n            });\r\n            this.resize();\r\n        }\r\n    }\r\n    _onPropMutate() {\r\n        super._onPropMutate();\r\n        this.resize();\r\n    }\r\n    /**\r\n     * Resize the canvas\r\n     */\r\n    resize() {\r\n        // vars\r\n        const { prop, canvas } = this;\r\n        if (!canvas) {\r\n            return;\r\n        }\r\n        const app = this._app;\r\n        const { viewport } = app;\r\n        // calculate dpr\r\n        if (typeof prop.dpr === 'number') {\r\n            this._dpr = prop.dpr;\r\n        }\r\n        else {\r\n            this._dpr = viewport.dpr;\r\n        }\r\n        const dpr = this._dpr;\r\n        // calculate new width & height\r\n        let newWidth = 0;\r\n        let newHeight = 0;\r\n        if (typeof prop.width === 'number' && typeof prop.height === 'number') {\r\n            newWidth = prop.width;\r\n            newHeight = prop.height;\r\n        }\r\n        else if (this.container) {\r\n            newWidth = this.container.clientWidth;\r\n            newHeight = this.container.clientHeight;\r\n        }\r\n        else {\r\n            newWidth = viewport.width;\r\n            newHeight = viewport.height;\r\n        }\r\n        newWidth *= dpr;\r\n        newHeight *= dpr;\r\n        // update sizes\r\n        this._width = newWidth;\r\n        this._height = newHeight;\r\n        // apply sizes\r\n        canvas.width = newWidth;\r\n        canvas.height = newHeight;\r\n        this.callbacks.tbt('resize', false);\r\n    }\r\n    /**\r\n     * Destroy the module\r\n     */\r\n    _destroy() {\r\n        super._destroy();\r\n        if (this.canvas) {\r\n            this.canvas.remove();\r\n        }\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/components/canvas/Ctx2D.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/components/canvas/Ctx2DPrerender.js":
/*!*************************************************************************!*\
  !*** ./node_modules/vevet/build/es/components/canvas/Ctx2DPrerender.js ***!
  \*************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   Ctx2DPrerender: () => (/* binding */ Ctx2DPrerender)\n/* harmony export */ });\n/* harmony import */ var get_image_pos__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! get-image-pos */ \"./node_modules/get-image-pos/dist/es/index.js\");\n/* harmony import */ var _Ctx2D__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./Ctx2D */ \"./node_modules/vevet/build/es/components/canvas/Ctx2D.js\");\n\r\n\r\n/**\r\n * The class allows you to prerender media for further use with reduced payloads.\r\n */\r\nclass Ctx2DPrerender extends _Ctx2D__WEBPACK_IMPORTED_MODULE_1__.Ctx2D {\r\n    _getDefaultProp() {\r\n        return Object.assign(Object.assign({}, super._getDefaultProp()), { media: false, posRule: 'cover' });\r\n    }\r\n    /**\r\n     * Resize the canvas\r\n     */\r\n    resize() {\r\n        super.resize();\r\n        this._prerender();\r\n    }\r\n    _prerender() {\r\n        // check if can render\r\n        if (this.width === 0 || this.height === 0) {\r\n            return;\r\n        }\r\n        // check if media exists\r\n        const { media } = this.prop;\r\n        if (!media) {\r\n            return;\r\n        }\r\n        // get source info\r\n        let source;\r\n        let sourceWidth;\r\n        let sourceHeight;\r\n        if (media instanceof _Ctx2D__WEBPACK_IMPORTED_MODULE_1__.Ctx2D) {\r\n            source = media.canvas;\r\n            sourceWidth = media.width;\r\n            sourceHeight = media.height;\r\n        }\r\n        else {\r\n            source = media;\r\n        }\r\n        // get media sizes\r\n        const size = (0,get_image_pos__WEBPACK_IMPORTED_MODULE_0__.getPos)({\r\n            source,\r\n            sourceWidth,\r\n            sourceHeight,\r\n            rule: this.prop.posRule,\r\n            scale: 1,\r\n            width: this.width,\r\n            height: this.height,\r\n        });\r\n        this._ctx.clearRect(0, 0, this.width, this.height);\r\n        // render the media\r\n        this._ctx.drawImage(source, size.x, size.y, size.width, size.height);\r\n        // launch callbacks on prerender\r\n        this.callbacks.tbt('prerender', false);\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/components/canvas/Ctx2DPrerender.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/components/cursor/CustomCursor.js":
/*!***********************************************************************!*\
  !*** ./node_modules/vevet/build/es/components/cursor/CustomCursor.js ***!
  \***********************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   CustomCursor: () => (/* binding */ CustomCursor)\n/* harmony export */ });\n/* harmony import */ var vevet_dom__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! vevet-dom */ \"./node_modules/vevet-dom/dist/es/index.js\");\n/* harmony import */ var _animation_frame_AnimationFrame__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../animation-frame/AnimationFrame */ \"./node_modules/vevet/build/es/components/animation-frame/AnimationFrame.js\");\n/* harmony import */ var _base_Component__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../base/Component */ \"./node_modules/vevet/build/es/base/Component.js\");\n/* harmony import */ var _utils_math_lerp__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../../utils/math/lerp */ \"./node_modules/vevet/build/es/utils/math/lerp.js\");\n/* harmony import */ var _utils_common__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../../utils/common */ \"./node_modules/vevet/build/es/utils/common/timeoutCallback.js\");\n\r\n\r\n\r\n\r\n\r\n/**\r\n * Creates a smooth custom cursor\r\n */\r\nclass CustomCursor extends _base_Component__WEBPACK_IMPORTED_MODULE_1__.Component {\r\n    constructor(initialProp, init = true) {\r\n        super(initialProp, false);\r\n        // get cursor container\r\n        const container = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.selectOne)(this.prop.container);\r\n        if (container) {\r\n            this._container = container;\r\n        }\r\n        else {\r\n            throw new Error(`No cursor container for ${this.prop.container}`);\r\n        }\r\n        this._containerIsWindow = container instanceof Window;\r\n        // set default vars\r\n        this._coords = {\r\n            x: 0, y: 0, w: 0, h: 0,\r\n        };\r\n        this._targetCoords = {\r\n            x: 0, y: 0, w: 0, h: 0,\r\n        };\r\n        this._currentFPS = 60;\r\n        this._canPlay = this.prop.run;\r\n        // initialize the class\r\n        if (init) {\r\n            this.init();\r\n        }\r\n    }\r\n    _getDefaultProp() {\r\n        return Object.assign(Object.assign({}, super._getDefaultProp()), { container: window, run: true, hideNativeCursor: false, size: {\r\n                width: 50,\r\n                height: 50,\r\n            }, render: {\r\n                lerp: 0.2,\r\n                lerpToFixed: 2,\r\n                normalizeLerp: false,\r\n            }, hover: {\r\n                sticky: false,\r\n                autoSize: false,\r\n            }, autoStop: true });\r\n    }\r\n    get prefix() {\r\n        return `${this._app.prefix}custom-cursor`;\r\n    }\r\n    /**\r\n     * Cursor container\r\n     */\r\n    get container() {\r\n        return this._container;\r\n    }\r\n    /**\r\n     * The element for events\r\n     */\r\n    get eventsContainer() {\r\n        return this._container;\r\n    }\r\n    /**\r\n     * The DOM parent for the cursor element\r\n     */\r\n    get domContainer() {\r\n        if (this.container instanceof Window) {\r\n            return this._app.html;\r\n        }\r\n        return this.container;\r\n    }\r\n    /**\r\n     * Cursor element (outer element)\r\n     */\r\n    get outerCursor() {\r\n        return this._outerCursor;\r\n    }\r\n    /**\r\n     * Cursor element (inner element)\r\n     */\r\n    get innerCursor() {\r\n        return this._innerCursor;\r\n    }\r\n    get hoveredEl() {\r\n        return this._hoveredEl;\r\n    }\r\n    set hoveredEl(val) {\r\n        this._hoveredEl = val;\r\n    }\r\n    get coords() {\r\n        return this._coords;\r\n    }\r\n    get targetCoords() {\r\n        var _a, _b, _c;\r\n        const { hoveredEl, prop } = this;\r\n        const sizes = prop.size;\r\n        let { x } = this._targetCoords;\r\n        let { y } = this._targetCoords;\r\n        let w = sizes.width;\r\n        let h = sizes.height;\r\n        let padding = 0;\r\n        if (hoveredEl) {\r\n            const bounding = hoveredEl.el.getBoundingClientRect();\r\n            if (prop.hover.sticky) {\r\n                x = bounding.left + bounding.width / 2;\r\n                y = bounding.top + bounding.height / 2;\r\n            }\r\n            if (prop.hover.autoSize) {\r\n                w = ((_a = this.hoveredEl) === null || _a === void 0 ? void 0 : _a.width) || bounding.width;\r\n                h = ((_b = this.hoveredEl) === null || _b === void 0 ? void 0 : _b.height) || bounding.height;\r\n                padding = ((_c = this.hoveredEl) === null || _c === void 0 ? void 0 : _c.padding) || 0;\r\n            }\r\n        }\r\n        w += padding * 2;\r\n        h += padding * 2;\r\n        return {\r\n            x, y, w, h,\r\n        };\r\n    }\r\n    // Extra constructor\r\n    _constructor() {\r\n        super._constructor();\r\n        this._createCursor();\r\n    }\r\n    // Set Module Events\r\n    _setEvents() {\r\n        super._setEvents();\r\n        const { domContainer } = this;\r\n        // create animation frame\r\n        this._animationFrame = new _animation_frame_AnimationFrame__WEBPACK_IMPORTED_MODULE_2__.AnimationFrame();\r\n        this._animationFrame.addCallback('frame', (data) => {\r\n            this._currentFPS = data.realFPS;\r\n            this.render();\r\n        });\r\n        if (this._canPlay) {\r\n            this.enable();\r\n        }\r\n        // set mouse hover events\r\n        this.addEventListeners(domContainer, 'mouseenter', this._handleMouseEnter.bind(this));\r\n        this.addEventListeners(domContainer, 'mouseleave', this._handleMouseLeave.bind(this));\r\n        // set move events\r\n        this.addEventListeners(domContainer, 'mousemove', this._handleMouseMove.bind(this));\r\n        // set click events events\r\n        this.addEventListeners(domContainer, 'mousedown', this._handleMouseDown.bind(this));\r\n        this.addEventListeners(domContainer, 'mouseup', this._handleMouseUp.bind(this));\r\n        this.addEventListeners(window, 'blur', this._handleWindowBlur.bind(this));\r\n    }\r\n    /**\r\n     * Set hover events on an element\r\n     */\r\n    addHoverEl(settings, enterTimeout = 100) {\r\n        const { el } = settings;\r\n        let timeout;\r\n        const mouseEnter = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.addEventListener)(el, 'mouseenter', () => {\r\n            timeout = (0,_utils_common__WEBPACK_IMPORTED_MODULE_3__[\"default\"])(() => {\r\n                this.hoveredEl = Object.assign({}, settings);\r\n            }, enterTimeout);\r\n        });\r\n        const mouseLeave = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.addEventListener)(el, 'mouseleave', () => {\r\n            this.hoveredEl = undefined;\r\n            if (timeout) {\r\n                timeout.clear();\r\n            }\r\n        });\r\n        return {\r\n            remove: () => {\r\n                var _a;\r\n                if (((_a = this.hoveredEl) === null || _a === void 0 ? void 0 : _a.el) === el) {\r\n                    this.hoveredEl = undefined;\r\n                }\r\n                mouseEnter.remove();\r\n                mouseLeave.remove();\r\n                timeout === null || timeout === void 0 ? void 0 : timeout.clear();\r\n            },\r\n        };\r\n    }\r\n    /**\r\n     * Create custom cursor\r\n     */\r\n    _createCursor() {\r\n        const { prefix, container, domContainer } = this;\r\n        // hide native cursor\r\n        if (this.prop.hideNativeCursor) {\r\n            domContainer.style.cursor = 'none';\r\n            domContainer.classList.add('hide-defaut-cursor');\r\n        }\r\n        // set classes\r\n        domContainer.classList.add(`${prefix}-container`);\r\n        // create cursor elements\r\n        this._outerCursor = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.createElement)('div', {\r\n            class: `${prefix} ${container instanceof Window ? 'in-window' : 'in-element'} disabled`,\r\n            parent: domContainer,\r\n        });\r\n        this._innerCursor = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.createElement)('div', {\r\n            class: `${prefix}__inner disabled`,\r\n            parent: this._outerCursor,\r\n        });\r\n        // launch events\r\n        this.callbacks.tbt('create', {\r\n            outerCursor: this.outerCursor,\r\n            innerCursor: this.innerCursor,\r\n        });\r\n    }\r\n    /**\r\n     * Destroy the cursor\r\n     */\r\n    _destroyCursor() {\r\n        const { prefix, domContainer } = this;\r\n        domContainer.style.cursor = '';\r\n        domContainer.classList.remove('hide-defaut-cursor');\r\n        domContainer.classList.remove(`${prefix}-container`);\r\n        this._outerCursor.remove();\r\n        this._innerCursor.remove();\r\n    }\r\n    /**\r\n     * Event on mouse enter\r\n     */\r\n    _handleMouseEnter(evt) {\r\n        // update coordinates\r\n        this._coords.x = evt.clientX;\r\n        this._coords.y = evt.clientY;\r\n        this._targetCoords.x = evt.clientX;\r\n        this._targetCoords.y = evt.clientY;\r\n        // set classes\r\n        this.outerCursor.classList.add('in-action');\r\n    }\r\n    /**\r\n     * Event on mouse leave\r\n     */\r\n    _handleMouseLeave() {\r\n        this.outerCursor.classList.remove('in-action');\r\n    }\r\n    /**\r\n     * Event on mouse move\r\n     */\r\n    _handleMouseMove(evt) {\r\n        this._targetCoords.x = evt.clientX;\r\n        this._targetCoords.y = evt.clientY;\r\n        // set classes\r\n        this.outerCursor.classList.add('in-action');\r\n        // launch animation\r\n        if (this._canPlay) {\r\n            this._animationFrame.play();\r\n        }\r\n    }\r\n    /**\r\n     * Event on mouse down\r\n     */\r\n    _handleMouseDown(evt) {\r\n        if (evt.which === 1) {\r\n            this.outerCursor.classList.add('click');\r\n            this.innerCursor.classList.add('click');\r\n        }\r\n    }\r\n    /**\r\n     * Event on mouse up\r\n     */\r\n    _handleMouseUp() {\r\n        this.outerCursor.classList.remove('click');\r\n        this.innerCursor.classList.remove('click');\r\n    }\r\n    /**\r\n     * Event on mouse down\r\n     */\r\n    _handleWindowBlur() {\r\n        this._handleMouseUp();\r\n    }\r\n    /**\r\n     * Render the scene\r\n     */\r\n    render() {\r\n        // props\r\n        const { prop, targetCoords } = this;\r\n        this._calcCoords();\r\n        const realCoords = this._renderElements();\r\n        // auto stop\r\n        if (prop.autoStop\r\n            && this.coords.x === targetCoords.x && this.coords.y === targetCoords.y\r\n            && this.coords.w === targetCoords.w && this.coords.h === targetCoords.h) {\r\n            this._animationFrame.pause();\r\n        }\r\n        // launch render events\r\n        this._callbacks.tbt('render', realCoords);\r\n    }\r\n    /**\r\n     * Recalculate current coordinates\r\n     */\r\n    _calcCoords() {\r\n        this._coords.x = this._lerp(this._coords.x, this.targetCoords.x);\r\n        this._coords.y = this._lerp(this._coords.y, this.targetCoords.y);\r\n        this._coords.w = this._lerp(this._coords.w, this.targetCoords.w);\r\n        this._coords.h = this._lerp(this._coords.h, this.targetCoords.h);\r\n    }\r\n    /**\r\n     * Linear interpolation\r\n     */\r\n    _lerp(current, target) {\r\n        const { normalizeLerp, lerp: ease, lerpToFixed } = this.prop.render;\r\n        const fpsMultiplier = normalizeLerp ? 60 / this._currentFPS : 1;\r\n        let val = (0,_utils_math_lerp__WEBPACK_IMPORTED_MODULE_4__[\"default\"])(current, target, ease * fpsMultiplier, 0.02);\r\n        // round the values\r\n        if (typeof lerpToFixed === 'number') {\r\n            const fixed = Math.round(Math.abs(lerpToFixed));\r\n            val = parseFloat(val.toFixed(fixed));\r\n        }\r\n        return val;\r\n    }\r\n    /**\r\n     * Render cursor elements\r\n     */\r\n    _renderElements() {\r\n        const { domContainer, outerCursor } = this;\r\n        // calculate real coordinates\r\n        let { x, y, } = this.coords;\r\n        const { w, h } = this.coords;\r\n        if (!this._containerIsWindow) {\r\n            const bounding = domContainer.getBoundingClientRect();\r\n            x -= bounding.left;\r\n            y -= bounding.top;\r\n        }\r\n        // update dom coordinates\r\n        outerCursor.style.transform = `translate(${x}px, ${y}px)`;\r\n        outerCursor.style.setProperty('--cursor-w', `${w}px`);\r\n        outerCursor.style.setProperty('--cursor-h', `${h}px`);\r\n        return {\r\n            x, y, w, h,\r\n        };\r\n    }\r\n    /**\r\n     * Enable cursor\r\n     */\r\n    enable() {\r\n        this._canPlay = true;\r\n        this.outerCursor.classList.remove('disabled');\r\n        this.innerCursor.classList.remove('disabled');\r\n        this._animationFrame.play();\r\n    }\r\n    /**\r\n     * Disable cursor\r\n     */\r\n    disable() {\r\n        this._canPlay = false;\r\n        this.outerCursor.classList.add('disabled');\r\n        this.innerCursor.classList.add('disabled');\r\n        this._animationFrame.pause();\r\n    }\r\n    /**\r\n     * Destroy the module\r\n     */\r\n    _destroy() {\r\n        super._destroy();\r\n        this._destroyCursor();\r\n        this._animationFrame.destroy();\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/components/cursor/CustomCursor.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/components/dragger/Dragger.js":
/*!*******************************************************************!*\
  !*** ./node_modules/vevet/build/es/components/dragger/Dragger.js ***!
  \*******************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   Dragger: () => (/* binding */ Dragger)\n/* harmony export */ });\n/* harmony import */ var vevet_dom__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! vevet-dom */ \"./node_modules/vevet-dom/dist/es/index.js\");\n/* harmony import */ var _base_Component__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../base/Component */ \"./node_modules/vevet/build/es/base/Component.js\");\n\r\n\r\n/**\r\n * Drag & Swipe events. An abstract class\r\n */\r\nclass Dragger extends _base_Component__WEBPACK_IMPORTED_MODULE_1__.Component {\r\n    constructor(initialProp, init = true) {\r\n        super(initialProp, false);\r\n        // get container\r\n        this._container = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.selectOne)(this.prop.container);\r\n        if (!(0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.isElement)(this._container) && !(0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.isWindow)(this._container)) {\r\n            throw new Error('No container');\r\n        }\r\n        // set default vars\r\n        this._runtimeEvents = [];\r\n        this._isDragging = false;\r\n        this._pointerID = null;\r\n        this._coords = { x: 0, y: 0 };\r\n        this._prevCoords = { x: 0, y: 0 };\r\n        this._startCoords = { x: 0, y: 0 };\r\n        // initialize the class\r\n        if (init) {\r\n            this.init();\r\n        }\r\n    }\r\n    _getDefaultProp() {\r\n        return Object.assign(Object.assign({}, super._getDefaultProp()), { container: `#${this.prefix}`, usePassive: false, enabled: true });\r\n    }\r\n    get prefix() {\r\n        return `${this._app.prefix}dragger`;\r\n    }\r\n    /**\r\n     * Preloader container\r\n     */\r\n    get container() {\r\n        return this._container;\r\n    }\r\n    /**\r\n     * If is dragging at the moment\r\n     */\r\n    get isDragging() {\r\n        return this._isDragging;\r\n    }\r\n    /**\r\n     * Current coordinates relative to the Window\r\n     */\r\n    get coords() {\r\n        return this._coords;\r\n    }\r\n    /**\r\n     * Coordinates on drag start\r\n     */\r\n    get startCoords() {\r\n        return this._startCoords;\r\n    }\r\n    /**\r\n     * Set component events\r\n     */\r\n    _setEvents() {\r\n        const { container } = this;\r\n        const { usePassive } = this.prop;\r\n        this.addEventListeners(container, 'mousedown', this._handleStart.bind(this), { passive: usePassive });\r\n        this.addEventListeners(container, 'touchstart', this._handleStart.bind(this), { passive: usePassive });\r\n    }\r\n    /**\r\n     * Add runtime events\r\n     */\r\n    _addRuntimeEvents() {\r\n        const { usePassive } = this.prop;\r\n        // end\r\n        this._runtimeEvents.push((0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.addEventListener)(window, 'mouseup', this._handleEnd.bind(this), {\r\n            passive: usePassive,\r\n        }));\r\n        this._runtimeEvents.push((0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.addEventListener)(window, 'touchend', this._handleEnd.bind(this), {\r\n            passive: usePassive,\r\n        }));\r\n        // cancel\r\n        this._runtimeEvents.push((0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.addEventListener)(window, 'touchcancel', this.cancel.bind(this), {\r\n            passive: usePassive,\r\n        }));\r\n        this._runtimeEvents.push((0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.addEventListener)(window, 'blur', this.cancel.bind(this), {\r\n            passive: usePassive,\r\n        }));\r\n    }\r\n    /**\r\n     * Remove runtime events\r\n     */\r\n    _removeRuntimeEvents() {\r\n        this._runtimeEvents.forEach((event) => {\r\n            event.remove();\r\n        });\r\n        this._runtimeEvents = [];\r\n    }\r\n    /**\r\n     * Normalize event data.\r\n     */\r\n    _normalizeEvent(e) {\r\n        if (e.type.includes('touch')) {\r\n            const evt = e;\r\n            const touch = evt.targetTouches[0] || evt.changedTouches[0];\r\n            return {\r\n                x: touch.clientX,\r\n                y: touch.clientY,\r\n                id: touch.identifier,\r\n            };\r\n        }\r\n        const evt = e;\r\n        return {\r\n            x: evt.clientX,\r\n            y: evt.clientY,\r\n            id: null,\r\n        };\r\n    }\r\n    /**\r\n     * Event on start dragging\r\n     */\r\n    _handleStart(e) {\r\n        if (!this.prop.enabled) {\r\n            return false;\r\n        }\r\n        if (this.isDragging) {\r\n            return false;\r\n        }\r\n        const evt = this._normalizeEvent(e);\r\n        // prevent actions\r\n        if (e.type === 'mousedown') {\r\n            if (e.which === 1) {\r\n                e.stopPropagation();\r\n            }\r\n            else {\r\n                return false;\r\n            }\r\n        }\r\n        // update states\r\n        this._isDragging = true;\r\n        this._pointerID = evt.id;\r\n        // change coordinates\r\n        this._coords = { x: evt.x, y: evt.y };\r\n        this._prevCoords = { x: evt.x, y: evt.y };\r\n        this._startCoords = { x: evt.x, y: evt.y };\r\n        // add additional events\r\n        this._addRuntimeEvents();\r\n        // launch callbacks\r\n        this.callbacks.tbt('start', {\r\n            evt: e,\r\n        });\r\n        return true;\r\n    }\r\n    /**\r\n     * Event on drag end\r\n     */\r\n    _handleEnd(e) {\r\n        const evt = this._normalizeEvent(e);\r\n        if (!this.isDragging || evt.id !== this._pointerID) {\r\n            return false;\r\n        }\r\n        this.cancel();\r\n        return true;\r\n    }\r\n    /**\r\n     * Cancel dragging\r\n     */\r\n    cancel() {\r\n        this._removeRuntimeEvents();\r\n        this._isDragging = false;\r\n        this.callbacks.tbt('end', false);\r\n    }\r\n    /**\r\n     * Destroy the module\r\n     */\r\n    _destroy() {\r\n        super._destroy();\r\n        this._removeRuntimeEvents();\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/components/dragger/Dragger.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/components/dragger/DraggerDirection.js":
/*!****************************************************************************!*\
  !*** ./node_modules/vevet/build/es/components/dragger/DraggerDirection.js ***!
  \****************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   DraggerDirection: () => (/* binding */ DraggerDirection)\n/* harmony export */ });\n/* harmony import */ var _Dragger__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./Dragger */ \"./node_modules/vevet/build/es/components/dragger/Dragger.js\");\n\r\n/**\r\n * Dragger that detects direction\r\n */\r\nclass DraggerDirection extends _Dragger__WEBPACK_IMPORTED_MODULE_0__.Dragger {\r\n    _getDefaultProp() {\r\n        return Object.assign(Object.assign({}, super._getDefaultProp()), { min: 75 });\r\n    }\r\n    /**\r\n     * Event on drag end\r\n     */\r\n    _handleEnd(e) {\r\n        const res = super._handleEnd(e);\r\n        if (!res) {\r\n            return res;\r\n        }\r\n        const evt = this._normalizeEvent(e);\r\n        const { startCoords } = this;\r\n        const min = Math.abs(this.prop.min);\r\n        // up\r\n        if (startCoords.y > evt.y\r\n            && Math.abs(evt.y - startCoords.y) >= min) {\r\n            this.callbacks.tbt('up', false);\r\n        }\r\n        // down\r\n        if (startCoords.y < evt.y\r\n            && Math.abs(evt.y - startCoords.y) >= min) {\r\n            this.callbacks.tbt('down', false);\r\n        }\r\n        // left\r\n        if (startCoords.x > evt.x\r\n            && Math.abs(evt.x - startCoords.x) >= min) {\r\n            this.callbacks.tbt('left', false);\r\n        }\r\n        // right\r\n        if (startCoords.x < evt.x\r\n            && Math.abs(evt.x - startCoords.x) >= min) {\r\n            this.callbacks.tbt('right', false);\r\n        }\r\n        return true;\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/components/dragger/DraggerDirection.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/components/dragger/DraggerMove.js":
/*!***********************************************************************!*\
  !*** ./node_modules/vevet/build/es/components/dragger/DraggerMove.js ***!
  \***********************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   DraggerMove: () => (/* binding */ DraggerMove)\n/* harmony export */ });\n/* harmony import */ var vevet_dom__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! vevet-dom */ \"./node_modules/vevet-dom/dist/es/index.js\");\n/* harmony import */ var _Dragger__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./Dragger */ \"./node_modules/vevet/build/es/components/dragger/Dragger.js\");\n\r\n\r\n/**\r\n * Dragger with 'move' callbacks and motion.\r\n */\r\nclass DraggerMove extends _Dragger__WEBPACK_IMPORTED_MODULE_1__.Dragger {\r\n    /**\r\n     * Step on move\r\n     */\r\n    get stepCoords() {\r\n        return {\r\n            x: this._coords.x - this._prevCoords.x,\r\n            y: this._coords.y - this._prevCoords.y,\r\n        };\r\n    }\r\n    /**\r\n     * Add runtime events\r\n     */\r\n    _addRuntimeEvents() {\r\n        super._addRuntimeEvents();\r\n        const { usePassive } = this.prop;\r\n        // move\r\n        this._runtimeEvents.push((0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.addEventListener)(window, 'mousemove', this._handleMove.bind(this), {\r\n            passive: usePassive,\r\n        }));\r\n        this._runtimeEvents.push((0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.addEventListener)(window, 'touchmove', this._handleMove.bind(this), {\r\n            passive: usePassive,\r\n        }));\r\n    }\r\n    /**\r\n     * Event on move\r\n     */\r\n    _handleMove(e) {\r\n        const evt = this._normalizeEvent(e);\r\n        if (!this.isDragging || evt.id !== this._pointerID) {\r\n            return false;\r\n        }\r\n        // update coordinates\r\n        this._prevCoords = { x: this._coords.x, y: this._coords.y };\r\n        this._coords = { x: evt.x, y: evt.y };\r\n        // call events\r\n        this.callbacks.tbt('move', {\r\n            evt: e,\r\n            start: this.startCoords,\r\n            coords: this.coords,\r\n            step: this.stepCoords,\r\n        });\r\n        return true;\r\n    }\r\n    /**\r\n     * Destroy the module\r\n     */\r\n    _destroy() {\r\n        super._destroy();\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/components/dragger/DraggerMove.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/components/loading/Preloader.js":
/*!*********************************************************************!*\
  !*** ./node_modules/vevet/build/es/components/loading/Preloader.js ***!
  \*********************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   Preloader: () => (/* binding */ Preloader)\n/* harmony export */ });\n/* harmony import */ var vevet_dom__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! vevet-dom */ \"./node_modules/vevet-dom/dist/es/index.js\");\n/* harmony import */ var p_cancelable__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! p-cancelable */ \"./node_modules/p-cancelable/index.js\");\n/* harmony import */ var _base_Component__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../base/Component */ \"./node_modules/vevet/build/es/base/Component.js\");\n/* harmony import */ var _utils_common_timeoutCallback__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../../utils/common/timeoutCallback */ \"./node_modules/vevet/build/es/utils/common/timeoutCallback.js\");\n\r\n\r\n\r\n\r\n/**\r\n * A page preloader\r\n */\r\nclass Preloader extends _base_Component__WEBPACK_IMPORTED_MODULE_2__.Component {\r\n    constructor(initialProp, init = true) {\r\n        super(initialProp, false);\r\n        // get preloader container\r\n        if (this.prop.container) {\r\n            const container = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.selectOne)(this.prop.container);\r\n            if (container instanceof HTMLElement) {\r\n                this._container = container;\r\n            }\r\n        }\r\n        // add classes\r\n        if (this._container) {\r\n            this._container.classList.add(this.prefix);\r\n        }\r\n        // set default vars\r\n        this._startTime = +new Date();\r\n        this._endTime = Infinity;\r\n        this._toBeHidden = false;\r\n        this._isHidden = false;\r\n        // initialize the class\r\n        if (init) {\r\n            this.init();\r\n        }\r\n    }\r\n    _getDefaultProp() {\r\n        return Object.assign(Object.assign({}, super._getDefaultProp()), { container: `#${this.prefix}`, hide: 250 });\r\n    }\r\n    get prefix() {\r\n        return `${this._app.prefix}preloader`;\r\n    }\r\n    /**\r\n     * Preloader container\r\n     */\r\n    get container() {\r\n        return this._container;\r\n    }\r\n    /**\r\n     * Preloader start time\r\n     */\r\n    get startTime() {\r\n        return this._startTime;\r\n    }\r\n    /**\r\n     * Preloader end time\r\n     */\r\n    get endTime() {\r\n        return this._endTime;\r\n    }\r\n    /**\r\n     * Total time of page loading before the preloader is to be hidden\r\n     */\r\n    get totalTime() {\r\n        return this._endTime - this._startTime;\r\n    }\r\n    /**\r\n     * If the preloader is hidden\r\n     */\r\n    get isHidden() {\r\n        return this._isHidden;\r\n    }\r\n    _setEvents() {\r\n        super._setEvents();\r\n        this._onLoaded().then(() => {\r\n            this._endTime = +new Date();\r\n            this._handleLoaded();\r\n        });\r\n    }\r\n    /**\r\n     * Trace the moment when the page is fully loaded\r\n     */\r\n    _onLoaded() {\r\n        return new p_cancelable__WEBPACK_IMPORTED_MODULE_1__[\"default\"]((resolve) => {\r\n            if (this._app.pageLoad.loaded) {\r\n                resolve();\r\n            }\r\n            else {\r\n                this._pageLoadEvent = this._app.pageLoad.add('', () => {\r\n                    resolve();\r\n                });\r\n            }\r\n        });\r\n    }\r\n    /**\r\n     * When the page is fully loaded\r\n     */\r\n    _handleLoaded() {\r\n        this.callbacks.tbt('loaded', false);\r\n        // hide the preloader\r\n        if (typeof this.prop.hide !== 'boolean') {\r\n            this.hideContainer();\r\n        }\r\n    }\r\n    /**\r\n     * Hide the container\r\n     */\r\n    hideContainer(duration = typeof this.prop.hide !== 'boolean' ? this.prop.hide : 250) {\r\n        this.callbacks.tbt('hide', false);\r\n        this._toBeHidden = true;\r\n        return new p_cancelable__WEBPACK_IMPORTED_MODULE_1__[\"default\"]((resolve) => {\r\n            const container = this._container;\r\n            // if container is not to be hidden\r\n            if (!container) {\r\n                this._handleHidden();\r\n                resolve();\r\n                return;\r\n            }\r\n            // if need to hide the container\r\n            container.style.transition = `opacity ${duration}ms, visibility ${duration}ms`;\r\n            container.style.opacity = '0';\r\n            container.style.visibility = 'hidden';\r\n            (0,_utils_common_timeoutCallback__WEBPACK_IMPORTED_MODULE_3__[\"default\"])(() => {\r\n                container.style.display = 'none';\r\n                this._handleHidden();\r\n                resolve();\r\n            }, duration);\r\n        });\r\n    }\r\n    /**\r\n     * Handle the moment when the container is hidden\r\n     */\r\n    _handleHidden() {\r\n        this._isHidden = true;\r\n        this.callbacks.tbt('hidden', false);\r\n    }\r\n    /**\r\n     * Callback at the moment when the preloader starts hiding\r\n     */\r\n    onHide(callback) {\r\n        if (this._toBeHidden) {\r\n            callback();\r\n            return undefined;\r\n        }\r\n        return this.callbacks.add('hide', () => {\r\n            callback();\r\n        });\r\n    }\r\n    /**\r\n     * Callback at the moment when the preloader is hidden\r\n     */\r\n    onHidden(callback) {\r\n        if (this._isHidden) {\r\n            callback();\r\n            return undefined;\r\n        }\r\n        return this.callbacks.add('hidden', () => {\r\n            callback();\r\n        });\r\n    }\r\n    /**\r\n     * Destroy the module\r\n     */\r\n    _destroy() {\r\n        var _a;\r\n        super._destroy();\r\n        (_a = this._pageLoadEvent) === null || _a === void 0 ? void 0 : _a.remove();\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/components/loading/Preloader.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/components/loading/ProgressPreloader.js":
/*!*****************************************************************************!*\
  !*** ./node_modules/vevet/build/es/components/loading/ProgressPreloader.js ***!
  \*****************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   ProgressPreloader: () => (/* binding */ ProgressPreloader)\n/* harmony export */ });\n/* harmony import */ var vevet_dom__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! vevet-dom */ \"./node_modules/vevet-dom/dist/es/index.js\");\n/* harmony import */ var p_cancelable__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! p-cancelable */ \"./node_modules/p-cancelable/index.js\");\n/* harmony import */ var _animation_frame_AnimationFrame__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../animation-frame/AnimationFrame */ \"./node_modules/vevet/build/es/components/animation-frame/AnimationFrame.js\");\n/* harmony import */ var _utils_math_clamp__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../../utils/math/clamp */ \"./node_modules/vevet/build/es/utils/math/clamp.js\");\n/* harmony import */ var _utils_math_lerp__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../../utils/math/lerp */ \"./node_modules/vevet/build/es/utils/math/lerp.js\");\n/* harmony import */ var _Preloader__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./Preloader */ \"./node_modules/vevet/build/es/components/loading/Preloader.js\");\n/* harmony import */ var _timeline_Timeline__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ../timeline/Timeline */ \"./node_modules/vevet/build/es/components/timeline/Timeline.js\");\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n/**\r\n * A page preloader with smooth progress calculation\r\n */\r\nclass ProgressPreloader extends _Preloader__WEBPACK_IMPORTED_MODULE_2__.Preloader {\r\n    constructor(initialProp, init = true) {\r\n        super(initialProp, false);\r\n        // set default vars\r\n        this._imgs = [];\r\n        this._videos = [];\r\n        this._customResources = [];\r\n        this._resourcesTotal = 1 + this.prop.resources;\r\n        this._resourcesLoaded = 0;\r\n        this._progress = 0;\r\n        // initialize the class\r\n        if (init) {\r\n            this.init();\r\n        }\r\n    }\r\n    _getDefaultProp() {\r\n        return Object.assign(Object.assign({}, super._getDefaultProp()), { resources: 0, loaders: {\r\n                img: true,\r\n                video: true,\r\n                custom: '.js-preload',\r\n                ignoreClassName: 'js-preload-ignore',\r\n            }, calc: {\r\n                lerp: 0.1,\r\n                forceEnd: 500,\r\n            } });\r\n    }\r\n    /**\r\n     * Image resources\r\n     */\r\n    get imgs() {\r\n        return this._imgs;\r\n    }\r\n    /**\r\n     * Video resources\r\n     */\r\n    get videos() {\r\n        return this._videos;\r\n    }\r\n    /**\r\n     * Custom resources\r\n     */\r\n    get customResources() {\r\n        return this._customResources;\r\n    }\r\n    /**\r\n     * Quantity of resources to be preloader\r\n     */\r\n    get resourcesTotal() {\r\n        return this._resourcesTotal;\r\n    }\r\n    /**\r\n     * Quantity of resources been already loaded\r\n     */\r\n    get resourcesLoaded() {\r\n        return this._resourcesLoaded;\r\n    }\r\n    /**\r\n     * Progress of resources loaded\r\n     */\r\n    get loadProgress() {\r\n        return this.resourcesLoaded / this.resourcesTotal;\r\n    }\r\n    /**\r\n     * Preloader progress\r\n     */\r\n    get progress() {\r\n        return this._progress;\r\n    }\r\n    set progress(val) {\r\n        this._progress = val;\r\n        this._handleProgress();\r\n    }\r\n    _constructor() {\r\n        super._constructor();\r\n        this._getResources();\r\n    }\r\n    _setEvents() {\r\n        super._setEvents();\r\n        const { calc } = this.prop;\r\n        const { lerp: lerpVal } = calc;\r\n        // create animation frame if needed\r\n        if (typeof lerpVal === 'number') {\r\n            this._animationFrame = new _animation_frame_AnimationFrame__WEBPACK_IMPORTED_MODULE_3__.AnimationFrame();\r\n            this._animationFrame.addCallback('frame', () => {\r\n                this.progress = (0,_utils_math_lerp__WEBPACK_IMPORTED_MODULE_4__[\"default\"])(this.progress, this.loadProgress, lerpVal);\r\n            });\r\n            this._animationFrame.play();\r\n        }\r\n        // preload resources\r\n        this._preloadResources();\r\n        // iterate resources on page load\r\n        this._app.onPageLoaded().then(() => {\r\n            this._handleLoadedResource();\r\n        });\r\n    }\r\n    /**\r\n     * Seek the moment when the page is fully loaded\r\n     */\r\n    _onLoaded() {\r\n        return new p_cancelable__WEBPACK_IMPORTED_MODULE_1__[\"default\"]((resolve) => {\r\n            let callbackLaunched = false;\r\n            this.callbacks.add('progress', (data) => {\r\n                if (data.progress >= 1 && !callbackLaunched) {\r\n                    resolve();\r\n                    callbackLaunched = true;\r\n                }\r\n            }, {\r\n                protected: true,\r\n            });\r\n        });\r\n    }\r\n    /**\r\n     * Get resources to be preloader\r\n     */\r\n    _getResources() {\r\n        const { loaders } = this.prop;\r\n        // get images\r\n        if (loaders.img) {\r\n            const imgs = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.selectAll)('img');\r\n            imgs.forEach((img) => {\r\n                if (img.classList.contains(loaders.ignoreClassName)) {\r\n                    return;\r\n                }\r\n                this._imgs.push(img);\r\n                this._resourcesTotal += 1;\r\n            });\r\n        }\r\n        // get videos\r\n        if (loaders.video) {\r\n            const videos = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.selectAll)('video');\r\n            videos.forEach((video) => {\r\n                if (video.classList.contains(loaders.ignoreClassName)) {\r\n                    return;\r\n                }\r\n                this._videos.push(video);\r\n                this._resourcesTotal += 1;\r\n            });\r\n        }\r\n        // get custom resources\r\n        if (loaders.custom) {\r\n            Array.from((0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.selectAll)(loaders.custom)).forEach((el) => {\r\n                // note that each custom resource may have the attribute \"data-load-count\"\r\n                // which defines how many resources should be added to the loading queue.\r\n                // here we check this quantity and duplicate elements\r\n                let loadCount = parseInt(el.getAttribute('data-load-count') || '1', 10);\r\n                loadCount = Number.isNaN(loadCount) ? 1 : (0,_utils_math_clamp__WEBPACK_IMPORTED_MODULE_5__[\"default\"])(loadCount, [1, Infinity]);\r\n                for (let index = 1; index <= loadCount; index += 1) {\r\n                    const targetProgress = index / loadCount;\r\n                    this._customResources.push({\r\n                        el,\r\n                        targetProgress,\r\n                    });\r\n                }\r\n            });\r\n            this._resourcesTotal += this._customResources.length;\r\n        }\r\n    }\r\n    /**\r\n     * Preload all resources\r\n     */\r\n    _preloadResources() {\r\n        // check if no resources\r\n        if (this._resourcesTotal === 0) {\r\n            this._resourcesTotal = 1;\r\n            this._handleLoadedResource();\r\n        }\r\n        // preload images\r\n        this._imgs.forEach((img) => {\r\n            if (img.complete) {\r\n                this._handleLoadedResource();\r\n            }\r\n            else {\r\n                const image = new Image();\r\n                img.addEventListener('load', () => {\r\n                    this._handleLoadedResource();\r\n                });\r\n                img.addEventListener('error', () => {\r\n                    this._handleLoadedResource();\r\n                });\r\n                image.src = img.currentSrc || img.src;\r\n            }\r\n        });\r\n        // preload videos\r\n        this._videos.forEach((video) => {\r\n            if (video.readyState > 0) {\r\n                this._handleLoadedResource();\r\n            }\r\n            else {\r\n                if (video.preload === 'none') {\r\n                    this._handleLoadedResource();\r\n                    return;\r\n                }\r\n                video.addEventListener('error', () => {\r\n                    this._handleLoadedResource();\r\n                });\r\n                video.addEventListener('loadedmetadata', () => {\r\n                    this._handleLoadedResource();\r\n                });\r\n            }\r\n        });\r\n        // preload custom resources\r\n        this._customResources.forEach((data) => {\r\n            this._seekCustomResourceLoaded(data).then(() => {\r\n                this._handleLoadedResource();\r\n            });\r\n        });\r\n    }\r\n    /**\r\n     * Seek the moment when a custom resource is loaded\r\n     */\r\n    _seekCustomResourceLoaded(data) {\r\n        return new Promise((resolve) => {\r\n            const { el, targetProgress } = data;\r\n            // get loaded value\r\n            let loadProgress = 0;\r\n            // from \"isComplete\" property\r\n            if (typeof el.isComplete !== 'undefined') {\r\n                if (typeof el.isComplete === 'boolean') {\r\n                    if (el.isComplete) {\r\n                        loadProgress = targetProgress;\r\n                    }\r\n                }\r\n                else if (typeof el.isComplete === 'number') {\r\n                    loadProgress = el.isComplete;\r\n                }\r\n            }\r\n            else if (typeof el.isLoaded !== 'undefined') {\r\n                // from \"isLoaded\" property\r\n                if (typeof el.isLoaded === 'boolean') {\r\n                    if (el.isLoaded) {\r\n                        loadProgress = targetProgress;\r\n                    }\r\n                }\r\n                else if (typeof el.isLoaded === 'number') {\r\n                    loadProgress = el.isLoaded;\r\n                }\r\n            }\r\n            // if loadProgress is still incomplete,\r\n            // we check the \"data-is-loaded\" attribute\r\n            if (loadProgress === 0) {\r\n                const isLoadedAttr = el.getAttribute('data-is-loaded');\r\n                if (isLoadedAttr !== null && isLoadedAttr !== '') {\r\n                    const isLoadedAttrNum = parseFloat(isLoadedAttr);\r\n                    // if the value is non-numeric, we define the the resource is loaded\r\n                    if (Number.isNaN(isLoadedAttrNum)) {\r\n                        loadProgress = targetProgress;\r\n                    }\r\n                    else {\r\n                        loadProgress = isLoadedAttrNum;\r\n                    }\r\n                }\r\n            }\r\n            // console.log(loadProgress, targetLoadProgress);\r\n            // and we finally check if the target progress was reached\r\n            if (loadProgress >= targetProgress) {\r\n                resolve();\r\n                return;\r\n            }\r\n            // otherwise, seek the moment when it will be loaded\r\n            setTimeout(() => {\r\n                if (this.destroyed) {\r\n                    return;\r\n                }\r\n                this._seekCustomResourceLoaded(data).then(() => {\r\n                    resolve();\r\n                });\r\n            }, 50);\r\n        });\r\n    }\r\n    /**\r\n     * Iterate quantity of loaded resources.\r\n     * @param quantity - Only integers\r\n     */\r\n    addResourcesLoaded(quantity = 1) {\r\n        for (let index = 0; index < quantity; index += 1) {\r\n            this._handleLoadedResource();\r\n        }\r\n    }\r\n    /**\r\n     * Iterate quantity of total resources.\r\n     * @param quantity - Only integers\r\n     */\r\n    addResourcesTotal(quantity = 1) {\r\n        this._resourcesTotal += quantity;\r\n    }\r\n    /**\r\n     * Event on resource loaded\r\n     */\r\n    _handleLoadedResource() {\r\n        if (this.loadProgress >= 1) {\r\n            return;\r\n        }\r\n        this._resourcesLoaded += 1;\r\n        this.callbacks.tbt('resourceLoad', false);\r\n        // update current preloader progress if no animation set\r\n        if (!this._animationFrame) {\r\n            this.progress = this.loadProgress;\r\n        }\r\n    }\r\n    /**\r\n     * Event on progress change\r\n     */\r\n    _handleProgress() {\r\n        // launch callback\r\n        this.callbacks.tbt('progress', {\r\n            progress: this._progress,\r\n        });\r\n        // if full progress,\r\n        // we hide the preloader\r\n        if (this.progress >= 1) {\r\n            // destroy animations\r\n            if (this._animationFrame) {\r\n                this._animationFrame.destroy();\r\n                this._animationFrame = undefined;\r\n            }\r\n            return;\r\n        }\r\n        // otherwise, we check if there's a need to launch a timeline\r\n        // to end the animation\r\n        if (typeof this.prop.calc.forceEnd === 'number'\r\n            && this.loadProgress >= 1\r\n            && !this._endTimeline) {\r\n            // destroy animation frame\r\n            if (this._animationFrame) {\r\n                this._animationFrame.destroy();\r\n                this._animationFrame = undefined;\r\n            }\r\n            // create a timeline\r\n            this._endTimeline = new _timeline_Timeline__WEBPACK_IMPORTED_MODULE_6__.Timeline({\r\n                duration: this.prop.calc.forceEnd,\r\n            });\r\n            const startProgress = this.progress;\r\n            this._endTimeline.addCallback('progress', (data) => {\r\n                const diff = 1 - startProgress;\r\n                this.progress = startProgress + (diff * data.progress);\r\n            });\r\n            this._endTimeline.play();\r\n        }\r\n        // and since some time the preloader will be hidden automatically.\r\n        // See '_onLoaded' method.\r\n    }\r\n    /**\r\n     * Destroy the component\r\n     */\r\n    _destroy() {\r\n        super._destroy();\r\n        if (this._animationFrame) {\r\n            this._animationFrame.destroy();\r\n        }\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/components/loading/ProgressPreloader.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/components/page/Page.js":
/*!*************************************************************!*\
  !*** ./node_modules/vevet/build/es/components/page/Page.js ***!
  \*************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   Page: () => (/* binding */ Page)\n/* harmony export */ });\n/* harmony import */ var p_cancelable__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! p-cancelable */ \"./node_modules/p-cancelable/index.js\");\n/* harmony import */ var vevet_dom__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! vevet-dom */ \"./node_modules/vevet-dom/dist/es/index.js\");\n/* harmony import */ var _base_Component__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../base/Component */ \"./node_modules/vevet/build/es/base/Component.js\");\n/* harmony import */ var _loading_ProgressPreloader__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../loading/ProgressPreloader */ \"./node_modules/vevet/build/es/components/loading/ProgressPreloader.js\");\n\r\n\r\n\r\n\r\n/**\r\n * Page is a class that may be used to initialize a page.\r\n * The reason to use this class is that it allows to set a definite name for the page and manipulate\r\n * with its states: creation, show, hide, and destruction.\r\n * Notice that the states must be called a definite order: create -> show -> hide -> destroy\r\n * and vice versa.\r\n */\r\nclass Page extends _base_Component__WEBPACK_IMPORTED_MODULE_2__.Component {\r\n    constructor(initialProp, init = true) {\r\n        super(initialProp, false);\r\n        // set default vars\r\n        this._blocked = false;\r\n        this._created = false;\r\n        this._beforeShown = false;\r\n        this._shown = false;\r\n        this._hidden = false;\r\n        this._destroyed = false;\r\n        this._viaAJAX = false;\r\n        // initialize the class\r\n        if (init) {\r\n            this.init();\r\n        }\r\n    }\r\n    get blocked() {\r\n        return this._blocked;\r\n    }\r\n    get created() {\r\n        return this._created;\r\n    }\r\n    get beforeShown() {\r\n        return this._beforeShown;\r\n    }\r\n    get shown() {\r\n        return this._shown;\r\n    }\r\n    get hidden() {\r\n        return this._hidden;\r\n    }\r\n    get viaAJAX() {\r\n        return this._viaAJAX;\r\n    }\r\n    get pageClassName() {\r\n        return `${this._app.prefix}page-${this.prop.name}`;\r\n    }\r\n    _getDefaultProp() {\r\n        return Object.assign(Object.assign({}, super._getDefaultProp()), { name: 'home', hasInnerPreloader: false });\r\n    }\r\n    /**\r\n     * Create the page.\r\n     * @param viaAJAX - If the page was loaded through AJAX\r\n     */\r\n    create(viaAJAX = false) {\r\n        return new Promise((resolve, reject) => {\r\n            this.canCreate().then(() => {\r\n                if (this.created || this.blocked) {\r\n                    reject();\r\n                    return;\r\n                }\r\n                // update vars\r\n                this._blocked = true;\r\n                this._beforeShown = false;\r\n                this._shown = false;\r\n                this._hidden = false;\r\n                this._destroyed = false;\r\n                this._viaAJAX = viaAJAX;\r\n                // update page\r\n                this._app.page = this;\r\n                // add page class\r\n                this._app.html.classList.add(this.pageClassName);\r\n                // actions\r\n                this._create().then(() => {\r\n                    this.callbacks.tbt('create', false);\r\n                    this._blocked = false;\r\n                    this._created = true;\r\n                    resolve();\r\n                }).catch(() => {\r\n                    reject();\r\n                });\r\n            }).catch(() => {\r\n                reject();\r\n            });\r\n        });\r\n    }\r\n    /**\r\n     * Use this method to do some actions when creating a page\r\n     */\r\n    _create() {\r\n        return new Promise((resolve) => {\r\n            resolve();\r\n        });\r\n    }\r\n    /**\r\n     * Check if the page can be created.\r\n     */\r\n    canCreate() {\r\n        return new Promise((resolve, reject) => {\r\n            if (!this.created) {\r\n                resolve();\r\n                return;\r\n            }\r\n            reject();\r\n        });\r\n    }\r\n    /**\r\n     * Show the page.\r\n     */\r\n    show() {\r\n        return new Promise((resolve, reject) => {\r\n            this.canShow().then(() => {\r\n                if (this.shown || this.blocked) {\r\n                    reject();\r\n                    return;\r\n                }\r\n                // update vars\r\n                this._blocked = true;\r\n                this._created = true;\r\n                this._hidden = false;\r\n                this._destroyed = false;\r\n                // actions\r\n                this.callbacks.tbt('beforeshow', false);\r\n                this._beforeShown = true;\r\n                this._show().then(() => {\r\n                    this.callbacks.tbt('show', false);\r\n                    this._blocked = false;\r\n                    this._shown = true;\r\n                    resolve();\r\n                }).catch(() => {\r\n                    reject();\r\n                });\r\n                // launch events\r\n            }).catch(() => {\r\n                reject();\r\n            });\r\n        });\r\n    }\r\n    /**\r\n     * Use this method to do some actions when showing a page\r\n     */\r\n    _show() {\r\n        const interactivePromise = this._ignorePreventPageShow();\r\n        this.addCallback('destroy', () => {\r\n            interactivePromise.cancel();\r\n        });\r\n        return new Promise((resolve, reject) => {\r\n            interactivePromise.then(() => {\r\n                resolve();\r\n            }).catch(() => {\r\n                reject();\r\n            });\r\n        });\r\n    }\r\n    /**\r\n     * Some elements may have the attribute `data-prevent-page-show=\"true\"`\r\n     * This means that the page should not be interactive (and scrollable).\r\n     * Here we search for such elements.\r\n     * When there's no elements with this attribute, the page is interactive.\r\n     */\r\n    _ignorePreventPageShow() {\r\n        return new p_cancelable__WEBPACK_IMPORTED_MODULE_0__[\"default\"]((resolve, reject) => {\r\n            if (this._destroyed) {\r\n                reject();\r\n            }\r\n            const elements = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_1__.selectAll)('[data-prevent-page-show]');\r\n            if (elements.length === 0) {\r\n                resolve();\r\n            }\r\n            else {\r\n                setTimeout(() => {\r\n                    this._ignorePreventPageShow().then(() => {\r\n                        resolve();\r\n                    }).catch(() => {\r\n                        reject();\r\n                    });\r\n                }, 30);\r\n            }\r\n        });\r\n    }\r\n    /**\r\n     * Check if the page can be shown.\r\n     */\r\n    canShow() {\r\n        return new Promise((resolve, reject) => {\r\n            if (this.created && !this.shown) {\r\n                // inner preloader\r\n                if (this.prop.hasInnerPreloader) {\r\n                    const preloader = new _loading_ProgressPreloader__WEBPACK_IMPORTED_MODULE_3__.ProgressPreloader({\r\n                        parent: this,\r\n                        container: false,\r\n                        hide: false,\r\n                        loaders: {\r\n                            img: false,\r\n                            video: false,\r\n                            custom: '.js-preload-inner',\r\n                        },\r\n                        calc: {\r\n                            lerp: false,\r\n                        },\r\n                    });\r\n                    preloader.addCallback('loaded', () => {\r\n                        resolve();\r\n                    });\r\n                }\r\n                else {\r\n                    resolve();\r\n                }\r\n                return;\r\n            }\r\n            reject();\r\n        });\r\n    }\r\n    /**\r\n     * Hide the page.\r\n     */\r\n    hide() {\r\n        return new Promise((resolve, reject) => {\r\n            this.canHide().then(() => {\r\n                if (this.hidden || this.blocked) {\r\n                    reject();\r\n                    return;\r\n                }\r\n                // update vars\r\n                this._blocked = true;\r\n                this._created = true;\r\n                this._beforeShown = false;\r\n                this._shown = false;\r\n                this._destroyed = false;\r\n                // actions\r\n                this._hide().then(() => {\r\n                    this.callbacks.tbt('hide', false);\r\n                    this._blocked = false;\r\n                    this._hidden = true;\r\n                    resolve();\r\n                }).catch(() => {\r\n                    reject();\r\n                });\r\n            }).catch(() => {\r\n                reject();\r\n            });\r\n        });\r\n    }\r\n    /**\r\n     * Use this method to do some actions when hiding a page\r\n     */\r\n    _hide() {\r\n        return new Promise((resolve) => {\r\n            resolve();\r\n        });\r\n    }\r\n    /**\r\n     * Check if the page can be hidden.\r\n     */\r\n    canHide() {\r\n        return new Promise((resolve, reject) => {\r\n            if (this.created && this.shown && !this.hidden) {\r\n                resolve();\r\n                return;\r\n            }\r\n            reject();\r\n        });\r\n    }\r\n    /**\r\n     * Destroy the page.\r\n     */\r\n    destroy() {\r\n        return new Promise((resolve, reject) => {\r\n            this.canDestroy().then(() => {\r\n                if (this.destroyed || this.blocked) {\r\n                    reject();\r\n                    return;\r\n                }\r\n                // change vars\r\n                this._blocked = true;\r\n                this._created = false;\r\n                this._beforeShown = false;\r\n                this._shown = false;\r\n                this._hidden = true;\r\n                // update page\r\n                this._app.page = false;\r\n                // remove page class\r\n                this._app.html.classList.remove(this.pageClassName);\r\n                // actions\r\n                this._destroy().then(() => {\r\n                    this._blocked = false;\r\n                    this._destroyed = true;\r\n                    resolve();\r\n                }).catch(() => {\r\n                    reject();\r\n                });\r\n            }).catch(() => {\r\n                reject();\r\n            });\r\n        });\r\n    }\r\n    /**\r\n     * Use this method to do some actions when destroying a page\r\n     */\r\n    _destroy() {\r\n        return new Promise((resolve) => {\r\n            super._destroy();\r\n            resolve();\r\n        });\r\n    }\r\n    /**\r\n     * Check if the page can be destroyed.\r\n     */\r\n    canDestroy() {\r\n        return new Promise((resolve, reject) => {\r\n            if (this.created && this.hidden && !this.destroyed) {\r\n                resolve();\r\n                return;\r\n            }\r\n            reject();\r\n        });\r\n    }\r\n    /**\r\n     * Add a 'create' callback.\r\n     * If the callback was added after the page was created, it will be triggered immediately.\r\n     */\r\n    onCreate() {\r\n        return new p_cancelable__WEBPACK_IMPORTED_MODULE_0__[\"default\"]((resolve) => {\r\n            if (this.created) {\r\n                resolve();\r\n            }\r\n            else {\r\n                this.addCallback('create', () => {\r\n                    resolve();\r\n                }, {\r\n                    once: true,\r\n                });\r\n            }\r\n        });\r\n    }\r\n    /**\r\n     * Add a 'show' callback.\r\n     * If the callback was added after the page was shown, it will be triggered immediately.\r\n     */\r\n    onShow() {\r\n        return new p_cancelable__WEBPACK_IMPORTED_MODULE_0__[\"default\"]((resolve) => {\r\n            if (this.shown) {\r\n                resolve();\r\n            }\r\n            else {\r\n                this.addCallback('show', () => {\r\n                    resolve();\r\n                }, {\r\n                    once: true,\r\n                });\r\n            }\r\n        });\r\n    }\r\n    /**\r\n     * Add a 'show' callback.\r\n     * If the callback was added after the page was shown, it will be triggered immediately.\r\n     */\r\n    onBeforeShow() {\r\n        return new p_cancelable__WEBPACK_IMPORTED_MODULE_0__[\"default\"]((resolve) => {\r\n            if (this.shown || this.beforeShown) {\r\n                resolve();\r\n            }\r\n            else {\r\n                this.addCallback('beforeshow', () => {\r\n                    resolve();\r\n                }, {\r\n                    once: true,\r\n                });\r\n            }\r\n        });\r\n    }\r\n    /**\r\n     * Add a 'hide' callback.\r\n     * If the callback was added after the page was hidden, it will be triggered immediately.\r\n     */\r\n    onHide() {\r\n        return new p_cancelable__WEBPACK_IMPORTED_MODULE_0__[\"default\"]((resolve) => {\r\n            if (this.hidden) {\r\n                resolve();\r\n            }\r\n            else {\r\n                this.addCallback('hide', () => {\r\n                    resolve();\r\n                }, {\r\n                    once: true,\r\n                });\r\n            }\r\n        });\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/components/page/Page.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/components/scroll/plugins/SmoothScrollDragPlugin.js":
/*!*****************************************************************************************!*\
  !*** ./node_modules/vevet/build/es/components/scroll/plugins/SmoothScrollDragPlugin.js ***!
  \*****************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   SmoothScrollDragPlugin: () => (/* binding */ SmoothScrollDragPlugin)\n/* harmony export */ });\n/* harmony import */ var _dragger_DraggerMove__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../dragger/DraggerMove */ \"./node_modules/vevet/build/es/components/dragger/DraggerMove.js\");\n/* harmony import */ var _base_Plugin__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../../base/Plugin */ \"./node_modules/vevet/build/es/base/Plugin.js\");\n\r\n\r\nconst draggingClassName = 'is-dragging';\r\n/**\r\n * A class for Plugins.\r\n */\r\nclass SmoothScrollDragPlugin extends _base_Plugin__WEBPACK_IMPORTED_MODULE_0__.Plugin {\r\n    constructor(initialProp) {\r\n        super(initialProp);\r\n        this._dragger = undefined;\r\n        this._componentEvents = [];\r\n        this._currentLerp = false;\r\n    }\r\n    _getDefaultProp() {\r\n        return Object.assign(Object.assign({}, super._getDefaultProp()), { enabled: true, speed: 1, lerp: false, reverseDir: false, stopPropagation: false });\r\n    }\r\n    /**\r\n     * If is dragging at the moment\r\n     */\r\n    get isDragging() {\r\n        if (this._dragger) {\r\n            return this._dragger.isDragging;\r\n        }\r\n        return false;\r\n    }\r\n    _constructor() {\r\n        super._constructor();\r\n        this._toggleDragger();\r\n    }\r\n    _onPropMutate() {\r\n        super._onPropMutate();\r\n        this._toggleDragger();\r\n    }\r\n    /**\r\n     * Add or remove dragger\r\n     */\r\n    _toggleDragger() {\r\n        if (this.prop.enabled) {\r\n            this._addDragger();\r\n        }\r\n        else {\r\n            this._removeDragger();\r\n        }\r\n    }\r\n    /**\r\n     * Add dragger events\r\n     */\r\n    _addDragger() {\r\n        if (this._dragger) {\r\n            return;\r\n        }\r\n        const { component } = this;\r\n        this._dragger = new _dragger_DraggerMove__WEBPACK_IMPORTED_MODULE_1__.DraggerMove({\r\n            container: component.outer,\r\n        });\r\n        this._dragger.addCallback('start', this._handleDragStart.bind(this));\r\n        this._dragger.addCallback('move', (data) => {\r\n            this._handleDragMove(data);\r\n        });\r\n        this._dragger.addCallback('end', this._handleDragEnd.bind(this));\r\n        this._componentEvents.push(component.addCallback('wheel', () => {\r\n            var _a;\r\n            (_a = this._dragger) === null || _a === void 0 ? void 0 : _a.cancel();\r\n        }));\r\n    }\r\n    /**\r\n     * Remove dragger events\r\n     */\r\n    _removeDragger() {\r\n        if (!this._dragger) {\r\n            return;\r\n        }\r\n        this._dragger.destroy();\r\n        this._dragger = undefined;\r\n        this._componentEvents.forEach((evt) => {\r\n            evt.remove();\r\n        });\r\n        this._componentEvents = [];\r\n    }\r\n    /**\r\n     * Callback on dragging start\r\n     */\r\n    _handleDragStart() {\r\n        const { component } = this;\r\n        if (!component.prop.enabled) {\r\n            return;\r\n        }\r\n        // check scrollable area\r\n        if (component.maxScrollableWidth <= 0 && component.maxScrollableHeight <= 0) {\r\n            return;\r\n        }\r\n        // change lerp\r\n        const { lerp } = this.prop;\r\n        if (typeof lerp !== 'boolean') {\r\n            this._currentLerp = component.prop.render.lerp;\r\n            component.changeProp({\r\n                render: {\r\n                    lerp,\r\n                },\r\n            });\r\n        }\r\n        // launch events\r\n        this._callbacks.tbt('start', false);\r\n    }\r\n    /**\r\n     * Callback on dragging move\r\n     */\r\n    _handleDragMove(data) {\r\n        const { component } = this;\r\n        if (!component.prop.enabled) {\r\n            return;\r\n        }\r\n        // check scrollable area\r\n        if (component.maxScrollableWidth <= 0 && component.maxScrollableHeight <= 0) {\r\n            return;\r\n        }\r\n        const { evt } = data;\r\n        // get difference between coordinates and decide\r\n        // if we need to stop propagation\r\n        const { stopPropagation } = this.prop;\r\n        if (stopPropagation) {\r\n            if (!evt.cancelable) {\r\n                return;\r\n            }\r\n            const diffX = Math.abs(data.coords.x - data.start.x);\r\n            const diffY = Math.abs(data.coords.y - data.start.y);\r\n            if (((diffX > stopPropagation.threshold) && stopPropagation.dir === 'x')\r\n                || ((diffY > stopPropagation.threshold) && stopPropagation.dir === 'y')) {\r\n                if (evt.cancelable) {\r\n                    evt.preventDefault();\r\n                    evt.stopPropagation();\r\n                }\r\n            }\r\n        }\r\n        const { speed, reverseDir } = this.prop;\r\n        // get coordinates\r\n        const x = data.step.x * speed;\r\n        const y = data.step.y * speed;\r\n        // update scroll values\r\n        component.targetLeft -= !reverseDir ? x : y;\r\n        component.targetTop -= !reverseDir ? y : x;\r\n        // set classes\r\n        component.outer.classList.add(draggingClassName);\r\n        component.container.classList.add(draggingClassName);\r\n        // launch events\r\n        this._callbacks.tbt('move', false);\r\n    }\r\n    /**\r\n     * Callback on dragging end\r\n     */\r\n    _handleDragEnd() {\r\n        const { component } = this;\r\n        // set classes\r\n        component.outer.classList.remove(draggingClassName);\r\n        component.container.classList.remove(draggingClassName);\r\n        // restore SmoothScroll Lerp\r\n        if (typeof this._currentLerp !== 'boolean') {\r\n            component.changeProp({\r\n                render: {\r\n                    lerp: this._currentLerp,\r\n                },\r\n            });\r\n            this._currentLerp = false;\r\n        }\r\n        // launch events\r\n        this._callbacks.tbt('end', false);\r\n    }\r\n    /**\r\n     * Destroy the plugin\r\n     */\r\n    _destroy() {\r\n        super._destroy();\r\n        this._removeDragger();\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/components/scroll/plugins/SmoothScrollDragPlugin.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/components/scroll/plugins/SmoothScrollKeyboardPlugin.js":
/*!*********************************************************************************************!*\
  !*** ./node_modules/vevet/build/es/components/scroll/plugins/SmoothScrollKeyboardPlugin.js ***!
  \*********************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   SmoothScrollKeyboardPlugin: () => (/* binding */ SmoothScrollKeyboardPlugin)\n/* harmony export */ });\n/* harmony import */ var vevet_dom__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! vevet-dom */ \"./node_modules/vevet-dom/dist/es/index.js\");\n/* harmony import */ var _base_Plugin__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../../base/Plugin */ \"./node_modules/vevet/build/es/base/Plugin.js\");\n/* harmony import */ var _utils_common_timeoutCallback__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../../utils/common/timeoutCallback */ \"./node_modules/vevet/build/es/utils/common/timeoutCallback.js\");\n\r\n\r\n\r\n/**\r\n * A class for Plugins.\r\n */\r\nclass SmoothScrollKeyboardPlugin extends _base_Plugin__WEBPACK_IMPORTED_MODULE_1__.Plugin {\r\n    _setEvents() {\r\n        super._setEvents();\r\n        this.addEventListeners(window, 'keydown', (e) => {\r\n            this._handleKeydown(e);\r\n        });\r\n    }\r\n    /**\r\n     * Event on a key pressed\r\n     */\r\n    _handleKeydown(e) {\r\n        const component = this._component;\r\n        if (!component.prop.enabled) {\r\n            return;\r\n        }\r\n        // tab is active for everethyng, forms too\r\n        if (e.keyCode === 9) {\r\n            this._handleTab();\r\n            return;\r\n        }\r\n        // check if some element is under focus\r\n        // if some exists, ignore key events\r\n        const { activeElement } = document;\r\n        if (activeElement) {\r\n            if (activeElement instanceof HTMLInputElement\r\n                || activeElement instanceof HTMLTextAreaElement) {\r\n                return;\r\n            }\r\n        }\r\n        // check if the scroll container is in viewport\r\n        const { viewport } = this._app;\r\n        const bounding = component.outer.getBoundingClientRect();\r\n        if (!(bounding.left < viewport.width\r\n            && bounding.right > 0\r\n            && bounding.top < viewport.height\r\n            && bounding.bottom > 0)) {\r\n            return;\r\n        }\r\n        // update scroll values\r\n        const scrollIterator = 40;\r\n        switch (e.keyCode) {\r\n            // up\r\n            case 38:\r\n                component.targetTop -= scrollIterator;\r\n                break;\r\n            // down\r\n            case 40:\r\n                component.targetTop += scrollIterator;\r\n                break;\r\n            // right\r\n            case 39:\r\n                component.targetLeft += scrollIterator;\r\n                break;\r\n            // left\r\n            case 37:\r\n                component.targetLeft -= scrollIterator;\r\n                break;\r\n            // page down\r\n            case 34:\r\n                component.targetTop += scrollIterator * 10;\r\n                break;\r\n            // page up\r\n            case 33:\r\n                component.targetTop -= scrollIterator * 10;\r\n                break;\r\n            // home\r\n            case 36:\r\n                component.targetTop = 0;\r\n                component.targetLeft = 0;\r\n                break;\r\n            // end\r\n            case 35:\r\n                component.targetTop = component.scrollHeight;\r\n                component.targetLeft = component.scrollWidth;\r\n                break;\r\n            // space\r\n            case 32:\r\n                component.targetTop += scrollIterator * 5;\r\n                break;\r\n            default:\r\n                break;\r\n        }\r\n    }\r\n    /**\r\n     * Handle Tab key pressed\r\n     */\r\n    _handleTab() {\r\n        // get active element and scroll to it\r\n        (0,_utils_common_timeoutCallback__WEBPACK_IMPORTED_MODULE_2__[\"default\"])(() => {\r\n            const scroll = this._component;\r\n            const { activeElement } = document;\r\n            // skip elements that do not belong to the SmoothScroll outer\r\n            if (!(activeElement instanceof HTMLElement)\r\n                || !(0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.childOf)(activeElement, scroll.outer)) {\r\n                return;\r\n            }\r\n            // get boundings\r\n            const scrollOuterBounding = scroll.outer.getBoundingClientRect();\r\n            const activeElementBounding = activeElement.getBoundingClientRect();\r\n            // get element position\r\n            const top = scroll.scrollTop\r\n                + (activeElementBounding.top - scrollOuterBounding.top)\r\n                - (scroll.clientHeight / 2 - activeElementBounding.height / 2);\r\n            const left = scroll.scrollLeft\r\n                + (activeElementBounding.left - scrollOuterBounding.left)\r\n                - (scroll.clientWidth / 2 - activeElementBounding.width / 2);\r\n            // apply new targets\r\n            scroll.targetTop = top;\r\n            scroll.targetLeft = left;\r\n        }, 120);\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/components/scroll/plugins/SmoothScrollKeyboardPlugin.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/components/scroll/scrollable/ScrollEventsBase.js":
/*!**************************************************************************************!*\
  !*** ./node_modules/vevet/build/es/components/scroll/scrollable/ScrollEventsBase.js ***!
  \**************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   ScrollEventsBase: () => (/* binding */ ScrollEventsBase)\n/* harmony export */ });\n/* harmony import */ var vevet_dom__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! vevet-dom */ \"./node_modules/vevet-dom/dist/es/index.js\");\n/* harmony import */ var _base_Component__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../../base/Component */ \"./node_modules/vevet/build/es/base/Component.js\");\n\r\n\r\n/**\r\n * A base for scroll event components\r\n */\r\nclass ScrollEventsBase extends _base_Component__WEBPACK_IMPORTED_MODULE_1__.Component {\r\n    constructor(initialProp, init = true) {\r\n        super(initialProp, false);\r\n        // get scroll container\r\n        if (typeof this.prop.container === 'string') {\r\n            this._scrollContainer = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.selectOne)(this.prop.container);\r\n        }\r\n        else {\r\n            this._scrollContainer = this.prop.container;\r\n        }\r\n        // initialize the class\r\n        if (init) {\r\n            this.init();\r\n        }\r\n    }\r\n    _getDefaultProp() {\r\n        return Object.assign(Object.assign({}, super._getDefaultProp()), { container: window, intersectionRoot: false });\r\n    }\r\n    get scrollContainer() {\r\n        return this._scrollContainer;\r\n    }\r\n    get scrollableElement() {\r\n        if (this._scrollContainer instanceof Window) {\r\n            return this._app.html;\r\n        }\r\n        return this._scrollContainer;\r\n    }\r\n    /**\r\n     * Parent of inner elements\r\n     */\r\n    get domParent() {\r\n        const { scrollContainer } = this;\r\n        if (scrollContainer instanceof Window) {\r\n            return this._app.body;\r\n        }\r\n        if (scrollContainer instanceof Element) {\r\n            return scrollContainer;\r\n        }\r\n        return scrollContainer.outer;\r\n    }\r\n    /**\r\n     * Used as a \"root\" for IntersectionObserver\r\n     */\r\n    get intersectionRoot() {\r\n        if (this.prop.intersectionRoot === false) {\r\n            const { scrollContainer } = this;\r\n            if (scrollContainer instanceof Window) {\r\n                return null;\r\n            }\r\n            if (scrollContainer instanceof Element) {\r\n                return scrollContainer;\r\n            }\r\n            return scrollContainer.outer;\r\n        }\r\n        return this.prop.intersectionRoot;\r\n    }\r\n    /**\r\n     * BoundingRect of 'scrollContainer'\r\n     */\r\n    get scrollContainerBounding() {\r\n        if (this.intersectionRoot) {\r\n            const bounding = this.intersectionRoot.getBoundingClientRect();\r\n            return {\r\n                top: bounding.top,\r\n                left: bounding.left,\r\n                width: bounding.width,\r\n                height: bounding.height,\r\n            };\r\n        }\r\n        return {\r\n            top: 0,\r\n            left: 0,\r\n            width: this._app.viewport.width,\r\n            height: this._app.viewport.height,\r\n        };\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/components/scroll/scrollable/ScrollEventsBase.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/components/scroll/scrollable/ScrollView.js":
/*!********************************************************************************!*\
  !*** ./node_modules/vevet/build/es/components/scroll/scrollable/ScrollView.js ***!
  \********************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   ScrollView: () => (/* binding */ ScrollView)\n/* harmony export */ });\n/* harmony import */ var _ScrollEventsBase__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./ScrollEventsBase */ \"./node_modules/vevet/build/es/components/scroll/scrollable/ScrollEventsBase.js\");\n/* harmony import */ var _utils_listeners_onScroll__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../../utils/listeners/onScroll */ \"./node_modules/vevet/build/es/utils/listeners/onScroll.js\");\n/* harmony import */ var _utils_listeners__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../../utils/listeners */ \"./node_modules/vevet/build/es/utils/listeners/intersectionObserverSupported.js\");\n/* harmony import */ var _utils_math_clamp__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../../../utils/math/clamp */ \"./node_modules/vevet/build/es/utils/math/clamp.js\");\n/* harmony import */ var _utils_common_timeoutCallback__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../../../utils/common/timeoutCallback */ \"./node_modules/vevet/build/es/utils/common/timeoutCallback.js\");\n\r\n\r\n\r\n\r\n\r\n/**\r\n * Elements into viewport\r\n */\r\nclass ScrollView extends _ScrollEventsBase__WEBPACK_IMPORTED_MODULE_0__.ScrollEventsBase {\r\n    constructor(initialProp, init = true) {\r\n        super(initialProp, false);\r\n        this._scrollEvent = undefined;\r\n        this._intersectionObserverIn = undefined;\r\n        this._intersectionObserverOut = undefined;\r\n        this._firstStart = true;\r\n        this._elements = [...this.prop.elements];\r\n        // initialize the class\r\n        if (init) {\r\n            this.init();\r\n        }\r\n    }\r\n    get prefix() {\r\n        return `${this._app.prefix}scroll-view`;\r\n    }\r\n    _getDefaultProp() {\r\n        return Object.assign(Object.assign({}, super._getDefaultProp()), { enabled: true, container: window, elements: [], threshold: 0.9, states: 'in', classToToggle: 'viewed', useDelay: false, useIntersectionObserver: true, viewportTarget: '', resizeTimeout: 0 });\r\n    }\r\n    /**\r\n     * Elements to seek\r\n     */\r\n    get elements() {\r\n        return this._elements;\r\n    }\r\n    init() {\r\n        super.init();\r\n    }\r\n    // Set Module Events\r\n    _setEvents() {\r\n        super._setEvents();\r\n        this.resize();\r\n        this.addViewportCallback(this.prop.viewportTarget, () => {\r\n            this.resize();\r\n        }, {\r\n            timeout: this.prop.resizeTimeout,\r\n        });\r\n    }\r\n    _onPropMutate() {\r\n        super._onPropMutate();\r\n        this.resize();\r\n    }\r\n    /**\r\n     * Resize the scene\r\n     */\r\n    resize() {\r\n        this._removeViewEvents();\r\n        if (this.prop.enabled) {\r\n            this._setViewEvents();\r\n            this.seekBounding();\r\n        }\r\n    }\r\n    /**\r\n     * Set View events: scroll or intersection\r\n     */\r\n    _setViewEvents() {\r\n        // set intersection observer\r\n        if ((0,_utils_listeners__WEBPACK_IMPORTED_MODULE_1__[\"default\"])() && this.prop.useIntersectionObserver) {\r\n            // get intersecton margins\r\n            const { scrollContainerBounding } = this;\r\n            const xMargin = this._firstStart\r\n                ? 0 : scrollContainerBounding.width * (1 - this.prop.threshold) * -1;\r\n            const yMargin = this._firstStart\r\n                ? 0 : scrollContainerBounding.height * (1 - this.prop.threshold) * -1;\r\n            // create intersection observers\r\n            this._intersectionObserverIn = new IntersectionObserver(this._handleIntersectionObserverIn.bind(this), {\r\n                root: this.intersectionRoot,\r\n                threshold: 0,\r\n                rootMargin: `0px ${xMargin}px ${yMargin}px 0px`,\r\n            });\r\n            this.elements.forEach((el) => {\r\n                var _a;\r\n                (_a = this._intersectionObserverIn) === null || _a === void 0 ? void 0 : _a.observe(el);\r\n            });\r\n            if (this.prop.states === 'inout') {\r\n                this._intersectionObserverOut = new IntersectionObserver(this._handleIntersectionObserverOut.bind(this), {\r\n                    root: this.intersectionRoot,\r\n                    threshold: 0,\r\n                    rootMargin: '0px 0px 0px 0px',\r\n                });\r\n                this.elements.forEach((el) => {\r\n                    var _a;\r\n                    (_a = this._intersectionObserverOut) === null || _a === void 0 ? void 0 : _a.observe(el);\r\n                });\r\n            }\r\n        }\r\n        else {\r\n            // set scroll bounding events\r\n            this._scrollEvent = (0,_utils_listeners_onScroll__WEBPACK_IMPORTED_MODULE_2__[\"default\"])({\r\n                container: this.prop.container,\r\n                callback: this._handleScroll.bind(this),\r\n            });\r\n        }\r\n    }\r\n    /**\r\n     * Remove View events: scroll or intersection\r\n     */\r\n    _removeViewEvents() {\r\n        var _a, _b;\r\n        // remove scroll events\r\n        if (this._scrollEvent) {\r\n            this._scrollEvent.remove();\r\n            this._scrollEvent = undefined;\r\n        }\r\n        // destroy intersection observers\r\n        (_a = this._intersectionObserverIn) === null || _a === void 0 ? void 0 : _a.disconnect();\r\n        this._intersectionObserverIn = undefined;\r\n        (_b = this._intersectionObserverOut) === null || _b === void 0 ? void 0 : _b.disconnect();\r\n        this._intersectionObserverOut = undefined;\r\n    }\r\n    /**\r\n     * Event on IntersectionObserver\r\n     */\r\n    _handleIntersectionObserverIn(data) {\r\n        const parentBounding = this._firstStart ? this.scrollContainerBounding : false;\r\n        for (let index = 0; index < data.length; index += 1) {\r\n            const entry = data[index];\r\n            let delay = 0;\r\n            if (this._firstStart && !!parentBounding && entry.isIntersecting) {\r\n                delay = this._elementInViewportData(entry.target, parentBounding).delay;\r\n            }\r\n            if (entry.isIntersecting) {\r\n                this._handleInOut(entry.target, entry.isIntersecting, delay);\r\n            }\r\n        }\r\n        this._processUnusedElements();\r\n        // change states\r\n        if (this._firstStart) {\r\n            this._firstStart = false;\r\n            this.resize();\r\n        }\r\n    }\r\n    /**\r\n     * Event on IntersectionObserver\r\n     */\r\n    _handleIntersectionObserverOut(data) {\r\n        for (let index = 0; index < data.length; index += 1) {\r\n            const entry = data[index];\r\n            if (!entry.isIntersecting) {\r\n                this._handleInOut(entry.target, entry.isIntersecting, 0);\r\n            }\r\n        }\r\n    }\r\n    /**\r\n     * Event on Scroll\r\n     */\r\n    _handleScroll() {\r\n        this.seekBounding();\r\n    }\r\n    /**\r\n     * Seek the elements by their bounding.\r\n     * Only if IntersectionObserver is not used.\r\n     */\r\n    seekBounding() {\r\n        // check if calculations are done through getBoundingClientRect\r\n        if ((0,_utils_listeners__WEBPACK_IMPORTED_MODULE_1__[\"default\"])() && this.prop.useIntersectionObserver) {\r\n            return;\r\n        }\r\n        // vars\r\n        const { scrollContainerBounding } = this;\r\n        // go through all elements and get their boudning\r\n        for (let index = 0; index < this.elements.length; index += 1) {\r\n            const el = this.elements[index];\r\n            const elData = this._elementInViewportData(el, scrollContainerBounding);\r\n            const delay = elData.isIntersecting ? elData.delay : 0;\r\n            if (typeof elData.isIntersecting === 'boolean') {\r\n                this._handleInOut(el, elData.isIntersecting, delay);\r\n            }\r\n        }\r\n        this._processUnusedElements();\r\n        // change states\r\n        if (this._firstStart) {\r\n            this._firstStart = false;\r\n        }\r\n    }\r\n    /**\r\n     * Check element's bounding to define if it is in viewport\r\n     */\r\n    _elementInViewportData(el, parentBounding) {\r\n        const { threshold: propThreshold, states, useDelay } = this.prop;\r\n        const threshold = this._firstStart ? 1 : propThreshold;\r\n        const bounding = el.getBoundingClientRect();\r\n        // check if the element is intersecting\r\n        let isIntersecting;\r\n        if (bounding.top < parentBounding.top + parentBounding.height * threshold\r\n            && bounding.left < parentBounding.left + parentBounding.width * threshold) {\r\n            if (states === 'in') {\r\n                isIntersecting = true;\r\n            }\r\n            else if (bounding.right < parentBounding.left * threshold\r\n                || bounding.bottom < parentBounding.top * threshold) {\r\n                isIntersecting = false;\r\n            }\r\n            else {\r\n                isIntersecting = true;\r\n            }\r\n        }\r\n        else if (bounding.top > parentBounding.top + parentBounding.height\r\n            || bounding.left > parentBounding.left + parentBounding.width) {\r\n            isIntersecting = false;\r\n        }\r\n        // calculate delay only if it is enabled & calculations\r\n        // are done for the first time\r\n        let delay = 0;\r\n        if (!!useDelay && this._firstStart) {\r\n            const progress = (0,_utils_math_clamp__WEBPACK_IMPORTED_MODULE_3__[\"default\"])(useDelay.dir === 'x'\r\n                ? (bounding.left - parentBounding.left) / parentBounding.width\r\n                : (bounding.top - parentBounding.top) / parentBounding.height, [0, 1]);\r\n            delay = progress * useDelay.max;\r\n        }\r\n        return {\r\n            isIntersecting,\r\n            delay,\r\n        };\r\n    }\r\n    /**\r\n     * Event that is triggered when an element appears or disappears\r\n     */\r\n    _handleInOut(el, inViewport, delay = 0) {\r\n        const { states } = this.prop;\r\n        // check if need to continue\r\n        if ((el.scrollViewIn && inViewport)\r\n            || (!el.scrollViewIn && !inViewport)) {\r\n            return;\r\n        }\r\n        // update props\r\n        el.scrollViewIn = inViewport;\r\n        // toggle classes\r\n        if (this.prop.classToToggle) {\r\n            (0,_utils_common_timeoutCallback__WEBPACK_IMPORTED_MODULE_4__[\"default\"])(() => {\r\n                el.classList.toggle(this.prop.classToToggle, inViewport);\r\n            }, delay);\r\n        }\r\n        // process callbacks\r\n        if (inViewport && (states === 'in' || states === 'inout')) {\r\n            (0,_utils_common_timeoutCallback__WEBPACK_IMPORTED_MODULE_4__[\"default\"])(() => {\r\n                this.callbacks.tbt('in', el);\r\n            }, delay);\r\n        }\r\n        else if (!inViewport && states === 'inout') {\r\n            (0,_utils_common_timeoutCallback__WEBPACK_IMPORTED_MODULE_4__[\"default\"])(() => {\r\n                this.callbacks.tbt('out', el);\r\n            }, delay);\r\n        }\r\n    }\r\n    /**\r\n     * Remove elements that are mo more in need\r\n     */\r\n    _processUnusedElements() {\r\n        if (this.prop.states !== 'in') {\r\n            return;\r\n        }\r\n        // remove unused elements\r\n        const elementsToRemove = this._elements.filter((el) => el.scrollViewIn);\r\n        elementsToRemove.forEach((el) => {\r\n            var _a, _b;\r\n            (_a = this._intersectionObserverIn) === null || _a === void 0 ? void 0 : _a.unobserve(el);\r\n            (_b = this._intersectionObserverOut) === null || _b === void 0 ? void 0 : _b.unobserve(el);\r\n        });\r\n        this._elements = this._elements.filter((el) => !el.scrollViewIn);\r\n    }\r\n    /**\r\n     * Add a view element\r\n     */\r\n    addElement(element) {\r\n        var _a, _b;\r\n        const viewEl = element;\r\n        viewEl.scrollViewIn = undefined;\r\n        this._elements.push(element);\r\n        (_a = this._intersectionObserverIn) === null || _a === void 0 ? void 0 : _a.observe(element);\r\n        (_b = this._intersectionObserverOut) === null || _b === void 0 ? void 0 : _b.observe(element);\r\n        if (this.prop.enabled) {\r\n            this.seekBounding();\r\n        }\r\n        return {\r\n            remove: () => {\r\n                this.removeElement(element);\r\n            },\r\n        };\r\n    }\r\n    /**\r\n     * Remove a view element\r\n     */\r\n    removeElement(element) {\r\n        var _a, _b;\r\n        const viewEl = element;\r\n        viewEl.scrollViewIn = undefined;\r\n        this._elements = this._elements.filter((el) => el !== element);\r\n        (_a = this._intersectionObserverIn) === null || _a === void 0 ? void 0 : _a.unobserve(element);\r\n        (_b = this._intersectionObserverOut) === null || _b === void 0 ? void 0 : _b.unobserve(element);\r\n    }\r\n    /**\r\n     * Remove all view elements\r\n     */\r\n    removeElements() {\r\n        this._elements.forEach((el) => {\r\n            this.removeElement(el);\r\n        });\r\n        this._elements = [];\r\n    }\r\n    /**\r\n     * Destroy the module\r\n     */\r\n    _destroy() {\r\n        super._destroy();\r\n        this._removeViewEvents();\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/components/scroll/scrollable/ScrollView.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/components/scroll/scrollbar/Bar.js":
/*!************************************************************************!*\
  !*** ./node_modules/vevet/build/es/components/scroll/scrollbar/Bar.js ***!
  \************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ Bar)\n/* harmony export */ });\n/* harmony import */ var vevet_dom__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! vevet-dom */ \"./node_modules/vevet-dom/dist/es/index.js\");\n/* harmony import */ var _utils_listeners_onScroll__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../../utils/listeners/onScroll */ \"./node_modules/vevet/build/es/utils/listeners/onScroll.js\");\n/* harmony import */ var _utils_math_clamp__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../../../utils/math/clamp */ \"./node_modules/vevet/build/es/utils/math/clamp.js\");\n/* harmony import */ var _dragger_DraggerMove__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../../dragger/DraggerMove */ \"./node_modules/vevet/build/es/components/dragger/DraggerMove.js\");\n/* harmony import */ var _smooth_scroll_SmoothScroll__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../smooth-scroll/SmoothScroll */ \"./node_modules/vevet/build/es/components/scroll/smooth-scroll/SmoothScroll.js\");\n\r\n\r\n\r\n\r\n\r\nclass Bar {\r\n    constructor(_prop) {\r\n        this._prop = _prop;\r\n        const { prefix, dir, domParent, container, autoHide, } = _prop;\r\n        // set default vars\r\n        this._outerHeight = 0;\r\n        this._outerWidth = 0;\r\n        this._thumbHeight = 0;\r\n        this._thumbWidth = 0;\r\n        this._scrollVal = 0;\r\n        this._coordsAtDragStart = {\r\n            left: 0,\r\n            top: 0,\r\n        };\r\n        this._scrollEvent = undefined;\r\n        this._actionTimeout = undefined;\r\n        this._dragger = undefined;\r\n        // create outer\r\n        let outerClassNames = `${prefix} ${prefix}_${dir}`;\r\n        if (container instanceof Window) {\r\n            outerClassNames += ' in-window';\r\n        }\r\n        this._outer = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.createElement)('div', {\r\n            parent: domParent,\r\n            class: outerClassNames,\r\n        });\r\n        // create a thumb\r\n        this._thumb = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.createElement)('div', {\r\n            parent: this._outer,\r\n            class: `${prefix}__thumb ${prefix}__thumb_${dir}`,\r\n        });\r\n        // set auto hide classes\r\n        if (autoHide) {\r\n            this.outer.classList.add('auto-hide');\r\n        }\r\n        // set events\r\n        this._listeners = [];\r\n        this._setEvents();\r\n    }\r\n    get outer() {\r\n        return this._outer;\r\n    }\r\n    get thumb() {\r\n        return this._thumb;\r\n    }\r\n    // global\r\n    get prefix() {\r\n        return this._prop.prefix;\r\n    }\r\n    get isX() {\r\n        return this.prop.dir === 'x';\r\n    }\r\n    get isY() {\r\n        return !this.isX;\r\n    }\r\n    get scrollElement() {\r\n        return this.prop.container instanceof Window\r\n            ? document.documentElement : this.prop.container;\r\n    }\r\n    get scrollLine() {\r\n        const { scrollElement } = this;\r\n        return this.isX\r\n            ? scrollElement.scrollWidth - scrollElement.clientWidth\r\n            : scrollElement.scrollHeight - scrollElement.clientHeight;\r\n    }\r\n    get scrollWidth() {\r\n        return this.scrollElement.scrollWidth;\r\n    }\r\n    get scrollHeight() {\r\n        return this.scrollElement.scrollHeight;\r\n    }\r\n    get prop() {\r\n        return this._prop;\r\n    }\r\n    get scrollValues() {\r\n        const { container } = this.prop;\r\n        let top = 0;\r\n        let left = 0;\r\n        if (container instanceof Window) {\r\n            top = container.pageYOffset;\r\n            left = container.pageXOffset;\r\n        }\r\n        else if (container instanceof _smooth_scroll_SmoothScroll__WEBPACK_IMPORTED_MODULE_1__.SmoothScroll) {\r\n            top = container.targetTop;\r\n            left = container.targetLeft;\r\n        }\r\n        else {\r\n            top = container.scrollTop;\r\n            left = container.scrollLeft;\r\n        }\r\n        return {\r\n            left,\r\n            top,\r\n        };\r\n    }\r\n    /**\r\n     * Set scrolblar events\r\n     */\r\n    _setEvents() {\r\n        // set hover events\r\n        this._listeners.push((0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.addEventListener)(this.outer, 'mouseenter', this._handleHover.bind(this, true)));\r\n        this._listeners.push((0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.addEventListener)(this.outer, 'mouseleave', this._handleHover.bind(this, false)));\r\n        // set scroll events\r\n        this._scrollEvent = (0,_utils_listeners_onScroll__WEBPACK_IMPORTED_MODULE_2__[\"default\"])({\r\n            container: this.prop.container,\r\n            callback: (data) => {\r\n                this._handleScroll(data);\r\n            },\r\n        });\r\n        // set dragger\r\n        if (this.prop.isDraggable) {\r\n            this._dragger = new _dragger_DraggerMove__WEBPACK_IMPORTED_MODULE_3__.DraggerMove({\r\n                container: this.thumb,\r\n            });\r\n            this._dragger.addCallback('start', () => {\r\n                this.thumb.classList.add('in-action');\r\n                this._disableScrollBehaviour(true);\r\n                this._coordsAtDragStart = this.scrollValues;\r\n            });\r\n            this._dragger.addCallback('move', (data) => {\r\n                this._handleThumbDrag(data);\r\n            });\r\n            this._dragger.addCallback('end', () => {\r\n                this.thumb.classList.remove('in-action');\r\n                this._disableScrollBehaviour(false);\r\n            });\r\n        }\r\n    }\r\n    /**\r\n     * Toggle scrollBehavior: disable & reset smooth scrolling\r\n     */\r\n    _disableScrollBehaviour(bool) {\r\n        const val = bool ? 'auto' : 'unset';\r\n        if (this.prop.container instanceof Window) {\r\n            document.documentElement.style.scrollBehavior = val;\r\n        }\r\n        else if (this.prop.container instanceof HTMLElement) {\r\n            document.documentElement.style.scrollBehavior = val;\r\n        }\r\n    }\r\n    /**\r\n     * Handle hover state\r\n     */\r\n    _handleHover(bool) {\r\n        this.outer.classList.toggle('is-hovered', bool);\r\n    }\r\n    /**\r\n     * Handle Scroll Event\r\n     */\r\n    _handleScroll(data) {\r\n        // resize if needed\r\n        if (!this.prop.optimizeCalculations) {\r\n            this.resize();\r\n        }\r\n        // check if changes happened\r\n        let hasChanged = false;\r\n        if (this.isX) {\r\n            hasChanged = data.scrollLeft !== this._scrollVal;\r\n            this._scrollVal = data.scrollLeft;\r\n        }\r\n        else {\r\n            hasChanged = data.scrollTop !== this._scrollVal;\r\n            this._scrollVal = data.scrollTop;\r\n        }\r\n        if (!hasChanged) {\r\n            return;\r\n        }\r\n        // set auto hide\r\n        if (this.prop.autoHide && hasChanged) {\r\n            this.outer.classList.add('in-action');\r\n            if (this._actionTimeout) {\r\n                clearTimeout(this._actionTimeout);\r\n                this._actionTimeout = undefined;\r\n            }\r\n            this._actionTimeout = setTimeout(() => {\r\n                this.outer.classList.remove('in-action');\r\n            }, 500);\r\n        }\r\n        // render thumb\r\n        if (this.prop.optimizeCalculations) {\r\n            this._renderThumb();\r\n        }\r\n    }\r\n    /**\r\n     * Event on dragger move\r\n     */\r\n    _handleThumbDrag(data) {\r\n        data.evt.preventDefault();\r\n        const { container } = this.prop;\r\n        // calculate scroll iterators\r\n        const leftIterator = ((data.coords.x - data.start.x) / (this._outerWidth - this._thumbWidth)) * this.scrollLine;\r\n        const topIterator = ((data.coords.y - data.start.y) / (this._outerHeight - this._thumbHeight)) * this.scrollLine;\r\n        // calculate new scroll values\r\n        let { left, top } = this._coordsAtDragStart;\r\n        if (this.isX) {\r\n            left += leftIterator;\r\n        }\r\n        else {\r\n            top += topIterator;\r\n        }\r\n        // apply the values\r\n        container.scrollTo({\r\n            top,\r\n            left,\r\n            behavior: container instanceof _smooth_scroll_SmoothScroll__WEBPACK_IMPORTED_MODULE_1__.SmoothScroll ? this.prop.draggableScrollBehavior : 'auto',\r\n        });\r\n    }\r\n    /**\r\n     * Render the thumb\r\n     */\r\n    _renderThumb() {\r\n        // calculate progress\r\n        const progress = (0,_utils_math_clamp__WEBPACK_IMPORTED_MODULE_4__[\"default\"])(this._scrollVal / this.scrollLine, [0, 1]);\r\n        // calculate transforms\r\n        const x = this.isX ? (this._outerWidth - this._thumbWidth) * progress : 0;\r\n        const y = this.isY ? (this._outerHeight - this._thumbHeight) * progress : 0;\r\n        // render the thumb\r\n        this._thumb.style.transform = `translate(${x}px, ${y}px)`;\r\n    }\r\n    /**\r\n     * Resize the scene\r\n     */\r\n    resize() {\r\n        const { outer, thumb, scrollLine } = this;\r\n        const { minSize } = this.prop;\r\n        // get outer sizes\r\n        this._outerHeight = outer.clientHeight;\r\n        this._outerWidth = outer.clientWidth;\r\n        // calculate thumb sizes\r\n        if (this.prop.autoSize) {\r\n            if (this.isX) {\r\n                const barSize = (0,_utils_math_clamp__WEBPACK_IMPORTED_MODULE_4__[\"default\"])(this._outerWidth / (this.scrollWidth / (this.scrollWidth - scrollLine)), [minSize, Infinity]);\r\n                thumb.style.width = `${barSize}px`;\r\n            }\r\n            else {\r\n                const barSize = (0,_utils_math_clamp__WEBPACK_IMPORTED_MODULE_4__[\"default\"])(this._outerHeight / (this.scrollHeight / (this.scrollHeight - scrollLine)), [minSize, Infinity]);\r\n                thumb.style.height = `${barSize}px`;\r\n            }\r\n        }\r\n        // get thumb sizes\r\n        this._thumbHeight = thumb.clientHeight;\r\n        this._thumbWidth = thumb.clientWidth;\r\n        // define if empty\r\n        outer.classList.toggle('is-empty', scrollLine === 0);\r\n        // render the scrollbar thumb\r\n        this._renderThumb();\r\n    }\r\n    destroy() {\r\n        this._listeners.forEach((listener) => {\r\n            listener.remove();\r\n        });\r\n        if (this._scrollEvent) {\r\n            this._scrollEvent.remove();\r\n        }\r\n        if (this._actionTimeout) {\r\n            clearTimeout(this._actionTimeout);\r\n        }\r\n        if (this._dragger) {\r\n            this._dragger.destroy();\r\n        }\r\n        this._outer.remove();\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/components/scroll/scrollbar/Bar.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/components/scroll/scrollbar/ScrollBar.js":
/*!******************************************************************************!*\
  !*** ./node_modules/vevet/build/es/components/scroll/scrollbar/ScrollBar.js ***!
  \******************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   ScrollBar: () => (/* binding */ ScrollBar)\n/* harmony export */ });\n/* harmony import */ var vevet_dom__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! vevet-dom */ \"./node_modules/vevet-dom/dist/es/index.js\");\n/* harmony import */ var _base_Component__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../../base/Component */ \"./node_modules/vevet/build/es/base/Component.js\");\n/* harmony import */ var _smooth_scroll_SmoothScroll__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../smooth-scroll/SmoothScroll */ \"./node_modules/vevet/build/es/components/scroll/smooth-scroll/SmoothScroll.js\");\n/* harmony import */ var _Bar__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./Bar */ \"./node_modules/vevet/build/es/components/scroll/scrollbar/Bar.js\");\n\r\n\r\n\r\n\r\n/**\r\n * Create custom scroll bar\r\n */\r\nclass ScrollBar extends _base_Component__WEBPACK_IMPORTED_MODULE_1__.Component {\r\n    constructor(initialProp, init = true) {\r\n        super(initialProp, false);\r\n        const { autoHide, autoSize, minSize, optimizeCalculations, isDraggable, draggableScrollBehavior, } = this.prop;\r\n        // get container\r\n        if (typeof this.prop.container === 'string') {\r\n            const el = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.selectOne)(this.prop.container);\r\n            if (el) {\r\n                this._container = el;\r\n            }\r\n            else {\r\n                throw new Error('No scroll container');\r\n            }\r\n        }\r\n        else {\r\n            this._container = this.prop.container;\r\n        }\r\n        // create scrollBarsParent if needed\r\n        if (this.container instanceof Element) {\r\n            const { parentElement } = this.container;\r\n            if (parentElement) {\r\n                this._scrollWrapper = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.createElement)('div');\r\n                this._scrollWrapper.style.position = 'relative';\r\n                this._scrollWrapper.style.display = 'inline-block';\r\n                parentElement.insertBefore(this._scrollWrapper, this.container);\r\n                this._scrollWrapper.appendChild(this.container);\r\n                this._scrollWrapper.classList.add(`${this.prefix}-wrapper`);\r\n            }\r\n        }\r\n        // create bars\r\n        const barMainProp = {\r\n            container: this.container,\r\n            domParent: this.domParent,\r\n            autoHide,\r\n            autoSize,\r\n            minSize,\r\n            optimizeCalculations,\r\n            prefix: this.prefix,\r\n            isDraggable,\r\n            draggableScrollBehavior,\r\n        };\r\n        this._xBar = new _Bar__WEBPACK_IMPORTED_MODULE_2__[\"default\"](Object.assign({ dir: 'x' }, barMainProp));\r\n        this._yBar = new _Bar__WEBPACK_IMPORTED_MODULE_2__[\"default\"](Object.assign({ dir: 'y' }, barMainProp));\r\n        // add styles\r\n        this.scrollableElement.classList.add(`${this.prefix}-parent`);\r\n        // initialize the class\r\n        if (init) {\r\n            this.init();\r\n        }\r\n    }\r\n    _getDefaultProp() {\r\n        return Object.assign(Object.assign({}, super._getDefaultProp()), { container: window, domParent: false, draggable: true, autoSize: true, autoHide: true, minSize: 50, optimizeCalculations: false, resizeTimeout: 0, isDraggable: true, draggableScrollBehavior: 'smooth' });\r\n    }\r\n    get prefix() {\r\n        return `${this._app.prefix}scrollbar`;\r\n    }\r\n    /**\r\n     * Scroll container\r\n     */\r\n    get container() {\r\n        return this._container;\r\n    }\r\n    /**\r\n     * Get scrollable element\r\n     */\r\n    get scrollableElement() {\r\n        const { container } = this;\r\n        if (container instanceof Window) {\r\n            return this._app.body;\r\n        }\r\n        if (container instanceof Element) {\r\n            return container;\r\n        }\r\n        return container.outer;\r\n    }\r\n    /**\r\n     * The element into wchich scroll bars will be appended\r\n     */\r\n    get domParent() {\r\n        const { domParent } = this.prop;\r\n        const { container } = this;\r\n        if (!domParent) {\r\n            if (this._scrollWrapper) {\r\n                return this._scrollWrapper;\r\n            }\r\n            if (container instanceof Window) {\r\n                return this._app.body;\r\n            }\r\n            if (container instanceof Element) {\r\n                return container;\r\n            }\r\n            return container.outer;\r\n        }\r\n        return domParent;\r\n    }\r\n    // Set Module Events\r\n    _setEvents() {\r\n        super._setEvents();\r\n        const { container } = this;\r\n        // set resize event\r\n        if (container instanceof _smooth_scroll_SmoothScroll__WEBPACK_IMPORTED_MODULE_3__.SmoothScroll) {\r\n            container.addCallback('resize', () => {\r\n                this.resize();\r\n            }, {\r\n                name: this.name,\r\n            });\r\n        }\r\n        else {\r\n            this.addViewportCallback('', () => {\r\n                this.resize();\r\n            }, {\r\n                timeout: this.prop.resizeTimeout,\r\n            });\r\n        }\r\n        this.resize();\r\n    }\r\n    _onPropMutate() {\r\n        super._onPropMutate();\r\n        this.resize();\r\n    }\r\n    /**\r\n     * Resize the canvas\r\n     */\r\n    resize() {\r\n        this._xBar.resize();\r\n        this._yBar.resize();\r\n    }\r\n    /**\r\n     * Destroy the module\r\n     */\r\n    _destroy() {\r\n        super._destroy();\r\n        // destroy bars\r\n        this._xBar.destroy();\r\n        this._yBar.destroy();\r\n        // remove scrollbars parent\r\n        if (!!this._scrollWrapper && this.container instanceof Element) {\r\n            const { parentElement } = this._scrollWrapper;\r\n            if (parentElement) {\r\n                parentElement.insertBefore(this.container, this._scrollWrapper);\r\n            }\r\n            this._scrollWrapper.remove();\r\n        }\r\n        // reset styles\r\n        this.scrollableElement.classList.remove(`${this.prefix}-parent`);\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/components/scroll/scrollbar/ScrollBar.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/components/scroll/section/ScrollSectionProgress.js":
/*!****************************************************************************************!*\
  !*** ./node_modules/vevet/build/es/components/scroll/section/ScrollSectionProgress.js ***!
  \****************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   ScrollSectionProgress: () => (/* binding */ ScrollSectionProgress)\n/* harmony export */ });\n/* harmony import */ var vevet_dom__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! vevet-dom */ \"./node_modules/vevet-dom/dist/es/index.js\");\n/* harmony import */ var _utils_listeners_onScroll__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../../utils/listeners/onScroll */ \"./node_modules/vevet/build/es/utils/listeners/onScroll.js\");\n/* harmony import */ var _base_Component__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../../base/Component */ \"./node_modules/vevet/build/es/base/Component.js\");\n/* harmony import */ var _utils_math_clampScope__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../../../utils/math/clampScope */ \"./node_modules/vevet/build/es/utils/math/clampScope.js\");\n/* harmony import */ var _utils_scroll_getScrollValues__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../../../utils/scroll/getScrollValues */ \"./node_modules/vevet/build/es/utils/scroll/getScrollValues.js\");\n\r\n\r\n\r\n\r\n\r\n/**\r\n * Elements into viewport\r\n */\r\nclass ScrollSectionProgress extends _base_Component__WEBPACK_IMPORTED_MODULE_1__.Component {\r\n    constructor(initialProp, init = true) {\r\n        super(initialProp, false);\r\n        // get scroll container\r\n        if (typeof this.prop.container === 'string') {\r\n            this._scrollContainer = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.selectOne)(this.prop.container);\r\n        }\r\n        else {\r\n            this._scrollContainer = this.prop.container;\r\n        }\r\n        // get section element\r\n        if (typeof this.prop.section === 'string') {\r\n            this._section = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.selectOne)(this.prop.section);\r\n        }\r\n        else {\r\n            this._section = this.prop.section;\r\n        }\r\n        // set defaults\r\n        this._progress = -0.001;\r\n        this._scopeScroll = [0, 0];\r\n        this._scopeIn = [0, 0];\r\n        this._scopeMove = [0, 0];\r\n        this._scopeOut = [0, 0];\r\n        // initialize the class\r\n        if (init) {\r\n            this.init();\r\n        }\r\n    }\r\n    _getDefaultProp() {\r\n        return Object.assign(Object.assign({}, super._getDefaultProp()), { container: window, viewportTarget: '', resizeTimeout: 0 });\r\n    }\r\n    get scrollContainer() {\r\n        return this._scrollContainer;\r\n    }\r\n    get section() {\r\n        return this._section;\r\n    }\r\n    get scopeScroll() {\r\n        return this._scopeScroll;\r\n    }\r\n    get scopeIn() {\r\n        return this._scopeIn;\r\n    }\r\n    get scopeMove() {\r\n        return this._scopeMove;\r\n    }\r\n    get scopeOut() {\r\n        return this._scopeOut;\r\n    }\r\n    /**\r\n     * Global progress\r\n     */\r\n    get progress() {\r\n        return this._progress;\r\n    }\r\n    set progress(val) {\r\n        this._progress = val;\r\n    }\r\n    init() {\r\n        super.init();\r\n    }\r\n    // Set Module Events\r\n    _setEvents() {\r\n        super._setEvents();\r\n        // set resize events\r\n        this.addViewportCallback(this.prop.viewportTarget, () => {\r\n            this.resize();\r\n        }, {\r\n            timeout: this.prop.resizeTimeout,\r\n        });\r\n        // resize on page loaded\r\n        this._loadedEvent = this._app.onPageLoaded();\r\n        this._loadedEvent.then(() => {\r\n            this.resize();\r\n        }).catch(() => { });\r\n        // set scroll events\r\n        this._scrollEvent = (0,_utils_listeners_onScroll__WEBPACK_IMPORTED_MODULE_2__[\"default\"])({\r\n            container: this.prop.container,\r\n            callback: this._handleScroll.bind(this),\r\n        });\r\n    }\r\n    _onPropMutate() {\r\n        super._onPropMutate();\r\n        this.resize();\r\n    }\r\n    /**\r\n     * 'in' progress\r\n     */\r\n    get progressIn() {\r\n        return (0,_utils_math_clampScope__WEBPACK_IMPORTED_MODULE_3__[\"default\"])(this.progress, this.scopeIn) || 0;\r\n    }\r\n    /**\r\n     * 'out' progress\r\n     */\r\n    get progressOut() {\r\n        return (0,_utils_math_clampScope__WEBPACK_IMPORTED_MODULE_3__[\"default\"])(this.progress, this.scopeOut) || 0;\r\n    }\r\n    /**\r\n     * 'move' progress\r\n     */\r\n    get progressMove() {\r\n        return (0,_utils_math_clampScope__WEBPACK_IMPORTED_MODULE_3__[\"default\"])(this.progress, this.scopeMove) || 0;\r\n    }\r\n    /**\r\n     * Handle scroll event\r\n     */\r\n    _handleScroll() {\r\n        // calculate scopes\r\n        const scrollData = (0,_utils_scroll_getScrollValues__WEBPACK_IMPORTED_MODULE_4__[\"default\"])(this.scrollContainer);\r\n        if (!scrollData) {\r\n            return;\r\n        }\r\n        this._render(scrollData);\r\n    }\r\n    /**\r\n     * Resize the scene\r\n     */\r\n    resize() {\r\n        // calculate scopes\r\n        const scrollData = (0,_utils_scroll_getScrollValues__WEBPACK_IMPORTED_MODULE_4__[\"default\"])(this.scrollContainer);\r\n        if (!scrollData) {\r\n            return;\r\n        }\r\n        // get sizes\r\n        const bounding = this.section.getBoundingClientRect();\r\n        const vHeight = this._app.viewport.height;\r\n        // calculate scroll scope\r\n        const inStart = scrollData.scrollTop + bounding.top - vHeight;\r\n        const moveEnd = scrollData.scrollTop + bounding.top + bounding.height;\r\n        const scrollLine = moveEnd - inStart;\r\n        this._scopeScroll = [inStart, moveEnd];\r\n        // calculate scopes\r\n        this._scopeIn = [0, vHeight / scrollLine];\r\n        this._scopeOut = [1 - vHeight / scrollLine, 1];\r\n        this._scopeMove = [this._scopeIn[1], this._scopeOut[0]];\r\n        // launch callbacks\r\n        this.callbacks.tbt('resize', false);\r\n        // render the scene\r\n        this._render(scrollData, true);\r\n    }\r\n    /**\r\n     * Render the scene\r\n     */\r\n    _render(scrollData, force = false) {\r\n        const canRender = this._canRender(scrollData, force);\r\n        if (!canRender) {\r\n            return;\r\n        }\r\n        this.callbacks.tbt('render', false);\r\n    }\r\n    /**\r\n     * Check if the section can be rendered\r\n     */\r\n    _canRender(scrollData, force = false) {\r\n        if (!scrollData) {\r\n            return false;\r\n        }\r\n        const { scrollTop } = scrollData;\r\n        const prevProgress = this.progress;\r\n        const progress = (0,_utils_math_clampScope__WEBPACK_IMPORTED_MODULE_3__[\"default\"])(scrollTop, [this._scopeScroll[0], this._scopeScroll[1]]) || 0;\r\n        this.progress = progress;\r\n        return force || (progress !== prevProgress);\r\n    }\r\n    /**\r\n     * Destroy the module\r\n     */\r\n    _destroy() {\r\n        var _a, _b;\r\n        super._destroy();\r\n        (_a = this._scrollEvent) === null || _a === void 0 ? void 0 : _a.remove();\r\n        (_b = this._loadedEvent) === null || _b === void 0 ? void 0 : _b.cancel();\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/components/scroll/section/ScrollSectionProgress.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/components/scroll/smooth-scroll/SmoothScroll.js":
/*!*************************************************************************************!*\
  !*** ./node_modules/vevet/build/es/components/scroll/smooth-scroll/SmoothScroll.js ***!
  \*************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   SmoothScroll: () => (/* binding */ SmoothScroll)\n/* harmony export */ });\n/* harmony import */ var vevet_dom__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! vevet-dom */ \"./node_modules/vevet-dom/dist/es/index.js\");\n/* harmony import */ var normalize_wheel__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! normalize-wheel */ \"./node_modules/normalize-wheel/index.js\");\n/* harmony import */ var normalize_wheel__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(normalize_wheel__WEBPACK_IMPORTED_MODULE_1__);\n/* harmony import */ var _base_Component__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../../base/Component */ \"./node_modules/vevet/build/es/base/Component.js\");\n/* harmony import */ var _animation_frame_AnimationFrame__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../../animation-frame/AnimationFrame */ \"./node_modules/vevet/build/es/components/animation-frame/AnimationFrame.js\");\n/* harmony import */ var _utils_math_clamp__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../../../utils/math/clamp */ \"./node_modules/vevet/build/es/utils/math/clamp.js\");\n/* harmony import */ var _utils_math__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../../../utils/math */ \"./node_modules/vevet/build/es/utils/math/lerp.js\");\n\r\n\r\n\r\n\r\n\r\n\r\n/**\r\n * Create smooth scrolling.\r\n */\r\nclass SmoothScroll extends _base_Component__WEBPACK_IMPORTED_MODULE_2__.Component {\r\n    constructor(initialProp, init = true) {\r\n        super(initialProp, false);\r\n        const { selectors } = this.prop;\r\n        // set default variables\r\n        this._targetLeft = 0;\r\n        this._targetTop = 0;\r\n        this._scrollLeft = 0;\r\n        this._scrollTop = 0;\r\n        this._scrollWidth = 0;\r\n        this._scrollHeight = 0;\r\n        this._clientWidth = 0;\r\n        this._clientHeight = 0;\r\n        this._instant = false;\r\n        this._animationFrame = undefined;\r\n        this._outerAnimationFrameEvent = undefined;\r\n        this._currentFPS = 60;\r\n        // get outer elements\r\n        this._outer = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.selectOne)(selectors.outer);\r\n        if (!(this._outer instanceof HTMLElement)) {\r\n            throw new Error(`${selectors.outer} is not a HTMLElement`);\r\n        }\r\n        this._outer.classList.add(this.prefix);\r\n        // get or create container\r\n        const existingContainer = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.selectOne)(`.${this.prefix}__container`, this.outer);\r\n        if ((0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.isElement)(existingContainer)) {\r\n            this._container = existingContainer;\r\n            this._containerExists = true;\r\n        }\r\n        else {\r\n            this._container = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.createElement)('div', {\r\n                class: `${this.prefix}__container`,\r\n                parent: this.outer,\r\n                children: Array.from(this.outer.children),\r\n            });\r\n            this._containerExists = false;\r\n        }\r\n        // get scrollable elements\r\n        if (selectors.elements) {\r\n            this._elements = Array.from((0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.selectAll)(selectors.elements, this._outer));\r\n        }\r\n        else {\r\n            this._elements = [this._container];\r\n        }\r\n        this._elementsLength = this._elements.length;\r\n        // add will-change\r\n        if (this.prop.useWillChange) {\r\n            this._elements.forEach((el) => {\r\n                el.style.willChange = 'transform';\r\n            });\r\n        }\r\n        // initialize the class\r\n        if (init) {\r\n            this.init();\r\n        }\r\n    }\r\n    get prefix() {\r\n        return `${this._app.prefix}smooth-scroll`;\r\n    }\r\n    get outer() {\r\n        return this._outer;\r\n    }\r\n    get container() {\r\n        return this._container;\r\n    }\r\n    get elements() {\r\n        return this._elements;\r\n    }\r\n    get targetLeft() {\r\n        return this._targetLeft;\r\n    }\r\n    set targetLeft(val) {\r\n        this.targetLeftBound = val;\r\n        this._enable();\r\n    }\r\n    /**\r\n     * Set value without animation request\r\n     */\r\n    set targetLeftBound(val) {\r\n        const min = !!this.prop.overscroll && this.prop.isHorizontal\r\n            ? -this.prop.overscroll.max : 0;\r\n        const max = this.maxScrollableWidth\r\n            + (!!this.prop.overscroll && this.prop.isHorizontal ? this.prop.overscroll.max : 0);\r\n        this._targetLeft = (0,_utils_math_clamp__WEBPACK_IMPORTED_MODULE_3__[\"default\"])(val, [min, max]);\r\n    }\r\n    get targetLeftBound() {\r\n        return this._targetLeft;\r\n    }\r\n    get targetTop() {\r\n        return this._targetTop;\r\n    }\r\n    set targetTop(val) {\r\n        this.targetTopBound = val;\r\n        this._enable();\r\n    }\r\n    /**\r\n     * Set value without animation request\r\n     */\r\n    set targetTopBound(val) {\r\n        const min = !!this.prop.overscroll && !this.prop.isHorizontal\r\n            ? -this.prop.overscroll.max : 0;\r\n        const max = this.maxScrollableHeight\r\n            + (!!this.prop.overscroll && !this.prop.isHorizontal ? this.prop.overscroll.max : 0);\r\n        this._targetTop = (0,_utils_math_clamp__WEBPACK_IMPORTED_MODULE_3__[\"default\"])(val, [min, max]);\r\n    }\r\n    get targetTopBound() {\r\n        return this._targetTop;\r\n    }\r\n    get scrollLeft() {\r\n        return this._scrollLeft;\r\n    }\r\n    set scrollLeft(val) {\r\n        this.targetLeftBound = val;\r\n        this._scrollLeft = this._targetLeft;\r\n        this._enable();\r\n    }\r\n    get scrollTop() {\r\n        return this._scrollTop;\r\n    }\r\n    set scrollTop(val) {\r\n        this.targetTopBound = val;\r\n        this._scrollTop = this._targetTop;\r\n        this._enable();\r\n    }\r\n    get scrollWidth() {\r\n        return this._scrollWidth;\r\n    }\r\n    get scrollHeight() {\r\n        return this._scrollHeight;\r\n    }\r\n    get clientWidth() {\r\n        return this._clientWidth;\r\n    }\r\n    get clientHeight() {\r\n        return this._clientHeight;\r\n    }\r\n    /**\r\n     * Maximum scrollable area of the X axis\r\n     */\r\n    get maxScrollableWidth() {\r\n        return this.scrollWidth - this.clientWidth;\r\n    }\r\n    /**\r\n     * Maximum scrollable area of the Y axis\r\n     */\r\n    get maxScrollableHeight() {\r\n        return this.scrollHeight - this.clientHeight;\r\n    }\r\n    _getDefaultProp() {\r\n        return Object.assign(Object.assign({}, super._getDefaultProp()), { selectors: {\r\n                outer: `#${this.prefix}`,\r\n                elements: false,\r\n            }, enabled: true, animationFrame: false, recalculateSizes: true, resizeTimeout: 0, useWheel: true, autoStop: true, isHorizontal: false, stopPropagation: true, useWillChange: true, render: {\r\n                lerp: 0.1,\r\n                lerpToFixed: 2,\r\n                approximation: 0.1,\r\n                normalizeLerp: false,\r\n            }, overscroll: {\r\n                friction: 0.5,\r\n                max: 250,\r\n            } });\r\n    }\r\n    // Extra constructor\r\n    _constructor() {\r\n        super._constructor();\r\n        this._toggle();\r\n    }\r\n    // Set Events\r\n    _setEvents() {\r\n        // update sizes\r\n        this.resize();\r\n        this.addViewportCallback('', () => {\r\n            this.resize(true);\r\n        }, {\r\n            timeout: this.prop.resizeTimeout,\r\n        });\r\n        // wheel\r\n        this.addEventListeners(this.outer, 'wheel', (e) => {\r\n            this._handleWheel(e);\r\n        });\r\n        // on scroll\r\n        this.addEventListeners(this.outer, 'scroll', () => {\r\n            this.outer.scrollTop = 0;\r\n            this.outer.scrollLeft = 0;\r\n        });\r\n    }\r\n    _onPropMutate() {\r\n        super._onPropMutate();\r\n        this.resize();\r\n        this._toggle();\r\n    }\r\n    /**\r\n     * Recalculate scroll sizes\r\n     */\r\n    resize(\r\n    /**\r\n     * If the method was called natively on window resize\r\n     */\r\n    native = false) {\r\n        // get elements\r\n        const { container, outer } = this;\r\n        // get sizes\r\n        this._clientWidth = outer.clientWidth;\r\n        this._clientHeight = outer.clientHeight;\r\n        this._scrollWidth = (0,_utils_math_clamp__WEBPACK_IMPORTED_MODULE_3__[\"default\"])(container.clientWidth, [this.clientWidth, Infinity]);\r\n        this._scrollHeight = (0,_utils_math_clamp__WEBPACK_IMPORTED_MODULE_3__[\"default\"])(container.clientHeight, [this.clientHeight, Infinity]);\r\n        // force instant change\r\n        // it means that after resizing, scrolling will be instantaneous for a while\r\n        if (native) {\r\n            this._instant = true;\r\n        }\r\n        // sometimes after resizing it may happen that targets are less or more\r\n        // than maximum values of scrolling\r\n        // that's why fix it here\r\n        if (native) {\r\n            this.targetLeft = parseInt(this.targetLeft.toFixed(0), 10);\r\n            this.targetTop = parseInt(this.targetTop.toFixed(0), 10);\r\n        }\r\n        // set scroll classes\r\n        const hasScroll = this.maxScrollableHeight > 0 || this.maxScrollableWidth > 0;\r\n        outer.classList.toggle('has-scroll', hasScroll);\r\n        outer.classList.toggle('no-scroll', !hasScroll);\r\n        // render elements\r\n        this._updateElementsProp();\r\n        // this.render();\r\n        // launch callbacks\r\n        this.callbacks.tbt('resize', false);\r\n    }\r\n    /**\r\n     * Recalculate scroll sizes\r\n     */\r\n    _recalculateSizes() {\r\n        const { container } = this;\r\n        const height = container.clientHeight;\r\n        const width = container.clientWidth;\r\n        if ((height !== this.scrollHeight) || (width !== this.scrollWidth)) {\r\n            this.resize();\r\n        }\r\n    }\r\n    /**\r\n     * Update elements' properties\r\n     */\r\n    _updateElementsProp() {\r\n        for (let index = 0; index < this._elementsLength; index += 1) {\r\n            const el = this._elements[index];\r\n            // update scroll values\r\n            el.smoothScrollLeft = this.scrollLeft;\r\n            el.smoothScrollTop = this.scrollTop;\r\n            // update easing\r\n            const easingAttr = el.getAttribute(`${this.prefix}-ease`);\r\n            if (easingAttr) {\r\n                try {\r\n                    el.smoothScrollLerpEase = parseFloat(easingAttr);\r\n                }\r\n                catch (e) {\r\n                    //\r\n                }\r\n            }\r\n        }\r\n    }\r\n    /**\r\n     * Event on wheel\r\n     */\r\n    _handleWheel(evt) {\r\n        const { prop } = this;\r\n        if (!prop.enabled || !prop.useWheel) {\r\n            return;\r\n        }\r\n        // stop propagation if needed\r\n        if (prop.stopPropagation\r\n            || (!prop.stopPropagation\r\n                && ((this.scrollLeft > 0\r\n                    && this.scrollLeft < this.maxScrollableWidth)\r\n                    || (this.scrollTop > 0\r\n                        && this.scrollTop < this.maxScrollableHeight)))) {\r\n            evt.stopImmediatePropagation();\r\n            evt.stopPropagation();\r\n            evt.preventDefault();\r\n        }\r\n        // get normalized delta\r\n        const delta = normalize_wheel__WEBPACK_IMPORTED_MODULE_1___default()(evt);\r\n        // set new scroll targets\r\n        this.targetLeftBound += prop.isHorizontal ? delta.pixelY : delta.pixelX;\r\n        this.targetTopBound += prop.isHorizontal ? delta.pixelX : delta.pixelY;\r\n        // play scroll\r\n        this._enable();\r\n        // launch events\r\n        this.callbacks.tbt('wheel', evt);\r\n    }\r\n    /**\r\n     * Toggle animation: Enable / Disable scrolling\r\n     */\r\n    _toggle() {\r\n        if (this.prop.enabled) {\r\n            this._enable();\r\n        }\r\n        else {\r\n            this._disable();\r\n        }\r\n    }\r\n    /**\r\n     * Enable scrolling\r\n     */\r\n    _enable() {\r\n        if (!this.prop.enabled) {\r\n            return;\r\n        }\r\n        // set animation callback for the outer AnimationFrame\r\n        if (!!this.prop.animationFrame && !this._outerAnimationFrameEvent) {\r\n            this._outerAnimationFrameEvent = this.prop.animationFrame.addCallback('frame', (data) => {\r\n                this._currentFPS = data.realFPS;\r\n                this.render();\r\n            });\r\n        }\r\n        else {\r\n            // otherwise, check if inner AnimationFrame is created\r\n            if (!this._animationFrame) {\r\n                this._animationFrame = new _animation_frame_AnimationFrame__WEBPACK_IMPORTED_MODULE_4__.AnimationFrame();\r\n                this._animationFrame.addCallback('frame', (data) => {\r\n                    this._currentFPS = data.realFPS;\r\n                    this.render();\r\n                });\r\n            }\r\n            this._animationFrame.play();\r\n        }\r\n    }\r\n    /**\r\n     * Disable scrolling\r\n     */\r\n    _disable() {\r\n        if (!!this._outerAnimationFrameEvent && !!this.prop.animationFrame) {\r\n            this._outerAnimationFrameEvent.remove();\r\n        }\r\n        if (this._animationFrame) {\r\n            this._animationFrame.pause();\r\n        }\r\n    }\r\n    /**\r\n     * Render elements\r\n     */\r\n    render() {\r\n        // vars\r\n        const { prop } = this;\r\n        // recalculate sizes\r\n        if (prop.recalculateSizes) {\r\n            this._recalculateSizes();\r\n        }\r\n        // calculate scroll values\r\n        this._calcScroll();\r\n        // change elements' values\r\n        this._calcElements();\r\n        // render\r\n        this._renderElements();\r\n        // disable instant scrolling\r\n        if (this._instant) {\r\n            this._instant = false;\r\n        }\r\n        // launch events\r\n        this.callbacks.tbt('scroll', false);\r\n        // stop animation frame if values are approximated\r\n        const yDiff = Math.abs(this.targetTop - this.scrollTop);\r\n        const xDiff = Math.abs(this.targetLeft - this.scrollLeft);\r\n        if (xDiff === 0 && yDiff === 0 && prop.autoStop) {\r\n            this._disable();\r\n            this.callbacks.tbt('approximate', false);\r\n        }\r\n    }\r\n    /**\r\n     * Calculate scroll value\r\n     */\r\n    _calcScroll() {\r\n        const { overscroll } = this.prop;\r\n        // use overscroll\r\n        if (overscroll) {\r\n            // left\r\n            if (this.targetLeft < 0) {\r\n                this.targetLeftBound = this._lerp(this.targetLeftBound, 0, overscroll.friction);\r\n            }\r\n            else if (this.targetLeft > this.maxScrollableWidth) {\r\n                this.targetLeftBound = this._lerp(this.targetLeftBound, this.maxScrollableWidth, overscroll.friction);\r\n            }\r\n            // top\r\n            if (this.targetTop < 0) {\r\n                this.targetTopBound = this._lerp(this.targetTopBound, 0, overscroll.friction);\r\n            }\r\n            else if (this.targetTop > this.maxScrollableHeight) {\r\n                this.targetTopBound = this._lerp(this.targetTopBound, this.maxScrollableHeight, overscroll.friction);\r\n            }\r\n        }\r\n        // update values\r\n        this._scrollLeft = this._lerp(this.scrollLeft, this.targetLeft);\r\n        this._scrollTop = this._lerp(this.scrollTop, this.targetTop);\r\n    }\r\n    /**\r\n     * Calculate elements' values.\r\n     */\r\n    _calcElements() {\r\n        const globalEase = this._getLerpEase();\r\n        for (let index = 0; index < this._elementsLength; index += 1) {\r\n            const el = this._elements[index];\r\n            // get element ease\r\n            const elEase = this._getLerpEase(el);\r\n            // change values\r\n            if (elEase === globalEase) {\r\n                el.smoothScrollLeft = this._scrollLeft;\r\n                el.smoothScrollTop = this._scrollTop;\r\n            }\r\n            else {\r\n                el.smoothScrollLeft = this._lerp(el.smoothScrollLeft, this._targetLeft, elEase);\r\n                el.smoothScrollTop = this._lerp(el.smoothScrollTop, this._targetTop, elEase);\r\n            }\r\n        }\r\n    }\r\n    /**\r\n     * Interpolate values\r\n     */\r\n    _lerp(current, target, ease = this._getLerpEase()) {\r\n        const { lerpToFixed, approximation } = this.prop.render;\r\n        const currentEase = this._instant ? 1 : ease;\r\n        const val = (0,_utils_math__WEBPACK_IMPORTED_MODULE_5__[\"default\"])(current, target, currentEase, approximation);\r\n        // round the values\r\n        if (typeof lerpToFixed === 'number') {\r\n            const fixed = Math.round(Math.abs(lerpToFixed));\r\n            return parseFloat(val.toFixed(fixed));\r\n        }\r\n        return val;\r\n    }\r\n    /**\r\n     * Get element ease\r\n     */\r\n    _getLerpEase(el = false) {\r\n        const { lerp: lerpEase, normalizeLerp } = this.prop.render;\r\n        const fpsMultiplier = normalizeLerp ? 60 / this._currentFPS : 1;\r\n        if (el) {\r\n            return el.smoothScrollLerpEase || lerpEase * fpsMultiplier;\r\n        }\r\n        return lerpEase * fpsMultiplier;\r\n    }\r\n    /**\r\n     * Render elements\r\n     */\r\n    _renderElements() {\r\n        for (let index = 0; index < this._elementsLength; index += 1) {\r\n            const el = this._elements[index];\r\n            // coords\r\n            const x = -el.smoothScrollLeft;\r\n            const y = -el.smoothScrollTop;\r\n            // set styles\r\n            el.style.transform = `matrix3d(1,0,0.00,0,0.00,1,0.00,0,0,0,1,0, ${x}, ${y}, 0,1)`;\r\n        }\r\n    }\r\n    // LIKE NATIVE SCROLL\r\n    /**\r\n     * Scroll to\r\n     */\r\n    scrollTo() {\r\n        // eslint-disable-next-line prefer-rest-params\r\n        const arg = arguments;\r\n        // if object\r\n        if (arg.length === 1 && typeof arg[0] === 'object') {\r\n            const options = arg[0];\r\n            const { top, left, behavior } = options;\r\n            if (typeof left !== 'undefined') {\r\n                if (behavior === 'smooth') {\r\n                    this.targetLeftBound = left;\r\n                }\r\n                else {\r\n                    this.scrollLeft = left;\r\n                }\r\n            }\r\n            if (typeof top !== 'undefined') {\r\n                if (behavior === 'smooth') {\r\n                    this.targetTopBound = top;\r\n                }\r\n                else {\r\n                    this.scrollTop = top;\r\n                }\r\n            }\r\n            if (behavior === 'smooth') {\r\n                this._enable();\r\n            }\r\n        }\r\n        // if numbers\r\n        if (typeof arg[0] === 'number' || typeof arg[1] === 'number') {\r\n            if (typeof arg[0] === 'number') {\r\n                // eslint-disable-next-line prefer-destructuring\r\n                this.scrollLeft = arg[0];\r\n            }\r\n            if (typeof arg[1] === 'number') {\r\n                // eslint-disable-next-line prefer-destructuring\r\n                this.scrollTop = arg[1];\r\n            }\r\n        }\r\n    }\r\n    /**\r\n     * Destroy the scroll\r\n     */\r\n    _destroy() {\r\n        super._destroy();\r\n        // disable animation\r\n        this._disable();\r\n        if (this._animationFrame) {\r\n            this._animationFrame.destroy();\r\n        }\r\n        // destroy container\r\n        if (!this._containerExists) {\r\n            while (this._container.firstChild) {\r\n                this._outer.appendChild(this._container.firstChild);\r\n            }\r\n            this._container.remove();\r\n        }\r\n        // remove classes\r\n        this._outer.classList.remove(this.prefix);\r\n        // reset styles\r\n        this._elements.forEach((el) => {\r\n            el.style.transform = '';\r\n            el.style.willChange = '';\r\n        });\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/components/scroll/smooth-scroll/SmoothScroll.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/components/text/SplitText.js":
/*!******************************************************************!*\
  !*** ./node_modules/vevet/build/es/components/text/SplitText.js ***!
  \******************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   SplitText: () => (/* binding */ SplitText)\n/* harmony export */ });\n/* harmony import */ var vevet_dom__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! vevet-dom */ \"./node_modules/vevet-dom/dist/es/index.js\");\n/* harmony import */ var _base_Component__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../base/Component */ \"./node_modules/vevet/build/es/base/Component.js\");\n\r\n\r\n/**\r\n * Split text into letters, words & lines\r\n */\r\nclass SplitText extends _base_Component__WEBPACK_IMPORTED_MODULE_1__.Component {\r\n    constructor(initialProp, init = true) {\r\n        super(initialProp, false);\r\n        // get text container\r\n        if (this.prop.container) {\r\n            const container = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.selectOne)(this.prop.container);\r\n            if (container instanceof HTMLElement) {\r\n                this._container = container;\r\n            }\r\n        }\r\n        // disable translation\r\n        this._container.translate = false;\r\n        // add classes\r\n        if (this._container) {\r\n            this._container.classList.add(this.prefix);\r\n        }\r\n        // get initial text\r\n        this._initHTML = this._container.innerHTML;\r\n        const innerText = this._container[this.prop.textSource].trim();\r\n        this._initText = innerText || 'no rendered text';\r\n        this._initText = this._initText.replace(/\\s+\\n/gm, '\\n');\r\n        this._initText = this._initText.replace(/<br>/gm, String.fromCharCode(10));\r\n        this._initText = this._initText.replace(/<br\\/>/gm, String.fromCharCode(10));\r\n        this._initText = this._initText.replace(/<br \\/>/gm, String.fromCharCode(10));\r\n        this._container.ariaLabel = this._initText;\r\n        // set default vars\r\n        this._isPrimarySplit = false;\r\n        this._letters = [];\r\n        this._words = [];\r\n        this._lines = [];\r\n        // initialize the class\r\n        if (init) {\r\n            this.init();\r\n        }\r\n    }\r\n    _getDefaultProp() {\r\n        return Object.assign(Object.assign({}, super._getDefaultProp()), { container: `#${this.prefix}`, textSource: 'innerText', appendLetters: true, appendLines: false, viewportTarget: '', resizeTimeout: 0 });\r\n    }\r\n    get prefix() {\r\n        return `${this._app.prefix}split-text`;\r\n    }\r\n    /**\r\n     * Text container\r\n     */\r\n    get container() {\r\n        return this._container;\r\n    }\r\n    get letters() {\r\n        return this._letters;\r\n    }\r\n    get words() {\r\n        return this._words;\r\n    }\r\n    get lines() {\r\n        return this._lines;\r\n    }\r\n    _setEvents() {\r\n        super._setEvents();\r\n        // split the text\r\n        this.splitText();\r\n        if (this.prop.appendLines) {\r\n            this.addViewportCallback(this.prop.viewportTarget, () => {\r\n                this.splitText();\r\n            }, {\r\n                timeout: this.prop.resizeTimeout,\r\n            });\r\n        }\r\n    }\r\n    /**\r\n     * Split the text\r\n     */\r\n    splitText() {\r\n        // split into words & letters\r\n        if (!this._isPrimarySplit) {\r\n            this.container.innerHTML = '';\r\n            this._splitIntoWords();\r\n            this._splitIntoLetters();\r\n            this._appendWords();\r\n            this._isPrimarySplit = true;\r\n        }\r\n        // split text into lines\r\n        if (this.prop.appendLines) {\r\n            this._splitIntoLines();\r\n        }\r\n        // launch callbacks\r\n        this.callbacks.tbt('split', false);\r\n    }\r\n    /**\r\n     * Split the text into words\r\n     */\r\n    _splitIntoWords() {\r\n        const chars = this._initText.split('');\r\n        // create words\r\n        let wordIndex = 0;\r\n        chars.forEach((char) => {\r\n            // get an existing word or create a base for a new one\r\n            const currentWord = this._words[wordIndex] || {\r\n                content: '',\r\n                hasNewLine: false,\r\n                el: (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.createElement)('span', {\r\n                    class: `${this.prefix}__word`,\r\n                    attr: [\r\n                        ['aria-hidden', 'true'],\r\n                    ],\r\n                }),\r\n                letters: [],\r\n            };\r\n            currentWord.el.style.display = 'inline-block';\r\n            this._words[wordIndex] = currentWord;\r\n            // get type of the char\r\n            const charCode = char.charCodeAt(0);\r\n            const isWhitespace = charCode === 32 || charCode === 160;\r\n            const isSeparator = [45, 8208, 8211, 8212, 8722].includes(charCode);\r\n            const isNewLine = charCode === 10;\r\n            // add elements\r\n            if (isWhitespace) {\r\n                currentWord.whitespace = document.createTextNode(' ');\r\n            }\r\n            if (isNewLine) {\r\n                currentWord.br = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.createElement)('br');\r\n            }\r\n            // update word states\r\n            currentWord.hasNewLine = isNewLine;\r\n            // go to next word if needed\r\n            if (isWhitespace || isNewLine) {\r\n                wordIndex += 1;\r\n                return;\r\n            }\r\n            // update contents\r\n            currentWord.content += char;\r\n            if (!this.prop.appendLetters) {\r\n                currentWord.el.innerHTML = currentWord.content;\r\n            }\r\n            // go to next word if needed\r\n            if (isSeparator) {\r\n                wordIndex += 1;\r\n            }\r\n        });\r\n        // only filled words\r\n        this._words = this._words.filter((word, index) => {\r\n            if (word.content.length === 0) {\r\n                if (index > 0) {\r\n                    this._words[index - 1].whitespace = word.whitespace;\r\n                    this._words[index - 1].br = word.br;\r\n                }\r\n                return false;\r\n            }\r\n            return true;\r\n        });\r\n        // post-processing\r\n        let prevWord;\r\n        this._words.forEach((word) => {\r\n            if (prevWord) {\r\n                if (prevWord.whitespace) {\r\n                    word.el.classList.add('pre-whitespace');\r\n                }\r\n            }\r\n            if (word.whitespace) {\r\n                word.el.classList.add('has-whitespace');\r\n            }\r\n            prevWord = word;\r\n        });\r\n    }\r\n    /**\r\n     * Append split words to the container\r\n     */\r\n    _appendWords() {\r\n        this._words.forEach((word) => {\r\n            this.container.appendChild(word.el);\r\n            if (word.whitespace) {\r\n                this.container.appendChild(word.whitespace);\r\n            }\r\n            if (word.br) {\r\n                this.container.appendChild(word.br);\r\n            }\r\n        });\r\n    }\r\n    /**\r\n     * Remove split words from DOM\r\n     */\r\n    _removeWords() {\r\n        this._words.forEach((word) => {\r\n            word.el.remove();\r\n            if (word.whitespace) {\r\n                word.whitespace.remove();\r\n            }\r\n            if (word.br) {\r\n                word.br.remove();\r\n            }\r\n        });\r\n    }\r\n    /**\r\n     * Split the text into letters\r\n     */\r\n    _splitIntoLetters() {\r\n        // check if need to have letters\r\n        if (!this.prop.appendLetters) {\r\n            return;\r\n        }\r\n        // create letters\r\n        this._words.forEach((word) => {\r\n            const chars = word.content.split('');\r\n            const wordLetters = [];\r\n            chars.forEach((char) => {\r\n                const letter = {\r\n                    el: (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.createElement)('span', {\r\n                        class: `${this.prefix}__letter`,\r\n                        html: char,\r\n                        attr: [\r\n                            ['aria-hidden', 'true'],\r\n                        ],\r\n                    }),\r\n                    content: char,\r\n                    word,\r\n                };\r\n                letter.el.style.display = 'inline-block';\r\n                this._letters.push(letter);\r\n                wordLetters.push(letter);\r\n            });\r\n            word.letters = wordLetters;\r\n        });\r\n        // append letters\r\n        this._letters.forEach((letter) => {\r\n            letter.word.el.appendChild(letter.el);\r\n        });\r\n    }\r\n    /**\r\n     * Split the text into lines\r\n     */\r\n    _splitIntoLines() {\r\n        // first of all, remove all previous lines\r\n        this._removeLines();\r\n        // create lines\r\n        let currentLine = false;\r\n        let prevOffsetTop = Infinity;\r\n        let prevWord = false;\r\n        this.words.forEach((word) => {\r\n            // check if need to create a new line\r\n            let isNewLine = false;\r\n            const top = word.el.offsetTop;\r\n            // check if the previous word contains BR\r\n            if (!!prevWord && !!prevWord.br) {\r\n                isNewLine = true;\r\n            }\r\n            else {\r\n                // otherwise check offset\r\n                isNewLine = top !== prevOffsetTop;\r\n            }\r\n            // update vars\r\n            prevWord = word;\r\n            prevOffsetTop = top;\r\n            // create new line\r\n            if (isNewLine) {\r\n                currentLine = {\r\n                    el: (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.createElement)('span', {\r\n                        class: `${this.prefix}__line`,\r\n                        attr: [\r\n                            ['aria-hidden', 'true'],\r\n                        ],\r\n                    }),\r\n                    content: '',\r\n                    words: [],\r\n                };\r\n                currentLine.el.style.display = 'block';\r\n                this._lines.push(currentLine);\r\n            }\r\n            // append words\r\n            if (currentLine) {\r\n                currentLine.words.push(word);\r\n            }\r\n        });\r\n        // update lines content\r\n        this._lines.forEach((line) => {\r\n            line.content = line.words.map((word) => word.content).join(' ');\r\n        });\r\n        // append lines\r\n        this._lines.forEach((line) => {\r\n            line.words.forEach((word) => {\r\n                line.el.appendChild(word.el);\r\n                if (word.br) {\r\n                    word.br.remove();\r\n                }\r\n                if (word.whitespace) {\r\n                    line.el.appendChild(word.whitespace);\r\n                }\r\n            });\r\n            this.container.appendChild(line.el);\r\n        });\r\n    }\r\n    /**\r\n     * Remove all lines\r\n     */\r\n    _removeLines() {\r\n        // remove lines\r\n        this._lines.forEach((line) => {\r\n            line.el.remove();\r\n        });\r\n        this._lines = [];\r\n        // and append words\r\n        this._appendWords();\r\n    }\r\n    /**\r\n     * Destroy the module\r\n     */\r\n    _destroy() {\r\n        super._destroy();\r\n        this._lines = [];\r\n        this._words = [];\r\n        this._letters = [];\r\n        this._container.innerHTML = this._initHTML;\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/components/text/SplitText.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/components/timeline/StaticTimeline.js":
/*!***************************************************************************!*\
  !*** ./node_modules/vevet/build/es/components/timeline/StaticTimeline.js ***!
  \***************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   StaticTimeline: () => (/* binding */ StaticTimeline)\n/* harmony export */ });\n/* harmony import */ var easing_progress__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! easing-progress */ \"./node_modules/easing-progress/dist/es/index.js\");\n/* harmony import */ var _base_Component__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../base/Component */ \"./node_modules/vevet/build/es/base/Component.js\");\n/* harmony import */ var _utils_math_scoped__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../../utils/math/scoped */ \"./node_modules/vevet/build/es/utils/math/scoped.js\");\n/* harmony import */ var _utils_math_clamp__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../utils/math/clamp */ \"./node_modules/vevet/build/es/utils/math/clamp.js\");\n\r\n\r\n\r\n\r\n/**\r\n * StaticTimeline is the base class for Timeline itself.\r\n * The difference between the coponents is that StaticTimeline has no animation:\r\n * no play & pause methods.\r\n */\r\nclass StaticTimeline extends _base_Component__WEBPACK_IMPORTED_MODULE_1__.Component {\r\n    constructor(initialProp, init = true) {\r\n        super(initialProp, false);\r\n        // set default vars\r\n        this._progress = 0;\r\n        this._easing = 0;\r\n        this._nestedTimelines = [];\r\n        if (init) {\r\n            this.init();\r\n        }\r\n    }\r\n    /**\r\n     * Get default properties\r\n     */\r\n    _getDefaultProp() {\r\n        return Object.assign(Object.assign({}, super._getDefaultProp()), { easing: this._app.prop.easing, scope: [0, 1], useNestedEasingProgress: false });\r\n    }\r\n    /**\r\n     * Absolute progress of the timeline\r\n     */\r\n    get progress() {\r\n        return this._progress;\r\n    }\r\n    set progress(val) {\r\n        this._progress = val;\r\n        this._handleProgress();\r\n    }\r\n    /**\r\n     * Easing progress of the timeline\r\n     */\r\n    get easing() {\r\n        return this._easing;\r\n    }\r\n    /**\r\n     * Add a nested timeline\r\n     */\r\n    addNestedTimeline(tm) {\r\n        // add the timeline to the stack\r\n        this._nestedTimelines.push(tm);\r\n    }\r\n    /**\r\n     * Handle progress event\r\n     */\r\n    _handleProgress() {\r\n        // calculate easing progress\r\n        this._easing = (0,easing_progress__WEBPACK_IMPORTED_MODULE_0__[\"default\"])(this._progress, this.prop.easing);\r\n        // launch progress events\r\n        this._callbacks.tbt('progress', {\r\n            progress: this._progress,\r\n            easing: this._easing,\r\n        });\r\n        // render\r\n        this._renderNestedTimelines();\r\n    }\r\n    /**\r\n     * Render nested timelines\r\n     */\r\n    _renderNestedTimelines() {\r\n        const { length } = this._nestedTimelines;\r\n        if (length === 0) {\r\n            return;\r\n        }\r\n        // vars\r\n        const progressForNested = this.prop.useNestedEasingProgress ? this.easing : this.progress;\r\n        // render nested timelines\r\n        for (let index = 0, l = length; index < l; index += 1) {\r\n            const tm = this._nestedTimelines[index];\r\n            // calculate progress of this very timeline\r\n            const tmProgress = (0,_utils_math_clamp__WEBPACK_IMPORTED_MODULE_2__[\"default\"])((0,_utils_math_scoped__WEBPACK_IMPORTED_MODULE_3__[\"default\"])(progressForNested, tm.prop.nestedScope), [0, 1]);\r\n            tm.progress = tmProgress;\r\n        }\r\n    }\r\n    /**\r\n     * Destroy the animation frame\r\n     */\r\n    _destroy() {\r\n        super._destroy();\r\n        // destroy nested timelines\r\n        this._nestedTimelines.forEach((tm) => {\r\n            tm.destroy();\r\n        });\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/components/timeline/StaticTimeline.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/components/timeline/Timeline.js":
/*!*********************************************************************!*\
  !*** ./node_modules/vevet/build/es/components/timeline/Timeline.js ***!
  \*********************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   Timeline: () => (/* binding */ Timeline)\n/* harmony export */ });\n/* harmony import */ var _StaticTimeline__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./StaticTimeline */ \"./node_modules/vevet/build/es/components/timeline/StaticTimeline.js\");\n/* harmony import */ var _utils_math_clamp__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../utils/math/clamp */ \"./node_modules/vevet/build/es/utils/math/clamp.js\");\n\r\n\r\n/**\r\n * Timeline is an animation helper.\r\n */\r\nclass Timeline extends _StaticTimeline__WEBPACK_IMPORTED_MODULE_0__.StaticTimeline {\r\n    constructor(initialProp, init = true) {\r\n        super(initialProp, false);\r\n        // set default variables\r\n        this._isPlaying = false;\r\n        this._isReversed = false;\r\n        this._isPaused = false;\r\n        this._animationFrame = undefined;\r\n        this._animationFrameLastTime = 0;\r\n        if (init) {\r\n            this.init();\r\n        }\r\n    }\r\n    /**\r\n     * Get default properties\r\n     */\r\n    _getDefaultProp() {\r\n        return Object.assign(Object.assign({}, super._getDefaultProp()), { duration: 1000, reset: false, destroyOnEnd: false });\r\n    }\r\n    /**\r\n     * Check if timeline is playing\r\n     */\r\n    get isPlaying() {\r\n        return typeof this._animationFrame !== 'undefined';\r\n    }\r\n    /**\r\n     * Check if timeline is reversed\r\n     */\r\n    get isReversed() {\r\n        return this._isReversed;\r\n    }\r\n    /**\r\n     * Check if timeline is paused\r\n     */\r\n    get isPaused() {\r\n        return this._isPaused;\r\n    }\r\n    /**\r\n     * Play the timeline\r\n     */\r\n    play() {\r\n        if (this._destroyed) {\r\n            return;\r\n        }\r\n        if (this.prop.reset && this.progress === 1) {\r\n            this._progress = 0;\r\n        }\r\n        this._isReversed = false;\r\n        this._isPaused = false;\r\n        if (!this.isPlaying) {\r\n            this._animationFrameLastTime = +new Date();\r\n            this._animate();\r\n        }\r\n    }\r\n    /**\r\n     * Play reversed timeline\r\n     */\r\n    reverse() {\r\n        if (this._destroyed) {\r\n            return;\r\n        }\r\n        if (this.prop.reset && this.progress === 0) {\r\n            this._progress = 1;\r\n        }\r\n        this._isReversed = true;\r\n        this._isPaused = false;\r\n        if (!this.isPlaying) {\r\n            this._animationFrameLastTime = +new Date();\r\n            this._animate();\r\n        }\r\n    }\r\n    /**\r\n     * Pause animation\r\n     */\r\n    pause() {\r\n        if (this._destroyed) {\r\n            return;\r\n        }\r\n        this._isPaused = true;\r\n        if (this._animationFrame) {\r\n            window.cancelAnimationFrame(this._animationFrame);\r\n        }\r\n        this._animationFrame = undefined;\r\n    }\r\n    /**\r\n     * Start animation\r\n     */\r\n    _animate() {\r\n        // stop if animation is paused\r\n        if (this._isPaused) {\r\n            return;\r\n        }\r\n        const { isReversed } = this;\r\n        // calculate difference between frames\r\n        const currentTime = +new Date();\r\n        const frameDiff = Math.abs(this._animationFrameLastTime - currentTime);\r\n        this._animationFrameLastTime = currentTime;\r\n        // calculate current progress\r\n        const progressIterator = frameDiff / this.prop.duration / (isReversed ? -1 : 1);\r\n        const progressTarget = (0,_utils_math_clamp__WEBPACK_IMPORTED_MODULE_1__[\"default\"])(this.progress + progressIterator, [0, 1]);\r\n        this.progress = progressTarget;\r\n        // end animation\r\n        if ((progressTarget === 1 && !isReversed)\r\n            || (progressTarget === 0 && isReversed)) {\r\n            this._isReversed = false;\r\n            this._isPaused = false;\r\n            this._animationFrame = undefined;\r\n            return;\r\n        }\r\n        // continue animation\r\n        this._animationFrame = window.requestAnimationFrame(this._animate.bind(this));\r\n    }\r\n    /**\r\n     * Events on progress\r\n     */\r\n    _handleProgress() {\r\n        super._handleProgress();\r\n        const progress = this._progress;\r\n        // launch callbacks\r\n        if (progress === 0) {\r\n            this.callbacks.tbt('start', false);\r\n        }\r\n        else if (progress === 1) {\r\n            this.callbacks.tbt('end', false);\r\n            if (this.prop.destroyOnEnd) {\r\n                this.destroy();\r\n            }\r\n        }\r\n    }\r\n    /**\r\n     * Destroy the timeline\r\n     */\r\n    _destroy() {\r\n        this.pause();\r\n        super._destroy();\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/components/timeline/Timeline.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/handlers/wheel/WheelHandler.js":
/*!********************************************************************!*\
  !*** ./node_modules/vevet/build/es/handlers/wheel/WheelHandler.js ***!
  \********************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   WheelHandler: () => (/* binding */ WheelHandler)\n/* harmony export */ });\n/* harmony import */ var vevet_dom__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! vevet-dom */ \"./node_modules/vevet-dom/dist/es/index.js\");\n/* harmony import */ var normalize_wheel__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! normalize-wheel */ \"./node_modules/normalize-wheel/index.js\");\n/* harmony import */ var normalize_wheel__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(normalize_wheel__WEBPACK_IMPORTED_MODULE_1__);\n/* harmony import */ var _base_Module__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../base/Module */ \"./node_modules/vevet/build/es/base/Module.js\");\n\r\n\r\n\r\n/**\r\n * Wheel events: up & down, left & right without repeating\r\n\r\n */\r\nclass WheelHandler extends _base_Module__WEBPACK_IMPORTED_MODULE_2__.Module {\r\n    constructor(initialProp, init = true) {\r\n        super(initialProp, false);\r\n        this._wheelListener = undefined;\r\n        this._lockWheel = false;\r\n        if (init) {\r\n            this.init();\r\n        }\r\n    }\r\n    _getDefaultProp() {\r\n        return Object.assign(Object.assign({}, super._getDefaultProp()), { container: false, enabled: true, stopPropagation: false, threshold: 20 });\r\n    }\r\n    /**\r\n     * Listener contianer\r\n     */\r\n    get container() {\r\n        if (!this.prop.container) {\r\n            return window;\r\n        }\r\n        return this.prop.container;\r\n    }\r\n    // Set Module Events\r\n    _setEvents() {\r\n        super._setEvents();\r\n        if (!(this.container instanceof Window)) {\r\n            this._listeners.push((0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.addEventListener)(this.container, 'mouseenter', this._handleMouseEnter.bind(this)));\r\n            this._listeners.push((0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.addEventListener)(this.container, 'mouseleave', this._handleMouseLeave.bind(this)));\r\n        }\r\n        else {\r\n            this._toggleWheelEvent();\r\n        }\r\n    }\r\n    _onPropMutate() {\r\n        super._onPropMutate();\r\n        this._toggleWheelEvent();\r\n    }\r\n    /**\r\n     * Handle element mouse enter\r\n     */\r\n    _handleMouseEnter() {\r\n        if (this.prop.enabled) {\r\n            this._createWheelEvent();\r\n        }\r\n    }\r\n    /**\r\n     * Handle element mouse leave\r\n     */\r\n    _handleMouseLeave() {\r\n        this._destroyWheelEvent();\r\n    }\r\n    /**\r\n     * Set wheel event\r\n     */\r\n    _toggleWheelEvent() {\r\n        if (this.prop.enabled) {\r\n            this._createWheelEvent();\r\n        }\r\n        else {\r\n            this._destroyWheelEvent();\r\n        }\r\n    }\r\n    /**\r\n     * Create a wheel event\r\n     */\r\n    _createWheelEvent() {\r\n        if (this._wheelListener) {\r\n            return;\r\n        }\r\n        // reset gaining\r\n        this._lockWheel = false;\r\n        if (this._lockWheelTimeout) {\r\n            clearTimeout(this._lockWheelTimeout);\r\n        }\r\n        // create a listener\r\n        this._wheelListener = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.addEventListener)(this.container, 'wheel', \r\n        // throttle(this._handleWheel.bind(this), 200),\r\n        this._handleWheel.bind(this));\r\n    }\r\n    /**\r\n     * Destroy the wheel event\r\n     */\r\n    _destroyWheelEvent() {\r\n        if (!this._wheelListener) {\r\n            return;\r\n        }\r\n        this._wheelListener.remove();\r\n        this._wheelListener = undefined;\r\n    }\r\n    /**\r\n     * Handle wheel event\r\n     */\r\n    _handleWheel(evt) {\r\n        if (!this._wheelListener) {\r\n            return;\r\n        }\r\n        // stop propagation\r\n        if (this.prop.stopPropagation) {\r\n            evt.preventDefault();\r\n            evt.stopPropagation();\r\n        }\r\n        // if locked\r\n        if (this._lockWheel) {\r\n            if (this._lockWheelTimeout) {\r\n                clearTimeout(this._lockWheelTimeout);\r\n            }\r\n            this._lockWheelTimeout = setTimeout(() => {\r\n                this._lockWheel = false;\r\n            }, 300);\r\n            return;\r\n        }\r\n        // data\r\n        const delta = normalize_wheel__WEBPACK_IMPORTED_MODULE_1___default()(evt);\r\n        const { threshold } = this.prop;\r\n        // launch events\r\n        let eventFired = false;\r\n        // Y\r\n        if (delta.pixelY > threshold) {\r\n            this._callbacks.tbt('down', false);\r\n            eventFired = true;\r\n        }\r\n        else if (delta.pixelY < threshold * -1) {\r\n            this._callbacks.tbt('up', false);\r\n            eventFired = true;\r\n        }\r\n        // X\r\n        if (delta.pixelX > threshold) {\r\n            this._callbacks.tbt('right', false);\r\n            eventFired = true;\r\n        }\r\n        else if (delta.pixelX < threshold * -1) {\r\n            this._callbacks.tbt('left', false);\r\n            eventFired = true;\r\n        }\r\n        // lock events\r\n        if (eventFired) {\r\n            this._lockWheel = true;\r\n        }\r\n    }\r\n    /**\r\n     * Destroy the module\r\n     */\r\n    _destroy() {\r\n        super._destroy();\r\n        this._destroyWheelEvent();\r\n    }\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/handlers/wheel/WheelHandler.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/index.js":
/*!**********************************************!*\
  !*** ./node_modules/vevet/build/es/index.js ***!
  \**********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   AnimationFrame: () => (/* reexport safe */ _components_animation_frame_AnimationFrame__WEBPACK_IMPORTED_MODULE_16__.AnimationFrame),\n/* harmony export */   Application: () => (/* reexport safe */ _app_Application__WEBPACK_IMPORTED_MODULE_6__.Application),\n/* harmony export */   Callbacks: () => (/* reexport safe */ _base_Callbacks__WEBPACK_IMPORTED_MODULE_9__.Callbacks),\n/* harmony export */   Component: () => (/* reexport safe */ _base_Component__WEBPACK_IMPORTED_MODULE_12__.Component),\n/* harmony export */   Ctx2D: () => (/* reexport safe */ _components_canvas_Ctx2D__WEBPACK_IMPORTED_MODULE_24__.Ctx2D),\n/* harmony export */   Ctx2DPrerender: () => (/* reexport safe */ _components_canvas_Ctx2DPrerender__WEBPACK_IMPORTED_MODULE_25__.Ctx2DPrerender),\n/* harmony export */   CustomCursor: () => (/* reexport safe */ _components_cursor_CustomCursor__WEBPACK_IMPORTED_MODULE_34__.CustomCursor),\n/* harmony export */   Dragger: () => (/* reexport safe */ _components_dragger_Dragger__WEBPACK_IMPORTED_MODULE_21__.Dragger),\n/* harmony export */   DraggerDirection: () => (/* reexport safe */ _components_dragger_DraggerDirection__WEBPACK_IMPORTED_MODULE_23__.DraggerDirection),\n/* harmony export */   DraggerMove: () => (/* reexport safe */ _components_dragger_DraggerMove__WEBPACK_IMPORTED_MODULE_22__.DraggerMove),\n/* harmony export */   GeneralTypes: () => (/* reexport module object */ _utils_types_general__WEBPACK_IMPORTED_MODULE_5__),\n/* harmony export */   Module: () => (/* reexport safe */ _base_Module__WEBPACK_IMPORTED_MODULE_11__.Module),\n/* harmony export */   MutableProp: () => (/* reexport safe */ _base_MutableProp__WEBPACK_IMPORTED_MODULE_10__.MutableProp),\n/* harmony export */   Page: () => (/* reexport safe */ _components_page_Page__WEBPACK_IMPORTED_MODULE_14__.Page),\n/* harmony export */   PageLoad: () => (/* reexport safe */ _app_events_PageLoad__WEBPACK_IMPORTED_MODULE_8__.PageLoad),\n/* harmony export */   Plugin: () => (/* reexport safe */ _base_Plugin__WEBPACK_IMPORTED_MODULE_13__.Plugin),\n/* harmony export */   Preloader: () => (/* reexport safe */ _components_loading_Preloader__WEBPACK_IMPORTED_MODULE_19__.Preloader),\n/* harmony export */   ProgressPreloader: () => (/* reexport safe */ _components_loading_ProgressPreloader__WEBPACK_IMPORTED_MODULE_20__.ProgressPreloader),\n/* harmony export */   ScrollBar: () => (/* reexport safe */ _components_scroll_scrollbar_ScrollBar__WEBPACK_IMPORTED_MODULE_27__.ScrollBar),\n/* harmony export */   ScrollEventsBase: () => (/* reexport safe */ _components_scroll_scrollable_ScrollEventsBase__WEBPACK_IMPORTED_MODULE_28__.ScrollEventsBase),\n/* harmony export */   ScrollSectionProgress: () => (/* reexport safe */ _components_scroll_section_ScrollSectionProgress__WEBPACK_IMPORTED_MODULE_32__.ScrollSectionProgress),\n/* harmony export */   ScrollView: () => (/* reexport safe */ _components_scroll_scrollable_ScrollView__WEBPACK_IMPORTED_MODULE_29__.ScrollView),\n/* harmony export */   SmoothScroll: () => (/* reexport safe */ _components_scroll_smooth_scroll_SmoothScroll__WEBPACK_IMPORTED_MODULE_26__.SmoothScroll),\n/* harmony export */   SmoothScrollDragPlugin: () => (/* reexport safe */ _components_scroll_plugins_SmoothScrollDragPlugin__WEBPACK_IMPORTED_MODULE_31__.SmoothScrollDragPlugin),\n/* harmony export */   SmoothScrollKeyboardPlugin: () => (/* reexport safe */ _components_scroll_plugins_SmoothScrollKeyboardPlugin__WEBPACK_IMPORTED_MODULE_30__.SmoothScrollKeyboardPlugin),\n/* harmony export */   SplitText: () => (/* reexport safe */ _components_text_SplitText__WEBPACK_IMPORTED_MODULE_33__.SplitText),\n/* harmony export */   StaticTimeline: () => (/* reexport safe */ _components_timeline_StaticTimeline__WEBPACK_IMPORTED_MODULE_17__.StaticTimeline),\n/* harmony export */   Timeline: () => (/* reexport safe */ _components_timeline_Timeline__WEBPACK_IMPORTED_MODULE_18__.Timeline),\n/* harmony export */   Viewport: () => (/* reexport safe */ _app_events_Viewport__WEBPACK_IMPORTED_MODULE_7__.Viewport),\n/* harmony export */   WheelHandler: () => (/* reexport safe */ _handlers_wheel_WheelHandler__WEBPACK_IMPORTED_MODULE_15__.WheelHandler),\n/* harmony export */   utils: () => (/* binding */ utils)\n/* harmony export */ });\n/* harmony import */ var _utils_common__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./utils/common */ \"./node_modules/vevet/build/es/utils/common/index.js\");\n/* harmony import */ var _utils_listeners__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./utils/listeners */ \"./node_modules/vevet/build/es/utils/listeners/index.js\");\n/* harmony import */ var _utils_math__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./utils/math */ \"./node_modules/vevet/build/es/utils/math/index.js\");\n/* harmony import */ var _utils_scroll__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./utils/scroll */ \"./node_modules/vevet/build/es/utils/scroll/index.js\");\n/* harmony import */ var _utils_image__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./utils/image */ \"./node_modules/vevet/build/es/utils/image/index.js\");\n/* harmony import */ var _utils_types_general__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./utils/types/general */ \"./node_modules/vevet/build/es/utils/types/general.js\");\n/* harmony import */ var _app_Application__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./app/Application */ \"./node_modules/vevet/build/es/app/Application.js\");\n/* harmony import */ var _app_events_Viewport__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./app/events/Viewport */ \"./node_modules/vevet/build/es/app/events/Viewport.js\");\n/* harmony import */ var _app_events_PageLoad__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./app/events/PageLoad */ \"./node_modules/vevet/build/es/app/events/PageLoad.js\");\n/* harmony import */ var _base_Callbacks__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./base/Callbacks */ \"./node_modules/vevet/build/es/base/Callbacks.js\");\n/* harmony import */ var _base_MutableProp__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ./base/MutableProp */ \"./node_modules/vevet/build/es/base/MutableProp.js\");\n/* harmony import */ var _base_Module__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ./base/Module */ \"./node_modules/vevet/build/es/base/Module.js\");\n/* harmony import */ var _base_Component__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ./base/Component */ \"./node_modules/vevet/build/es/base/Component.js\");\n/* harmony import */ var _base_Plugin__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! ./base/Plugin */ \"./node_modules/vevet/build/es/base/Plugin.js\");\n/* harmony import */ var _components_page_Page__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! ./components/page/Page */ \"./node_modules/vevet/build/es/components/page/Page.js\");\n/* harmony import */ var _handlers_wheel_WheelHandler__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! ./handlers/wheel/WheelHandler */ \"./node_modules/vevet/build/es/handlers/wheel/WheelHandler.js\");\n/* harmony import */ var _components_animation_frame_AnimationFrame__WEBPACK_IMPORTED_MODULE_16__ = __webpack_require__(/*! ./components/animation-frame/AnimationFrame */ \"./node_modules/vevet/build/es/components/animation-frame/AnimationFrame.js\");\n/* harmony import */ var _components_timeline_StaticTimeline__WEBPACK_IMPORTED_MODULE_17__ = __webpack_require__(/*! ./components/timeline/StaticTimeline */ \"./node_modules/vevet/build/es/components/timeline/StaticTimeline.js\");\n/* harmony import */ var _components_timeline_Timeline__WEBPACK_IMPORTED_MODULE_18__ = __webpack_require__(/*! ./components/timeline/Timeline */ \"./node_modules/vevet/build/es/components/timeline/Timeline.js\");\n/* harmony import */ var _components_loading_Preloader__WEBPACK_IMPORTED_MODULE_19__ = __webpack_require__(/*! ./components/loading/Preloader */ \"./node_modules/vevet/build/es/components/loading/Preloader.js\");\n/* harmony import */ var _components_loading_ProgressPreloader__WEBPACK_IMPORTED_MODULE_20__ = __webpack_require__(/*! ./components/loading/ProgressPreloader */ \"./node_modules/vevet/build/es/components/loading/ProgressPreloader.js\");\n/* harmony import */ var _components_dragger_Dragger__WEBPACK_IMPORTED_MODULE_21__ = __webpack_require__(/*! ./components/dragger/Dragger */ \"./node_modules/vevet/build/es/components/dragger/Dragger.js\");\n/* harmony import */ var _components_dragger_DraggerMove__WEBPACK_IMPORTED_MODULE_22__ = __webpack_require__(/*! ./components/dragger/DraggerMove */ \"./node_modules/vevet/build/es/components/dragger/DraggerMove.js\");\n/* harmony import */ var _components_dragger_DraggerDirection__WEBPACK_IMPORTED_MODULE_23__ = __webpack_require__(/*! ./components/dragger/DraggerDirection */ \"./node_modules/vevet/build/es/components/dragger/DraggerDirection.js\");\n/* harmony import */ var _components_canvas_Ctx2D__WEBPACK_IMPORTED_MODULE_24__ = __webpack_require__(/*! ./components/canvas/Ctx2D */ \"./node_modules/vevet/build/es/components/canvas/Ctx2D.js\");\n/* harmony import */ var _components_canvas_Ctx2DPrerender__WEBPACK_IMPORTED_MODULE_25__ = __webpack_require__(/*! ./components/canvas/Ctx2DPrerender */ \"./node_modules/vevet/build/es/components/canvas/Ctx2DPrerender.js\");\n/* harmony import */ var _components_scroll_smooth_scroll_SmoothScroll__WEBPACK_IMPORTED_MODULE_26__ = __webpack_require__(/*! ./components/scroll/smooth-scroll/SmoothScroll */ \"./node_modules/vevet/build/es/components/scroll/smooth-scroll/SmoothScroll.js\");\n/* harmony import */ var _components_scroll_scrollbar_ScrollBar__WEBPACK_IMPORTED_MODULE_27__ = __webpack_require__(/*! ./components/scroll/scrollbar/ScrollBar */ \"./node_modules/vevet/build/es/components/scroll/scrollbar/ScrollBar.js\");\n/* harmony import */ var _components_scroll_scrollable_ScrollEventsBase__WEBPACK_IMPORTED_MODULE_28__ = __webpack_require__(/*! ./components/scroll/scrollable/ScrollEventsBase */ \"./node_modules/vevet/build/es/components/scroll/scrollable/ScrollEventsBase.js\");\n/* harmony import */ var _components_scroll_scrollable_ScrollView__WEBPACK_IMPORTED_MODULE_29__ = __webpack_require__(/*! ./components/scroll/scrollable/ScrollView */ \"./node_modules/vevet/build/es/components/scroll/scrollable/ScrollView.js\");\n/* harmony import */ var _components_scroll_plugins_SmoothScrollKeyboardPlugin__WEBPACK_IMPORTED_MODULE_30__ = __webpack_require__(/*! ./components/scroll/plugins/SmoothScrollKeyboardPlugin */ \"./node_modules/vevet/build/es/components/scroll/plugins/SmoothScrollKeyboardPlugin.js\");\n/* harmony import */ var _components_scroll_plugins_SmoothScrollDragPlugin__WEBPACK_IMPORTED_MODULE_31__ = __webpack_require__(/*! ./components/scroll/plugins/SmoothScrollDragPlugin */ \"./node_modules/vevet/build/es/components/scroll/plugins/SmoothScrollDragPlugin.js\");\n/* harmony import */ var _components_scroll_section_ScrollSectionProgress__WEBPACK_IMPORTED_MODULE_32__ = __webpack_require__(/*! ./components/scroll/section/ScrollSectionProgress */ \"./node_modules/vevet/build/es/components/scroll/section/ScrollSectionProgress.js\");\n/* harmony import */ var _components_text_SplitText__WEBPACK_IMPORTED_MODULE_33__ = __webpack_require__(/*! ./components/text/SplitText */ \"./node_modules/vevet/build/es/components/text/SplitText.js\");\n/* harmony import */ var _components_cursor_CustomCursor__WEBPACK_IMPORTED_MODULE_34__ = __webpack_require__(/*! ./components/cursor/CustomCursor */ \"./node_modules/vevet/build/es/components/cursor/CustomCursor.js\");\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\nconst utils = {\r\n    common: _utils_common__WEBPACK_IMPORTED_MODULE_0__,\r\n    listeners: _utils_listeners__WEBPACK_IMPORTED_MODULE_1__,\r\n    math: _utils_math__WEBPACK_IMPORTED_MODULE_2__,\r\n    scroll: _utils_scroll__WEBPACK_IMPORTED_MODULE_3__,\r\n    image: _utils_image__WEBPACK_IMPORTED_MODULE_4__,\r\n};\r\n\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/index.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/common/id.js":
/*!********************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/common/id.js ***!
  \********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ id)\n/* harmony export */ });\nlet index = 0;\r\n/**\r\n * Generate an ID\r\n */\r\nfunction id(prefix = 'id') {\r\n    index += 1;\r\n    return `${prefix}_${index}`;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/common/id.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/common/index.js":
/*!***********************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/common/index.js ***!
  \***********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   id: () => (/* reexport safe */ _id__WEBPACK_IMPORTED_MODULE_0__[\"default\"]),\n/* harmony export */   mergeWithoutArrays: () => (/* reexport safe */ _mergeWithoutArrays__WEBPACK_IMPORTED_MODULE_1__[\"default\"]),\n/* harmony export */   timeoutCallback: () => (/* reexport safe */ _timeoutCallback__WEBPACK_IMPORTED_MODULE_2__[\"default\"])\n/* harmony export */ });\n/* harmony import */ var _id__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./id */ \"./node_modules/vevet/build/es/utils/common/id.js\");\n/* harmony import */ var _mergeWithoutArrays__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./mergeWithoutArrays */ \"./node_modules/vevet/build/es/utils/common/mergeWithoutArrays.js\");\n/* harmony import */ var _timeoutCallback__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./timeoutCallback */ \"./node_modules/vevet/build/es/utils/common/timeoutCallback.js\");\n\r\n\r\n\r\n\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/common/index.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/common/mergeWithoutArrays.js":
/*!************************************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/common/mergeWithoutArrays.js ***!
  \************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ mergeWithoutArrays)\n/* harmony export */ });\n/* harmony import */ var lodash_mergewith__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! lodash.mergewith */ \"./node_modules/lodash.mergewith/index.js\");\n/* harmony import */ var lodash_mergewith__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(lodash_mergewith__WEBPACK_IMPORTED_MODULE_0__);\n\r\n/**\r\n * Deep merge of two objects except for arrays.\r\n * The function doesn't change the \"object\" and \"source\".\r\n */\r\nfunction mergeWithoutArrays(object, source) {\r\n    return lodash_mergewith__WEBPACK_IMPORTED_MODULE_0___default()(object, source, (objValue, srcValue) => {\r\n        if (Array.isArray(objValue)) {\r\n            return srcValue;\r\n        }\r\n        return undefined;\r\n    });\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/common/mergeWithoutArrays.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/common/timeoutCallback.js":
/*!*********************************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/common/timeoutCallback.js ***!
  \*********************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ timeoutCallback)\n/* harmony export */ });\n/**\r\n * Launch a function in a certain amount of time\r\n * If the timeout argument is zero, the callback will be launched synchronously\r\n */\r\nfunction timeoutCallback(callback, delay) {\r\n    let timeout;\r\n    if (delay === 0) {\r\n        callback();\r\n    }\r\n    else {\r\n        timeout = setTimeout(() => {\r\n            callback();\r\n        }, delay);\r\n    }\r\n    return {\r\n        clear: () => {\r\n            if (timeout) {\r\n                clearTimeout(timeout);\r\n            }\r\n        },\r\n    };\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/common/timeoutCallback.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/errors.js":
/*!*****************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/errors.js ***!
  \*****************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   throwVevetAppError: () => (/* binding */ throwVevetAppError),\n/* harmony export */   vevetAppErrorText: () => (/* binding */ vevetAppErrorText)\n/* harmony export */ });\nconst vevetAppErrorText = `Vevet.Application does not exist yet. \r\nCall \"new Vevet.Application()\" before using all the stuff`;\r\nfunction throwVevetAppError() {\r\n    throw new Error(vevetAppErrorText);\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/errors.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/image/index.js":
/*!**********************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/image/index.js ***!
  \**********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   load: () => (/* reexport safe */ _load__WEBPACK_IMPORTED_MODULE_0__[\"default\"]),\n/* harmony export */   pathsToProps: () => (/* reexport safe */ _pathsToProps__WEBPACK_IMPORTED_MODULE_2__[\"default\"]),\n/* harmony export */   sizesToSrcSet: () => (/* reexport safe */ _sizesToSrcSet__WEBPACK_IMPORTED_MODULE_1__[\"default\"])\n/* harmony export */ });\n/* harmony import */ var _load__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./load */ \"./node_modules/vevet/build/es/utils/image/load.js\");\n/* harmony import */ var _sizesToSrcSet__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./sizesToSrcSet */ \"./node_modules/vevet/build/es/utils/image/sizesToSrcSet.js\");\n/* harmony import */ var _pathsToProps__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./pathsToProps */ \"./node_modules/vevet/build/es/utils/image/pathsToProps.js\");\n\r\n\r\n\r\n\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/image/index.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/image/load.js":
/*!*********************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/image/load.js ***!
  \*********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ loadImage)\n/* harmony export */ });\n/* harmony import */ var p_cancelable__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! p-cancelable */ \"./node_modules/p-cancelable/index.js\");\n\r\nconst cachedImages = [];\r\n/**\r\n * Load an image\r\n */\r\nfunction loadImage(source, props) {\r\n    const defaultLoadProps = {\r\n        crossOrigin: null,\r\n        useCache: false,\r\n    };\r\n    const loadProps = Object.assign(Object.assign({}, defaultLoadProps), props);\r\n    const imageSrc = typeof source === 'string' ? source : source.src;\r\n    return new p_cancelable__WEBPACK_IMPORTED_MODULE_0__[\"default\"]((resolve, reject) => {\r\n        const cachedImage = loadProps.useCache && cachedImages.find((img) => img.src === imageSrc);\r\n        // get image from cache\r\n        if (cachedImage) {\r\n            resolve(cachedImage.image);\r\n            return;\r\n        }\r\n        // load the image by src\r\n        if (typeof source === 'string') {\r\n            const img = new Image();\r\n            img.crossOrigin = loadProps.crossOrigin;\r\n            img.onload = () => {\r\n                if (loadProps.useCache) {\r\n                    cachedImages.push({\r\n                        src: imageSrc,\r\n                        image: img,\r\n                    });\r\n                }\r\n                resolve(img);\r\n            };\r\n            img.onerror = () => {\r\n                reject();\r\n            };\r\n            img.src = source;\r\n            return;\r\n        }\r\n        // load the image by image element\r\n        if (source instanceof HTMLImageElement) {\r\n            if (source.complete) {\r\n                resolve(source);\r\n            }\r\n            else {\r\n                source.addEventListener('load', () => {\r\n                    if (loadProps.useCache) {\r\n                        cachedImages.push({\r\n                            src: imageSrc,\r\n                            image: source,\r\n                        });\r\n                    }\r\n                    resolve(source);\r\n                });\r\n                source.addEventListener('error', () => {\r\n                    reject();\r\n                });\r\n            }\r\n        }\r\n    });\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/image/load.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/image/pathsToProps.js":
/*!*****************************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/image/pathsToProps.js ***!
  \*****************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ imagePathsToProps)\n/* harmony export */ });\n/* harmony import */ var _sizesToSrcSet__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./sizesToSrcSet */ \"./node_modules/vevet/build/es/utils/image/sizesToSrcSet.js\");\n\r\n/**\r\n * Transform image paths to properties\r\n */\r\nfunction imagePathsToProps(data) {\r\n    const app = window.vevetApp;\r\n    // get src\r\n    let src = data.original;\r\n    if (data.thumb) {\r\n        src = data.thumb;\r\n    }\r\n    if (app && app.supportsWebp && !!data.thumbWebp) {\r\n        src = data.thumbWebp;\r\n    }\r\n    // get src set\r\n    let srcSet = '';\r\n    // get webp images\r\n    if (app && app.supportsWebp && 'sizesWebp' in data && !!data.sizesWebp) {\r\n        srcSet = (0,_sizesToSrcSet__WEBPACK_IMPORTED_MODULE_0__[\"default\"])(data.sizesWebp);\r\n    }\r\n    else if ('sizes' in data && !!data.sizes) {\r\n        srcSet = (0,_sizesToSrcSet__WEBPACK_IMPORTED_MODULE_0__[\"default\"])(data.sizes);\r\n    }\r\n    return {\r\n        src,\r\n        srcSet,\r\n    };\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/image/pathsToProps.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/image/sizesToSrcSet.js":
/*!******************************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/image/sizesToSrcSet.js ***!
  \******************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ imageSizesToSrcSet)\n/* harmony export */ });\n/**\r\n * Transform image sizes to src set\r\n */\r\nfunction imageSizesToSrcSet(sizes) {\r\n    const keys = Object.keys(sizes);\r\n    const srcParts = keys.map((key) => {\r\n        const value = sizes[key];\r\n        if (value) {\r\n            return `${value} ${key}w`;\r\n        }\r\n        return undefined;\r\n    });\r\n    return srcParts.filter((item) => !!item).join(', ');\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/image/sizesToSrcSet.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/listeners/index.js":
/*!**************************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/listeners/index.js ***!
  \**************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   intersectionObserverSupported: () => (/* reexport safe */ _intersectionObserverSupported__WEBPACK_IMPORTED_MODULE_1__[\"default\"]),\n/* harmony export */   onScroll: () => (/* reexport safe */ _onScroll__WEBPACK_IMPORTED_MODULE_0__[\"default\"])\n/* harmony export */ });\n/* harmony import */ var _onScroll__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./onScroll */ \"./node_modules/vevet/build/es/utils/listeners/onScroll.js\");\n/* harmony import */ var _intersectionObserverSupported__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./intersectionObserverSupported */ \"./node_modules/vevet/build/es/utils/listeners/intersectionObserverSupported.js\");\n\r\n\r\n\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/listeners/index.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/listeners/intersectionObserverSupported.js":
/*!**************************************************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/listeners/intersectionObserverSupported.js ***!
  \**************************************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ intersectionObserverSupported)\n/* harmony export */ });\nfunction intersectionObserverSupported() {\r\n    if (!('IntersectionObserver' in window)\r\n        || !('IntersectionObserverEntry' in window)\r\n        || !('intersectionRatio' in window.IntersectionObserverEntry.prototype)) {\r\n        return false;\r\n    }\r\n    return true;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/listeners/intersectionObserverSupported.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/listeners/onScroll.js":
/*!*****************************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/listeners/onScroll.js ***!
  \*****************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ onScroll)\n/* harmony export */ });\n/* harmony import */ var vevet_dom__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! vevet-dom */ \"./node_modules/vevet-dom/dist/es/index.js\");\n/* harmony import */ var _components_scroll_smooth_scroll_SmoothScroll__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../components/scroll/smooth-scroll/SmoothScroll */ \"./node_modules/vevet/build/es/components/scroll/smooth-scroll/SmoothScroll.js\");\n/* harmony import */ var _common__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../common */ \"./node_modules/vevet/build/es/utils/common/id.js\");\n\r\n\r\n\r\nlet instances = [];\r\n/**\r\n * Add OnScroll event\r\n */\r\nfunction onScroll({ container, callback, isPassive = false, }) {\r\n    // check if listeners for this element already exist\r\n    let instance = instances.find((data) => (data.container === container && data.isPassive === isPassive));\r\n    const callbackId = (0,_common__WEBPACK_IMPORTED_MODULE_1__[\"default\"])('scroll-event');\r\n    // if a listener exists, we just add a new callback to its stack\r\n    if (instance) {\r\n        instance.callbacks.push({\r\n            id: callbackId,\r\n            callback,\r\n        });\r\n    }\r\n    else {\r\n        // otherwise we create a new instance\r\n        instance = {\r\n            id: (0,_common__WEBPACK_IMPORTED_MODULE_1__[\"default\"])('scroll-event-instance'),\r\n            container,\r\n            callbacks: [{\r\n                    id: callbackId,\r\n                    callback,\r\n                }],\r\n            isPassive,\r\n            listeners: [],\r\n        };\r\n        instances.push(instance);\r\n        // vars\r\n        const { listeners } = instance;\r\n        // smooth scroll events\r\n        if (container instanceof _components_scroll_smooth_scroll_SmoothScroll__WEBPACK_IMPORTED_MODULE_2__.SmoothScroll) {\r\n            listeners.push(container.addCallback('scroll', () => {\r\n                const { scrollTop, scrollLeft } = container;\r\n                for (let index = 0; index < instance.callbacks.length; index += 1) {\r\n                    instance.callbacks[index].callback({\r\n                        scrollTop,\r\n                        scrollLeft,\r\n                    });\r\n                }\r\n            }));\r\n        }\r\n        else {\r\n            // dom scroll events\r\n            const isWindow = container instanceof Window;\r\n            const domContainer = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.selectOne)(container);\r\n            listeners.push((0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.addEventListener)(domContainer, 'scroll', () => {\r\n                const scrollTop = isWindow\r\n                    ? domContainer.pageYOffset : domContainer.scrollTop;\r\n                const scrollLeft = isWindow\r\n                    ? domContainer.pageXOffset : domContainer.scrollLeft;\r\n                for (let index = 0; index < instance.callbacks.length; index += 1) {\r\n                    instance.callbacks[index].callback({\r\n                        scrollTop,\r\n                        scrollLeft,\r\n                    });\r\n                }\r\n            }, {\r\n                passive: isPassive,\r\n            }));\r\n        }\r\n    }\r\n    return {\r\n        remove: () => {\r\n            const newCallbacks = instance.callbacks.filter((item) => {\r\n                if (item.id !== callbackId) {\r\n                    return true;\r\n                }\r\n                return false;\r\n            });\r\n            instance.callbacks = newCallbacks;\r\n            if (newCallbacks.length === 0) {\r\n                instance.listeners.forEach((listener) => {\r\n                    listener.remove();\r\n                });\r\n                instances = instances.filter((item) => item.id !== instance.id);\r\n            }\r\n        },\r\n    };\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/listeners/onScroll.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/math/clamp.js":
/*!*********************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/math/clamp.js ***!
  \*********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ clamp)\n/* harmony export */ });\n/**\r\n * Clamp the value between two points\r\n */\r\nfunction clamp(val, scope = [0, 1]) {\r\n    if (val < scope[0]) {\r\n        return scope[0];\r\n    }\r\n    if (val > scope[1]) {\r\n        return scope[1];\r\n    }\r\n    return val;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/math/clamp.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/math/clampScope.js":
/*!**************************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/math/clampScope.js ***!
  \**************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ clampScoped)\n/* harmony export */ });\n/* harmony import */ var _scoped__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./scoped */ \"./node_modules/vevet/build/es/utils/math/scoped.js\");\n/* harmony import */ var _clamp__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./clamp */ \"./node_modules/vevet/build/es/utils/math/clamp.js\");\n\r\n\r\n/**\r\n * Get progress relatively to the scope and clamp it within two points\r\n */\r\nfunction clampScoped(val, scope = [0, 1], clampScope = [0, 1]) {\r\n    return (0,_clamp__WEBPACK_IMPORTED_MODULE_0__[\"default\"])((0,_scoped__WEBPACK_IMPORTED_MODULE_1__[\"default\"])(val, scope), clampScope);\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/math/clampScope.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/math/easing.js":
/*!**********************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/math/easing.js ***!
  \**********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (__WEBPACK_DEFAULT_EXPORT__)\n/* harmony export */ });\n/* harmony import */ var easing_progress__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! easing-progress */ \"./node_modules/easing-progress/dist/es/index.js\");\n\r\n/**\r\n * Easing progress\r\n */\r\nconst easing = easing_progress__WEBPACK_IMPORTED_MODULE_0__[\"default\"];\r\n/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (easing);\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/math/easing.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/math/inScope.js":
/*!***********************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/math/inScope.js ***!
  \***********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ inScope)\n/* harmony export */ });\n/**\r\n * Check if the value is within the scope\r\n */\r\nfunction inScope(val, scopeValue = [0, 1]) {\r\n    return val >= scopeValue[0] && val <= scopeValue[1];\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/math/inScope.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/math/index.js":
/*!*********************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/math/index.js ***!
  \*********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   clamp: () => (/* reexport safe */ _clamp__WEBPACK_IMPORTED_MODULE_0__[\"default\"]),\n/* harmony export */   clampScope: () => (/* reexport safe */ _clampScope__WEBPACK_IMPORTED_MODULE_5__[\"default\"]),\n/* harmony export */   easing: () => (/* reexport safe */ _easing__WEBPACK_IMPORTED_MODULE_7__[\"default\"]),\n/* harmony export */   inScope: () => (/* reexport safe */ _inScope__WEBPACK_IMPORTED_MODULE_4__[\"default\"]),\n/* harmony export */   lerp: () => (/* reexport safe */ _lerp__WEBPACK_IMPORTED_MODULE_1__[\"default\"]),\n/* harmony export */   scoped: () => (/* reexport safe */ _scoped__WEBPACK_IMPORTED_MODULE_2__[\"default\"]),\n/* harmony export */   spreadScope: () => (/* reexport safe */ _spreadScope__WEBPACK_IMPORTED_MODULE_3__[\"default\"]),\n/* harmony export */   wrap: () => (/* reexport safe */ _wrap__WEBPACK_IMPORTED_MODULE_6__[\"default\"])\n/* harmony export */ });\n/* harmony import */ var _clamp__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./clamp */ \"./node_modules/vevet/build/es/utils/math/clamp.js\");\n/* harmony import */ var _lerp__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./lerp */ \"./node_modules/vevet/build/es/utils/math/lerp.js\");\n/* harmony import */ var _scoped__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./scoped */ \"./node_modules/vevet/build/es/utils/math/scoped.js\");\n/* harmony import */ var _spreadScope__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./spreadScope */ \"./node_modules/vevet/build/es/utils/math/spreadScope.js\");\n/* harmony import */ var _inScope__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./inScope */ \"./node_modules/vevet/build/es/utils/math/inScope.js\");\n/* harmony import */ var _clampScope__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./clampScope */ \"./node_modules/vevet/build/es/utils/math/clampScope.js\");\n/* harmony import */ var _wrap__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./wrap */ \"./node_modules/vevet/build/es/utils/math/wrap.js\");\n/* harmony import */ var _easing__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./easing */ \"./node_modules/vevet/build/es/utils/math/easing.js\");\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/math/index.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/math/lerp.js":
/*!********************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/math/lerp.js ***!
  \********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ lerp)\n/* harmony export */ });\n/**\r\n * Linear interpolation\r\n */\r\nfunction lerp(current, target, ease, approximationLeft = 0.001) {\r\n    const val = current * (1 - ease) + target * ease;\r\n    const diff = Math.abs(target - val);\r\n    if (diff <= approximationLeft) {\r\n        return target;\r\n    }\r\n    return val;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/math/lerp.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/math/scoped.js":
/*!**********************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/math/scoped.js ***!
  \**********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ scoped)\n/* harmony export */ });\n/**\r\n * Get progress relatively to the scope.\r\n *\r\n * @example\r\n *\r\n * scope(.35, [0, 1]);\r\n * // => .5\r\n * scope(.35, [.25, 1]);\r\n * // => .133\r\n */\r\nfunction scoped(val, scopeValue = [0, 1]) {\r\n    const result = (val - scopeValue[0]) / (scopeValue[1] - scopeValue[0]);\r\n    return result;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/math/scoped.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/math/spreadScope.js":
/*!***************************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/math/spreadScope.js ***!
  \***************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ spreadScope)\n/* harmony export */ });\n/**\r\n * Distribute scope progress among a certain quantity of timelines.\r\n */\r\nfunction spreadScope(quantity, shift) {\r\n    const timelines = [];\r\n    // duration for each element\r\n    const duration = 1 / (quantity - shift * (quantity - 1));\r\n    // calculate timelines\r\n    for (let index = 0; index < quantity; index += 1) {\r\n        const start = (duration * (1 - shift)) * index;\r\n        const end = start + duration;\r\n        timelines.push([start, end]);\r\n    }\r\n    return timelines;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/math/spreadScope.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/math/wrap.js":
/*!********************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/math/wrap.js ***!
  \********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ wrap)\n/* harmony export */ });\n/**\r\n * Wrap values\r\n */\r\nfunction wrap(min, max, value) {\r\n    const range = max - min;\r\n    return conditionalReturn(value, (val) => ((range + ((val - min) % range)) % range) + min);\r\n}\r\nfunction conditionalReturn(value, func) {\r\n    return value || value === 0 ? func(value) : func;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/math/wrap.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/scroll/getScrollValues.js":
/*!*********************************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/scroll/getScrollValues.js ***!
  \*********************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ getScrollValues)\n/* harmony export */ });\n/**\r\n * Get scroll values of a certain element\r\n */\r\nfunction getScrollValues(selector = window) {\r\n    if (selector) {\r\n        const scrollTop = selector instanceof Window\r\n            ? selector.pageYOffset : selector.scrollTop;\r\n        const scrollLeft = selector instanceof Window\r\n            ? selector.pageXOffset : selector.scrollLeft;\r\n        return {\r\n            scrollTop,\r\n            scrollLeft,\r\n        };\r\n    }\r\n    return undefined;\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/scroll/getScrollValues.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/scroll/index.js":
/*!***********************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/scroll/index.js ***!
  \***********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   getValues: () => (/* reexport safe */ _getScrollValues__WEBPACK_IMPORTED_MODULE_0__[\"default\"]),\n/* harmony export */   to: () => (/* reexport safe */ _scrollTo__WEBPACK_IMPORTED_MODULE_1__[\"default\"]),\n/* harmony export */   toElement: () => (/* reexport safe */ _scrollToElement__WEBPACK_IMPORTED_MODULE_2__[\"default\"])\n/* harmony export */ });\n/* harmony import */ var _getScrollValues__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./getScrollValues */ \"./node_modules/vevet/build/es/utils/scroll/getScrollValues.js\");\n/* harmony import */ var _scrollTo__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./scrollTo */ \"./node_modules/vevet/build/es/utils/scroll/scrollTo.js\");\n/* harmony import */ var _scrollToElement__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./scrollToElement */ \"./node_modules/vevet/build/es/utils/scroll/scrollToElement.js\");\n\r\n\r\n\r\n\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/scroll/index.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/scroll/scrollTo.js":
/*!**************************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/scroll/scrollTo.js ***!
  \**************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ scrollTo)\n/* harmony export */ });\n/* harmony import */ var _components_timeline_Timeline__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../components/timeline/Timeline */ \"./node_modules/vevet/build/es/components/timeline/Timeline.js\");\n/* harmony import */ var _getScrollValues__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./getScrollValues */ \"./node_modules/vevet/build/es/utils/scroll/getScrollValues.js\");\n\r\n\r\n/**\r\n * Scroll to coordinates\r\n */\r\nfunction scrollTo({ container = window, top = 0, left = 0, duration = 250, }) {\r\n    return new Promise((resolve, reject) => {\r\n        // save start values\r\n        const startValues = (0,_getScrollValues__WEBPACK_IMPORTED_MODULE_0__[\"default\"])(container);\r\n        if (startValues) {\r\n            // create animation\r\n            const timeline = new _components_timeline_Timeline__WEBPACK_IMPORTED_MODULE_1__.Timeline({\r\n                duration,\r\n            });\r\n            timeline.addCallback('progress', (data) => {\r\n                if (container) {\r\n                    container.scrollTo({\r\n                        top: startValues.scrollTop\r\n                            + (top - startValues.scrollTop) * data.easing,\r\n                        left: startValues.scrollLeft\r\n                            + (left - startValues.scrollLeft) * data.easing,\r\n                        behavior: 'auto',\r\n                    });\r\n                }\r\n            });\r\n            timeline.addCallback('end', () => {\r\n                resolve();\r\n            });\r\n            timeline.play();\r\n            return;\r\n        }\r\n        reject();\r\n    });\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/scroll/scrollTo.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/scroll/scrollToElement.js":
/*!*********************************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/scroll/scrollToElement.js ***!
  \*********************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ scrollToElement)\n/* harmony export */ });\n/* harmony import */ var vevet_dom__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! vevet-dom */ \"./node_modules/vevet-dom/dist/es/index.js\");\n/* harmony import */ var _getScrollValues__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./getScrollValues */ \"./node_modules/vevet/build/es/utils/scroll/getScrollValues.js\");\n/* harmony import */ var _math_clamp__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../math/clamp */ \"./node_modules/vevet/build/es/utils/math/clamp.js\");\n/* harmony import */ var _scrollTo__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./scrollTo */ \"./node_modules/vevet/build/es/utils/scroll/scrollTo.js\");\n\r\n\r\n\r\n\r\n/**\r\n * Scroll to element\r\n */\r\nfunction scrollToElement({ container = window, el, top = 0, left = 0, duration = 250, }) {\r\n    return new Promise((resolve, reject) => {\r\n        const startValues = (0,_getScrollValues__WEBPACK_IMPORTED_MODULE_1__[\"default\"])(container);\r\n        if (startValues) {\r\n            const element = (0,vevet_dom__WEBPACK_IMPORTED_MODULE_0__.selectOne)(el);\r\n            if (element) {\r\n                const bounding = element.getBoundingClientRect();\r\n                const toTop = (0,_math_clamp__WEBPACK_IMPORTED_MODULE_2__[\"default\"])(bounding.top + startValues.scrollTop - top, [0, Infinity]);\r\n                const toLeft = (0,_math_clamp__WEBPACK_IMPORTED_MODULE_2__[\"default\"])(bounding.left + startValues.scrollLeft - left, [0, Infinity]);\r\n                (0,_scrollTo__WEBPACK_IMPORTED_MODULE_3__[\"default\"])({\r\n                    container,\r\n                    top: toTop,\r\n                    left: toLeft,\r\n                    duration,\r\n                }).then(() => {\r\n                    resolve();\r\n                }).catch(() => {\r\n                    reject();\r\n                });\r\n            }\r\n            return;\r\n        }\r\n        reject();\r\n    });\r\n}\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/scroll/scrollToElement.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/utils/types/general.js":
/*!************************************************************!*\
  !*** ./node_modules/vevet/build/es/utils/types/general.js ***!
  \************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/utils/types/general.js?");

/***/ }),

/***/ "./node_modules/vevet/build/es/version.js":
/*!************************************************!*\
  !*** ./node_modules/vevet/build/es/version.js ***!
  \************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (__WEBPACK_DEFAULT_EXPORT__)\n/* harmony export */ });\nconst version = '2.17.0';\r\n/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (version);\r\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/vevet/build/es/version.js?");

/***/ }),

/***/ "./node_modules/p-cancelable/index.js":
/*!********************************************!*\
  !*** ./node_modules/p-cancelable/index.js ***!
  \********************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   CancelError: () => (/* binding */ CancelError),\n/* harmony export */   \"default\": () => (/* binding */ PCancelable)\n/* harmony export */ });\nclass CancelError extends Error {\n\tconstructor(reason) {\n\t\tsuper(reason || 'Promise was canceled');\n\t\tthis.name = 'CancelError';\n\t}\n\n\tget isCanceled() {\n\t\treturn true;\n\t}\n}\n\n// TODO: Use private class fields when ESLint 8 is out.\n\nclass PCancelable {\n\tstatic fn(userFunction) {\n\t\treturn (...arguments_) => {\n\t\t\treturn new PCancelable((resolve, reject, onCancel) => {\n\t\t\t\targuments_.push(onCancel);\n\t\t\t\t// eslint-disable-next-line promise/prefer-await-to-then\n\t\t\t\tuserFunction(...arguments_).then(resolve, reject);\n\t\t\t});\n\t\t};\n\t}\n\n\tconstructor(executor) {\n\t\tthis._cancelHandlers = [];\n\t\tthis._isPending = true;\n\t\tthis._isCanceled = false;\n\t\tthis._rejectOnCancel = true;\n\n\t\tthis._promise = new Promise((resolve, reject) => {\n\t\t\tthis._reject = reject;\n\n\t\t\tconst onResolve = value => {\n\t\t\t\tif (!this._isCanceled || !onCancel.shouldReject) {\n\t\t\t\t\tthis._isPending = false;\n\t\t\t\t\tresolve(value);\n\t\t\t\t}\n\t\t\t};\n\n\t\t\tconst onReject = error => {\n\t\t\t\tthis._isPending = false;\n\t\t\t\treject(error);\n\t\t\t};\n\n\t\t\tconst onCancel = handler => {\n\t\t\t\tif (!this._isPending) {\n\t\t\t\t\tthrow new Error('The `onCancel` handler was attached after the promise settled.');\n\t\t\t\t}\n\n\t\t\t\tthis._cancelHandlers.push(handler);\n\t\t\t};\n\n\t\t\tObject.defineProperties(onCancel, {\n\t\t\t\tshouldReject: {\n\t\t\t\t\tget: () => this._rejectOnCancel,\n\t\t\t\t\tset: boolean => {\n\t\t\t\t\t\tthis._rejectOnCancel = boolean;\n\t\t\t\t\t}\n\t\t\t\t}\n\t\t\t});\n\n\t\t\texecutor(onResolve, onReject, onCancel);\n\t\t});\n\t}\n\n\tthen(onFulfilled, onRejected) {\n\t\t// eslint-disable-next-line promise/prefer-await-to-then\n\t\treturn this._promise.then(onFulfilled, onRejected);\n\t}\n\n\tcatch(onRejected) {\n\t\t// eslint-disable-next-line promise/prefer-await-to-then\n\t\treturn this._promise.catch(onRejected);\n\t}\n\n\tfinally(onFinally) {\n\t\t// eslint-disable-next-line promise/prefer-await-to-then\n\t\treturn this._promise.finally(onFinally);\n\t}\n\n\tcancel(reason) {\n\t\tif (!this._isPending || this._isCanceled) {\n\t\t\treturn;\n\t\t}\n\n\t\tthis._isCanceled = true;\n\n\t\tif (this._cancelHandlers.length > 0) {\n\t\t\ttry {\n\t\t\t\tfor (const handler of this._cancelHandlers) {\n\t\t\t\t\thandler();\n\t\t\t\t}\n\t\t\t} catch (error) {\n\t\t\t\tthis._reject(error);\n\t\t\t\treturn;\n\t\t\t}\n\t\t}\n\n\t\tif (this._rejectOnCancel) {\n\t\t\tthis._reject(new CancelError(reason));\n\t\t}\n\t}\n\n\tget isCanceled() {\n\t\treturn this._isCanceled;\n\t}\n}\n\nObject.setPrototypeOf(PCancelable.prototype, Promise.prototype);\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/p-cancelable/index.js?");

/***/ }),

/***/ "./node_modules/swiper/modules/a11y.mjs":
/*!**********************************************!*\
  !*** ./node_modules/swiper/modules/a11y.mjs ***!
  \**********************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ A11y)\n/* harmony export */ });\n/* harmony import */ var _shared_classes_to_selector_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/classes-to-selector.mjs */ \"./node_modules/swiper/shared/classes-to-selector.mjs\");\n/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/utils.mjs */ \"./node_modules/swiper/shared/utils.mjs\");\n\n\n\nfunction A11y(_ref) {\n  let {\n    swiper,\n    extendParams,\n    on\n  } = _ref;\n  extendParams({\n    a11y: {\n      enabled: true,\n      notificationClass: 'swiper-notification',\n      prevSlideMessage: 'Previous slide',\n      nextSlideMessage: 'Next slide',\n      firstSlideMessage: 'This is the first slide',\n      lastSlideMessage: 'This is the last slide',\n      paginationBulletMessage: 'Go to slide {{index}}',\n      slideLabelMessage: '{{index}} / {{slidesLength}}',\n      containerMessage: null,\n      containerRoleDescriptionMessage: null,\n      itemRoleDescriptionMessage: null,\n      slideRole: 'group',\n      id: null\n    }\n  });\n  swiper.a11y = {\n    clicked: false\n  };\n  let liveRegion = null;\n  function notify(message) {\n    const notification = liveRegion;\n    if (notification.length === 0) return;\n    notification.innerHTML = '';\n    notification.innerHTML = message;\n  }\n  const makeElementsArray = el => (Array.isArray(el) ? el : [el]).filter(e => !!e);\n  function getRandomNumber(size) {\n    if (size === void 0) {\n      size = 16;\n    }\n    const randomChar = () => Math.round(16 * Math.random()).toString(16);\n    return 'x'.repeat(size).replace(/x/g, randomChar);\n  }\n  function makeElFocusable(el) {\n    el = makeElementsArray(el);\n    el.forEach(subEl => {\n      subEl.setAttribute('tabIndex', '0');\n    });\n  }\n  function makeElNotFocusable(el) {\n    el = makeElementsArray(el);\n    el.forEach(subEl => {\n      subEl.setAttribute('tabIndex', '-1');\n    });\n  }\n  function addElRole(el, role) {\n    el = makeElementsArray(el);\n    el.forEach(subEl => {\n      subEl.setAttribute('role', role);\n    });\n  }\n  function addElRoleDescription(el, description) {\n    el = makeElementsArray(el);\n    el.forEach(subEl => {\n      subEl.setAttribute('aria-roledescription', description);\n    });\n  }\n  function addElControls(el, controls) {\n    el = makeElementsArray(el);\n    el.forEach(subEl => {\n      subEl.setAttribute('aria-controls', controls);\n    });\n  }\n  function addElLabel(el, label) {\n    el = makeElementsArray(el);\n    el.forEach(subEl => {\n      subEl.setAttribute('aria-label', label);\n    });\n  }\n  function addElId(el, id) {\n    el = makeElementsArray(el);\n    el.forEach(subEl => {\n      subEl.setAttribute('id', id);\n    });\n  }\n  function addElLive(el, live) {\n    el = makeElementsArray(el);\n    el.forEach(subEl => {\n      subEl.setAttribute('aria-live', live);\n    });\n  }\n  function disableEl(el) {\n    el = makeElementsArray(el);\n    el.forEach(subEl => {\n      subEl.setAttribute('aria-disabled', true);\n    });\n  }\n  function enableEl(el) {\n    el = makeElementsArray(el);\n    el.forEach(subEl => {\n      subEl.setAttribute('aria-disabled', false);\n    });\n  }\n  function onEnterOrSpaceKey(e) {\n    if (e.keyCode !== 13 && e.keyCode !== 32) return;\n    const params = swiper.params.a11y;\n    const targetEl = e.target;\n    if (swiper.pagination && swiper.pagination.el && (targetEl === swiper.pagination.el || swiper.pagination.el.contains(e.target))) {\n      if (!e.target.matches((0,_shared_classes_to_selector_mjs__WEBPACK_IMPORTED_MODULE_0__.c)(swiper.params.pagination.bulletClass))) return;\n    }\n    if (swiper.navigation && swiper.navigation.nextEl && targetEl === swiper.navigation.nextEl) {\n      if (!(swiper.isEnd && !swiper.params.loop)) {\n        swiper.slideNext();\n      }\n      if (swiper.isEnd) {\n        notify(params.lastSlideMessage);\n      } else {\n        notify(params.nextSlideMessage);\n      }\n    }\n    if (swiper.navigation && swiper.navigation.prevEl && targetEl === swiper.navigation.prevEl) {\n      if (!(swiper.isBeginning && !swiper.params.loop)) {\n        swiper.slidePrev();\n      }\n      if (swiper.isBeginning) {\n        notify(params.firstSlideMessage);\n      } else {\n        notify(params.prevSlideMessage);\n      }\n    }\n    if (swiper.pagination && targetEl.matches((0,_shared_classes_to_selector_mjs__WEBPACK_IMPORTED_MODULE_0__.c)(swiper.params.pagination.bulletClass))) {\n      targetEl.click();\n    }\n  }\n  function updateNavigation() {\n    if (swiper.params.loop || swiper.params.rewind || !swiper.navigation) return;\n    const {\n      nextEl,\n      prevEl\n    } = swiper.navigation;\n    if (prevEl) {\n      if (swiper.isBeginning) {\n        disableEl(prevEl);\n        makeElNotFocusable(prevEl);\n      } else {\n        enableEl(prevEl);\n        makeElFocusable(prevEl);\n      }\n    }\n    if (nextEl) {\n      if (swiper.isEnd) {\n        disableEl(nextEl);\n        makeElNotFocusable(nextEl);\n      } else {\n        enableEl(nextEl);\n        makeElFocusable(nextEl);\n      }\n    }\n  }\n  function hasPagination() {\n    return swiper.pagination && swiper.pagination.bullets && swiper.pagination.bullets.length;\n  }\n  function hasClickablePagination() {\n    return hasPagination() && swiper.params.pagination.clickable;\n  }\n  function updatePagination() {\n    const params = swiper.params.a11y;\n    if (!hasPagination()) return;\n    swiper.pagination.bullets.forEach(bulletEl => {\n      if (swiper.params.pagination.clickable) {\n        makeElFocusable(bulletEl);\n        if (!swiper.params.pagination.renderBullet) {\n          addElRole(bulletEl, 'button');\n          addElLabel(bulletEl, params.paginationBulletMessage.replace(/\\{\\{index\\}\\}/, (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.g)(bulletEl) + 1));\n        }\n      }\n      if (bulletEl.matches((0,_shared_classes_to_selector_mjs__WEBPACK_IMPORTED_MODULE_0__.c)(swiper.params.pagination.bulletActiveClass))) {\n        bulletEl.setAttribute('aria-current', 'true');\n      } else {\n        bulletEl.removeAttribute('aria-current');\n      }\n    });\n  }\n  const initNavEl = (el, wrapperId, message) => {\n    makeElFocusable(el);\n    if (el.tagName !== 'BUTTON') {\n      addElRole(el, 'button');\n      el.addEventListener('keydown', onEnterOrSpaceKey);\n    }\n    addElLabel(el, message);\n    addElControls(el, wrapperId);\n  };\n  const handlePointerDown = () => {\n    swiper.a11y.clicked = true;\n  };\n  const handlePointerUp = () => {\n    requestAnimationFrame(() => {\n      requestAnimationFrame(() => {\n        if (!swiper.destroyed) {\n          swiper.a11y.clicked = false;\n        }\n      });\n    });\n  };\n  const handleFocus = e => {\n    if (swiper.a11y.clicked) return;\n    const slideEl = e.target.closest(`.${swiper.params.slideClass}, swiper-slide`);\n    if (!slideEl || !swiper.slides.includes(slideEl)) return;\n    const isActive = swiper.slides.indexOf(slideEl) === swiper.activeIndex;\n    const isVisible = swiper.params.watchSlidesProgress && swiper.visibleSlides && swiper.visibleSlides.includes(slideEl);\n    if (isActive || isVisible) return;\n    if (e.sourceCapabilities && e.sourceCapabilities.firesTouchEvents) return;\n    if (swiper.isHorizontal()) {\n      swiper.el.scrollLeft = 0;\n    } else {\n      swiper.el.scrollTop = 0;\n    }\n    swiper.slideTo(swiper.slides.indexOf(slideEl), 0);\n  };\n  const initSlides = () => {\n    const params = swiper.params.a11y;\n    if (params.itemRoleDescriptionMessage) {\n      addElRoleDescription(swiper.slides, params.itemRoleDescriptionMessage);\n    }\n    if (params.slideRole) {\n      addElRole(swiper.slides, params.slideRole);\n    }\n    const slidesLength = swiper.slides.length;\n    if (params.slideLabelMessage) {\n      swiper.slides.forEach((slideEl, index) => {\n        const slideIndex = swiper.params.loop ? parseInt(slideEl.getAttribute('data-swiper-slide-index'), 10) : index;\n        const ariaLabelMessage = params.slideLabelMessage.replace(/\\{\\{index\\}\\}/, slideIndex + 1).replace(/\\{\\{slidesLength\\}\\}/, slidesLength);\n        addElLabel(slideEl, ariaLabelMessage);\n      });\n    }\n  };\n  const init = () => {\n    const params = swiper.params.a11y;\n    swiper.el.append(liveRegion);\n\n    // Container\n    const containerEl = swiper.el;\n    if (params.containerRoleDescriptionMessage) {\n      addElRoleDescription(containerEl, params.containerRoleDescriptionMessage);\n    }\n    if (params.containerMessage) {\n      addElLabel(containerEl, params.containerMessage);\n    }\n\n    // Wrapper\n    const wrapperEl = swiper.wrapperEl;\n    const wrapperId = params.id || wrapperEl.getAttribute('id') || `swiper-wrapper-${getRandomNumber(16)}`;\n    const live = swiper.params.autoplay && swiper.params.autoplay.enabled ? 'off' : 'polite';\n    addElId(wrapperEl, wrapperId);\n    addElLive(wrapperEl, live);\n\n    // Slide\n    initSlides();\n\n    // Navigation\n    let {\n      nextEl,\n      prevEl\n    } = swiper.navigation ? swiper.navigation : {};\n    nextEl = makeElementsArray(nextEl);\n    prevEl = makeElementsArray(prevEl);\n    if (nextEl) {\n      nextEl.forEach(el => initNavEl(el, wrapperId, params.nextSlideMessage));\n    }\n    if (prevEl) {\n      prevEl.forEach(el => initNavEl(el, wrapperId, params.prevSlideMessage));\n    }\n\n    // Pagination\n    if (hasClickablePagination()) {\n      const paginationEl = Array.isArray(swiper.pagination.el) ? swiper.pagination.el : [swiper.pagination.el];\n      paginationEl.forEach(el => {\n        el.addEventListener('keydown', onEnterOrSpaceKey);\n      });\n    }\n\n    // Tab focus\n    swiper.el.addEventListener('focus', handleFocus, true);\n    swiper.el.addEventListener('pointerdown', handlePointerDown, true);\n    swiper.el.addEventListener('pointerup', handlePointerUp, true);\n  };\n  function destroy() {\n    if (liveRegion) liveRegion.remove();\n    let {\n      nextEl,\n      prevEl\n    } = swiper.navigation ? swiper.navigation : {};\n    nextEl = makeElementsArray(nextEl);\n    prevEl = makeElementsArray(prevEl);\n    if (nextEl) {\n      nextEl.forEach(el => el.removeEventListener('keydown', onEnterOrSpaceKey));\n    }\n    if (prevEl) {\n      prevEl.forEach(el => el.removeEventListener('keydown', onEnterOrSpaceKey));\n    }\n\n    // Pagination\n    if (hasClickablePagination()) {\n      const paginationEl = Array.isArray(swiper.pagination.el) ? swiper.pagination.el : [swiper.pagination.el];\n      paginationEl.forEach(el => {\n        el.removeEventListener('keydown', onEnterOrSpaceKey);\n      });\n    }\n\n    // Tab focus\n    swiper.el.removeEventListener('focus', handleFocus, true);\n    swiper.el.removeEventListener('pointerdown', handlePointerDown, true);\n    swiper.el.removeEventListener('pointerup', handlePointerUp, true);\n  }\n  on('beforeInit', () => {\n    liveRegion = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.c)('span', swiper.params.a11y.notificationClass);\n    liveRegion.setAttribute('aria-live', 'assertive');\n    liveRegion.setAttribute('aria-atomic', 'true');\n  });\n  on('afterInit', () => {\n    if (!swiper.params.a11y.enabled) return;\n    init();\n  });\n  on('slidesLengthChange snapGridLengthChange slidesGridLengthChange', () => {\n    if (!swiper.params.a11y.enabled) return;\n    initSlides();\n  });\n  on('fromEdge toEdge afterInit lock unlock', () => {\n    if (!swiper.params.a11y.enabled) return;\n    updateNavigation();\n  });\n  on('paginationUpdate', () => {\n    if (!swiper.params.a11y.enabled) return;\n    updatePagination();\n  });\n  on('destroy', () => {\n    if (!swiper.params.a11y.enabled) return;\n    destroy();\n  });\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/modules/a11y.mjs?");

/***/ }),

/***/ "./node_modules/swiper/modules/autoplay.mjs":
/*!**************************************************!*\
  !*** ./node_modules/swiper/modules/autoplay.mjs ***!
  \**************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ Autoplay)\n/* harmony export */ });\n/* harmony import */ var _shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/ssr-window.esm.mjs */ \"./node_modules/swiper/shared/ssr-window.esm.mjs\");\n\n\n/* eslint no-underscore-dangle: \"off\" */\n/* eslint no-use-before-define: \"off\" */\nfunction Autoplay(_ref) {\n  let {\n    swiper,\n    extendParams,\n    on,\n    emit,\n    params\n  } = _ref;\n  swiper.autoplay = {\n    running: false,\n    paused: false,\n    timeLeft: 0\n  };\n  extendParams({\n    autoplay: {\n      enabled: false,\n      delay: 3000,\n      waitForTransition: true,\n      disableOnInteraction: true,\n      stopOnLastSlide: false,\n      reverseDirection: false,\n      pauseOnMouseEnter: false\n    }\n  });\n  let timeout;\n  let raf;\n  let autoplayDelayTotal = params && params.autoplay ? params.autoplay.delay : 3000;\n  let autoplayDelayCurrent = params && params.autoplay ? params.autoplay.delay : 3000;\n  let autoplayTimeLeft;\n  let autoplayStartTime = new Date().getTime;\n  let wasPaused;\n  let isTouched;\n  let pausedByTouch;\n  let touchStartTimeout;\n  let slideChanged;\n  let pausedByInteraction;\n  function onTransitionEnd(e) {\n    if (!swiper || swiper.destroyed || !swiper.wrapperEl) return;\n    if (e.target !== swiper.wrapperEl) return;\n    swiper.wrapperEl.removeEventListener('transitionend', onTransitionEnd);\n    resume();\n  }\n  const calcTimeLeft = () => {\n    if (swiper.destroyed || !swiper.autoplay.running) return;\n    if (swiper.autoplay.paused) {\n      wasPaused = true;\n    } else if (wasPaused) {\n      autoplayDelayCurrent = autoplayTimeLeft;\n      wasPaused = false;\n    }\n    const timeLeft = swiper.autoplay.paused ? autoplayTimeLeft : autoplayStartTime + autoplayDelayCurrent - new Date().getTime();\n    swiper.autoplay.timeLeft = timeLeft;\n    emit('autoplayTimeLeft', timeLeft, timeLeft / autoplayDelayTotal);\n    raf = requestAnimationFrame(() => {\n      calcTimeLeft();\n    });\n  };\n  const getSlideDelay = () => {\n    let activeSlideEl;\n    if (swiper.virtual && swiper.params.virtual.enabled) {\n      activeSlideEl = swiper.slides.filter(slideEl => slideEl.classList.contains('swiper-slide-active'))[0];\n    } else {\n      activeSlideEl = swiper.slides[swiper.activeIndex];\n    }\n    if (!activeSlideEl) return undefined;\n    const currentSlideDelay = parseInt(activeSlideEl.getAttribute('data-swiper-autoplay'), 10);\n    return currentSlideDelay;\n  };\n  const run = delayForce => {\n    if (swiper.destroyed || !swiper.autoplay.running) return;\n    cancelAnimationFrame(raf);\n    calcTimeLeft();\n    let delay = typeof delayForce === 'undefined' ? swiper.params.autoplay.delay : delayForce;\n    autoplayDelayTotal = swiper.params.autoplay.delay;\n    autoplayDelayCurrent = swiper.params.autoplay.delay;\n    const currentSlideDelay = getSlideDelay();\n    if (!Number.isNaN(currentSlideDelay) && currentSlideDelay > 0 && typeof delayForce === 'undefined') {\n      delay = currentSlideDelay;\n      autoplayDelayTotal = currentSlideDelay;\n      autoplayDelayCurrent = currentSlideDelay;\n    }\n    autoplayTimeLeft = delay;\n    const speed = swiper.params.speed;\n    const proceed = () => {\n      if (!swiper || swiper.destroyed) return;\n      if (swiper.params.autoplay.reverseDirection) {\n        if (!swiper.isBeginning || swiper.params.loop || swiper.params.rewind) {\n          swiper.slidePrev(speed, true, true);\n          emit('autoplay');\n        } else if (!swiper.params.autoplay.stopOnLastSlide) {\n          swiper.slideTo(swiper.slides.length - 1, speed, true, true);\n          emit('autoplay');\n        }\n      } else {\n        if (!swiper.isEnd || swiper.params.loop || swiper.params.rewind) {\n          swiper.slideNext(speed, true, true);\n          emit('autoplay');\n        } else if (!swiper.params.autoplay.stopOnLastSlide) {\n          swiper.slideTo(0, speed, true, true);\n          emit('autoplay');\n        }\n      }\n      if (swiper.params.cssMode) {\n        autoplayStartTime = new Date().getTime();\n        requestAnimationFrame(() => {\n          run();\n        });\n      }\n    };\n    if (delay > 0) {\n      clearTimeout(timeout);\n      timeout = setTimeout(() => {\n        proceed();\n      }, delay);\n    } else {\n      requestAnimationFrame(() => {\n        proceed();\n      });\n    }\n\n    // eslint-disable-next-line\n    return delay;\n  };\n  const start = () => {\n    swiper.autoplay.running = true;\n    run();\n    emit('autoplayStart');\n  };\n  const stop = () => {\n    swiper.autoplay.running = false;\n    clearTimeout(timeout);\n    cancelAnimationFrame(raf);\n    emit('autoplayStop');\n  };\n  const pause = (internal, reset) => {\n    if (swiper.destroyed || !swiper.autoplay.running) return;\n    clearTimeout(timeout);\n    if (!internal) {\n      pausedByInteraction = true;\n    }\n    const proceed = () => {\n      emit('autoplayPause');\n      if (swiper.params.autoplay.waitForTransition) {\n        swiper.wrapperEl.addEventListener('transitionend', onTransitionEnd);\n      } else {\n        resume();\n      }\n    };\n    swiper.autoplay.paused = true;\n    if (reset) {\n      if (slideChanged) {\n        autoplayTimeLeft = swiper.params.autoplay.delay;\n      }\n      slideChanged = false;\n      proceed();\n      return;\n    }\n    const delay = autoplayTimeLeft || swiper.params.autoplay.delay;\n    autoplayTimeLeft = delay - (new Date().getTime() - autoplayStartTime);\n    if (swiper.isEnd && autoplayTimeLeft < 0 && !swiper.params.loop) return;\n    if (autoplayTimeLeft < 0) autoplayTimeLeft = 0;\n    proceed();\n  };\n  const resume = () => {\n    if (swiper.isEnd && autoplayTimeLeft < 0 && !swiper.params.loop || swiper.destroyed || !swiper.autoplay.running) return;\n    autoplayStartTime = new Date().getTime();\n    if (pausedByInteraction) {\n      pausedByInteraction = false;\n      run(autoplayTimeLeft);\n    } else {\n      run();\n    }\n    swiper.autoplay.paused = false;\n    emit('autoplayResume');\n  };\n  const onVisibilityChange = () => {\n    if (swiper.destroyed || !swiper.autoplay.running) return;\n    const document = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();\n    if (document.visibilityState === 'hidden') {\n      pausedByInteraction = true;\n      pause(true);\n    }\n    if (document.visibilityState === 'visible') {\n      resume();\n    }\n  };\n  const onPointerEnter = e => {\n    if (e.pointerType !== 'mouse') return;\n    pausedByInteraction = true;\n    pause(true);\n  };\n  const onPointerLeave = e => {\n    if (e.pointerType !== 'mouse') return;\n    if (swiper.autoplay.paused) {\n      resume();\n    }\n  };\n  const attachMouseEvents = () => {\n    if (swiper.params.autoplay.pauseOnMouseEnter) {\n      swiper.el.addEventListener('pointerenter', onPointerEnter);\n      swiper.el.addEventListener('pointerleave', onPointerLeave);\n    }\n  };\n  const detachMouseEvents = () => {\n    swiper.el.removeEventListener('pointerenter', onPointerEnter);\n    swiper.el.removeEventListener('pointerleave', onPointerLeave);\n  };\n  const attachDocumentEvents = () => {\n    const document = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();\n    document.addEventListener('visibilitychange', onVisibilityChange);\n  };\n  const detachDocumentEvents = () => {\n    const document = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();\n    document.removeEventListener('visibilitychange', onVisibilityChange);\n  };\n  on('init', () => {\n    if (swiper.params.autoplay.enabled) {\n      attachMouseEvents();\n      attachDocumentEvents();\n      autoplayStartTime = new Date().getTime();\n      start();\n    }\n  });\n  on('destroy', () => {\n    detachMouseEvents();\n    detachDocumentEvents();\n    if (swiper.autoplay.running) {\n      stop();\n    }\n  });\n  on('beforeTransitionStart', (_s, speed, internal) => {\n    if (swiper.destroyed || !swiper.autoplay.running) return;\n    if (internal || !swiper.params.autoplay.disableOnInteraction) {\n      pause(true, true);\n    } else {\n      stop();\n    }\n  });\n  on('sliderFirstMove', () => {\n    if (swiper.destroyed || !swiper.autoplay.running) return;\n    if (swiper.params.autoplay.disableOnInteraction) {\n      stop();\n      return;\n    }\n    isTouched = true;\n    pausedByTouch = false;\n    pausedByInteraction = false;\n    touchStartTimeout = setTimeout(() => {\n      pausedByInteraction = true;\n      pausedByTouch = true;\n      pause(true);\n    }, 200);\n  });\n  on('touchEnd', () => {\n    if (swiper.destroyed || !swiper.autoplay.running || !isTouched) return;\n    clearTimeout(touchStartTimeout);\n    clearTimeout(timeout);\n    if (swiper.params.autoplay.disableOnInteraction) {\n      pausedByTouch = false;\n      isTouched = false;\n      return;\n    }\n    if (pausedByTouch && swiper.params.cssMode) resume();\n    pausedByTouch = false;\n    isTouched = false;\n  });\n  on('slideChange', () => {\n    if (swiper.destroyed || !swiper.autoplay.running) return;\n    slideChanged = true;\n  });\n  Object.assign(swiper.autoplay, {\n    start,\n    stop,\n    pause,\n    resume\n  });\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/modules/autoplay.mjs?");

/***/ }),

/***/ "./node_modules/swiper/modules/controller.mjs":
/*!****************************************************!*\
  !*** ./node_modules/swiper/modules/controller.mjs ***!
  \****************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ Controller)\n/* harmony export */ });\n/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/utils.mjs */ \"./node_modules/swiper/shared/utils.mjs\");\n\n\n/* eslint no-bitwise: [\"error\", { \"allow\": [\">>\"] }] */\nfunction Controller(_ref) {\n  let {\n    swiper,\n    extendParams,\n    on\n  } = _ref;\n  extendParams({\n    controller: {\n      control: undefined,\n      inverse: false,\n      by: 'slide' // or 'container'\n    }\n  });\n\n  swiper.controller = {\n    control: undefined\n  };\n  function LinearSpline(x, y) {\n    const binarySearch = function search() {\n      let maxIndex;\n      let minIndex;\n      let guess;\n      return (array, val) => {\n        minIndex = -1;\n        maxIndex = array.length;\n        while (maxIndex - minIndex > 1) {\n          guess = maxIndex + minIndex >> 1;\n          if (array[guess] <= val) {\n            minIndex = guess;\n          } else {\n            maxIndex = guess;\n          }\n        }\n        return maxIndex;\n      };\n    }();\n    this.x = x;\n    this.y = y;\n    this.lastIndex = x.length - 1;\n    // Given an x value (x2), return the expected y2 value:\n    // (x1,y1) is the known point before given value,\n    // (x3,y3) is the known point after given value.\n    let i1;\n    let i3;\n    this.interpolate = function interpolate(x2) {\n      if (!x2) return 0;\n\n      // Get the indexes of x1 and x3 (the array indexes before and after given x2):\n      i3 = binarySearch(this.x, x2);\n      i1 = i3 - 1;\n\n      // We have our indexes i1 & i3, so we can calculate already:\n      // y2 := ((x2−x1) × (y3−y1)) ÷ (x3−x1) + y1\n      return (x2 - this.x[i1]) * (this.y[i3] - this.y[i1]) / (this.x[i3] - this.x[i1]) + this.y[i1];\n    };\n    return this;\n  }\n  function getInterpolateFunction(c) {\n    swiper.controller.spline = swiper.params.loop ? new LinearSpline(swiper.slidesGrid, c.slidesGrid) : new LinearSpline(swiper.snapGrid, c.snapGrid);\n  }\n  function setTranslate(_t, byController) {\n    const controlled = swiper.controller.control;\n    let multiplier;\n    let controlledTranslate;\n    const Swiper = swiper.constructor;\n    function setControlledTranslate(c) {\n      if (c.destroyed) return;\n\n      // this will create an Interpolate function based on the snapGrids\n      // x is the Grid of the scrolled scroller and y will be the controlled scroller\n      // it makes sense to create this only once and recall it for the interpolation\n      // the function does a lot of value caching for performance\n      const translate = swiper.rtlTranslate ? -swiper.translate : swiper.translate;\n      if (swiper.params.controller.by === 'slide') {\n        getInterpolateFunction(c);\n        // i am not sure why the values have to be multiplicated this way, tried to invert the snapGrid\n        // but it did not work out\n        controlledTranslate = -swiper.controller.spline.interpolate(-translate);\n      }\n      if (!controlledTranslate || swiper.params.controller.by === 'container') {\n        multiplier = (c.maxTranslate() - c.minTranslate()) / (swiper.maxTranslate() - swiper.minTranslate());\n        if (Number.isNaN(multiplier) || !Number.isFinite(multiplier)) {\n          multiplier = 1;\n        }\n        controlledTranslate = (translate - swiper.minTranslate()) * multiplier + c.minTranslate();\n      }\n      if (swiper.params.controller.inverse) {\n        controlledTranslate = c.maxTranslate() - controlledTranslate;\n      }\n      c.updateProgress(controlledTranslate);\n      c.setTranslate(controlledTranslate, swiper);\n      c.updateActiveIndex();\n      c.updateSlidesClasses();\n    }\n    if (Array.isArray(controlled)) {\n      for (let i = 0; i < controlled.length; i += 1) {\n        if (controlled[i] !== byController && controlled[i] instanceof Swiper) {\n          setControlledTranslate(controlled[i]);\n        }\n      }\n    } else if (controlled instanceof Swiper && byController !== controlled) {\n      setControlledTranslate(controlled);\n    }\n  }\n  function setTransition(duration, byController) {\n    const Swiper = swiper.constructor;\n    const controlled = swiper.controller.control;\n    let i;\n    function setControlledTransition(c) {\n      if (c.destroyed) return;\n      c.setTransition(duration, swiper);\n      if (duration !== 0) {\n        c.transitionStart();\n        if (c.params.autoHeight) {\n          (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.n)(() => {\n            c.updateAutoHeight();\n          });\n        }\n        (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.i)(c.wrapperEl, () => {\n          if (!controlled) return;\n          c.transitionEnd();\n        });\n      }\n    }\n    if (Array.isArray(controlled)) {\n      for (i = 0; i < controlled.length; i += 1) {\n        if (controlled[i] !== byController && controlled[i] instanceof Swiper) {\n          setControlledTransition(controlled[i]);\n        }\n      }\n    } else if (controlled instanceof Swiper && byController !== controlled) {\n      setControlledTransition(controlled);\n    }\n  }\n  function removeSpline() {\n    if (!swiper.controller.control) return;\n    if (swiper.controller.spline) {\n      swiper.controller.spline = undefined;\n      delete swiper.controller.spline;\n    }\n  }\n  on('beforeInit', () => {\n    if (typeof window !== 'undefined' && (\n    // eslint-disable-line\n    typeof swiper.params.controller.control === 'string' || swiper.params.controller.control instanceof HTMLElement)) {\n      const controlElement = document.querySelector(swiper.params.controller.control);\n      if (controlElement && controlElement.swiper) {\n        swiper.controller.control = controlElement.swiper;\n      } else if (controlElement) {\n        const onControllerSwiper = e => {\n          swiper.controller.control = e.detail[0];\n          swiper.update();\n          controlElement.removeEventListener('init', onControllerSwiper);\n        };\n        controlElement.addEventListener('init', onControllerSwiper);\n      }\n      return;\n    }\n    swiper.controller.control = swiper.params.controller.control;\n  });\n  on('update', () => {\n    removeSpline();\n  });\n  on('resize', () => {\n    removeSpline();\n  });\n  on('observerUpdate', () => {\n    removeSpline();\n  });\n  on('setTranslate', (_s, translate, byController) => {\n    if (!swiper.controller.control || swiper.controller.control.destroyed) return;\n    swiper.controller.setTranslate(translate, byController);\n  });\n  on('setTransition', (_s, duration, byController) => {\n    if (!swiper.controller.control || swiper.controller.control.destroyed) return;\n    swiper.controller.setTransition(duration, byController);\n  });\n  Object.assign(swiper.controller, {\n    setTranslate,\n    setTransition\n  });\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/modules/controller.mjs?");

/***/ }),

/***/ "./node_modules/swiper/modules/effect-cards.mjs":
/*!******************************************************!*\
  !*** ./node_modules/swiper/modules/effect-cards.mjs ***!
  \******************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ EffectCards)\n/* harmony export */ });\n/* harmony import */ var _shared_create_shadow_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/create-shadow.mjs */ \"./node_modules/swiper/shared/create-shadow.mjs\");\n/* harmony import */ var _shared_effect_init_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/effect-init.mjs */ \"./node_modules/swiper/shared/effect-init.mjs\");\n/* harmony import */ var _shared_effect_target_mjs__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../shared/effect-target.mjs */ \"./node_modules/swiper/shared/effect-target.mjs\");\n/* harmony import */ var _shared_effect_virtual_transition_end_mjs__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../shared/effect-virtual-transition-end.mjs */ \"./node_modules/swiper/shared/effect-virtual-transition-end.mjs\");\n/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../shared/utils.mjs */ \"./node_modules/swiper/shared/utils.mjs\");\n\n\n\n\n\n\nfunction EffectCards(_ref) {\n  let {\n    swiper,\n    extendParams,\n    on\n  } = _ref;\n  extendParams({\n    cardsEffect: {\n      slideShadows: true,\n      rotate: true,\n      perSlideRotate: 2,\n      perSlideOffset: 8\n    }\n  });\n  const setTranslate = () => {\n    const {\n      slides,\n      activeIndex,\n      rtlTranslate: rtl\n    } = swiper;\n    const params = swiper.params.cardsEffect;\n    const {\n      startTranslate,\n      isTouched\n    } = swiper.touchEventsData;\n    const currentTranslate = rtl ? -swiper.translate : swiper.translate;\n    for (let i = 0; i < slides.length; i += 1) {\n      const slideEl = slides[i];\n      const slideProgress = slideEl.progress;\n      const progress = Math.min(Math.max(slideProgress, -4), 4);\n      let offset = slideEl.swiperSlideOffset;\n      if (swiper.params.centeredSlides && !swiper.params.cssMode) {\n        swiper.wrapperEl.style.transform = `translateX(${swiper.minTranslate()}px)`;\n      }\n      if (swiper.params.centeredSlides && swiper.params.cssMode) {\n        offset -= slides[0].swiperSlideOffset;\n      }\n      let tX = swiper.params.cssMode ? -offset - swiper.translate : -offset;\n      let tY = 0;\n      const tZ = -100 * Math.abs(progress);\n      let scale = 1;\n      let rotate = -params.perSlideRotate * progress;\n      let tXAdd = params.perSlideOffset - Math.abs(progress) * 0.75;\n      const slideIndex = swiper.virtual && swiper.params.virtual.enabled ? swiper.virtual.from + i : i;\n      const isSwipeToNext = (slideIndex === activeIndex || slideIndex === activeIndex - 1) && progress > 0 && progress < 1 && (isTouched || swiper.params.cssMode) && currentTranslate < startTranslate;\n      const isSwipeToPrev = (slideIndex === activeIndex || slideIndex === activeIndex + 1) && progress < 0 && progress > -1 && (isTouched || swiper.params.cssMode) && currentTranslate > startTranslate;\n      if (isSwipeToNext || isSwipeToPrev) {\n        const subProgress = (1 - Math.abs((Math.abs(progress) - 0.5) / 0.5)) ** 0.5;\n        rotate += -28 * progress * subProgress;\n        scale += -0.5 * subProgress;\n        tXAdd += 96 * subProgress;\n        tY = `${-25 * subProgress * Math.abs(progress)}%`;\n      }\n      if (progress < 0) {\n        // next\n        tX = `calc(${tX}px ${rtl ? '-' : '+'} (${tXAdd * Math.abs(progress)}%))`;\n      } else if (progress > 0) {\n        // prev\n        tX = `calc(${tX}px ${rtl ? '-' : '+'} (-${tXAdd * Math.abs(progress)}%))`;\n      } else {\n        tX = `${tX}px`;\n      }\n      if (!swiper.isHorizontal()) {\n        const prevY = tY;\n        tY = tX;\n        tX = prevY;\n      }\n      const scaleString = progress < 0 ? `${1 + (1 - scale) * progress}` : `${1 - (1 - scale) * progress}`;\n\n      /* eslint-disable */\n      const transform = `\n        translate3d(${tX}, ${tY}, ${tZ}px)\n        rotateZ(${params.rotate ? rtl ? -rotate : rotate : 0}deg)\n        scale(${scaleString})\n      `;\n      /* eslint-enable */\n\n      if (params.slideShadows) {\n        // Set shadows\n        let shadowEl = slideEl.querySelector('.swiper-slide-shadow');\n        if (!shadowEl) {\n          shadowEl = (0,_shared_create_shadow_mjs__WEBPACK_IMPORTED_MODULE_0__.c)('cards', slideEl);\n        }\n        if (shadowEl) shadowEl.style.opacity = Math.min(Math.max((Math.abs(progress) - 0.5) / 0.5, 0), 1);\n      }\n      slideEl.style.zIndex = -Math.abs(Math.round(slideProgress)) + slides.length;\n      const targetEl = (0,_shared_effect_target_mjs__WEBPACK_IMPORTED_MODULE_2__.e)(params, slideEl);\n      targetEl.style.transform = transform;\n    }\n  };\n  const setTransition = duration => {\n    const transformElements = swiper.slides.map(slideEl => (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_4__.k)(slideEl));\n    transformElements.forEach(el => {\n      el.style.transitionDuration = `${duration}ms`;\n      el.querySelectorAll('.swiper-slide-shadow').forEach(shadowEl => {\n        shadowEl.style.transitionDuration = `${duration}ms`;\n      });\n    });\n    (0,_shared_effect_virtual_transition_end_mjs__WEBPACK_IMPORTED_MODULE_3__.e)({\n      swiper,\n      duration,\n      transformElements\n    });\n  };\n  (0,_shared_effect_init_mjs__WEBPACK_IMPORTED_MODULE_1__.e)({\n    effect: 'cards',\n    swiper,\n    on,\n    setTranslate,\n    setTransition,\n    perspective: () => true,\n    overwriteParams: () => ({\n      watchSlidesProgress: true,\n      virtualTranslate: !swiper.params.cssMode\n    })\n  });\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/modules/effect-cards.mjs?");

/***/ }),

/***/ "./node_modules/swiper/modules/effect-coverflow.mjs":
/*!**********************************************************!*\
  !*** ./node_modules/swiper/modules/effect-coverflow.mjs ***!
  \**********************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ EffectCoverflow)\n/* harmony export */ });\n/* harmony import */ var _shared_create_shadow_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/create-shadow.mjs */ \"./node_modules/swiper/shared/create-shadow.mjs\");\n/* harmony import */ var _shared_effect_init_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/effect-init.mjs */ \"./node_modules/swiper/shared/effect-init.mjs\");\n/* harmony import */ var _shared_effect_target_mjs__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../shared/effect-target.mjs */ \"./node_modules/swiper/shared/effect-target.mjs\");\n/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../shared/utils.mjs */ \"./node_modules/swiper/shared/utils.mjs\");\n\n\n\n\n\nfunction EffectCoverflow(_ref) {\n  let {\n    swiper,\n    extendParams,\n    on\n  } = _ref;\n  extendParams({\n    coverflowEffect: {\n      rotate: 50,\n      stretch: 0,\n      depth: 100,\n      scale: 1,\n      modifier: 1,\n      slideShadows: true\n    }\n  });\n  const setTranslate = () => {\n    const {\n      width: swiperWidth,\n      height: swiperHeight,\n      slides,\n      slidesSizesGrid\n    } = swiper;\n    const params = swiper.params.coverflowEffect;\n    const isHorizontal = swiper.isHorizontal();\n    const transform = swiper.translate;\n    const center = isHorizontal ? -transform + swiperWidth / 2 : -transform + swiperHeight / 2;\n    const rotate = isHorizontal ? params.rotate : -params.rotate;\n    const translate = params.depth;\n    // Each slide offset from center\n    for (let i = 0, length = slides.length; i < length; i += 1) {\n      const slideEl = slides[i];\n      const slideSize = slidesSizesGrid[i];\n      const slideOffset = slideEl.swiperSlideOffset;\n      const centerOffset = (center - slideOffset - slideSize / 2) / slideSize;\n      const offsetMultiplier = typeof params.modifier === 'function' ? params.modifier(centerOffset) : centerOffset * params.modifier;\n      let rotateY = isHorizontal ? rotate * offsetMultiplier : 0;\n      let rotateX = isHorizontal ? 0 : rotate * offsetMultiplier;\n      // var rotateZ = 0\n      let translateZ = -translate * Math.abs(offsetMultiplier);\n      let stretch = params.stretch;\n      // Allow percentage to make a relative stretch for responsive sliders\n      if (typeof stretch === 'string' && stretch.indexOf('%') !== -1) {\n        stretch = parseFloat(params.stretch) / 100 * slideSize;\n      }\n      let translateY = isHorizontal ? 0 : stretch * offsetMultiplier;\n      let translateX = isHorizontal ? stretch * offsetMultiplier : 0;\n      let scale = 1 - (1 - params.scale) * Math.abs(offsetMultiplier);\n\n      // Fix for ultra small values\n      if (Math.abs(translateX) < 0.001) translateX = 0;\n      if (Math.abs(translateY) < 0.001) translateY = 0;\n      if (Math.abs(translateZ) < 0.001) translateZ = 0;\n      if (Math.abs(rotateY) < 0.001) rotateY = 0;\n      if (Math.abs(rotateX) < 0.001) rotateX = 0;\n      if (Math.abs(scale) < 0.001) scale = 0;\n      const slideTransform = `translate3d(${translateX}px,${translateY}px,${translateZ}px)  rotateX(${rotateX}deg) rotateY(${rotateY}deg) scale(${scale})`;\n      const targetEl = (0,_shared_effect_target_mjs__WEBPACK_IMPORTED_MODULE_2__.e)(params, slideEl);\n      targetEl.style.transform = slideTransform;\n      slideEl.style.zIndex = -Math.abs(Math.round(offsetMultiplier)) + 1;\n      if (params.slideShadows) {\n        // Set shadows\n        let shadowBeforeEl = isHorizontal ? slideEl.querySelector('.swiper-slide-shadow-left') : slideEl.querySelector('.swiper-slide-shadow-top');\n        let shadowAfterEl = isHorizontal ? slideEl.querySelector('.swiper-slide-shadow-right') : slideEl.querySelector('.swiper-slide-shadow-bottom');\n        if (!shadowBeforeEl) {\n          shadowBeforeEl = (0,_shared_create_shadow_mjs__WEBPACK_IMPORTED_MODULE_0__.c)('coverflow', slideEl, isHorizontal ? 'left' : 'top');\n        }\n        if (!shadowAfterEl) {\n          shadowAfterEl = (0,_shared_create_shadow_mjs__WEBPACK_IMPORTED_MODULE_0__.c)('coverflow', slideEl, isHorizontal ? 'right' : 'bottom');\n        }\n        if (shadowBeforeEl) shadowBeforeEl.style.opacity = offsetMultiplier > 0 ? offsetMultiplier : 0;\n        if (shadowAfterEl) shadowAfterEl.style.opacity = -offsetMultiplier > 0 ? -offsetMultiplier : 0;\n      }\n    }\n  };\n  const setTransition = duration => {\n    const transformElements = swiper.slides.map(slideEl => (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_3__.k)(slideEl));\n    transformElements.forEach(el => {\n      el.style.transitionDuration = `${duration}ms`;\n      el.querySelectorAll('.swiper-slide-shadow-top, .swiper-slide-shadow-right, .swiper-slide-shadow-bottom, .swiper-slide-shadow-left').forEach(shadowEl => {\n        shadowEl.style.transitionDuration = `${duration}ms`;\n      });\n    });\n  };\n  (0,_shared_effect_init_mjs__WEBPACK_IMPORTED_MODULE_1__.e)({\n    effect: 'coverflow',\n    swiper,\n    on,\n    setTranslate,\n    setTransition,\n    perspective: () => true,\n    overwriteParams: () => ({\n      watchSlidesProgress: true\n    })\n  });\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/modules/effect-coverflow.mjs?");

/***/ }),

/***/ "./node_modules/swiper/modules/effect-creative.mjs":
/*!*********************************************************!*\
  !*** ./node_modules/swiper/modules/effect-creative.mjs ***!
  \*********************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ EffectCreative)\n/* harmony export */ });\n/* harmony import */ var _shared_create_shadow_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/create-shadow.mjs */ \"./node_modules/swiper/shared/create-shadow.mjs\");\n/* harmony import */ var _shared_effect_init_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/effect-init.mjs */ \"./node_modules/swiper/shared/effect-init.mjs\");\n/* harmony import */ var _shared_effect_target_mjs__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../shared/effect-target.mjs */ \"./node_modules/swiper/shared/effect-target.mjs\");\n/* harmony import */ var _shared_effect_virtual_transition_end_mjs__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../shared/effect-virtual-transition-end.mjs */ \"./node_modules/swiper/shared/effect-virtual-transition-end.mjs\");\n/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../shared/utils.mjs */ \"./node_modules/swiper/shared/utils.mjs\");\n\n\n\n\n\n\nfunction EffectCreative(_ref) {\n  let {\n    swiper,\n    extendParams,\n    on\n  } = _ref;\n  extendParams({\n    creativeEffect: {\n      limitProgress: 1,\n      shadowPerProgress: false,\n      progressMultiplier: 1,\n      perspective: true,\n      prev: {\n        translate: [0, 0, 0],\n        rotate: [0, 0, 0],\n        opacity: 1,\n        scale: 1\n      },\n      next: {\n        translate: [0, 0, 0],\n        rotate: [0, 0, 0],\n        opacity: 1,\n        scale: 1\n      }\n    }\n  });\n  const getTranslateValue = value => {\n    if (typeof value === 'string') return value;\n    return `${value}px`;\n  };\n  const setTranslate = () => {\n    const {\n      slides,\n      wrapperEl,\n      slidesSizesGrid\n    } = swiper;\n    const params = swiper.params.creativeEffect;\n    const {\n      progressMultiplier: multiplier\n    } = params;\n    const isCenteredSlides = swiper.params.centeredSlides;\n    if (isCenteredSlides) {\n      const margin = slidesSizesGrid[0] / 2 - swiper.params.slidesOffsetBefore || 0;\n      wrapperEl.style.transform = `translateX(calc(50% - ${margin}px))`;\n    }\n    for (let i = 0; i < slides.length; i += 1) {\n      const slideEl = slides[i];\n      const slideProgress = slideEl.progress;\n      const progress = Math.min(Math.max(slideEl.progress, -params.limitProgress), params.limitProgress);\n      let originalProgress = progress;\n      if (!isCenteredSlides) {\n        originalProgress = Math.min(Math.max(slideEl.originalProgress, -params.limitProgress), params.limitProgress);\n      }\n      const offset = slideEl.swiperSlideOffset;\n      const t = [swiper.params.cssMode ? -offset - swiper.translate : -offset, 0, 0];\n      const r = [0, 0, 0];\n      let custom = false;\n      if (!swiper.isHorizontal()) {\n        t[1] = t[0];\n        t[0] = 0;\n      }\n      let data = {\n        translate: [0, 0, 0],\n        rotate: [0, 0, 0],\n        scale: 1,\n        opacity: 1\n      };\n      if (progress < 0) {\n        data = params.next;\n        custom = true;\n      } else if (progress > 0) {\n        data = params.prev;\n        custom = true;\n      }\n      // set translate\n      t.forEach((value, index) => {\n        t[index] = `calc(${value}px + (${getTranslateValue(data.translate[index])} * ${Math.abs(progress * multiplier)}))`;\n      });\n      // set rotates\n      r.forEach((value, index) => {\n        r[index] = data.rotate[index] * Math.abs(progress * multiplier);\n      });\n      slideEl.style.zIndex = -Math.abs(Math.round(slideProgress)) + slides.length;\n      const translateString = t.join(', ');\n      const rotateString = `rotateX(${r[0]}deg) rotateY(${r[1]}deg) rotateZ(${r[2]}deg)`;\n      const scaleString = originalProgress < 0 ? `scale(${1 + (1 - data.scale) * originalProgress * multiplier})` : `scale(${1 - (1 - data.scale) * originalProgress * multiplier})`;\n      const opacityString = originalProgress < 0 ? 1 + (1 - data.opacity) * originalProgress * multiplier : 1 - (1 - data.opacity) * originalProgress * multiplier;\n      const transform = `translate3d(${translateString}) ${rotateString} ${scaleString}`;\n\n      // Set shadows\n      if (custom && data.shadow || !custom) {\n        let shadowEl = slideEl.querySelector('.swiper-slide-shadow');\n        if (!shadowEl && data.shadow) {\n          shadowEl = (0,_shared_create_shadow_mjs__WEBPACK_IMPORTED_MODULE_0__.c)('creative', slideEl);\n        }\n        if (shadowEl) {\n          const shadowOpacity = params.shadowPerProgress ? progress * (1 / params.limitProgress) : progress;\n          shadowEl.style.opacity = Math.min(Math.max(Math.abs(shadowOpacity), 0), 1);\n        }\n      }\n      const targetEl = (0,_shared_effect_target_mjs__WEBPACK_IMPORTED_MODULE_2__.e)(params, slideEl);\n      targetEl.style.transform = transform;\n      targetEl.style.opacity = opacityString;\n      if (data.origin) {\n        targetEl.style.transformOrigin = data.origin;\n      }\n    }\n  };\n  const setTransition = duration => {\n    const transformElements = swiper.slides.map(slideEl => (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_4__.k)(slideEl));\n    transformElements.forEach(el => {\n      el.style.transitionDuration = `${duration}ms`;\n      el.querySelectorAll('.swiper-slide-shadow').forEach(shadowEl => {\n        shadowEl.style.transitionDuration = `${duration}ms`;\n      });\n    });\n    (0,_shared_effect_virtual_transition_end_mjs__WEBPACK_IMPORTED_MODULE_3__.e)({\n      swiper,\n      duration,\n      transformElements,\n      allSlides: true\n    });\n  };\n  (0,_shared_effect_init_mjs__WEBPACK_IMPORTED_MODULE_1__.e)({\n    effect: 'creative',\n    swiper,\n    on,\n    setTranslate,\n    setTransition,\n    perspective: () => swiper.params.creativeEffect.perspective,\n    overwriteParams: () => ({\n      watchSlidesProgress: true,\n      virtualTranslate: !swiper.params.cssMode\n    })\n  });\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/modules/effect-creative.mjs?");

/***/ }),

/***/ "./node_modules/swiper/modules/effect-cube.mjs":
/*!*****************************************************!*\
  !*** ./node_modules/swiper/modules/effect-cube.mjs ***!
  \*****************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ EffectCube)\n/* harmony export */ });\n/* harmony import */ var _shared_effect_init_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/effect-init.mjs */ \"./node_modules/swiper/shared/effect-init.mjs\");\n/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/utils.mjs */ \"./node_modules/swiper/shared/utils.mjs\");\n\n\n\nfunction EffectCube(_ref) {\n  let {\n    swiper,\n    extendParams,\n    on\n  } = _ref;\n  extendParams({\n    cubeEffect: {\n      slideShadows: true,\n      shadow: true,\n      shadowOffset: 20,\n      shadowScale: 0.94\n    }\n  });\n  const createSlideShadows = (slideEl, progress, isHorizontal) => {\n    let shadowBefore = isHorizontal ? slideEl.querySelector('.swiper-slide-shadow-left') : slideEl.querySelector('.swiper-slide-shadow-top');\n    let shadowAfter = isHorizontal ? slideEl.querySelector('.swiper-slide-shadow-right') : slideEl.querySelector('.swiper-slide-shadow-bottom');\n    if (!shadowBefore) {\n      shadowBefore = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.c)('div', `swiper-slide-shadow-cube swiper-slide-shadow-${isHorizontal ? 'left' : 'top'}`.split(' '));\n      slideEl.append(shadowBefore);\n    }\n    if (!shadowAfter) {\n      shadowAfter = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.c)('div', `swiper-slide-shadow-cube swiper-slide-shadow-${isHorizontal ? 'right' : 'bottom'}`.split(' '));\n      slideEl.append(shadowAfter);\n    }\n    if (shadowBefore) shadowBefore.style.opacity = Math.max(-progress, 0);\n    if (shadowAfter) shadowAfter.style.opacity = Math.max(progress, 0);\n  };\n  const recreateShadows = () => {\n    // create new ones\n    const isHorizontal = swiper.isHorizontal();\n    swiper.slides.forEach(slideEl => {\n      const progress = Math.max(Math.min(slideEl.progress, 1), -1);\n      createSlideShadows(slideEl, progress, isHorizontal);\n    });\n  };\n  const setTranslate = () => {\n    const {\n      el,\n      wrapperEl,\n      slides,\n      width: swiperWidth,\n      height: swiperHeight,\n      rtlTranslate: rtl,\n      size: swiperSize,\n      browser\n    } = swiper;\n    const params = swiper.params.cubeEffect;\n    const isHorizontal = swiper.isHorizontal();\n    const isVirtual = swiper.virtual && swiper.params.virtual.enabled;\n    let wrapperRotate = 0;\n    let cubeShadowEl;\n    if (params.shadow) {\n      if (isHorizontal) {\n        cubeShadowEl = swiper.wrapperEl.querySelector('.swiper-cube-shadow');\n        if (!cubeShadowEl) {\n          cubeShadowEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.c)('div', 'swiper-cube-shadow');\n          swiper.wrapperEl.append(cubeShadowEl);\n        }\n        cubeShadowEl.style.height = `${swiperWidth}px`;\n      } else {\n        cubeShadowEl = el.querySelector('.swiper-cube-shadow');\n        if (!cubeShadowEl) {\n          cubeShadowEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.c)('div', 'swiper-cube-shadow');\n          el.append(cubeShadowEl);\n        }\n      }\n    }\n    for (let i = 0; i < slides.length; i += 1) {\n      const slideEl = slides[i];\n      let slideIndex = i;\n      if (isVirtual) {\n        slideIndex = parseInt(slideEl.getAttribute('data-swiper-slide-index'), 10);\n      }\n      let slideAngle = slideIndex * 90;\n      let round = Math.floor(slideAngle / 360);\n      if (rtl) {\n        slideAngle = -slideAngle;\n        round = Math.floor(-slideAngle / 360);\n      }\n      const progress = Math.max(Math.min(slideEl.progress, 1), -1);\n      let tx = 0;\n      let ty = 0;\n      let tz = 0;\n      if (slideIndex % 4 === 0) {\n        tx = -round * 4 * swiperSize;\n        tz = 0;\n      } else if ((slideIndex - 1) % 4 === 0) {\n        tx = 0;\n        tz = -round * 4 * swiperSize;\n      } else if ((slideIndex - 2) % 4 === 0) {\n        tx = swiperSize + round * 4 * swiperSize;\n        tz = swiperSize;\n      } else if ((slideIndex - 3) % 4 === 0) {\n        tx = -swiperSize;\n        tz = 3 * swiperSize + swiperSize * 4 * round;\n      }\n      if (rtl) {\n        tx = -tx;\n      }\n      if (!isHorizontal) {\n        ty = tx;\n        tx = 0;\n      }\n      const transform = `rotateX(${isHorizontal ? 0 : -slideAngle}deg) rotateY(${isHorizontal ? slideAngle : 0}deg) translate3d(${tx}px, ${ty}px, ${tz}px)`;\n      if (progress <= 1 && progress > -1) {\n        wrapperRotate = slideIndex * 90 + progress * 90;\n        if (rtl) wrapperRotate = -slideIndex * 90 - progress * 90;\n      }\n      slideEl.style.transform = transform;\n      if (params.slideShadows) {\n        createSlideShadows(slideEl, progress, isHorizontal);\n      }\n    }\n    wrapperEl.style.transformOrigin = `50% 50% -${swiperSize / 2}px`;\n    wrapperEl.style['-webkit-transform-origin'] = `50% 50% -${swiperSize / 2}px`;\n    if (params.shadow) {\n      if (isHorizontal) {\n        cubeShadowEl.style.transform = `translate3d(0px, ${swiperWidth / 2 + params.shadowOffset}px, ${-swiperWidth / 2}px) rotateX(90deg) rotateZ(0deg) scale(${params.shadowScale})`;\n      } else {\n        const shadowAngle = Math.abs(wrapperRotate) - Math.floor(Math.abs(wrapperRotate) / 90) * 90;\n        const multiplier = 1.5 - (Math.sin(shadowAngle * 2 * Math.PI / 360) / 2 + Math.cos(shadowAngle * 2 * Math.PI / 360) / 2);\n        const scale1 = params.shadowScale;\n        const scale2 = params.shadowScale / multiplier;\n        const offset = params.shadowOffset;\n        cubeShadowEl.style.transform = `scale3d(${scale1}, 1, ${scale2}) translate3d(0px, ${swiperHeight / 2 + offset}px, ${-swiperHeight / 2 / scale2}px) rotateX(-90deg)`;\n      }\n    }\n    const zFactor = (browser.isSafari || browser.isWebView) && browser.needPerspectiveFix ? -swiperSize / 2 : 0;\n    wrapperEl.style.transform = `translate3d(0px,0,${zFactor}px) rotateX(${swiper.isHorizontal() ? 0 : wrapperRotate}deg) rotateY(${swiper.isHorizontal() ? -wrapperRotate : 0}deg)`;\n    wrapperEl.style.setProperty('--swiper-cube-translate-z', `${zFactor}px`);\n  };\n  const setTransition = duration => {\n    const {\n      el,\n      slides\n    } = swiper;\n    slides.forEach(slideEl => {\n      slideEl.style.transitionDuration = `${duration}ms`;\n      slideEl.querySelectorAll('.swiper-slide-shadow-top, .swiper-slide-shadow-right, .swiper-slide-shadow-bottom, .swiper-slide-shadow-left').forEach(subEl => {\n        subEl.style.transitionDuration = `${duration}ms`;\n      });\n    });\n    if (swiper.params.cubeEffect.shadow && !swiper.isHorizontal()) {\n      const shadowEl = el.querySelector('.swiper-cube-shadow');\n      if (shadowEl) shadowEl.style.transitionDuration = `${duration}ms`;\n    }\n  };\n  (0,_shared_effect_init_mjs__WEBPACK_IMPORTED_MODULE_0__.e)({\n    effect: 'cube',\n    swiper,\n    on,\n    setTranslate,\n    setTransition,\n    recreateShadows,\n    getEffectParams: () => swiper.params.cubeEffect,\n    perspective: () => true,\n    overwriteParams: () => ({\n      slidesPerView: 1,\n      slidesPerGroup: 1,\n      watchSlidesProgress: true,\n      resistanceRatio: 0,\n      spaceBetween: 0,\n      centeredSlides: false,\n      virtualTranslate: true\n    })\n  });\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/modules/effect-cube.mjs?");

/***/ }),

/***/ "./node_modules/swiper/modules/effect-fade.mjs":
/*!*****************************************************!*\
  !*** ./node_modules/swiper/modules/effect-fade.mjs ***!
  \*****************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ EffectFade)\n/* harmony export */ });\n/* harmony import */ var _shared_effect_init_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/effect-init.mjs */ \"./node_modules/swiper/shared/effect-init.mjs\");\n/* harmony import */ var _shared_effect_target_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/effect-target.mjs */ \"./node_modules/swiper/shared/effect-target.mjs\");\n/* harmony import */ var _shared_effect_virtual_transition_end_mjs__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../shared/effect-virtual-transition-end.mjs */ \"./node_modules/swiper/shared/effect-virtual-transition-end.mjs\");\n/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../shared/utils.mjs */ \"./node_modules/swiper/shared/utils.mjs\");\n\n\n\n\n\nfunction EffectFade(_ref) {\n  let {\n    swiper,\n    extendParams,\n    on\n  } = _ref;\n  extendParams({\n    fadeEffect: {\n      crossFade: false\n    }\n  });\n  const setTranslate = () => {\n    const {\n      slides\n    } = swiper;\n    const params = swiper.params.fadeEffect;\n    for (let i = 0; i < slides.length; i += 1) {\n      const slideEl = swiper.slides[i];\n      const offset = slideEl.swiperSlideOffset;\n      let tx = -offset;\n      if (!swiper.params.virtualTranslate) tx -= swiper.translate;\n      let ty = 0;\n      if (!swiper.isHorizontal()) {\n        ty = tx;\n        tx = 0;\n      }\n      const slideOpacity = swiper.params.fadeEffect.crossFade ? Math.max(1 - Math.abs(slideEl.progress), 0) : 1 + Math.min(Math.max(slideEl.progress, -1), 0);\n      const targetEl = (0,_shared_effect_target_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(params, slideEl);\n      targetEl.style.opacity = slideOpacity;\n      targetEl.style.transform = `translate3d(${tx}px, ${ty}px, 0px)`;\n    }\n  };\n  const setTransition = duration => {\n    const transformElements = swiper.slides.map(slideEl => (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_3__.k)(slideEl));\n    transformElements.forEach(el => {\n      el.style.transitionDuration = `${duration}ms`;\n    });\n    (0,_shared_effect_virtual_transition_end_mjs__WEBPACK_IMPORTED_MODULE_2__.e)({\n      swiper,\n      duration,\n      transformElements,\n      allSlides: true\n    });\n  };\n  (0,_shared_effect_init_mjs__WEBPACK_IMPORTED_MODULE_0__.e)({\n    effect: 'fade',\n    swiper,\n    on,\n    setTranslate,\n    setTransition,\n    overwriteParams: () => ({\n      slidesPerView: 1,\n      slidesPerGroup: 1,\n      watchSlidesProgress: true,\n      spaceBetween: 0,\n      virtualTranslate: !swiper.params.cssMode\n    })\n  });\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/modules/effect-fade.mjs?");

/***/ }),

/***/ "./node_modules/swiper/modules/effect-flip.mjs":
/*!*****************************************************!*\
  !*** ./node_modules/swiper/modules/effect-flip.mjs ***!
  \*****************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ EffectFlip)\n/* harmony export */ });\n/* harmony import */ var _shared_create_shadow_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/create-shadow.mjs */ \"./node_modules/swiper/shared/create-shadow.mjs\");\n/* harmony import */ var _shared_effect_init_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/effect-init.mjs */ \"./node_modules/swiper/shared/effect-init.mjs\");\n/* harmony import */ var _shared_effect_target_mjs__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../shared/effect-target.mjs */ \"./node_modules/swiper/shared/effect-target.mjs\");\n/* harmony import */ var _shared_effect_virtual_transition_end_mjs__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../shared/effect-virtual-transition-end.mjs */ \"./node_modules/swiper/shared/effect-virtual-transition-end.mjs\");\n/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../shared/utils.mjs */ \"./node_modules/swiper/shared/utils.mjs\");\n\n\n\n\n\n\nfunction EffectFlip(_ref) {\n  let {\n    swiper,\n    extendParams,\n    on\n  } = _ref;\n  extendParams({\n    flipEffect: {\n      slideShadows: true,\n      limitRotation: true\n    }\n  });\n  const createSlideShadows = (slideEl, progress) => {\n    let shadowBefore = swiper.isHorizontal() ? slideEl.querySelector('.swiper-slide-shadow-left') : slideEl.querySelector('.swiper-slide-shadow-top');\n    let shadowAfter = swiper.isHorizontal() ? slideEl.querySelector('.swiper-slide-shadow-right') : slideEl.querySelector('.swiper-slide-shadow-bottom');\n    if (!shadowBefore) {\n      shadowBefore = (0,_shared_create_shadow_mjs__WEBPACK_IMPORTED_MODULE_0__.c)('flip', slideEl, swiper.isHorizontal() ? 'left' : 'top');\n    }\n    if (!shadowAfter) {\n      shadowAfter = (0,_shared_create_shadow_mjs__WEBPACK_IMPORTED_MODULE_0__.c)('flip', slideEl, swiper.isHorizontal() ? 'right' : 'bottom');\n    }\n    if (shadowBefore) shadowBefore.style.opacity = Math.max(-progress, 0);\n    if (shadowAfter) shadowAfter.style.opacity = Math.max(progress, 0);\n  };\n  const recreateShadows = () => {\n    // Set shadows\n    swiper.params.flipEffect;\n    swiper.slides.forEach(slideEl => {\n      let progress = slideEl.progress;\n      if (swiper.params.flipEffect.limitRotation) {\n        progress = Math.max(Math.min(slideEl.progress, 1), -1);\n      }\n      createSlideShadows(slideEl, progress);\n    });\n  };\n  const setTranslate = () => {\n    const {\n      slides,\n      rtlTranslate: rtl\n    } = swiper;\n    const params = swiper.params.flipEffect;\n    for (let i = 0; i < slides.length; i += 1) {\n      const slideEl = slides[i];\n      let progress = slideEl.progress;\n      if (swiper.params.flipEffect.limitRotation) {\n        progress = Math.max(Math.min(slideEl.progress, 1), -1);\n      }\n      const offset = slideEl.swiperSlideOffset;\n      const rotate = -180 * progress;\n      let rotateY = rotate;\n      let rotateX = 0;\n      let tx = swiper.params.cssMode ? -offset - swiper.translate : -offset;\n      let ty = 0;\n      if (!swiper.isHorizontal()) {\n        ty = tx;\n        tx = 0;\n        rotateX = -rotateY;\n        rotateY = 0;\n      } else if (rtl) {\n        rotateY = -rotateY;\n      }\n      slideEl.style.zIndex = -Math.abs(Math.round(progress)) + slides.length;\n      if (params.slideShadows) {\n        createSlideShadows(slideEl, progress);\n      }\n      const transform = `translate3d(${tx}px, ${ty}px, 0px) rotateX(${rotateX}deg) rotateY(${rotateY}deg)`;\n      const targetEl = (0,_shared_effect_target_mjs__WEBPACK_IMPORTED_MODULE_2__.e)(params, slideEl);\n      targetEl.style.transform = transform;\n    }\n  };\n  const setTransition = duration => {\n    const transformElements = swiper.slides.map(slideEl => (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_4__.k)(slideEl));\n    transformElements.forEach(el => {\n      el.style.transitionDuration = `${duration}ms`;\n      el.querySelectorAll('.swiper-slide-shadow-top, .swiper-slide-shadow-right, .swiper-slide-shadow-bottom, .swiper-slide-shadow-left').forEach(shadowEl => {\n        shadowEl.style.transitionDuration = `${duration}ms`;\n      });\n    });\n    (0,_shared_effect_virtual_transition_end_mjs__WEBPACK_IMPORTED_MODULE_3__.e)({\n      swiper,\n      duration,\n      transformElements\n    });\n  };\n  (0,_shared_effect_init_mjs__WEBPACK_IMPORTED_MODULE_1__.e)({\n    effect: 'flip',\n    swiper,\n    on,\n    setTranslate,\n    setTransition,\n    recreateShadows,\n    getEffectParams: () => swiper.params.flipEffect,\n    perspective: () => true,\n    overwriteParams: () => ({\n      slidesPerView: 1,\n      slidesPerGroup: 1,\n      watchSlidesProgress: true,\n      spaceBetween: 0,\n      virtualTranslate: !swiper.params.cssMode\n    })\n  });\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/modules/effect-flip.mjs?");

/***/ }),

/***/ "./node_modules/swiper/modules/free-mode.mjs":
/*!***************************************************!*\
  !*** ./node_modules/swiper/modules/free-mode.mjs ***!
  \***************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ freeMode)\n/* harmony export */ });\n/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/utils.mjs */ \"./node_modules/swiper/shared/utils.mjs\");\n\n\nfunction freeMode(_ref) {\n  let {\n    swiper,\n    extendParams,\n    emit,\n    once\n  } = _ref;\n  extendParams({\n    freeMode: {\n      enabled: false,\n      momentum: true,\n      momentumRatio: 1,\n      momentumBounce: true,\n      momentumBounceRatio: 1,\n      momentumVelocityRatio: 1,\n      sticky: false,\n      minimumVelocity: 0.02\n    }\n  });\n  function onTouchStart() {\n    if (swiper.params.cssMode) return;\n    const translate = swiper.getTranslate();\n    swiper.setTranslate(translate);\n    swiper.setTransition(0);\n    swiper.touchEventsData.velocities.length = 0;\n    swiper.freeMode.onTouchEnd({\n      currentPos: swiper.rtl ? swiper.translate : -swiper.translate\n    });\n  }\n  function onTouchMove() {\n    if (swiper.params.cssMode) return;\n    const {\n      touchEventsData: data,\n      touches\n    } = swiper;\n    // Velocity\n    if (data.velocities.length === 0) {\n      data.velocities.push({\n        position: touches[swiper.isHorizontal() ? 'startX' : 'startY'],\n        time: data.touchStartTime\n      });\n    }\n    data.velocities.push({\n      position: touches[swiper.isHorizontal() ? 'currentX' : 'currentY'],\n      time: (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.d)()\n    });\n  }\n  function onTouchEnd(_ref2) {\n    let {\n      currentPos\n    } = _ref2;\n    if (swiper.params.cssMode) return;\n    const {\n      params,\n      wrapperEl,\n      rtlTranslate: rtl,\n      snapGrid,\n      touchEventsData: data\n    } = swiper;\n    // Time diff\n    const touchEndTime = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.d)();\n    const timeDiff = touchEndTime - data.touchStartTime;\n    if (currentPos < -swiper.minTranslate()) {\n      swiper.slideTo(swiper.activeIndex);\n      return;\n    }\n    if (currentPos > -swiper.maxTranslate()) {\n      if (swiper.slides.length < snapGrid.length) {\n        swiper.slideTo(snapGrid.length - 1);\n      } else {\n        swiper.slideTo(swiper.slides.length - 1);\n      }\n      return;\n    }\n    if (params.freeMode.momentum) {\n      if (data.velocities.length > 1) {\n        const lastMoveEvent = data.velocities.pop();\n        const velocityEvent = data.velocities.pop();\n        const distance = lastMoveEvent.position - velocityEvent.position;\n        const time = lastMoveEvent.time - velocityEvent.time;\n        swiper.velocity = distance / time;\n        swiper.velocity /= 2;\n        if (Math.abs(swiper.velocity) < params.freeMode.minimumVelocity) {\n          swiper.velocity = 0;\n        }\n        // this implies that the user stopped moving a finger then released.\n        // There would be no events with distance zero, so the last event is stale.\n        if (time > 150 || (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.d)() - lastMoveEvent.time > 300) {\n          swiper.velocity = 0;\n        }\n      } else {\n        swiper.velocity = 0;\n      }\n      swiper.velocity *= params.freeMode.momentumVelocityRatio;\n      data.velocities.length = 0;\n      let momentumDuration = 1000 * params.freeMode.momentumRatio;\n      const momentumDistance = swiper.velocity * momentumDuration;\n      let newPosition = swiper.translate + momentumDistance;\n      if (rtl) newPosition = -newPosition;\n      let doBounce = false;\n      let afterBouncePosition;\n      const bounceAmount = Math.abs(swiper.velocity) * 20 * params.freeMode.momentumBounceRatio;\n      let needsLoopFix;\n      if (newPosition < swiper.maxTranslate()) {\n        if (params.freeMode.momentumBounce) {\n          if (newPosition + swiper.maxTranslate() < -bounceAmount) {\n            newPosition = swiper.maxTranslate() - bounceAmount;\n          }\n          afterBouncePosition = swiper.maxTranslate();\n          doBounce = true;\n          data.allowMomentumBounce = true;\n        } else {\n          newPosition = swiper.maxTranslate();\n        }\n        if (params.loop && params.centeredSlides) needsLoopFix = true;\n      } else if (newPosition > swiper.minTranslate()) {\n        if (params.freeMode.momentumBounce) {\n          if (newPosition - swiper.minTranslate() > bounceAmount) {\n            newPosition = swiper.minTranslate() + bounceAmount;\n          }\n          afterBouncePosition = swiper.minTranslate();\n          doBounce = true;\n          data.allowMomentumBounce = true;\n        } else {\n          newPosition = swiper.minTranslate();\n        }\n        if (params.loop && params.centeredSlides) needsLoopFix = true;\n      } else if (params.freeMode.sticky) {\n        let nextSlide;\n        for (let j = 0; j < snapGrid.length; j += 1) {\n          if (snapGrid[j] > -newPosition) {\n            nextSlide = j;\n            break;\n          }\n        }\n        if (Math.abs(snapGrid[nextSlide] - newPosition) < Math.abs(snapGrid[nextSlide - 1] - newPosition) || swiper.swipeDirection === 'next') {\n          newPosition = snapGrid[nextSlide];\n        } else {\n          newPosition = snapGrid[nextSlide - 1];\n        }\n        newPosition = -newPosition;\n      }\n      if (needsLoopFix) {\n        once('transitionEnd', () => {\n          swiper.loopFix();\n        });\n      }\n      // Fix duration\n      if (swiper.velocity !== 0) {\n        if (rtl) {\n          momentumDuration = Math.abs((-newPosition - swiper.translate) / swiper.velocity);\n        } else {\n          momentumDuration = Math.abs((newPosition - swiper.translate) / swiper.velocity);\n        }\n        if (params.freeMode.sticky) {\n          // If freeMode.sticky is active and the user ends a swipe with a slow-velocity\n          // event, then durations can be 20+ seconds to slide one (or zero!) slides.\n          // It's easy to see this when simulating touch with mouse events. To fix this,\n          // limit single-slide swipes to the default slide duration. This also has the\n          // nice side effect of matching slide speed if the user stopped moving before\n          // lifting finger or mouse vs. moving slowly before lifting the finger/mouse.\n          // For faster swipes, also apply limits (albeit higher ones).\n          const moveDistance = Math.abs((rtl ? -newPosition : newPosition) - swiper.translate);\n          const currentSlideSize = swiper.slidesSizesGrid[swiper.activeIndex];\n          if (moveDistance < currentSlideSize) {\n            momentumDuration = params.speed;\n          } else if (moveDistance < 2 * currentSlideSize) {\n            momentumDuration = params.speed * 1.5;\n          } else {\n            momentumDuration = params.speed * 2.5;\n          }\n        }\n      } else if (params.freeMode.sticky) {\n        swiper.slideToClosest();\n        return;\n      }\n      if (params.freeMode.momentumBounce && doBounce) {\n        swiper.updateProgress(afterBouncePosition);\n        swiper.setTransition(momentumDuration);\n        swiper.setTranslate(newPosition);\n        swiper.transitionStart(true, swiper.swipeDirection);\n        swiper.animating = true;\n        (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.i)(wrapperEl, () => {\n          if (!swiper || swiper.destroyed || !data.allowMomentumBounce) return;\n          emit('momentumBounce');\n          swiper.setTransition(params.speed);\n          setTimeout(() => {\n            swiper.setTranslate(afterBouncePosition);\n            (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.i)(wrapperEl, () => {\n              if (!swiper || swiper.destroyed) return;\n              swiper.transitionEnd();\n            });\n          }, 0);\n        });\n      } else if (swiper.velocity) {\n        emit('_freeModeNoMomentumRelease');\n        swiper.updateProgress(newPosition);\n        swiper.setTransition(momentumDuration);\n        swiper.setTranslate(newPosition);\n        swiper.transitionStart(true, swiper.swipeDirection);\n        if (!swiper.animating) {\n          swiper.animating = true;\n          (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.i)(wrapperEl, () => {\n            if (!swiper || swiper.destroyed) return;\n            swiper.transitionEnd();\n          });\n        }\n      } else {\n        swiper.updateProgress(newPosition);\n      }\n      swiper.updateActiveIndex();\n      swiper.updateSlidesClasses();\n    } else if (params.freeMode.sticky) {\n      swiper.slideToClosest();\n      return;\n    } else if (params.freeMode) {\n      emit('_freeModeNoMomentumRelease');\n    }\n    if (!params.freeMode.momentum || timeDiff >= params.longSwipesMs) {\n      swiper.updateProgress();\n      swiper.updateActiveIndex();\n      swiper.updateSlidesClasses();\n    }\n  }\n  Object.assign(swiper, {\n    freeMode: {\n      onTouchStart,\n      onTouchMove,\n      onTouchEnd\n    }\n  });\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/modules/free-mode.mjs?");

/***/ }),

/***/ "./node_modules/swiper/modules/grid.mjs":
/*!**********************************************!*\
  !*** ./node_modules/swiper/modules/grid.mjs ***!
  \**********************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ Grid)\n/* harmony export */ });\nfunction Grid(_ref) {\n  let {\n    swiper,\n    extendParams\n  } = _ref;\n  extendParams({\n    grid: {\n      rows: 1,\n      fill: 'column'\n    }\n  });\n  let slidesNumberEvenToRows;\n  let slidesPerRow;\n  let numFullColumns;\n  const getSpaceBetween = () => {\n    let spaceBetween = swiper.params.spaceBetween;\n    if (typeof spaceBetween === 'string' && spaceBetween.indexOf('%') >= 0) {\n      spaceBetween = parseFloat(spaceBetween.replace('%', '')) / 100 * swiper.size;\n    } else if (typeof spaceBetween === 'string') {\n      spaceBetween = parseFloat(spaceBetween);\n    }\n    return spaceBetween;\n  };\n  const initSlides = slidesLength => {\n    const {\n      slidesPerView\n    } = swiper.params;\n    const {\n      rows,\n      fill\n    } = swiper.params.grid;\n    numFullColumns = Math.floor(slidesLength / rows);\n    if (Math.floor(slidesLength / rows) === slidesLength / rows) {\n      slidesNumberEvenToRows = slidesLength;\n    } else {\n      slidesNumberEvenToRows = Math.ceil(slidesLength / rows) * rows;\n    }\n    if (slidesPerView !== 'auto' && fill === 'row') {\n      slidesNumberEvenToRows = Math.max(slidesNumberEvenToRows, slidesPerView * rows);\n    }\n    slidesPerRow = slidesNumberEvenToRows / rows;\n  };\n  const updateSlide = (i, slide, slidesLength, getDirectionLabel) => {\n    const {\n      slidesPerGroup\n    } = swiper.params;\n    const spaceBetween = getSpaceBetween();\n    const {\n      rows,\n      fill\n    } = swiper.params.grid;\n    // Set slides order\n    let newSlideOrderIndex;\n    let column;\n    let row;\n    if (fill === 'row' && slidesPerGroup > 1) {\n      const groupIndex = Math.floor(i / (slidesPerGroup * rows));\n      const slideIndexInGroup = i - rows * slidesPerGroup * groupIndex;\n      const columnsInGroup = groupIndex === 0 ? slidesPerGroup : Math.min(Math.ceil((slidesLength - groupIndex * rows * slidesPerGroup) / rows), slidesPerGroup);\n      row = Math.floor(slideIndexInGroup / columnsInGroup);\n      column = slideIndexInGroup - row * columnsInGroup + groupIndex * slidesPerGroup;\n      newSlideOrderIndex = column + row * slidesNumberEvenToRows / rows;\n      slide.style.order = newSlideOrderIndex;\n    } else if (fill === 'column') {\n      column = Math.floor(i / rows);\n      row = i - column * rows;\n      if (column > numFullColumns || column === numFullColumns && row === rows - 1) {\n        row += 1;\n        if (row >= rows) {\n          row = 0;\n          column += 1;\n        }\n      }\n    } else {\n      row = Math.floor(i / slidesPerRow);\n      column = i - row * slidesPerRow;\n    }\n    slide.row = row;\n    slide.column = column;\n    slide.style[getDirectionLabel('margin-top')] = row !== 0 ? spaceBetween && `${spaceBetween}px` : '';\n  };\n  const updateWrapperSize = (slideSize, snapGrid, getDirectionLabel) => {\n    const {\n      centeredSlides,\n      roundLengths\n    } = swiper.params;\n    const spaceBetween = getSpaceBetween();\n    const {\n      rows\n    } = swiper.params.grid;\n    swiper.virtualSize = (slideSize + spaceBetween) * slidesNumberEvenToRows;\n    swiper.virtualSize = Math.ceil(swiper.virtualSize / rows) - spaceBetween;\n    swiper.wrapperEl.style[getDirectionLabel('width')] = `${swiper.virtualSize + spaceBetween}px`;\n    if (centeredSlides) {\n      const newSlidesGrid = [];\n      for (let i = 0; i < snapGrid.length; i += 1) {\n        let slidesGridItem = snapGrid[i];\n        if (roundLengths) slidesGridItem = Math.floor(slidesGridItem);\n        if (snapGrid[i] < swiper.virtualSize + snapGrid[0]) newSlidesGrid.push(slidesGridItem);\n      }\n      snapGrid.splice(0, snapGrid.length);\n      snapGrid.push(...newSlidesGrid);\n    }\n  };\n  swiper.grid = {\n    initSlides,\n    updateSlide,\n    updateWrapperSize\n  };\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/modules/grid.mjs?");

/***/ }),

/***/ "./node_modules/swiper/modules/hash-navigation.mjs":
/*!*********************************************************!*\
  !*** ./node_modules/swiper/modules/hash-navigation.mjs ***!
  \*********************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ HashNavigation)\n/* harmony export */ });\n/* harmony import */ var _shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/ssr-window.esm.mjs */ \"./node_modules/swiper/shared/ssr-window.esm.mjs\");\n/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/utils.mjs */ \"./node_modules/swiper/shared/utils.mjs\");\n\n\n\nfunction HashNavigation(_ref) {\n  let {\n    swiper,\n    extendParams,\n    emit,\n    on\n  } = _ref;\n  let initialized = false;\n  const document = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();\n  const window = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();\n  extendParams({\n    hashNavigation: {\n      enabled: false,\n      replaceState: false,\n      watchState: false,\n      getSlideIndex(_s, hash) {\n        if (swiper.virtual && swiper.params.virtual.enabled) {\n          const slideWithHash = swiper.slides.filter(slideEl => slideEl.getAttribute('data-hash') === hash)[0];\n          if (!slideWithHash) return 0;\n          const index = parseInt(slideWithHash.getAttribute('data-swiper-slide-index'), 10);\n          return index;\n        }\n        return swiper.getSlideIndex((0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(swiper.slidesEl, `.${swiper.params.slideClass}[data-hash=\"${hash}\"], swiper-slide[data-hash=\"${hash}\"]`)[0]);\n      }\n    }\n  });\n  const onHashChange = () => {\n    emit('hashChange');\n    const newHash = document.location.hash.replace('#', '');\n    const activeSlideEl = swiper.virtual && swiper.params.virtual.enabled ? swiper.slidesEl.querySelector(`[data-swiper-slide-index=\"${swiper.activeIndex}\"]`) : swiper.slides[swiper.activeIndex];\n    const activeSlideHash = activeSlideEl ? activeSlideEl.getAttribute('data-hash') : '';\n    if (newHash !== activeSlideHash) {\n      const newIndex = swiper.params.hashNavigation.getSlideIndex(swiper, newHash);\n      if (typeof newIndex === 'undefined' || Number.isNaN(newIndex)) return;\n      swiper.slideTo(newIndex);\n    }\n  };\n  const setHash = () => {\n    if (!initialized || !swiper.params.hashNavigation.enabled) return;\n    const activeSlideEl = swiper.virtual && swiper.params.virtual.enabled ? swiper.slidesEl.querySelector(`[data-swiper-slide-index=\"${swiper.activeIndex}\"]`) : swiper.slides[swiper.activeIndex];\n    const activeSlideHash = activeSlideEl ? activeSlideEl.getAttribute('data-hash') || activeSlideEl.getAttribute('data-history') : '';\n    if (swiper.params.hashNavigation.replaceState && window.history && window.history.replaceState) {\n      window.history.replaceState(null, null, `#${activeSlideHash}` || '');\n      emit('hashSet');\n    } else {\n      document.location.hash = activeSlideHash || '';\n      emit('hashSet');\n    }\n  };\n  const init = () => {\n    if (!swiper.params.hashNavigation.enabled || swiper.params.history && swiper.params.history.enabled) return;\n    initialized = true;\n    const hash = document.location.hash.replace('#', '');\n    if (hash) {\n      const speed = 0;\n      const index = swiper.params.hashNavigation.getSlideIndex(swiper, hash);\n      swiper.slideTo(index || 0, speed, swiper.params.runCallbacksOnInit, true);\n    }\n    if (swiper.params.hashNavigation.watchState) {\n      window.addEventListener('hashchange', onHashChange);\n    }\n  };\n  const destroy = () => {\n    if (swiper.params.hashNavigation.watchState) {\n      window.removeEventListener('hashchange', onHashChange);\n    }\n  };\n  on('init', () => {\n    if (swiper.params.hashNavigation.enabled) {\n      init();\n    }\n  });\n  on('destroy', () => {\n    if (swiper.params.hashNavigation.enabled) {\n      destroy();\n    }\n  });\n  on('transitionEnd _freeModeNoMomentumRelease', () => {\n    if (initialized) {\n      setHash();\n    }\n  });\n  on('slideChange', () => {\n    if (initialized && swiper.params.cssMode) {\n      setHash();\n    }\n  });\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/modules/hash-navigation.mjs?");

/***/ }),

/***/ "./node_modules/swiper/modules/history.mjs":
/*!*************************************************!*\
  !*** ./node_modules/swiper/modules/history.mjs ***!
  \*************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ History)\n/* harmony export */ });\n/* harmony import */ var _shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/ssr-window.esm.mjs */ \"./node_modules/swiper/shared/ssr-window.esm.mjs\");\n\n\nfunction History(_ref) {\n  let {\n    swiper,\n    extendParams,\n    on\n  } = _ref;\n  extendParams({\n    history: {\n      enabled: false,\n      root: '',\n      replaceState: false,\n      key: 'slides',\n      keepQuery: false\n    }\n  });\n  let initialized = false;\n  let paths = {};\n  const slugify = text => {\n    return text.toString().replace(/\\s+/g, '-').replace(/[^\\w-]+/g, '').replace(/--+/g, '-').replace(/^-+/, '').replace(/-+$/, '');\n  };\n  const getPathValues = urlOverride => {\n    const window = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();\n    let location;\n    if (urlOverride) {\n      location = new URL(urlOverride);\n    } else {\n      location = window.location;\n    }\n    const pathArray = location.pathname.slice(1).split('/').filter(part => part !== '');\n    const total = pathArray.length;\n    const key = pathArray[total - 2];\n    const value = pathArray[total - 1];\n    return {\n      key,\n      value\n    };\n  };\n  const setHistory = (key, index) => {\n    const window = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();\n    if (!initialized || !swiper.params.history.enabled) return;\n    let location;\n    if (swiper.params.url) {\n      location = new URL(swiper.params.url);\n    } else {\n      location = window.location;\n    }\n    const slide = swiper.slides[index];\n    let value = slugify(slide.getAttribute('data-history'));\n    if (swiper.params.history.root.length > 0) {\n      let root = swiper.params.history.root;\n      if (root[root.length - 1] === '/') root = root.slice(0, root.length - 1);\n      value = `${root}/${key ? `${key}/` : ''}${value}`;\n    } else if (!location.pathname.includes(key)) {\n      value = `${key ? `${key}/` : ''}${value}`;\n    }\n    if (swiper.params.history.keepQuery) {\n      value += location.search;\n    }\n    const currentState = window.history.state;\n    if (currentState && currentState.value === value) {\n      return;\n    }\n    if (swiper.params.history.replaceState) {\n      window.history.replaceState({\n        value\n      }, null, value);\n    } else {\n      window.history.pushState({\n        value\n      }, null, value);\n    }\n  };\n  const scrollToSlide = (speed, value, runCallbacks) => {\n    if (value) {\n      for (let i = 0, length = swiper.slides.length; i < length; i += 1) {\n        const slide = swiper.slides[i];\n        const slideHistory = slugify(slide.getAttribute('data-history'));\n        if (slideHistory === value) {\n          const index = swiper.getSlideIndex(slide);\n          swiper.slideTo(index, speed, runCallbacks);\n        }\n      }\n    } else {\n      swiper.slideTo(0, speed, runCallbacks);\n    }\n  };\n  const setHistoryPopState = () => {\n    paths = getPathValues(swiper.params.url);\n    scrollToSlide(swiper.params.speed, paths.value, false);\n  };\n  const init = () => {\n    const window = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();\n    if (!swiper.params.history) return;\n    if (!window.history || !window.history.pushState) {\n      swiper.params.history.enabled = false;\n      swiper.params.hashNavigation.enabled = true;\n      return;\n    }\n    initialized = true;\n    paths = getPathValues(swiper.params.url);\n    if (!paths.key && !paths.value) {\n      if (!swiper.params.history.replaceState) {\n        window.addEventListener('popstate', setHistoryPopState);\n      }\n      return;\n    }\n    scrollToSlide(0, paths.value, swiper.params.runCallbacksOnInit);\n    if (!swiper.params.history.replaceState) {\n      window.addEventListener('popstate', setHistoryPopState);\n    }\n  };\n  const destroy = () => {\n    const window = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();\n    if (!swiper.params.history.replaceState) {\n      window.removeEventListener('popstate', setHistoryPopState);\n    }\n  };\n  on('init', () => {\n    if (swiper.params.history.enabled) {\n      init();\n    }\n  });\n  on('destroy', () => {\n    if (swiper.params.history.enabled) {\n      destroy();\n    }\n  });\n  on('transitionEnd _freeModeNoMomentumRelease', () => {\n    if (initialized) {\n      setHistory(swiper.params.history.key, swiper.activeIndex);\n    }\n  });\n  on('slideChange', () => {\n    if (initialized && swiper.params.cssMode) {\n      setHistory(swiper.params.history.key, swiper.activeIndex);\n    }\n  });\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/modules/history.mjs?");

/***/ }),

/***/ "./node_modules/swiper/modules/index.mjs":
/*!***********************************************!*\
  !*** ./node_modules/swiper/modules/index.mjs ***!
  \***********************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   A11y: () => (/* reexport safe */ _a11y_mjs__WEBPACK_IMPORTED_MODULE_9__[\"default\"]),\n/* harmony export */   Autoplay: () => (/* reexport safe */ _autoplay_mjs__WEBPACK_IMPORTED_MODULE_12__[\"default\"]),\n/* harmony export */   Controller: () => (/* reexport safe */ _controller_mjs__WEBPACK_IMPORTED_MODULE_8__[\"default\"]),\n/* harmony export */   EffectCards: () => (/* reexport safe */ _effect_cards_mjs__WEBPACK_IMPORTED_MODULE_22__[\"default\"]),\n/* harmony export */   EffectCoverflow: () => (/* reexport safe */ _effect_coverflow_mjs__WEBPACK_IMPORTED_MODULE_20__[\"default\"]),\n/* harmony export */   EffectCreative: () => (/* reexport safe */ _effect_creative_mjs__WEBPACK_IMPORTED_MODULE_21__[\"default\"]),\n/* harmony export */   EffectCube: () => (/* reexport safe */ _effect_cube_mjs__WEBPACK_IMPORTED_MODULE_18__[\"default\"]),\n/* harmony export */   EffectFade: () => (/* reexport safe */ _effect_fade_mjs__WEBPACK_IMPORTED_MODULE_17__[\"default\"]),\n/* harmony export */   EffectFlip: () => (/* reexport safe */ _effect_flip_mjs__WEBPACK_IMPORTED_MODULE_19__[\"default\"]),\n/* harmony export */   FreeMode: () => (/* reexport safe */ _free_mode_mjs__WEBPACK_IMPORTED_MODULE_14__[\"default\"]),\n/* harmony export */   Grid: () => (/* reexport safe */ _grid_mjs__WEBPACK_IMPORTED_MODULE_15__[\"default\"]),\n/* harmony export */   HashNavigation: () => (/* reexport safe */ _hash_navigation_mjs__WEBPACK_IMPORTED_MODULE_11__[\"default\"]),\n/* harmony export */   History: () => (/* reexport safe */ _history_mjs__WEBPACK_IMPORTED_MODULE_10__[\"default\"]),\n/* harmony export */   Keyboard: () => (/* reexport safe */ _keyboard_mjs__WEBPACK_IMPORTED_MODULE_1__[\"default\"]),\n/* harmony export */   Manipulation: () => (/* reexport safe */ _manipulation_mjs__WEBPACK_IMPORTED_MODULE_16__[\"default\"]),\n/* harmony export */   Mousewheel: () => (/* reexport safe */ _mousewheel_mjs__WEBPACK_IMPORTED_MODULE_2__[\"default\"]),\n/* harmony export */   Navigation: () => (/* reexport safe */ _navigation_mjs__WEBPACK_IMPORTED_MODULE_3__[\"default\"]),\n/* harmony export */   Pagination: () => (/* reexport safe */ _pagination_mjs__WEBPACK_IMPORTED_MODULE_4__[\"default\"]),\n/* harmony export */   Parallax: () => (/* reexport safe */ _parallax_mjs__WEBPACK_IMPORTED_MODULE_6__[\"default\"]),\n/* harmony export */   Scrollbar: () => (/* reexport safe */ _scrollbar_mjs__WEBPACK_IMPORTED_MODULE_5__[\"default\"]),\n/* harmony export */   Thumbs: () => (/* reexport safe */ _thumbs_mjs__WEBPACK_IMPORTED_MODULE_13__[\"default\"]),\n/* harmony export */   Virtual: () => (/* reexport safe */ _virtual_mjs__WEBPACK_IMPORTED_MODULE_0__[\"default\"]),\n/* harmony export */   Zoom: () => (/* reexport safe */ _zoom_mjs__WEBPACK_IMPORTED_MODULE_7__[\"default\"])\n/* harmony export */ });\n/* harmony import */ var _virtual_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./virtual.mjs */ \"./node_modules/swiper/modules/virtual.mjs\");\n/* harmony import */ var _keyboard_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./keyboard.mjs */ \"./node_modules/swiper/modules/keyboard.mjs\");\n/* harmony import */ var _mousewheel_mjs__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./mousewheel.mjs */ \"./node_modules/swiper/modules/mousewheel.mjs\");\n/* harmony import */ var _navigation_mjs__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./navigation.mjs */ \"./node_modules/swiper/modules/navigation.mjs\");\n/* harmony import */ var _pagination_mjs__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./pagination.mjs */ \"./node_modules/swiper/modules/pagination.mjs\");\n/* harmony import */ var _scrollbar_mjs__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./scrollbar.mjs */ \"./node_modules/swiper/modules/scrollbar.mjs\");\n/* harmony import */ var _parallax_mjs__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./parallax.mjs */ \"./node_modules/swiper/modules/parallax.mjs\");\n/* harmony import */ var _zoom_mjs__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./zoom.mjs */ \"./node_modules/swiper/modules/zoom.mjs\");\n/* harmony import */ var _controller_mjs__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./controller.mjs */ \"./node_modules/swiper/modules/controller.mjs\");\n/* harmony import */ var _a11y_mjs__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./a11y.mjs */ \"./node_modules/swiper/modules/a11y.mjs\");\n/* harmony import */ var _history_mjs__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ./history.mjs */ \"./node_modules/swiper/modules/history.mjs\");\n/* harmony import */ var _hash_navigation_mjs__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ./hash-navigation.mjs */ \"./node_modules/swiper/modules/hash-navigation.mjs\");\n/* harmony import */ var _autoplay_mjs__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ./autoplay.mjs */ \"./node_modules/swiper/modules/autoplay.mjs\");\n/* harmony import */ var _thumbs_mjs__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! ./thumbs.mjs */ \"./node_modules/swiper/modules/thumbs.mjs\");\n/* harmony import */ var _free_mode_mjs__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! ./free-mode.mjs */ \"./node_modules/swiper/modules/free-mode.mjs\");\n/* harmony import */ var _grid_mjs__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! ./grid.mjs */ \"./node_modules/swiper/modules/grid.mjs\");\n/* harmony import */ var _manipulation_mjs__WEBPACK_IMPORTED_MODULE_16__ = __webpack_require__(/*! ./manipulation.mjs */ \"./node_modules/swiper/modules/manipulation.mjs\");\n/* harmony import */ var _effect_fade_mjs__WEBPACK_IMPORTED_MODULE_17__ = __webpack_require__(/*! ./effect-fade.mjs */ \"./node_modules/swiper/modules/effect-fade.mjs\");\n/* harmony import */ var _effect_cube_mjs__WEBPACK_IMPORTED_MODULE_18__ = __webpack_require__(/*! ./effect-cube.mjs */ \"./node_modules/swiper/modules/effect-cube.mjs\");\n/* harmony import */ var _effect_flip_mjs__WEBPACK_IMPORTED_MODULE_19__ = __webpack_require__(/*! ./effect-flip.mjs */ \"./node_modules/swiper/modules/effect-flip.mjs\");\n/* harmony import */ var _effect_coverflow_mjs__WEBPACK_IMPORTED_MODULE_20__ = __webpack_require__(/*! ./effect-coverflow.mjs */ \"./node_modules/swiper/modules/effect-coverflow.mjs\");\n/* harmony import */ var _effect_creative_mjs__WEBPACK_IMPORTED_MODULE_21__ = __webpack_require__(/*! ./effect-creative.mjs */ \"./node_modules/swiper/modules/effect-creative.mjs\");\n/* harmony import */ var _effect_cards_mjs__WEBPACK_IMPORTED_MODULE_22__ = __webpack_require__(/*! ./effect-cards.mjs */ \"./node_modules/swiper/modules/effect-cards.mjs\");\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/modules/index.mjs?");

/***/ }),

/***/ "./node_modules/swiper/modules/keyboard.mjs":
/*!**************************************************!*\
  !*** ./node_modules/swiper/modules/keyboard.mjs ***!
  \**************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ Keyboard)\n/* harmony export */ });\n/* harmony import */ var _shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/ssr-window.esm.mjs */ \"./node_modules/swiper/shared/ssr-window.esm.mjs\");\n/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/utils.mjs */ \"./node_modules/swiper/shared/utils.mjs\");\n\n\n\n/* eslint-disable consistent-return */\nfunction Keyboard(_ref) {\n  let {\n    swiper,\n    extendParams,\n    on,\n    emit\n  } = _ref;\n  const document = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();\n  const window = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();\n  swiper.keyboard = {\n    enabled: false\n  };\n  extendParams({\n    keyboard: {\n      enabled: false,\n      onlyInViewport: true,\n      pageUpDown: true\n    }\n  });\n  function handle(event) {\n    if (!swiper.enabled) return;\n    const {\n      rtlTranslate: rtl\n    } = swiper;\n    let e = event;\n    if (e.originalEvent) e = e.originalEvent; // jquery fix\n    const kc = e.keyCode || e.charCode;\n    const pageUpDown = swiper.params.keyboard.pageUpDown;\n    const isPageUp = pageUpDown && kc === 33;\n    const isPageDown = pageUpDown && kc === 34;\n    const isArrowLeft = kc === 37;\n    const isArrowRight = kc === 39;\n    const isArrowUp = kc === 38;\n    const isArrowDown = kc === 40;\n    // Directions locks\n    if (!swiper.allowSlideNext && (swiper.isHorizontal() && isArrowRight || swiper.isVertical() && isArrowDown || isPageDown)) {\n      return false;\n    }\n    if (!swiper.allowSlidePrev && (swiper.isHorizontal() && isArrowLeft || swiper.isVertical() && isArrowUp || isPageUp)) {\n      return false;\n    }\n    if (e.shiftKey || e.altKey || e.ctrlKey || e.metaKey) {\n      return undefined;\n    }\n    if (document.activeElement && document.activeElement.nodeName && (document.activeElement.nodeName.toLowerCase() === 'input' || document.activeElement.nodeName.toLowerCase() === 'textarea')) {\n      return undefined;\n    }\n    if (swiper.params.keyboard.onlyInViewport && (isPageUp || isPageDown || isArrowLeft || isArrowRight || isArrowUp || isArrowDown)) {\n      let inView = false;\n      // Check that swiper should be inside of visible area of window\n      if ((0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.a)(swiper.el, `.${swiper.params.slideClass}, swiper-slide`).length > 0 && (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.a)(swiper.el, `.${swiper.params.slideActiveClass}`).length === 0) {\n        return undefined;\n      }\n      const el = swiper.el;\n      const swiperWidth = el.clientWidth;\n      const swiperHeight = el.clientHeight;\n      const windowWidth = window.innerWidth;\n      const windowHeight = window.innerHeight;\n      const swiperOffset = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.b)(el);\n      if (rtl) swiperOffset.left -= el.scrollLeft;\n      const swiperCoord = [[swiperOffset.left, swiperOffset.top], [swiperOffset.left + swiperWidth, swiperOffset.top], [swiperOffset.left, swiperOffset.top + swiperHeight], [swiperOffset.left + swiperWidth, swiperOffset.top + swiperHeight]];\n      for (let i = 0; i < swiperCoord.length; i += 1) {\n        const point = swiperCoord[i];\n        if (point[0] >= 0 && point[0] <= windowWidth && point[1] >= 0 && point[1] <= windowHeight) {\n          if (point[0] === 0 && point[1] === 0) continue; // eslint-disable-line\n          inView = true;\n        }\n      }\n      if (!inView) return undefined;\n    }\n    if (swiper.isHorizontal()) {\n      if (isPageUp || isPageDown || isArrowLeft || isArrowRight) {\n        if (e.preventDefault) e.preventDefault();else e.returnValue = false;\n      }\n      if ((isPageDown || isArrowRight) && !rtl || (isPageUp || isArrowLeft) && rtl) swiper.slideNext();\n      if ((isPageUp || isArrowLeft) && !rtl || (isPageDown || isArrowRight) && rtl) swiper.slidePrev();\n    } else {\n      if (isPageUp || isPageDown || isArrowUp || isArrowDown) {\n        if (e.preventDefault) e.preventDefault();else e.returnValue = false;\n      }\n      if (isPageDown || isArrowDown) swiper.slideNext();\n      if (isPageUp || isArrowUp) swiper.slidePrev();\n    }\n    emit('keyPress', kc);\n    return undefined;\n  }\n  function enable() {\n    if (swiper.keyboard.enabled) return;\n    document.addEventListener('keydown', handle);\n    swiper.keyboard.enabled = true;\n  }\n  function disable() {\n    if (!swiper.keyboard.enabled) return;\n    document.removeEventListener('keydown', handle);\n    swiper.keyboard.enabled = false;\n  }\n  on('init', () => {\n    if (swiper.params.keyboard.enabled) {\n      enable();\n    }\n  });\n  on('destroy', () => {\n    if (swiper.keyboard.enabled) {\n      disable();\n    }\n  });\n  Object.assign(swiper.keyboard, {\n    enable,\n    disable\n  });\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/modules/keyboard.mjs?");

/***/ }),

/***/ "./node_modules/swiper/modules/manipulation.mjs":
/*!******************************************************!*\
  !*** ./node_modules/swiper/modules/manipulation.mjs ***!
  \******************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ Manipulation)\n/* harmony export */ });\nfunction appendSlide(slides) {\n  const swiper = this;\n  const {\n    params,\n    slidesEl\n  } = swiper;\n  if (params.loop) {\n    swiper.loopDestroy();\n  }\n  const appendElement = slideEl => {\n    if (typeof slideEl === 'string') {\n      const tempDOM = document.createElement('div');\n      tempDOM.innerHTML = slideEl;\n      slidesEl.append(tempDOM.children[0]);\n      tempDOM.innerHTML = '';\n    } else {\n      slidesEl.append(slideEl);\n    }\n  };\n  if (typeof slides === 'object' && 'length' in slides) {\n    for (let i = 0; i < slides.length; i += 1) {\n      if (slides[i]) appendElement(slides[i]);\n    }\n  } else {\n    appendElement(slides);\n  }\n  swiper.recalcSlides();\n  if (params.loop) {\n    swiper.loopCreate();\n  }\n  if (!params.observer || swiper.isElement) {\n    swiper.update();\n  }\n}\n\nfunction prependSlide(slides) {\n  const swiper = this;\n  const {\n    params,\n    activeIndex,\n    slidesEl\n  } = swiper;\n  if (params.loop) {\n    swiper.loopDestroy();\n  }\n  let newActiveIndex = activeIndex + 1;\n  const prependElement = slideEl => {\n    if (typeof slideEl === 'string') {\n      const tempDOM = document.createElement('div');\n      tempDOM.innerHTML = slideEl;\n      slidesEl.prepend(tempDOM.children[0]);\n      tempDOM.innerHTML = '';\n    } else {\n      slidesEl.prepend(slideEl);\n    }\n  };\n  if (typeof slides === 'object' && 'length' in slides) {\n    for (let i = 0; i < slides.length; i += 1) {\n      if (slides[i]) prependElement(slides[i]);\n    }\n    newActiveIndex = activeIndex + slides.length;\n  } else {\n    prependElement(slides);\n  }\n  swiper.recalcSlides();\n  if (params.loop) {\n    swiper.loopCreate();\n  }\n  if (!params.observer || swiper.isElement) {\n    swiper.update();\n  }\n  swiper.slideTo(newActiveIndex, 0, false);\n}\n\nfunction addSlide(index, slides) {\n  const swiper = this;\n  const {\n    params,\n    activeIndex,\n    slidesEl\n  } = swiper;\n  let activeIndexBuffer = activeIndex;\n  if (params.loop) {\n    activeIndexBuffer -= swiper.loopedSlides;\n    swiper.loopDestroy();\n    swiper.recalcSlides();\n  }\n  const baseLength = swiper.slides.length;\n  if (index <= 0) {\n    swiper.prependSlide(slides);\n    return;\n  }\n  if (index >= baseLength) {\n    swiper.appendSlide(slides);\n    return;\n  }\n  let newActiveIndex = activeIndexBuffer > index ? activeIndexBuffer + 1 : activeIndexBuffer;\n  const slidesBuffer = [];\n  for (let i = baseLength - 1; i >= index; i -= 1) {\n    const currentSlide = swiper.slides[i];\n    currentSlide.remove();\n    slidesBuffer.unshift(currentSlide);\n  }\n  if (typeof slides === 'object' && 'length' in slides) {\n    for (let i = 0; i < slides.length; i += 1) {\n      if (slides[i]) slidesEl.append(slides[i]);\n    }\n    newActiveIndex = activeIndexBuffer > index ? activeIndexBuffer + slides.length : activeIndexBuffer;\n  } else {\n    slidesEl.append(slides);\n  }\n  for (let i = 0; i < slidesBuffer.length; i += 1) {\n    slidesEl.append(slidesBuffer[i]);\n  }\n  swiper.recalcSlides();\n  if (params.loop) {\n    swiper.loopCreate();\n  }\n  if (!params.observer || swiper.isElement) {\n    swiper.update();\n  }\n  if (params.loop) {\n    swiper.slideTo(newActiveIndex + swiper.loopedSlides, 0, false);\n  } else {\n    swiper.slideTo(newActiveIndex, 0, false);\n  }\n}\n\nfunction removeSlide(slidesIndexes) {\n  const swiper = this;\n  const {\n    params,\n    activeIndex\n  } = swiper;\n  let activeIndexBuffer = activeIndex;\n  if (params.loop) {\n    activeIndexBuffer -= swiper.loopedSlides;\n    swiper.loopDestroy();\n  }\n  let newActiveIndex = activeIndexBuffer;\n  let indexToRemove;\n  if (typeof slidesIndexes === 'object' && 'length' in slidesIndexes) {\n    for (let i = 0; i < slidesIndexes.length; i += 1) {\n      indexToRemove = slidesIndexes[i];\n      if (swiper.slides[indexToRemove]) swiper.slides[indexToRemove].remove();\n      if (indexToRemove < newActiveIndex) newActiveIndex -= 1;\n    }\n    newActiveIndex = Math.max(newActiveIndex, 0);\n  } else {\n    indexToRemove = slidesIndexes;\n    if (swiper.slides[indexToRemove]) swiper.slides[indexToRemove].remove();\n    if (indexToRemove < newActiveIndex) newActiveIndex -= 1;\n    newActiveIndex = Math.max(newActiveIndex, 0);\n  }\n  swiper.recalcSlides();\n  if (params.loop) {\n    swiper.loopCreate();\n  }\n  if (!params.observer || swiper.isElement) {\n    swiper.update();\n  }\n  if (params.loop) {\n    swiper.slideTo(newActiveIndex + swiper.loopedSlides, 0, false);\n  } else {\n    swiper.slideTo(newActiveIndex, 0, false);\n  }\n}\n\nfunction removeAllSlides() {\n  const swiper = this;\n  const slidesIndexes = [];\n  for (let i = 0; i < swiper.slides.length; i += 1) {\n    slidesIndexes.push(i);\n  }\n  swiper.removeSlide(slidesIndexes);\n}\n\nfunction Manipulation(_ref) {\n  let {\n    swiper\n  } = _ref;\n  Object.assign(swiper, {\n    appendSlide: appendSlide.bind(swiper),\n    prependSlide: prependSlide.bind(swiper),\n    addSlide: addSlide.bind(swiper),\n    removeSlide: removeSlide.bind(swiper),\n    removeAllSlides: removeAllSlides.bind(swiper)\n  });\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/modules/manipulation.mjs?");

/***/ }),

/***/ "./node_modules/swiper/modules/mousewheel.mjs":
/*!****************************************************!*\
  !*** ./node_modules/swiper/modules/mousewheel.mjs ***!
  \****************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ Mousewheel)\n/* harmony export */ });\n/* harmony import */ var _shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/ssr-window.esm.mjs */ \"./node_modules/swiper/shared/ssr-window.esm.mjs\");\n/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/utils.mjs */ \"./node_modules/swiper/shared/utils.mjs\");\n\n\n\n/* eslint-disable consistent-return */\nfunction Mousewheel(_ref) {\n  let {\n    swiper,\n    extendParams,\n    on,\n    emit\n  } = _ref;\n  const window = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();\n  extendParams({\n    mousewheel: {\n      enabled: false,\n      releaseOnEdges: false,\n      invert: false,\n      forceToAxis: false,\n      sensitivity: 1,\n      eventsTarget: 'container',\n      thresholdDelta: null,\n      thresholdTime: null,\n      noMousewheelClass: 'swiper-no-mousewheel'\n    }\n  });\n  swiper.mousewheel = {\n    enabled: false\n  };\n  let timeout;\n  let lastScrollTime = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.d)();\n  let lastEventBeforeSnap;\n  const recentWheelEvents = [];\n  function normalize(e) {\n    // Reasonable defaults\n    const PIXEL_STEP = 10;\n    const LINE_HEIGHT = 40;\n    const PAGE_HEIGHT = 800;\n    let sX = 0;\n    let sY = 0; // spinX, spinY\n    let pX = 0;\n    let pY = 0; // pixelX, pixelY\n\n    // Legacy\n    if ('detail' in e) {\n      sY = e.detail;\n    }\n    if ('wheelDelta' in e) {\n      sY = -e.wheelDelta / 120;\n    }\n    if ('wheelDeltaY' in e) {\n      sY = -e.wheelDeltaY / 120;\n    }\n    if ('wheelDeltaX' in e) {\n      sX = -e.wheelDeltaX / 120;\n    }\n\n    // side scrolling on FF with DOMMouseScroll\n    if ('axis' in e && e.axis === e.HORIZONTAL_AXIS) {\n      sX = sY;\n      sY = 0;\n    }\n    pX = sX * PIXEL_STEP;\n    pY = sY * PIXEL_STEP;\n    if ('deltaY' in e) {\n      pY = e.deltaY;\n    }\n    if ('deltaX' in e) {\n      pX = e.deltaX;\n    }\n    if (e.shiftKey && !pX) {\n      // if user scrolls with shift he wants horizontal scroll\n      pX = pY;\n      pY = 0;\n    }\n    if ((pX || pY) && e.deltaMode) {\n      if (e.deltaMode === 1) {\n        // delta in LINE units\n        pX *= LINE_HEIGHT;\n        pY *= LINE_HEIGHT;\n      } else {\n        // delta in PAGE units\n        pX *= PAGE_HEIGHT;\n        pY *= PAGE_HEIGHT;\n      }\n    }\n\n    // Fall-back if spin cannot be determined\n    if (pX && !sX) {\n      sX = pX < 1 ? -1 : 1;\n    }\n    if (pY && !sY) {\n      sY = pY < 1 ? -1 : 1;\n    }\n    return {\n      spinX: sX,\n      spinY: sY,\n      pixelX: pX,\n      pixelY: pY\n    };\n  }\n  function handleMouseEnter() {\n    if (!swiper.enabled) return;\n    swiper.mouseEntered = true;\n  }\n  function handleMouseLeave() {\n    if (!swiper.enabled) return;\n    swiper.mouseEntered = false;\n  }\n  function animateSlider(newEvent) {\n    if (swiper.params.mousewheel.thresholdDelta && newEvent.delta < swiper.params.mousewheel.thresholdDelta) {\n      // Prevent if delta of wheel scroll delta is below configured threshold\n      return false;\n    }\n    if (swiper.params.mousewheel.thresholdTime && (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.d)() - lastScrollTime < swiper.params.mousewheel.thresholdTime) {\n      // Prevent if time between scrolls is below configured threshold\n      return false;\n    }\n\n    // If the movement is NOT big enough and\n    // if the last time the user scrolled was too close to the current one (avoid continuously triggering the slider):\n    //   Don't go any further (avoid insignificant scroll movement).\n    if (newEvent.delta >= 6 && (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.d)() - lastScrollTime < 60) {\n      // Return false as a default\n      return true;\n    }\n    // If user is scrolling towards the end:\n    //   If the slider hasn't hit the latest slide or\n    //   if the slider is a loop and\n    //   if the slider isn't moving right now:\n    //     Go to next slide and\n    //     emit a scroll event.\n    // Else (the user is scrolling towards the beginning) and\n    // if the slider hasn't hit the first slide or\n    // if the slider is a loop and\n    // if the slider isn't moving right now:\n    //   Go to prev slide and\n    //   emit a scroll event.\n    if (newEvent.direction < 0) {\n      if ((!swiper.isEnd || swiper.params.loop) && !swiper.animating) {\n        swiper.slideNext();\n        emit('scroll', newEvent.raw);\n      }\n    } else if ((!swiper.isBeginning || swiper.params.loop) && !swiper.animating) {\n      swiper.slidePrev();\n      emit('scroll', newEvent.raw);\n    }\n    // If you got here is because an animation has been triggered so store the current time\n    lastScrollTime = new window.Date().getTime();\n    // Return false as a default\n    return false;\n  }\n  function releaseScroll(newEvent) {\n    const params = swiper.params.mousewheel;\n    if (newEvent.direction < 0) {\n      if (swiper.isEnd && !swiper.params.loop && params.releaseOnEdges) {\n        // Return true to animate scroll on edges\n        return true;\n      }\n    } else if (swiper.isBeginning && !swiper.params.loop && params.releaseOnEdges) {\n      // Return true to animate scroll on edges\n      return true;\n    }\n    return false;\n  }\n  function handle(event) {\n    let e = event;\n    let disableParentSwiper = true;\n    if (!swiper.enabled) return;\n\n    // Ignore event if the target or its parents have the swiper-no-mousewheel class\n    if (event.target.closest(`.${swiper.params.mousewheel.noMousewheelClass}`)) return;\n    const params = swiper.params.mousewheel;\n    if (swiper.params.cssMode) {\n      e.preventDefault();\n    }\n    let targetEl = swiper.el;\n    if (swiper.params.mousewheel.eventsTarget !== 'container') {\n      targetEl = document.querySelector(swiper.params.mousewheel.eventsTarget);\n    }\n    const targetElContainsTarget = targetEl && targetEl.contains(e.target);\n    if (!swiper.mouseEntered && !targetElContainsTarget && !params.releaseOnEdges) return true;\n    if (e.originalEvent) e = e.originalEvent; // jquery fix\n    let delta = 0;\n    const rtlFactor = swiper.rtlTranslate ? -1 : 1;\n    const data = normalize(e);\n    if (params.forceToAxis) {\n      if (swiper.isHorizontal()) {\n        if (Math.abs(data.pixelX) > Math.abs(data.pixelY)) delta = -data.pixelX * rtlFactor;else return true;\n      } else if (Math.abs(data.pixelY) > Math.abs(data.pixelX)) delta = -data.pixelY;else return true;\n    } else {\n      delta = Math.abs(data.pixelX) > Math.abs(data.pixelY) ? -data.pixelX * rtlFactor : -data.pixelY;\n    }\n    if (delta === 0) return true;\n    if (params.invert) delta = -delta;\n\n    // Get the scroll positions\n    let positions = swiper.getTranslate() + delta * params.sensitivity;\n    if (positions >= swiper.minTranslate()) positions = swiper.minTranslate();\n    if (positions <= swiper.maxTranslate()) positions = swiper.maxTranslate();\n\n    // When loop is true:\n    //     the disableParentSwiper will be true.\n    // When loop is false:\n    //     if the scroll positions is not on edge,\n    //     then the disableParentSwiper will be true.\n    //     if the scroll on edge positions,\n    //     then the disableParentSwiper will be false.\n    disableParentSwiper = swiper.params.loop ? true : !(positions === swiper.minTranslate() || positions === swiper.maxTranslate());\n    if (disableParentSwiper && swiper.params.nested) e.stopPropagation();\n    if (!swiper.params.freeMode || !swiper.params.freeMode.enabled) {\n      // Register the new event in a variable which stores the relevant data\n      const newEvent = {\n        time: (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.d)(),\n        delta: Math.abs(delta),\n        direction: Math.sign(delta),\n        raw: event\n      };\n\n      // Keep the most recent events\n      if (recentWheelEvents.length >= 2) {\n        recentWheelEvents.shift(); // only store the last N events\n      }\n\n      const prevEvent = recentWheelEvents.length ? recentWheelEvents[recentWheelEvents.length - 1] : undefined;\n      recentWheelEvents.push(newEvent);\n\n      // If there is at least one previous recorded event:\n      //   If direction has changed or\n      //   if the scroll is quicker than the previous one:\n      //     Animate the slider.\n      // Else (this is the first time the wheel is moved):\n      //     Animate the slider.\n      if (prevEvent) {\n        if (newEvent.direction !== prevEvent.direction || newEvent.delta > prevEvent.delta || newEvent.time > prevEvent.time + 150) {\n          animateSlider(newEvent);\n        }\n      } else {\n        animateSlider(newEvent);\n      }\n\n      // If it's time to release the scroll:\n      //   Return now so you don't hit the preventDefault.\n      if (releaseScroll(newEvent)) {\n        return true;\n      }\n    } else {\n      // Freemode or scrollContainer:\n\n      // If we recently snapped after a momentum scroll, then ignore wheel events\n      // to give time for the deceleration to finish. Stop ignoring after 500 msecs\n      // or if it's a new scroll (larger delta or inverse sign as last event before\n      // an end-of-momentum snap).\n      const newEvent = {\n        time: (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.d)(),\n        delta: Math.abs(delta),\n        direction: Math.sign(delta)\n      };\n      const ignoreWheelEvents = lastEventBeforeSnap && newEvent.time < lastEventBeforeSnap.time + 500 && newEvent.delta <= lastEventBeforeSnap.delta && newEvent.direction === lastEventBeforeSnap.direction;\n      if (!ignoreWheelEvents) {\n        lastEventBeforeSnap = undefined;\n        let position = swiper.getTranslate() + delta * params.sensitivity;\n        const wasBeginning = swiper.isBeginning;\n        const wasEnd = swiper.isEnd;\n        if (position >= swiper.minTranslate()) position = swiper.minTranslate();\n        if (position <= swiper.maxTranslate()) position = swiper.maxTranslate();\n        swiper.setTransition(0);\n        swiper.setTranslate(position);\n        swiper.updateProgress();\n        swiper.updateActiveIndex();\n        swiper.updateSlidesClasses();\n        if (!wasBeginning && swiper.isBeginning || !wasEnd && swiper.isEnd) {\n          swiper.updateSlidesClasses();\n        }\n        if (swiper.params.loop) {\n          swiper.loopFix({\n            direction: newEvent.direction < 0 ? 'next' : 'prev',\n            byMousewheel: true\n          });\n        }\n        if (swiper.params.freeMode.sticky) {\n          // When wheel scrolling starts with sticky (aka snap) enabled, then detect\n          // the end of a momentum scroll by storing recent (N=15?) wheel events.\n          // 1. do all N events have decreasing or same (absolute value) delta?\n          // 2. did all N events arrive in the last M (M=500?) msecs?\n          // 3. does the earliest event have an (absolute value) delta that's\n          //    at least P (P=1?) larger than the most recent event's delta?\n          // 4. does the latest event have a delta that's smaller than Q (Q=6?) pixels?\n          // If 1-4 are \"yes\" then we're near the end of a momentum scroll deceleration.\n          // Snap immediately and ignore remaining wheel events in this scroll.\n          // See comment above for \"remaining wheel events in this scroll\" determination.\n          // If 1-4 aren't satisfied, then wait to snap until 500ms after the last event.\n          clearTimeout(timeout);\n          timeout = undefined;\n          if (recentWheelEvents.length >= 15) {\n            recentWheelEvents.shift(); // only store the last N events\n          }\n\n          const prevEvent = recentWheelEvents.length ? recentWheelEvents[recentWheelEvents.length - 1] : undefined;\n          const firstEvent = recentWheelEvents[0];\n          recentWheelEvents.push(newEvent);\n          if (prevEvent && (newEvent.delta > prevEvent.delta || newEvent.direction !== prevEvent.direction)) {\n            // Increasing or reverse-sign delta means the user started scrolling again. Clear the wheel event log.\n            recentWheelEvents.splice(0);\n          } else if (recentWheelEvents.length >= 15 && newEvent.time - firstEvent.time < 500 && firstEvent.delta - newEvent.delta >= 1 && newEvent.delta <= 6) {\n            // We're at the end of the deceleration of a momentum scroll, so there's no need\n            // to wait for more events. Snap ASAP on the next tick.\n            // Also, because there's some remaining momentum we'll bias the snap in the\n            // direction of the ongoing scroll because it's better UX for the scroll to snap\n            // in the same direction as the scroll instead of reversing to snap.  Therefore,\n            // if it's already scrolled more than 20% in the current direction, keep going.\n            const snapToThreshold = delta > 0 ? 0.8 : 0.2;\n            lastEventBeforeSnap = newEvent;\n            recentWheelEvents.splice(0);\n            timeout = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.n)(() => {\n              swiper.slideToClosest(swiper.params.speed, true, undefined, snapToThreshold);\n            }, 0); // no delay; move on next tick\n          }\n\n          if (!timeout) {\n            // if we get here, then we haven't detected the end of a momentum scroll, so\n            // we'll consider a scroll \"complete\" when there haven't been any wheel events\n            // for 500ms.\n            timeout = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.n)(() => {\n              const snapToThreshold = 0.5;\n              lastEventBeforeSnap = newEvent;\n              recentWheelEvents.splice(0);\n              swiper.slideToClosest(swiper.params.speed, true, undefined, snapToThreshold);\n            }, 500);\n          }\n        }\n\n        // Emit event\n        if (!ignoreWheelEvents) emit('scroll', e);\n\n        // Stop autoplay\n        if (swiper.params.autoplay && swiper.params.autoplayDisableOnInteraction) swiper.autoplay.stop();\n        // Return page scroll on edge positions\n        if (params.releaseOnEdges && (position === swiper.minTranslate() || position === swiper.maxTranslate())) {\n          return true;\n        }\n      }\n    }\n    if (e.preventDefault) e.preventDefault();else e.returnValue = false;\n    return false;\n  }\n  function events(method) {\n    let targetEl = swiper.el;\n    if (swiper.params.mousewheel.eventsTarget !== 'container') {\n      targetEl = document.querySelector(swiper.params.mousewheel.eventsTarget);\n    }\n    targetEl[method]('mouseenter', handleMouseEnter);\n    targetEl[method]('mouseleave', handleMouseLeave);\n    targetEl[method]('wheel', handle);\n  }\n  function enable() {\n    if (swiper.params.cssMode) {\n      swiper.wrapperEl.removeEventListener('wheel', handle);\n      return true;\n    }\n    if (swiper.mousewheel.enabled) return false;\n    events('addEventListener');\n    swiper.mousewheel.enabled = true;\n    return true;\n  }\n  function disable() {\n    if (swiper.params.cssMode) {\n      swiper.wrapperEl.addEventListener(event, handle);\n      return true;\n    }\n    if (!swiper.mousewheel.enabled) return false;\n    events('removeEventListener');\n    swiper.mousewheel.enabled = false;\n    return true;\n  }\n  on('init', () => {\n    if (!swiper.params.mousewheel.enabled && swiper.params.cssMode) {\n      disable();\n    }\n    if (swiper.params.mousewheel.enabled) enable();\n  });\n  on('destroy', () => {\n    if (swiper.params.cssMode) {\n      enable();\n    }\n    if (swiper.mousewheel.enabled) disable();\n  });\n  Object.assign(swiper.mousewheel, {\n    enable,\n    disable\n  });\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/modules/mousewheel.mjs?");

/***/ }),

/***/ "./node_modules/swiper/modules/navigation.mjs":
/*!****************************************************!*\
  !*** ./node_modules/swiper/modules/navigation.mjs ***!
  \****************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ Navigation)\n/* harmony export */ });\n/* harmony import */ var _shared_create_element_if_not_defined_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/create-element-if-not-defined.mjs */ \"./node_modules/swiper/shared/create-element-if-not-defined.mjs\");\n\n\nfunction Navigation(_ref) {\n  let {\n    swiper,\n    extendParams,\n    on,\n    emit\n  } = _ref;\n  extendParams({\n    navigation: {\n      nextEl: null,\n      prevEl: null,\n      hideOnClick: false,\n      disabledClass: 'swiper-button-disabled',\n      hiddenClass: 'swiper-button-hidden',\n      lockClass: 'swiper-button-lock',\n      navigationDisabledClass: 'swiper-navigation-disabled'\n    }\n  });\n  swiper.navigation = {\n    nextEl: null,\n    prevEl: null\n  };\n  const makeElementsArray = el => (Array.isArray(el) ? el : [el]).filter(e => !!e);\n  function getEl(el) {\n    let res;\n    if (el && typeof el === 'string' && swiper.isElement) {\n      res = swiper.el.querySelector(el);\n      if (res) return res;\n    }\n    if (el) {\n      if (typeof el === 'string') res = [...document.querySelectorAll(el)];\n      if (swiper.params.uniqueNavElements && typeof el === 'string' && res.length > 1 && swiper.el.querySelectorAll(el).length === 1) {\n        res = swiper.el.querySelector(el);\n      }\n    }\n    if (el && !res) return el;\n    // if (Array.isArray(res) && res.length === 1) res = res[0];\n    return res;\n  }\n  function toggleEl(el, disabled) {\n    const params = swiper.params.navigation;\n    el = makeElementsArray(el);\n    el.forEach(subEl => {\n      if (subEl) {\n        subEl.classList[disabled ? 'add' : 'remove'](...params.disabledClass.split(' '));\n        if (subEl.tagName === 'BUTTON') subEl.disabled = disabled;\n        if (swiper.params.watchOverflow && swiper.enabled) {\n          subEl.classList[swiper.isLocked ? 'add' : 'remove'](params.lockClass);\n        }\n      }\n    });\n  }\n  function update() {\n    // Update Navigation Buttons\n    const {\n      nextEl,\n      prevEl\n    } = swiper.navigation;\n    if (swiper.params.loop) {\n      toggleEl(prevEl, false);\n      toggleEl(nextEl, false);\n      return;\n    }\n    toggleEl(prevEl, swiper.isBeginning && !swiper.params.rewind);\n    toggleEl(nextEl, swiper.isEnd && !swiper.params.rewind);\n  }\n  function onPrevClick(e) {\n    e.preventDefault();\n    if (swiper.isBeginning && !swiper.params.loop && !swiper.params.rewind) return;\n    swiper.slidePrev();\n    emit('navigationPrev');\n  }\n  function onNextClick(e) {\n    e.preventDefault();\n    if (swiper.isEnd && !swiper.params.loop && !swiper.params.rewind) return;\n    swiper.slideNext();\n    emit('navigationNext');\n  }\n  function init() {\n    const params = swiper.params.navigation;\n    swiper.params.navigation = (0,_shared_create_element_if_not_defined_mjs__WEBPACK_IMPORTED_MODULE_0__.c)(swiper, swiper.originalParams.navigation, swiper.params.navigation, {\n      nextEl: 'swiper-button-next',\n      prevEl: 'swiper-button-prev'\n    });\n    if (!(params.nextEl || params.prevEl)) return;\n    let nextEl = getEl(params.nextEl);\n    let prevEl = getEl(params.prevEl);\n    Object.assign(swiper.navigation, {\n      nextEl,\n      prevEl\n    });\n    nextEl = makeElementsArray(nextEl);\n    prevEl = makeElementsArray(prevEl);\n    const initButton = (el, dir) => {\n      if (el) {\n        el.addEventListener('click', dir === 'next' ? onNextClick : onPrevClick);\n      }\n      if (!swiper.enabled && el) {\n        el.classList.add(...params.lockClass.split(' '));\n      }\n    };\n    nextEl.forEach(el => initButton(el, 'next'));\n    prevEl.forEach(el => initButton(el, 'prev'));\n  }\n  function destroy() {\n    let {\n      nextEl,\n      prevEl\n    } = swiper.navigation;\n    nextEl = makeElementsArray(nextEl);\n    prevEl = makeElementsArray(prevEl);\n    const destroyButton = (el, dir) => {\n      el.removeEventListener('click', dir === 'next' ? onNextClick : onPrevClick);\n      el.classList.remove(...swiper.params.navigation.disabledClass.split(' '));\n    };\n    nextEl.forEach(el => destroyButton(el, 'next'));\n    prevEl.forEach(el => destroyButton(el, 'prev'));\n  }\n  on('init', () => {\n    if (swiper.params.navigation.enabled === false) {\n      // eslint-disable-next-line\n      disable();\n    } else {\n      init();\n      update();\n    }\n  });\n  on('toEdge fromEdge lock unlock', () => {\n    update();\n  });\n  on('destroy', () => {\n    destroy();\n  });\n  on('enable disable', () => {\n    let {\n      nextEl,\n      prevEl\n    } = swiper.navigation;\n    nextEl = makeElementsArray(nextEl);\n    prevEl = makeElementsArray(prevEl);\n    [...nextEl, ...prevEl].filter(el => !!el).forEach(el => el.classList[swiper.enabled ? 'remove' : 'add'](swiper.params.navigation.lockClass));\n  });\n  on('click', (_s, e) => {\n    let {\n      nextEl,\n      prevEl\n    } = swiper.navigation;\n    nextEl = makeElementsArray(nextEl);\n    prevEl = makeElementsArray(prevEl);\n    const targetEl = e.target;\n    if (swiper.params.navigation.hideOnClick && !prevEl.includes(targetEl) && !nextEl.includes(targetEl)) {\n      if (swiper.pagination && swiper.params.pagination && swiper.params.pagination.clickable && (swiper.pagination.el === targetEl || swiper.pagination.el.contains(targetEl))) return;\n      let isHidden;\n      if (nextEl.length) {\n        isHidden = nextEl[0].classList.contains(swiper.params.navigation.hiddenClass);\n      } else if (prevEl.length) {\n        isHidden = prevEl[0].classList.contains(swiper.params.navigation.hiddenClass);\n      }\n      if (isHidden === true) {\n        emit('navigationShow');\n      } else {\n        emit('navigationHide');\n      }\n      [...nextEl, ...prevEl].filter(el => !!el).forEach(el => el.classList.toggle(swiper.params.navigation.hiddenClass));\n    }\n  });\n  const enable = () => {\n    swiper.el.classList.remove(...swiper.params.navigation.navigationDisabledClass.split(' '));\n    init();\n    update();\n  };\n  const disable = () => {\n    swiper.el.classList.add(...swiper.params.navigation.navigationDisabledClass.split(' '));\n    destroy();\n  };\n  Object.assign(swiper.navigation, {\n    enable,\n    disable,\n    update,\n    init,\n    destroy\n  });\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/modules/navigation.mjs?");

/***/ }),

/***/ "./node_modules/swiper/modules/pagination.mjs":
/*!****************************************************!*\
  !*** ./node_modules/swiper/modules/pagination.mjs ***!
  \****************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ Pagination)\n/* harmony export */ });\n/* harmony import */ var _shared_classes_to_selector_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/classes-to-selector.mjs */ \"./node_modules/swiper/shared/classes-to-selector.mjs\");\n/* harmony import */ var _shared_create_element_if_not_defined_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/create-element-if-not-defined.mjs */ \"./node_modules/swiper/shared/create-element-if-not-defined.mjs\");\n/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../shared/utils.mjs */ \"./node_modules/swiper/shared/utils.mjs\");\n\n\n\n\nfunction Pagination(_ref) {\n  let {\n    swiper,\n    extendParams,\n    on,\n    emit\n  } = _ref;\n  const pfx = 'swiper-pagination';\n  extendParams({\n    pagination: {\n      el: null,\n      bulletElement: 'span',\n      clickable: false,\n      hideOnClick: false,\n      renderBullet: null,\n      renderProgressbar: null,\n      renderFraction: null,\n      renderCustom: null,\n      progressbarOpposite: false,\n      type: 'bullets',\n      // 'bullets' or 'progressbar' or 'fraction' or 'custom'\n      dynamicBullets: false,\n      dynamicMainBullets: 1,\n      formatFractionCurrent: number => number,\n      formatFractionTotal: number => number,\n      bulletClass: `${pfx}-bullet`,\n      bulletActiveClass: `${pfx}-bullet-active`,\n      modifierClass: `${pfx}-`,\n      currentClass: `${pfx}-current`,\n      totalClass: `${pfx}-total`,\n      hiddenClass: `${pfx}-hidden`,\n      progressbarFillClass: `${pfx}-progressbar-fill`,\n      progressbarOppositeClass: `${pfx}-progressbar-opposite`,\n      clickableClass: `${pfx}-clickable`,\n      lockClass: `${pfx}-lock`,\n      horizontalClass: `${pfx}-horizontal`,\n      verticalClass: `${pfx}-vertical`,\n      paginationDisabledClass: `${pfx}-disabled`\n    }\n  });\n  swiper.pagination = {\n    el: null,\n    bullets: []\n  };\n  let bulletSize;\n  let dynamicBulletIndex = 0;\n  const makeElementsArray = el => (Array.isArray(el) ? el : [el]).filter(e => !!e);\n  function isPaginationDisabled() {\n    return !swiper.params.pagination.el || !swiper.pagination.el || Array.isArray(swiper.pagination.el) && swiper.pagination.el.length === 0;\n  }\n  function setSideBullets(bulletEl, position) {\n    const {\n      bulletActiveClass\n    } = swiper.params.pagination;\n    if (!bulletEl) return;\n    bulletEl = bulletEl[`${position === 'prev' ? 'previous' : 'next'}ElementSibling`];\n    if (bulletEl) {\n      bulletEl.classList.add(`${bulletActiveClass}-${position}`);\n      bulletEl = bulletEl[`${position === 'prev' ? 'previous' : 'next'}ElementSibling`];\n      if (bulletEl) {\n        bulletEl.classList.add(`${bulletActiveClass}-${position}-${position}`);\n      }\n    }\n  }\n  function onBulletClick(e) {\n    const bulletEl = e.target.closest((0,_shared_classes_to_selector_mjs__WEBPACK_IMPORTED_MODULE_0__.c)(swiper.params.pagination.bulletClass));\n    if (!bulletEl) {\n      return;\n    }\n    e.preventDefault();\n    const index = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.g)(bulletEl) * swiper.params.slidesPerGroup;\n    if (swiper.params.loop) {\n      if (swiper.realIndex === index) return;\n      const newSlideIndex = swiper.getSlideIndexByData(index);\n      const currentSlideIndex = swiper.getSlideIndexByData(swiper.realIndex);\n      if (newSlideIndex > swiper.slides.length - swiper.loopedSlides) {\n        swiper.loopFix({\n          direction: newSlideIndex > currentSlideIndex ? 'next' : 'prev',\n          activeSlideIndex: newSlideIndex,\n          slideTo: false\n        });\n      }\n      swiper.slideToLoop(index);\n    } else {\n      swiper.slideTo(index);\n    }\n  }\n  function update() {\n    // Render || Update Pagination bullets/items\n    const rtl = swiper.rtl;\n    const params = swiper.params.pagination;\n    if (isPaginationDisabled()) return;\n    let el = swiper.pagination.el;\n    el = makeElementsArray(el);\n    // Current/Total\n    let current;\n    let previousIndex;\n    const slidesLength = swiper.virtual && swiper.params.virtual.enabled ? swiper.virtual.slides.length : swiper.slides.length;\n    const total = swiper.params.loop ? Math.ceil(slidesLength / swiper.params.slidesPerGroup) : swiper.snapGrid.length;\n    if (swiper.params.loop) {\n      previousIndex = swiper.previousRealIndex || 0;\n      current = swiper.params.slidesPerGroup > 1 ? Math.floor(swiper.realIndex / swiper.params.slidesPerGroup) : swiper.realIndex;\n    } else if (typeof swiper.snapIndex !== 'undefined') {\n      current = swiper.snapIndex;\n      previousIndex = swiper.previousSnapIndex;\n    } else {\n      previousIndex = swiper.previousIndex || 0;\n      current = swiper.activeIndex || 0;\n    }\n    // Types\n    if (params.type === 'bullets' && swiper.pagination.bullets && swiper.pagination.bullets.length > 0) {\n      const bullets = swiper.pagination.bullets;\n      let firstIndex;\n      let lastIndex;\n      let midIndex;\n      if (params.dynamicBullets) {\n        bulletSize = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.f)(bullets[0], swiper.isHorizontal() ? 'width' : 'height', true);\n        el.forEach(subEl => {\n          subEl.style[swiper.isHorizontal() ? 'width' : 'height'] = `${bulletSize * (params.dynamicMainBullets + 4)}px`;\n        });\n        if (params.dynamicMainBullets > 1 && previousIndex !== undefined) {\n          dynamicBulletIndex += current - (previousIndex || 0);\n          if (dynamicBulletIndex > params.dynamicMainBullets - 1) {\n            dynamicBulletIndex = params.dynamicMainBullets - 1;\n          } else if (dynamicBulletIndex < 0) {\n            dynamicBulletIndex = 0;\n          }\n        }\n        firstIndex = Math.max(current - dynamicBulletIndex, 0);\n        lastIndex = firstIndex + (Math.min(bullets.length, params.dynamicMainBullets) - 1);\n        midIndex = (lastIndex + firstIndex) / 2;\n      }\n      bullets.forEach(bulletEl => {\n        const classesToRemove = [...['', '-next', '-next-next', '-prev', '-prev-prev', '-main'].map(suffix => `${params.bulletActiveClass}${suffix}`)].map(s => typeof s === 'string' && s.includes(' ') ? s.split(' ') : s).flat();\n        bulletEl.classList.remove(...classesToRemove);\n      });\n      if (el.length > 1) {\n        bullets.forEach(bullet => {\n          const bulletIndex = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.g)(bullet);\n          if (bulletIndex === current) {\n            bullet.classList.add(...params.bulletActiveClass.split(' '));\n          } else if (swiper.isElement) {\n            bullet.setAttribute('part', 'bullet');\n          }\n          if (params.dynamicBullets) {\n            if (bulletIndex >= firstIndex && bulletIndex <= lastIndex) {\n              bullet.classList.add(...`${params.bulletActiveClass}-main`.split(' '));\n            }\n            if (bulletIndex === firstIndex) {\n              setSideBullets(bullet, 'prev');\n            }\n            if (bulletIndex === lastIndex) {\n              setSideBullets(bullet, 'next');\n            }\n          }\n        });\n      } else {\n        const bullet = bullets[current];\n        if (bullet) {\n          bullet.classList.add(...params.bulletActiveClass.split(' '));\n        }\n        if (swiper.isElement) {\n          bullets.forEach((bulletEl, bulletIndex) => {\n            bulletEl.setAttribute('part', bulletIndex === current ? 'bullet-active' : 'bullet');\n          });\n        }\n        if (params.dynamicBullets) {\n          const firstDisplayedBullet = bullets[firstIndex];\n          const lastDisplayedBullet = bullets[lastIndex];\n          for (let i = firstIndex; i <= lastIndex; i += 1) {\n            if (bullets[i]) {\n              bullets[i].classList.add(...`${params.bulletActiveClass}-main`.split(' '));\n            }\n          }\n          setSideBullets(firstDisplayedBullet, 'prev');\n          setSideBullets(lastDisplayedBullet, 'next');\n        }\n      }\n      if (params.dynamicBullets) {\n        const dynamicBulletsLength = Math.min(bullets.length, params.dynamicMainBullets + 4);\n        const bulletsOffset = (bulletSize * dynamicBulletsLength - bulletSize) / 2 - midIndex * bulletSize;\n        const offsetProp = rtl ? 'right' : 'left';\n        bullets.forEach(bullet => {\n          bullet.style[swiper.isHorizontal() ? offsetProp : 'top'] = `${bulletsOffset}px`;\n        });\n      }\n    }\n    el.forEach((subEl, subElIndex) => {\n      if (params.type === 'fraction') {\n        subEl.querySelectorAll((0,_shared_classes_to_selector_mjs__WEBPACK_IMPORTED_MODULE_0__.c)(params.currentClass)).forEach(fractionEl => {\n          fractionEl.textContent = params.formatFractionCurrent(current + 1);\n        });\n        subEl.querySelectorAll((0,_shared_classes_to_selector_mjs__WEBPACK_IMPORTED_MODULE_0__.c)(params.totalClass)).forEach(totalEl => {\n          totalEl.textContent = params.formatFractionTotal(total);\n        });\n      }\n      if (params.type === 'progressbar') {\n        let progressbarDirection;\n        if (params.progressbarOpposite) {\n          progressbarDirection = swiper.isHorizontal() ? 'vertical' : 'horizontal';\n        } else {\n          progressbarDirection = swiper.isHorizontal() ? 'horizontal' : 'vertical';\n        }\n        const scale = (current + 1) / total;\n        let scaleX = 1;\n        let scaleY = 1;\n        if (progressbarDirection === 'horizontal') {\n          scaleX = scale;\n        } else {\n          scaleY = scale;\n        }\n        subEl.querySelectorAll((0,_shared_classes_to_selector_mjs__WEBPACK_IMPORTED_MODULE_0__.c)(params.progressbarFillClass)).forEach(progressEl => {\n          progressEl.style.transform = `translate3d(0,0,0) scaleX(${scaleX}) scaleY(${scaleY})`;\n          progressEl.style.transitionDuration = `${swiper.params.speed}ms`;\n        });\n      }\n      if (params.type === 'custom' && params.renderCustom) {\n        subEl.innerHTML = params.renderCustom(swiper, current + 1, total);\n        if (subElIndex === 0) emit('paginationRender', subEl);\n      } else {\n        if (subElIndex === 0) emit('paginationRender', subEl);\n        emit('paginationUpdate', subEl);\n      }\n      if (swiper.params.watchOverflow && swiper.enabled) {\n        subEl.classList[swiper.isLocked ? 'add' : 'remove'](params.lockClass);\n      }\n    });\n  }\n  function render() {\n    // Render Container\n    const params = swiper.params.pagination;\n    if (isPaginationDisabled()) return;\n    const slidesLength = swiper.virtual && swiper.params.virtual.enabled ? swiper.virtual.slides.length : swiper.slides.length;\n    let el = swiper.pagination.el;\n    el = makeElementsArray(el);\n    let paginationHTML = '';\n    if (params.type === 'bullets') {\n      let numberOfBullets = swiper.params.loop ? Math.ceil(slidesLength / swiper.params.slidesPerGroup) : swiper.snapGrid.length;\n      if (swiper.params.freeMode && swiper.params.freeMode.enabled && numberOfBullets > slidesLength) {\n        numberOfBullets = slidesLength;\n      }\n      for (let i = 0; i < numberOfBullets; i += 1) {\n        if (params.renderBullet) {\n          paginationHTML += params.renderBullet.call(swiper, i, params.bulletClass);\n        } else {\n          // prettier-ignore\n          paginationHTML += `<${params.bulletElement} ${swiper.isElement ? 'part=\"bullet\"' : ''} class=\"${params.bulletClass}\"></${params.bulletElement}>`;\n        }\n      }\n    }\n    if (params.type === 'fraction') {\n      if (params.renderFraction) {\n        paginationHTML = params.renderFraction.call(swiper, params.currentClass, params.totalClass);\n      } else {\n        paginationHTML = `<span class=\"${params.currentClass}\"></span>` + ' / ' + `<span class=\"${params.totalClass}\"></span>`;\n      }\n    }\n    if (params.type === 'progressbar') {\n      if (params.renderProgressbar) {\n        paginationHTML = params.renderProgressbar.call(swiper, params.progressbarFillClass);\n      } else {\n        paginationHTML = `<span class=\"${params.progressbarFillClass}\"></span>`;\n      }\n    }\n    swiper.pagination.bullets = [];\n    el.forEach(subEl => {\n      if (params.type !== 'custom') {\n        subEl.innerHTML = paginationHTML || '';\n      }\n      if (params.type === 'bullets') {\n        swiper.pagination.bullets.push(...subEl.querySelectorAll((0,_shared_classes_to_selector_mjs__WEBPACK_IMPORTED_MODULE_0__.c)(params.bulletClass)));\n      }\n    });\n    if (params.type !== 'custom') {\n      emit('paginationRender', el[0]);\n    }\n  }\n  function init() {\n    swiper.params.pagination = (0,_shared_create_element_if_not_defined_mjs__WEBPACK_IMPORTED_MODULE_1__.c)(swiper, swiper.originalParams.pagination, swiper.params.pagination, {\n      el: 'swiper-pagination'\n    });\n    const params = swiper.params.pagination;\n    if (!params.el) return;\n    let el;\n    if (typeof params.el === 'string' && swiper.isElement) {\n      el = swiper.el.querySelector(params.el);\n    }\n    if (!el && typeof params.el === 'string') {\n      el = [...document.querySelectorAll(params.el)];\n    }\n    if (!el) {\n      el = params.el;\n    }\n    if (!el || el.length === 0) return;\n    if (swiper.params.uniqueNavElements && typeof params.el === 'string' && Array.isArray(el) && el.length > 1) {\n      el = [...swiper.el.querySelectorAll(params.el)];\n      // check if it belongs to another nested Swiper\n      if (el.length > 1) {\n        el = el.filter(subEl => {\n          if ((0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_2__.a)(subEl, '.swiper')[0] !== swiper.el) return false;\n          return true;\n        })[0];\n      }\n    }\n    if (Array.isArray(el) && el.length === 1) el = el[0];\n    Object.assign(swiper.pagination, {\n      el\n    });\n    el = makeElementsArray(el);\n    el.forEach(subEl => {\n      if (params.type === 'bullets' && params.clickable) {\n        subEl.classList.add(params.clickableClass);\n      }\n      subEl.classList.add(params.modifierClass + params.type);\n      subEl.classList.add(swiper.isHorizontal() ? params.horizontalClass : params.verticalClass);\n      if (params.type === 'bullets' && params.dynamicBullets) {\n        subEl.classList.add(`${params.modifierClass}${params.type}-dynamic`);\n        dynamicBulletIndex = 0;\n        if (params.dynamicMainBullets < 1) {\n          params.dynamicMainBullets = 1;\n        }\n      }\n      if (params.type === 'progressbar' && params.progressbarOpposite) {\n        subEl.classList.add(params.progressbarOppositeClass);\n      }\n      if (params.clickable) {\n        subEl.addEventListener('click', onBulletClick);\n      }\n      if (!swiper.enabled) {\n        subEl.classList.add(params.lockClass);\n      }\n    });\n  }\n  function destroy() {\n    const params = swiper.params.pagination;\n    if (isPaginationDisabled()) return;\n    let el = swiper.pagination.el;\n    if (el) {\n      el = makeElementsArray(el);\n      el.forEach(subEl => {\n        subEl.classList.remove(params.hiddenClass);\n        subEl.classList.remove(params.modifierClass + params.type);\n        subEl.classList.remove(swiper.isHorizontal() ? params.horizontalClass : params.verticalClass);\n        if (params.clickable) {\n          subEl.removeEventListener('click', onBulletClick);\n        }\n      });\n    }\n    if (swiper.pagination.bullets) swiper.pagination.bullets.forEach(subEl => subEl.classList.remove(...params.bulletActiveClass.split(' ')));\n  }\n  on('changeDirection', () => {\n    if (!swiper.pagination || !swiper.pagination.el) return;\n    const params = swiper.params.pagination;\n    let {\n      el\n    } = swiper.pagination;\n    el = makeElementsArray(el);\n    el.forEach(subEl => {\n      subEl.classList.remove(params.horizontalClass, params.verticalClass);\n      subEl.classList.add(swiper.isHorizontal() ? params.horizontalClass : params.verticalClass);\n    });\n  });\n  on('init', () => {\n    if (swiper.params.pagination.enabled === false) {\n      // eslint-disable-next-line\n      disable();\n    } else {\n      init();\n      render();\n      update();\n    }\n  });\n  on('activeIndexChange', () => {\n    if (typeof swiper.snapIndex === 'undefined') {\n      update();\n    }\n  });\n  on('snapIndexChange', () => {\n    update();\n  });\n  on('snapGridLengthChange', () => {\n    render();\n    update();\n  });\n  on('destroy', () => {\n    destroy();\n  });\n  on('enable disable', () => {\n    let {\n      el\n    } = swiper.pagination;\n    if (el) {\n      el = makeElementsArray(el);\n      el.forEach(subEl => subEl.classList[swiper.enabled ? 'remove' : 'add'](swiper.params.pagination.lockClass));\n    }\n  });\n  on('lock unlock', () => {\n    update();\n  });\n  on('click', (_s, e) => {\n    const targetEl = e.target;\n    const el = makeElementsArray(swiper.pagination.el);\n    if (swiper.params.pagination.el && swiper.params.pagination.hideOnClick && el && el.length > 0 && !targetEl.classList.contains(swiper.params.pagination.bulletClass)) {\n      if (swiper.navigation && (swiper.navigation.nextEl && targetEl === swiper.navigation.nextEl || swiper.navigation.prevEl && targetEl === swiper.navigation.prevEl)) return;\n      const isHidden = el[0].classList.contains(swiper.params.pagination.hiddenClass);\n      if (isHidden === true) {\n        emit('paginationShow');\n      } else {\n        emit('paginationHide');\n      }\n      el.forEach(subEl => subEl.classList.toggle(swiper.params.pagination.hiddenClass));\n    }\n  });\n  const enable = () => {\n    swiper.el.classList.remove(swiper.params.pagination.paginationDisabledClass);\n    let {\n      el\n    } = swiper.pagination;\n    if (el) {\n      el = makeElementsArray(el);\n      el.forEach(subEl => subEl.classList.remove(swiper.params.pagination.paginationDisabledClass));\n    }\n    init();\n    render();\n    update();\n  };\n  const disable = () => {\n    swiper.el.classList.add(swiper.params.pagination.paginationDisabledClass);\n    let {\n      el\n    } = swiper.pagination;\n    if (el) {\n      el = makeElementsArray(el);\n      el.forEach(subEl => subEl.classList.add(swiper.params.pagination.paginationDisabledClass));\n    }\n    destroy();\n  };\n  Object.assign(swiper.pagination, {\n    enable,\n    disable,\n    render,\n    update,\n    init,\n    destroy\n  });\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/modules/pagination.mjs?");

/***/ }),

/***/ "./node_modules/swiper/modules/parallax.mjs":
/*!**************************************************!*\
  !*** ./node_modules/swiper/modules/parallax.mjs ***!
  \**************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ Parallax)\n/* harmony export */ });\n/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/utils.mjs */ \"./node_modules/swiper/shared/utils.mjs\");\n\n\nfunction Parallax(_ref) {\n  let {\n    swiper,\n    extendParams,\n    on\n  } = _ref;\n  extendParams({\n    parallax: {\n      enabled: false\n    }\n  });\n  const setTransform = (el, progress) => {\n    const {\n      rtl\n    } = swiper;\n    const rtlFactor = rtl ? -1 : 1;\n    const p = el.getAttribute('data-swiper-parallax') || '0';\n    let x = el.getAttribute('data-swiper-parallax-x');\n    let y = el.getAttribute('data-swiper-parallax-y');\n    const scale = el.getAttribute('data-swiper-parallax-scale');\n    const opacity = el.getAttribute('data-swiper-parallax-opacity');\n    const rotate = el.getAttribute('data-swiper-parallax-rotate');\n    if (x || y) {\n      x = x || '0';\n      y = y || '0';\n    } else if (swiper.isHorizontal()) {\n      x = p;\n      y = '0';\n    } else {\n      y = p;\n      x = '0';\n    }\n    if (x.indexOf('%') >= 0) {\n      x = `${parseInt(x, 10) * progress * rtlFactor}%`;\n    } else {\n      x = `${x * progress * rtlFactor}px`;\n    }\n    if (y.indexOf('%') >= 0) {\n      y = `${parseInt(y, 10) * progress}%`;\n    } else {\n      y = `${y * progress}px`;\n    }\n    if (typeof opacity !== 'undefined' && opacity !== null) {\n      const currentOpacity = opacity - (opacity - 1) * (1 - Math.abs(progress));\n      el.style.opacity = currentOpacity;\n    }\n    let transform = `translate3d(${x}, ${y}, 0px)`;\n    if (typeof scale !== 'undefined' && scale !== null) {\n      const currentScale = scale - (scale - 1) * (1 - Math.abs(progress));\n      transform += ` scale(${currentScale})`;\n    }\n    if (rotate && typeof rotate !== 'undefined' && rotate !== null) {\n      const currentRotate = rotate * progress * -1;\n      transform += ` rotate(${currentRotate}deg)`;\n    }\n    el.style.transform = transform;\n  };\n  const setTranslate = () => {\n    const {\n      el,\n      slides,\n      progress,\n      snapGrid\n    } = swiper;\n    (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.e)(el, '[data-swiper-parallax], [data-swiper-parallax-x], [data-swiper-parallax-y], [data-swiper-parallax-opacity], [data-swiper-parallax-scale]').forEach(subEl => {\n      setTransform(subEl, progress);\n    });\n    slides.forEach((slideEl, slideIndex) => {\n      let slideProgress = slideEl.progress;\n      if (swiper.params.slidesPerGroup > 1 && swiper.params.slidesPerView !== 'auto') {\n        slideProgress += Math.ceil(slideIndex / 2) - progress * (snapGrid.length - 1);\n      }\n      slideProgress = Math.min(Math.max(slideProgress, -1), 1);\n      slideEl.querySelectorAll('[data-swiper-parallax], [data-swiper-parallax-x], [data-swiper-parallax-y], [data-swiper-parallax-opacity], [data-swiper-parallax-scale], [data-swiper-parallax-rotate]').forEach(subEl => {\n        setTransform(subEl, slideProgress);\n      });\n    });\n  };\n  const setTransition = function (duration) {\n    if (duration === void 0) {\n      duration = swiper.params.speed;\n    }\n    const {\n      el\n    } = swiper;\n    el.querySelectorAll('[data-swiper-parallax], [data-swiper-parallax-x], [data-swiper-parallax-y], [data-swiper-parallax-opacity], [data-swiper-parallax-scale]').forEach(parallaxEl => {\n      let parallaxDuration = parseInt(parallaxEl.getAttribute('data-swiper-parallax-duration'), 10) || duration;\n      if (duration === 0) parallaxDuration = 0;\n      parallaxEl.style.transitionDuration = `${parallaxDuration}ms`;\n    });\n  };\n  on('beforeInit', () => {\n    if (!swiper.params.parallax.enabled) return;\n    swiper.params.watchSlidesProgress = true;\n    swiper.originalParams.watchSlidesProgress = true;\n  });\n  on('init', () => {\n    if (!swiper.params.parallax.enabled) return;\n    setTranslate();\n  });\n  on('setTranslate', () => {\n    if (!swiper.params.parallax.enabled) return;\n    setTranslate();\n  });\n  on('setTransition', (_swiper, duration) => {\n    if (!swiper.params.parallax.enabled) return;\n    setTransition(duration);\n  });\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/modules/parallax.mjs?");

/***/ }),

/***/ "./node_modules/swiper/modules/scrollbar.mjs":
/*!***************************************************!*\
  !*** ./node_modules/swiper/modules/scrollbar.mjs ***!
  \***************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ Scrollbar)\n/* harmony export */ });\n/* harmony import */ var _shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/ssr-window.esm.mjs */ \"./node_modules/swiper/shared/ssr-window.esm.mjs\");\n/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/utils.mjs */ \"./node_modules/swiper/shared/utils.mjs\");\n/* harmony import */ var _shared_create_element_if_not_defined_mjs__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../shared/create-element-if-not-defined.mjs */ \"./node_modules/swiper/shared/create-element-if-not-defined.mjs\");\n\n\n\n\nfunction Scrollbar(_ref) {\n  let {\n    swiper,\n    extendParams,\n    on,\n    emit\n  } = _ref;\n  const document = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();\n  let isTouched = false;\n  let timeout = null;\n  let dragTimeout = null;\n  let dragStartPos;\n  let dragSize;\n  let trackSize;\n  let divider;\n  extendParams({\n    scrollbar: {\n      el: null,\n      dragSize: 'auto',\n      hide: false,\n      draggable: false,\n      snapOnRelease: true,\n      lockClass: 'swiper-scrollbar-lock',\n      dragClass: 'swiper-scrollbar-drag',\n      scrollbarDisabledClass: 'swiper-scrollbar-disabled',\n      horizontalClass: `swiper-scrollbar-horizontal`,\n      verticalClass: `swiper-scrollbar-vertical`\n    }\n  });\n  swiper.scrollbar = {\n    el: null,\n    dragEl: null\n  };\n  function setTranslate() {\n    if (!swiper.params.scrollbar.el || !swiper.scrollbar.el) return;\n    const {\n      scrollbar,\n      rtlTranslate: rtl\n    } = swiper;\n    const {\n      dragEl,\n      el\n    } = scrollbar;\n    const params = swiper.params.scrollbar;\n    const progress = swiper.params.loop ? swiper.progressLoop : swiper.progress;\n    let newSize = dragSize;\n    let newPos = (trackSize - dragSize) * progress;\n    if (rtl) {\n      newPos = -newPos;\n      if (newPos > 0) {\n        newSize = dragSize - newPos;\n        newPos = 0;\n      } else if (-newPos + dragSize > trackSize) {\n        newSize = trackSize + newPos;\n      }\n    } else if (newPos < 0) {\n      newSize = dragSize + newPos;\n      newPos = 0;\n    } else if (newPos + dragSize > trackSize) {\n      newSize = trackSize - newPos;\n    }\n    if (swiper.isHorizontal()) {\n      dragEl.style.transform = `translate3d(${newPos}px, 0, 0)`;\n      dragEl.style.width = `${newSize}px`;\n    } else {\n      dragEl.style.transform = `translate3d(0px, ${newPos}px, 0)`;\n      dragEl.style.height = `${newSize}px`;\n    }\n    if (params.hide) {\n      clearTimeout(timeout);\n      el.style.opacity = 1;\n      timeout = setTimeout(() => {\n        el.style.opacity = 0;\n        el.style.transitionDuration = '400ms';\n      }, 1000);\n    }\n  }\n  function setTransition(duration) {\n    if (!swiper.params.scrollbar.el || !swiper.scrollbar.el) return;\n    swiper.scrollbar.dragEl.style.transitionDuration = `${duration}ms`;\n  }\n  function updateSize() {\n    if (!swiper.params.scrollbar.el || !swiper.scrollbar.el) return;\n    const {\n      scrollbar\n    } = swiper;\n    const {\n      dragEl,\n      el\n    } = scrollbar;\n    dragEl.style.width = '';\n    dragEl.style.height = '';\n    trackSize = swiper.isHorizontal() ? el.offsetWidth : el.offsetHeight;\n    divider = swiper.size / (swiper.virtualSize + swiper.params.slidesOffsetBefore - (swiper.params.centeredSlides ? swiper.snapGrid[0] : 0));\n    if (swiper.params.scrollbar.dragSize === 'auto') {\n      dragSize = trackSize * divider;\n    } else {\n      dragSize = parseInt(swiper.params.scrollbar.dragSize, 10);\n    }\n    if (swiper.isHorizontal()) {\n      dragEl.style.width = `${dragSize}px`;\n    } else {\n      dragEl.style.height = `${dragSize}px`;\n    }\n    if (divider >= 1) {\n      el.style.display = 'none';\n    } else {\n      el.style.display = '';\n    }\n    if (swiper.params.scrollbar.hide) {\n      el.style.opacity = 0;\n    }\n    if (swiper.params.watchOverflow && swiper.enabled) {\n      scrollbar.el.classList[swiper.isLocked ? 'add' : 'remove'](swiper.params.scrollbar.lockClass);\n    }\n  }\n  function getPointerPosition(e) {\n    return swiper.isHorizontal() ? e.clientX : e.clientY;\n  }\n  function setDragPosition(e) {\n    const {\n      scrollbar,\n      rtlTranslate: rtl\n    } = swiper;\n    const {\n      el\n    } = scrollbar;\n    let positionRatio;\n    positionRatio = (getPointerPosition(e) - (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.b)(el)[swiper.isHorizontal() ? 'left' : 'top'] - (dragStartPos !== null ? dragStartPos : dragSize / 2)) / (trackSize - dragSize);\n    positionRatio = Math.max(Math.min(positionRatio, 1), 0);\n    if (rtl) {\n      positionRatio = 1 - positionRatio;\n    }\n    const position = swiper.minTranslate() + (swiper.maxTranslate() - swiper.minTranslate()) * positionRatio;\n    swiper.updateProgress(position);\n    swiper.setTranslate(position);\n    swiper.updateActiveIndex();\n    swiper.updateSlidesClasses();\n  }\n  function onDragStart(e) {\n    const params = swiper.params.scrollbar;\n    const {\n      scrollbar,\n      wrapperEl\n    } = swiper;\n    const {\n      el,\n      dragEl\n    } = scrollbar;\n    isTouched = true;\n    dragStartPos = e.target === dragEl ? getPointerPosition(e) - e.target.getBoundingClientRect()[swiper.isHorizontal() ? 'left' : 'top'] : null;\n    e.preventDefault();\n    e.stopPropagation();\n    wrapperEl.style.transitionDuration = '100ms';\n    dragEl.style.transitionDuration = '100ms';\n    setDragPosition(e);\n    clearTimeout(dragTimeout);\n    el.style.transitionDuration = '0ms';\n    if (params.hide) {\n      el.style.opacity = 1;\n    }\n    if (swiper.params.cssMode) {\n      swiper.wrapperEl.style['scroll-snap-type'] = 'none';\n    }\n    emit('scrollbarDragStart', e);\n  }\n  function onDragMove(e) {\n    const {\n      scrollbar,\n      wrapperEl\n    } = swiper;\n    const {\n      el,\n      dragEl\n    } = scrollbar;\n    if (!isTouched) return;\n    if (e.preventDefault) e.preventDefault();else e.returnValue = false;\n    setDragPosition(e);\n    wrapperEl.style.transitionDuration = '0ms';\n    el.style.transitionDuration = '0ms';\n    dragEl.style.transitionDuration = '0ms';\n    emit('scrollbarDragMove', e);\n  }\n  function onDragEnd(e) {\n    const params = swiper.params.scrollbar;\n    const {\n      scrollbar,\n      wrapperEl\n    } = swiper;\n    const {\n      el\n    } = scrollbar;\n    if (!isTouched) return;\n    isTouched = false;\n    if (swiper.params.cssMode) {\n      swiper.wrapperEl.style['scroll-snap-type'] = '';\n      wrapperEl.style.transitionDuration = '';\n    }\n    if (params.hide) {\n      clearTimeout(dragTimeout);\n      dragTimeout = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.n)(() => {\n        el.style.opacity = 0;\n        el.style.transitionDuration = '400ms';\n      }, 1000);\n    }\n    emit('scrollbarDragEnd', e);\n    if (params.snapOnRelease) {\n      swiper.slideToClosest();\n    }\n  }\n  function events(method) {\n    const {\n      scrollbar,\n      params\n    } = swiper;\n    const el = scrollbar.el;\n    if (!el) return;\n    const target = el;\n    const activeListener = params.passiveListeners ? {\n      passive: false,\n      capture: false\n    } : false;\n    const passiveListener = params.passiveListeners ? {\n      passive: true,\n      capture: false\n    } : false;\n    if (!target) return;\n    const eventMethod = method === 'on' ? 'addEventListener' : 'removeEventListener';\n    target[eventMethod]('pointerdown', onDragStart, activeListener);\n    document[eventMethod]('pointermove', onDragMove, activeListener);\n    document[eventMethod]('pointerup', onDragEnd, passiveListener);\n  }\n  function enableDraggable() {\n    if (!swiper.params.scrollbar.el || !swiper.scrollbar.el) return;\n    events('on');\n  }\n  function disableDraggable() {\n    if (!swiper.params.scrollbar.el || !swiper.scrollbar.el) return;\n    events('off');\n  }\n  function init() {\n    const {\n      scrollbar,\n      el: swiperEl\n    } = swiper;\n    swiper.params.scrollbar = (0,_shared_create_element_if_not_defined_mjs__WEBPACK_IMPORTED_MODULE_2__.c)(swiper, swiper.originalParams.scrollbar, swiper.params.scrollbar, {\n      el: 'swiper-scrollbar'\n    });\n    const params = swiper.params.scrollbar;\n    if (!params.el) return;\n    let el;\n    if (typeof params.el === 'string' && swiper.isElement) {\n      el = swiper.el.querySelector(params.el);\n    }\n    if (!el && typeof params.el === 'string') {\n      el = document.querySelectorAll(params.el);\n    } else if (!el) {\n      el = params.el;\n    }\n    if (swiper.params.uniqueNavElements && typeof params.el === 'string' && el.length > 1 && swiperEl.querySelectorAll(params.el).length === 1) {\n      el = swiperEl.querySelector(params.el);\n    }\n    if (el.length > 0) el = el[0];\n    el.classList.add(swiper.isHorizontal() ? params.horizontalClass : params.verticalClass);\n    let dragEl;\n    if (el) {\n      dragEl = el.querySelector(`.${swiper.params.scrollbar.dragClass}`);\n      if (!dragEl) {\n        dragEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.c)('div', swiper.params.scrollbar.dragClass);\n        el.append(dragEl);\n      }\n    }\n    Object.assign(scrollbar, {\n      el,\n      dragEl\n    });\n    if (params.draggable) {\n      enableDraggable();\n    }\n    if (el) {\n      el.classList[swiper.enabled ? 'remove' : 'add'](swiper.params.scrollbar.lockClass);\n    }\n  }\n  function destroy() {\n    const params = swiper.params.scrollbar;\n    const el = swiper.scrollbar.el;\n    if (el) {\n      el.classList.remove(swiper.isHorizontal() ? params.horizontalClass : params.verticalClass);\n    }\n    disableDraggable();\n  }\n  on('init', () => {\n    if (swiper.params.scrollbar.enabled === false) {\n      // eslint-disable-next-line\n      disable();\n    } else {\n      init();\n      updateSize();\n      setTranslate();\n    }\n  });\n  on('update resize observerUpdate lock unlock', () => {\n    updateSize();\n  });\n  on('setTranslate', () => {\n    setTranslate();\n  });\n  on('setTransition', (_s, duration) => {\n    setTransition(duration);\n  });\n  on('enable disable', () => {\n    const {\n      el\n    } = swiper.scrollbar;\n    if (el) {\n      el.classList[swiper.enabled ? 'remove' : 'add'](swiper.params.scrollbar.lockClass);\n    }\n  });\n  on('destroy', () => {\n    destroy();\n  });\n  const enable = () => {\n    swiper.el.classList.remove(swiper.params.scrollbar.scrollbarDisabledClass);\n    if (swiper.scrollbar.el) {\n      swiper.scrollbar.el.classList.remove(swiper.params.scrollbar.scrollbarDisabledClass);\n    }\n    init();\n    updateSize();\n    setTranslate();\n  };\n  const disable = () => {\n    swiper.el.classList.add(swiper.params.scrollbar.scrollbarDisabledClass);\n    if (swiper.scrollbar.el) {\n      swiper.scrollbar.el.classList.add(swiper.params.scrollbar.scrollbarDisabledClass);\n    }\n    destroy();\n  };\n  Object.assign(swiper.scrollbar, {\n    enable,\n    disable,\n    updateSize,\n    setTranslate,\n    init,\n    destroy\n  });\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/modules/scrollbar.mjs?");

/***/ }),

/***/ "./node_modules/swiper/modules/thumbs.mjs":
/*!************************************************!*\
  !*** ./node_modules/swiper/modules/thumbs.mjs ***!
  \************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ Thumb)\n/* harmony export */ });\n/* harmony import */ var _shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/ssr-window.esm.mjs */ \"./node_modules/swiper/shared/ssr-window.esm.mjs\");\n/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/utils.mjs */ \"./node_modules/swiper/shared/utils.mjs\");\n\n\n\nfunction Thumb(_ref) {\n  let {\n    swiper,\n    extendParams,\n    on\n  } = _ref;\n  extendParams({\n    thumbs: {\n      swiper: null,\n      multipleActiveThumbs: true,\n      autoScrollOffset: 0,\n      slideThumbActiveClass: 'swiper-slide-thumb-active',\n      thumbsContainerClass: 'swiper-thumbs'\n    }\n  });\n  let initialized = false;\n  let swiperCreated = false;\n  swiper.thumbs = {\n    swiper: null\n  };\n  function onThumbClick() {\n    const thumbsSwiper = swiper.thumbs.swiper;\n    if (!thumbsSwiper || thumbsSwiper.destroyed) return;\n    const clickedIndex = thumbsSwiper.clickedIndex;\n    const clickedSlide = thumbsSwiper.clickedSlide;\n    if (clickedSlide && clickedSlide.classList.contains(swiper.params.thumbs.slideThumbActiveClass)) return;\n    if (typeof clickedIndex === 'undefined' || clickedIndex === null) return;\n    let slideToIndex;\n    if (thumbsSwiper.params.loop) {\n      slideToIndex = parseInt(thumbsSwiper.clickedSlide.getAttribute('data-swiper-slide-index'), 10);\n    } else {\n      slideToIndex = clickedIndex;\n    }\n    if (swiper.params.loop) {\n      swiper.slideToLoop(slideToIndex);\n    } else {\n      swiper.slideTo(slideToIndex);\n    }\n  }\n  function init() {\n    const {\n      thumbs: thumbsParams\n    } = swiper.params;\n    if (initialized) return false;\n    initialized = true;\n    const SwiperClass = swiper.constructor;\n    if (thumbsParams.swiper instanceof SwiperClass) {\n      swiper.thumbs.swiper = thumbsParams.swiper;\n      Object.assign(swiper.thumbs.swiper.originalParams, {\n        watchSlidesProgress: true,\n        slideToClickedSlide: false\n      });\n      Object.assign(swiper.thumbs.swiper.params, {\n        watchSlidesProgress: true,\n        slideToClickedSlide: false\n      });\n      swiper.thumbs.swiper.update();\n    } else if ((0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.j)(thumbsParams.swiper)) {\n      const thumbsSwiperParams = Object.assign({}, thumbsParams.swiper);\n      Object.assign(thumbsSwiperParams, {\n        watchSlidesProgress: true,\n        slideToClickedSlide: false\n      });\n      swiper.thumbs.swiper = new SwiperClass(thumbsSwiperParams);\n      swiperCreated = true;\n    }\n    swiper.thumbs.swiper.el.classList.add(swiper.params.thumbs.thumbsContainerClass);\n    swiper.thumbs.swiper.on('tap', onThumbClick);\n    return true;\n  }\n  function update(initial) {\n    const thumbsSwiper = swiper.thumbs.swiper;\n    if (!thumbsSwiper || thumbsSwiper.destroyed) return;\n    const slidesPerView = thumbsSwiper.params.slidesPerView === 'auto' ? thumbsSwiper.slidesPerViewDynamic() : thumbsSwiper.params.slidesPerView;\n\n    // Activate thumbs\n    let thumbsToActivate = 1;\n    const thumbActiveClass = swiper.params.thumbs.slideThumbActiveClass;\n    if (swiper.params.slidesPerView > 1 && !swiper.params.centeredSlides) {\n      thumbsToActivate = swiper.params.slidesPerView;\n    }\n    if (!swiper.params.thumbs.multipleActiveThumbs) {\n      thumbsToActivate = 1;\n    }\n    thumbsToActivate = Math.floor(thumbsToActivate);\n    thumbsSwiper.slides.forEach(slideEl => slideEl.classList.remove(thumbActiveClass));\n    if (thumbsSwiper.params.loop || thumbsSwiper.params.virtual && thumbsSwiper.params.virtual.enabled) {\n      for (let i = 0; i < thumbsToActivate; i += 1) {\n        (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(thumbsSwiper.slidesEl, `[data-swiper-slide-index=\"${swiper.realIndex + i}\"]`).forEach(slideEl => {\n          slideEl.classList.add(thumbActiveClass);\n        });\n      }\n    } else {\n      for (let i = 0; i < thumbsToActivate; i += 1) {\n        if (thumbsSwiper.slides[swiper.realIndex + i]) {\n          thumbsSwiper.slides[swiper.realIndex + i].classList.add(thumbActiveClass);\n        }\n      }\n    }\n    const autoScrollOffset = swiper.params.thumbs.autoScrollOffset;\n    const useOffset = autoScrollOffset && !thumbsSwiper.params.loop;\n    if (swiper.realIndex !== thumbsSwiper.realIndex || useOffset) {\n      const currentThumbsIndex = thumbsSwiper.activeIndex;\n      let newThumbsIndex;\n      let direction;\n      if (thumbsSwiper.params.loop) {\n        const newThumbsSlide = thumbsSwiper.slides.filter(slideEl => slideEl.getAttribute('data-swiper-slide-index') === `${swiper.realIndex}`)[0];\n        newThumbsIndex = thumbsSwiper.slides.indexOf(newThumbsSlide);\n        direction = swiper.activeIndex > swiper.previousIndex ? 'next' : 'prev';\n      } else {\n        newThumbsIndex = swiper.realIndex;\n        direction = newThumbsIndex > swiper.previousIndex ? 'next' : 'prev';\n      }\n      if (useOffset) {\n        newThumbsIndex += direction === 'next' ? autoScrollOffset : -1 * autoScrollOffset;\n      }\n      if (thumbsSwiper.visibleSlidesIndexes && thumbsSwiper.visibleSlidesIndexes.indexOf(newThumbsIndex) < 0) {\n        if (thumbsSwiper.params.centeredSlides) {\n          if (newThumbsIndex > currentThumbsIndex) {\n            newThumbsIndex = newThumbsIndex - Math.floor(slidesPerView / 2) + 1;\n          } else {\n            newThumbsIndex = newThumbsIndex + Math.floor(slidesPerView / 2) - 1;\n          }\n        } else if (newThumbsIndex > currentThumbsIndex && thumbsSwiper.params.slidesPerGroup === 1) ;\n        thumbsSwiper.slideTo(newThumbsIndex, initial ? 0 : undefined);\n      }\n    }\n  }\n  on('beforeInit', () => {\n    const {\n      thumbs\n    } = swiper.params;\n    if (!thumbs || !thumbs.swiper) return;\n    if (typeof thumbs.swiper === 'string' || thumbs.swiper instanceof HTMLElement) {\n      const document = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();\n      const getThumbsElementAndInit = () => {\n        const thumbsElement = typeof thumbs.swiper === 'string' ? document.querySelector(thumbs.swiper) : thumbs.swiper;\n        if (thumbsElement && thumbsElement.swiper) {\n          thumbs.swiper = thumbsElement.swiper;\n          init();\n          update(true);\n        } else if (thumbsElement) {\n          const onThumbsSwiper = e => {\n            thumbs.swiper = e.detail[0];\n            thumbsElement.removeEventListener('init', onThumbsSwiper);\n            init();\n            update(true);\n            thumbs.swiper.update();\n            swiper.update();\n          };\n          thumbsElement.addEventListener('init', onThumbsSwiper);\n        }\n        return thumbsElement;\n      };\n      const watchForThumbsToAppear = () => {\n        if (swiper.destroyed) return;\n        const thumbsElement = getThumbsElementAndInit();\n        if (!thumbsElement) {\n          requestAnimationFrame(watchForThumbsToAppear);\n        }\n      };\n      requestAnimationFrame(watchForThumbsToAppear);\n    } else {\n      init();\n      update(true);\n    }\n  });\n  on('slideChange update resize observerUpdate', () => {\n    update();\n  });\n  on('setTransition', (_s, duration) => {\n    const thumbsSwiper = swiper.thumbs.swiper;\n    if (!thumbsSwiper || thumbsSwiper.destroyed) return;\n    thumbsSwiper.setTransition(duration);\n  });\n  on('beforeDestroy', () => {\n    const thumbsSwiper = swiper.thumbs.swiper;\n    if (!thumbsSwiper || thumbsSwiper.destroyed) return;\n    if (swiperCreated) {\n      thumbsSwiper.destroy();\n    }\n  });\n  Object.assign(swiper.thumbs, {\n    init,\n    update\n  });\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/modules/thumbs.mjs?");

/***/ }),

/***/ "./node_modules/swiper/modules/virtual.mjs":
/*!*************************************************!*\
  !*** ./node_modules/swiper/modules/virtual.mjs ***!
  \*************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ Virtual)\n/* harmony export */ });\n/* harmony import */ var _shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/ssr-window.esm.mjs */ \"./node_modules/swiper/shared/ssr-window.esm.mjs\");\n/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/utils.mjs */ \"./node_modules/swiper/shared/utils.mjs\");\n\n\n\nfunction Virtual(_ref) {\n  let {\n    swiper,\n    extendParams,\n    on,\n    emit\n  } = _ref;\n  extendParams({\n    virtual: {\n      enabled: false,\n      slides: [],\n      cache: true,\n      renderSlide: null,\n      renderExternal: null,\n      renderExternalUpdate: true,\n      addSlidesBefore: 0,\n      addSlidesAfter: 0\n    }\n  });\n  let cssModeTimeout;\n  const document = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();\n  swiper.virtual = {\n    cache: {},\n    from: undefined,\n    to: undefined,\n    slides: [],\n    offset: 0,\n    slidesGrid: []\n  };\n  const tempDOM = document.createElement('div');\n  function renderSlide(slide, index) {\n    const params = swiper.params.virtual;\n    if (params.cache && swiper.virtual.cache[index]) {\n      return swiper.virtual.cache[index];\n    }\n    // eslint-disable-next-line\n    let slideEl;\n    if (params.renderSlide) {\n      slideEl = params.renderSlide.call(swiper, slide, index);\n      if (typeof slideEl === 'string') {\n        tempDOM.innerHTML = slideEl;\n        slideEl = tempDOM.children[0];\n      }\n    } else if (swiper.isElement) {\n      slideEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.c)('swiper-slide');\n    } else {\n      slideEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.c)('div', swiper.params.slideClass);\n    }\n    slideEl.setAttribute('data-swiper-slide-index', index);\n    if (!params.renderSlide) {\n      slideEl.innerHTML = slide;\n    }\n    if (params.cache) swiper.virtual.cache[index] = slideEl;\n    return slideEl;\n  }\n  function update(force) {\n    const {\n      slidesPerView,\n      slidesPerGroup,\n      centeredSlides,\n      loop: isLoop\n    } = swiper.params;\n    const {\n      addSlidesBefore,\n      addSlidesAfter\n    } = swiper.params.virtual;\n    const {\n      from: previousFrom,\n      to: previousTo,\n      slides,\n      slidesGrid: previousSlidesGrid,\n      offset: previousOffset\n    } = swiper.virtual;\n    if (!swiper.params.cssMode) {\n      swiper.updateActiveIndex();\n    }\n    const activeIndex = swiper.activeIndex || 0;\n    let offsetProp;\n    if (swiper.rtlTranslate) offsetProp = 'right';else offsetProp = swiper.isHorizontal() ? 'left' : 'top';\n    let slidesAfter;\n    let slidesBefore;\n    if (centeredSlides) {\n      slidesAfter = Math.floor(slidesPerView / 2) + slidesPerGroup + addSlidesAfter;\n      slidesBefore = Math.floor(slidesPerView / 2) + slidesPerGroup + addSlidesBefore;\n    } else {\n      slidesAfter = slidesPerView + (slidesPerGroup - 1) + addSlidesAfter;\n      slidesBefore = (isLoop ? slidesPerView : slidesPerGroup) + addSlidesBefore;\n    }\n    let from = activeIndex - slidesBefore;\n    let to = activeIndex + slidesAfter;\n    if (!isLoop) {\n      from = Math.max(from, 0);\n      to = Math.min(to, slides.length - 1);\n    }\n    let offset = (swiper.slidesGrid[from] || 0) - (swiper.slidesGrid[0] || 0);\n    if (isLoop && activeIndex >= slidesBefore) {\n      from -= slidesBefore;\n      if (!centeredSlides) offset += swiper.slidesGrid[0];\n    } else if (isLoop && activeIndex < slidesBefore) {\n      from = -slidesBefore;\n      if (centeredSlides) offset += swiper.slidesGrid[0];\n    }\n    Object.assign(swiper.virtual, {\n      from,\n      to,\n      offset,\n      slidesGrid: swiper.slidesGrid,\n      slidesBefore,\n      slidesAfter\n    });\n    function onRendered() {\n      swiper.updateSlides();\n      swiper.updateProgress();\n      swiper.updateSlidesClasses();\n      emit('virtualUpdate');\n    }\n    if (previousFrom === from && previousTo === to && !force) {\n      if (swiper.slidesGrid !== previousSlidesGrid && offset !== previousOffset) {\n        swiper.slides.forEach(slideEl => {\n          slideEl.style[offsetProp] = `${offset - Math.abs(swiper.cssOverflowAdjustment())}px`;\n        });\n      }\n      swiper.updateProgress();\n      emit('virtualUpdate');\n      return;\n    }\n    if (swiper.params.virtual.renderExternal) {\n      swiper.params.virtual.renderExternal.call(swiper, {\n        offset,\n        from,\n        to,\n        slides: function getSlides() {\n          const slidesToRender = [];\n          for (let i = from; i <= to; i += 1) {\n            slidesToRender.push(slides[i]);\n          }\n          return slidesToRender;\n        }()\n      });\n      if (swiper.params.virtual.renderExternalUpdate) {\n        onRendered();\n      } else {\n        emit('virtualUpdate');\n      }\n      return;\n    }\n    const prependIndexes = [];\n    const appendIndexes = [];\n    const getSlideIndex = index => {\n      let slideIndex = index;\n      if (index < 0) {\n        slideIndex = slides.length + index;\n      } else if (slideIndex >= slides.length) {\n        // eslint-disable-next-line\n        slideIndex = slideIndex - slides.length;\n      }\n      return slideIndex;\n    };\n    if (force) {\n      swiper.slidesEl.querySelectorAll(`.${swiper.params.slideClass}, swiper-slide`).forEach(slideEl => {\n        slideEl.remove();\n      });\n    } else {\n      for (let i = previousFrom; i <= previousTo; i += 1) {\n        if (i < from || i > to) {\n          const slideIndex = getSlideIndex(i);\n          swiper.slidesEl.querySelectorAll(`.${swiper.params.slideClass}[data-swiper-slide-index=\"${slideIndex}\"], swiper-slide[data-swiper-slide-index=\"${slideIndex}\"]`).forEach(slideEl => {\n            slideEl.remove();\n          });\n        }\n      }\n    }\n    const loopFrom = isLoop ? -slides.length : 0;\n    const loopTo = isLoop ? slides.length * 2 : slides.length;\n    for (let i = loopFrom; i < loopTo; i += 1) {\n      if (i >= from && i <= to) {\n        const slideIndex = getSlideIndex(i);\n        if (typeof previousTo === 'undefined' || force) {\n          appendIndexes.push(slideIndex);\n        } else {\n          if (i > previousTo) appendIndexes.push(slideIndex);\n          if (i < previousFrom) prependIndexes.push(slideIndex);\n        }\n      }\n    }\n    appendIndexes.forEach(index => {\n      swiper.slidesEl.append(renderSlide(slides[index], index));\n    });\n    if (isLoop) {\n      for (let i = prependIndexes.length - 1; i >= 0; i -= 1) {\n        const index = prependIndexes[i];\n        swiper.slidesEl.prepend(renderSlide(slides[index], index));\n      }\n    } else {\n      prependIndexes.sort((a, b) => b - a);\n      prependIndexes.forEach(index => {\n        swiper.slidesEl.prepend(renderSlide(slides[index], index));\n      });\n    }\n    (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(swiper.slidesEl, '.swiper-slide, swiper-slide').forEach(slideEl => {\n      slideEl.style[offsetProp] = `${offset - Math.abs(swiper.cssOverflowAdjustment())}px`;\n    });\n    onRendered();\n  }\n  function appendSlide(slides) {\n    if (typeof slides === 'object' && 'length' in slides) {\n      for (let i = 0; i < slides.length; i += 1) {\n        if (slides[i]) swiper.virtual.slides.push(slides[i]);\n      }\n    } else {\n      swiper.virtual.slides.push(slides);\n    }\n    update(true);\n  }\n  function prependSlide(slides) {\n    const activeIndex = swiper.activeIndex;\n    let newActiveIndex = activeIndex + 1;\n    let numberOfNewSlides = 1;\n    if (Array.isArray(slides)) {\n      for (let i = 0; i < slides.length; i += 1) {\n        if (slides[i]) swiper.virtual.slides.unshift(slides[i]);\n      }\n      newActiveIndex = activeIndex + slides.length;\n      numberOfNewSlides = slides.length;\n    } else {\n      swiper.virtual.slides.unshift(slides);\n    }\n    if (swiper.params.virtual.cache) {\n      const cache = swiper.virtual.cache;\n      const newCache = {};\n      Object.keys(cache).forEach(cachedIndex => {\n        const cachedEl = cache[cachedIndex];\n        const cachedElIndex = cachedEl.getAttribute('data-swiper-slide-index');\n        if (cachedElIndex) {\n          cachedEl.setAttribute('data-swiper-slide-index', parseInt(cachedElIndex, 10) + numberOfNewSlides);\n        }\n        newCache[parseInt(cachedIndex, 10) + numberOfNewSlides] = cachedEl;\n      });\n      swiper.virtual.cache = newCache;\n    }\n    update(true);\n    swiper.slideTo(newActiveIndex, 0);\n  }\n  function removeSlide(slidesIndexes) {\n    if (typeof slidesIndexes === 'undefined' || slidesIndexes === null) return;\n    let activeIndex = swiper.activeIndex;\n    if (Array.isArray(slidesIndexes)) {\n      for (let i = slidesIndexes.length - 1; i >= 0; i -= 1) {\n        swiper.virtual.slides.splice(slidesIndexes[i], 1);\n        if (swiper.params.virtual.cache) {\n          delete swiper.virtual.cache[slidesIndexes[i]];\n        }\n        if (slidesIndexes[i] < activeIndex) activeIndex -= 1;\n        activeIndex = Math.max(activeIndex, 0);\n      }\n    } else {\n      swiper.virtual.slides.splice(slidesIndexes, 1);\n      if (swiper.params.virtual.cache) {\n        delete swiper.virtual.cache[slidesIndexes];\n      }\n      if (slidesIndexes < activeIndex) activeIndex -= 1;\n      activeIndex = Math.max(activeIndex, 0);\n    }\n    update(true);\n    swiper.slideTo(activeIndex, 0);\n  }\n  function removeAllSlides() {\n    swiper.virtual.slides = [];\n    if (swiper.params.virtual.cache) {\n      swiper.virtual.cache = {};\n    }\n    update(true);\n    swiper.slideTo(0, 0);\n  }\n  on('beforeInit', () => {\n    if (!swiper.params.virtual.enabled) return;\n    let domSlidesAssigned;\n    if (typeof swiper.passedParams.virtual.slides === 'undefined') {\n      const slides = [...swiper.slidesEl.children].filter(el => el.matches(`.${swiper.params.slideClass}, swiper-slide`));\n      if (slides && slides.length) {\n        swiper.virtual.slides = [...slides];\n        domSlidesAssigned = true;\n        slides.forEach((slideEl, slideIndex) => {\n          slideEl.setAttribute('data-swiper-slide-index', slideIndex);\n          swiper.virtual.cache[slideIndex] = slideEl;\n          slideEl.remove();\n        });\n      }\n    }\n    if (!domSlidesAssigned) {\n      swiper.virtual.slides = swiper.params.virtual.slides;\n    }\n    swiper.classNames.push(`${swiper.params.containerModifierClass}virtual`);\n    swiper.params.watchSlidesProgress = true;\n    swiper.originalParams.watchSlidesProgress = true;\n    if (!swiper.params.initialSlide) {\n      update();\n    }\n  });\n  on('setTranslate', () => {\n    if (!swiper.params.virtual.enabled) return;\n    if (swiper.params.cssMode && !swiper._immediateVirtual) {\n      clearTimeout(cssModeTimeout);\n      cssModeTimeout = setTimeout(() => {\n        update();\n      }, 100);\n    } else {\n      update();\n    }\n  });\n  on('init update resize', () => {\n    if (!swiper.params.virtual.enabled) return;\n    if (swiper.params.cssMode) {\n      (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.s)(swiper.wrapperEl, '--swiper-virtual-size', `${swiper.virtualSize}px`);\n    }\n  });\n  Object.assign(swiper.virtual, {\n    appendSlide,\n    prependSlide,\n    removeSlide,\n    removeAllSlides,\n    update\n  });\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/modules/virtual.mjs?");

/***/ }),

/***/ "./node_modules/swiper/modules/zoom.mjs":
/*!**********************************************!*\
  !*** ./node_modules/swiper/modules/zoom.mjs ***!
  \**********************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ Zoom)\n/* harmony export */ });\n/* harmony import */ var _shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../shared/ssr-window.esm.mjs */ \"./node_modules/swiper/shared/ssr-window.esm.mjs\");\n/* harmony import */ var _shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../shared/utils.mjs */ \"./node_modules/swiper/shared/utils.mjs\");\n\n\n\nfunction Zoom(_ref) {\n  let {\n    swiper,\n    extendParams,\n    on,\n    emit\n  } = _ref;\n  const window = (0,_shared_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();\n  extendParams({\n    zoom: {\n      enabled: false,\n      maxRatio: 3,\n      minRatio: 1,\n      toggle: true,\n      containerClass: 'swiper-zoom-container',\n      zoomedSlideClass: 'swiper-slide-zoomed'\n    }\n  });\n  swiper.zoom = {\n    enabled: false\n  };\n  let currentScale = 1;\n  let isScaling = false;\n  let fakeGestureTouched;\n  let fakeGestureMoved;\n  const evCache = [];\n  const gesture = {\n    originX: 0,\n    originY: 0,\n    slideEl: undefined,\n    slideWidth: undefined,\n    slideHeight: undefined,\n    imageEl: undefined,\n    imageWrapEl: undefined,\n    maxRatio: 3\n  };\n  const image = {\n    isTouched: undefined,\n    isMoved: undefined,\n    currentX: undefined,\n    currentY: undefined,\n    minX: undefined,\n    minY: undefined,\n    maxX: undefined,\n    maxY: undefined,\n    width: undefined,\n    height: undefined,\n    startX: undefined,\n    startY: undefined,\n    touchesStart: {},\n    touchesCurrent: {}\n  };\n  const velocity = {\n    x: undefined,\n    y: undefined,\n    prevPositionX: undefined,\n    prevPositionY: undefined,\n    prevTime: undefined\n  };\n  let scale = 1;\n  Object.defineProperty(swiper.zoom, 'scale', {\n    get() {\n      return scale;\n    },\n    set(value) {\n      if (scale !== value) {\n        const imageEl = gesture.imageEl;\n        const slideEl = gesture.slideEl;\n        emit('zoomChange', value, imageEl, slideEl);\n      }\n      scale = value;\n    }\n  });\n  function getDistanceBetweenTouches() {\n    if (evCache.length < 2) return 1;\n    const x1 = evCache[0].pageX;\n    const y1 = evCache[0].pageY;\n    const x2 = evCache[1].pageX;\n    const y2 = evCache[1].pageY;\n    const distance = Math.sqrt((x2 - x1) ** 2 + (y2 - y1) ** 2);\n    return distance;\n  }\n  function getScaleOrigin() {\n    if (evCache.length < 2) return {\n      x: null,\n      y: null\n    };\n    const box = gesture.imageEl.getBoundingClientRect();\n    return [(evCache[0].pageX + (evCache[1].pageX - evCache[0].pageX) / 2 - box.x) / currentScale, (evCache[0].pageY + (evCache[1].pageY - evCache[0].pageY) / 2 - box.y) / currentScale];\n  }\n  function getSlideSelector() {\n    return swiper.isElement ? `swiper-slide` : `.${swiper.params.slideClass}`;\n  }\n  function eventWithinSlide(e) {\n    const slideSelector = getSlideSelector();\n    if (e.target.matches(slideSelector)) return true;\n    if (swiper.slides.filter(slideEl => slideEl.contains(e.target)).length > 0) return true;\n    return false;\n  }\n  function eventWithinZoomContainer(e) {\n    const selector = `.${swiper.params.zoom.containerClass}`;\n    if (e.target.matches(selector)) return true;\n    if ([...swiper.hostEl.querySelectorAll(selector)].filter(containerEl => containerEl.contains(e.target)).length > 0) return true;\n    return false;\n  }\n\n  // Events\n  function onGestureStart(e) {\n    if (e.pointerType === 'mouse') {\n      evCache.splice(0, evCache.length);\n    }\n    if (!eventWithinSlide(e)) return;\n    const params = swiper.params.zoom;\n    fakeGestureTouched = false;\n    fakeGestureMoved = false;\n    evCache.push(e);\n    if (evCache.length < 2) {\n      return;\n    }\n    fakeGestureTouched = true;\n    gesture.scaleStart = getDistanceBetweenTouches();\n    if (!gesture.slideEl) {\n      gesture.slideEl = e.target.closest(`.${swiper.params.slideClass}, swiper-slide`);\n      if (!gesture.slideEl) gesture.slideEl = swiper.slides[swiper.activeIndex];\n      let imageEl = gesture.slideEl.querySelector(`.${params.containerClass}`);\n      if (imageEl) {\n        imageEl = imageEl.querySelectorAll('picture, img, svg, canvas, .swiper-zoom-target')[0];\n      }\n      gesture.imageEl = imageEl;\n      if (imageEl) {\n        gesture.imageWrapEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.a)(gesture.imageEl, `.${params.containerClass}`)[0];\n      } else {\n        gesture.imageWrapEl = undefined;\n      }\n      if (!gesture.imageWrapEl) {\n        gesture.imageEl = undefined;\n        return;\n      }\n      gesture.maxRatio = gesture.imageWrapEl.getAttribute('data-swiper-zoom') || params.maxRatio;\n    }\n    if (gesture.imageEl) {\n      const [originX, originY] = getScaleOrigin();\n      gesture.originX = originX;\n      gesture.originY = originY;\n      gesture.imageEl.style.transitionDuration = '0ms';\n    }\n    isScaling = true;\n  }\n  function onGestureChange(e) {\n    if (!eventWithinSlide(e)) return;\n    const params = swiper.params.zoom;\n    const zoom = swiper.zoom;\n    const pointerIndex = evCache.findIndex(cachedEv => cachedEv.pointerId === e.pointerId);\n    if (pointerIndex >= 0) evCache[pointerIndex] = e;\n    if (evCache.length < 2) {\n      return;\n    }\n    fakeGestureMoved = true;\n    gesture.scaleMove = getDistanceBetweenTouches();\n    if (!gesture.imageEl) {\n      return;\n    }\n    zoom.scale = gesture.scaleMove / gesture.scaleStart * currentScale;\n    if (zoom.scale > gesture.maxRatio) {\n      zoom.scale = gesture.maxRatio - 1 + (zoom.scale - gesture.maxRatio + 1) ** 0.5;\n    }\n    if (zoom.scale < params.minRatio) {\n      zoom.scale = params.minRatio + 1 - (params.minRatio - zoom.scale + 1) ** 0.5;\n    }\n    gesture.imageEl.style.transform = `translate3d(0,0,0) scale(${zoom.scale})`;\n  }\n  function onGestureEnd(e) {\n    if (!eventWithinSlide(e)) return;\n    if (e.pointerType === 'mouse' && e.type === 'pointerout') return;\n    const params = swiper.params.zoom;\n    const zoom = swiper.zoom;\n    const pointerIndex = evCache.findIndex(cachedEv => cachedEv.pointerId === e.pointerId);\n    if (pointerIndex >= 0) evCache.splice(pointerIndex, 1);\n    if (!fakeGestureTouched || !fakeGestureMoved) {\n      return;\n    }\n    fakeGestureTouched = false;\n    fakeGestureMoved = false;\n    if (!gesture.imageEl) return;\n    zoom.scale = Math.max(Math.min(zoom.scale, gesture.maxRatio), params.minRatio);\n    gesture.imageEl.style.transitionDuration = `${swiper.params.speed}ms`;\n    gesture.imageEl.style.transform = `translate3d(0,0,0) scale(${zoom.scale})`;\n    currentScale = zoom.scale;\n    isScaling = false;\n    if (zoom.scale > 1 && gesture.slideEl) {\n      gesture.slideEl.classList.add(`${params.zoomedSlideClass}`);\n    } else if (zoom.scale <= 1 && gesture.slideEl) {\n      gesture.slideEl.classList.remove(`${params.zoomedSlideClass}`);\n    }\n    if (zoom.scale === 1) {\n      gesture.originX = 0;\n      gesture.originY = 0;\n      gesture.slideEl = undefined;\n    }\n  }\n  function onTouchStart(e) {\n    const device = swiper.device;\n    if (!gesture.imageEl) return;\n    if (image.isTouched) return;\n    if (device.android && e.cancelable) e.preventDefault();\n    image.isTouched = true;\n    const event = evCache.length > 0 ? evCache[0] : e;\n    image.touchesStart.x = event.pageX;\n    image.touchesStart.y = event.pageY;\n  }\n  function onTouchMove(e) {\n    if (!eventWithinSlide(e) || !eventWithinZoomContainer(e)) return;\n    const zoom = swiper.zoom;\n    if (!gesture.imageEl) return;\n    if (!image.isTouched || !gesture.slideEl) return;\n    if (!image.isMoved) {\n      image.width = gesture.imageEl.offsetWidth;\n      image.height = gesture.imageEl.offsetHeight;\n      image.startX = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.h)(gesture.imageWrapEl, 'x') || 0;\n      image.startY = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.h)(gesture.imageWrapEl, 'y') || 0;\n      gesture.slideWidth = gesture.slideEl.offsetWidth;\n      gesture.slideHeight = gesture.slideEl.offsetHeight;\n      gesture.imageWrapEl.style.transitionDuration = '0ms';\n    }\n    // Define if we need image drag\n    const scaledWidth = image.width * zoom.scale;\n    const scaledHeight = image.height * zoom.scale;\n    if (scaledWidth < gesture.slideWidth && scaledHeight < gesture.slideHeight) return;\n    image.minX = Math.min(gesture.slideWidth / 2 - scaledWidth / 2, 0);\n    image.maxX = -image.minX;\n    image.minY = Math.min(gesture.slideHeight / 2 - scaledHeight / 2, 0);\n    image.maxY = -image.minY;\n    image.touchesCurrent.x = evCache.length > 0 ? evCache[0].pageX : e.pageX;\n    image.touchesCurrent.y = evCache.length > 0 ? evCache[0].pageY : e.pageY;\n    const touchesDiff = Math.max(Math.abs(image.touchesCurrent.x - image.touchesStart.x), Math.abs(image.touchesCurrent.y - image.touchesStart.y));\n    if (touchesDiff > 5) {\n      swiper.allowClick = false;\n    }\n    if (!image.isMoved && !isScaling) {\n      if (swiper.isHorizontal() && (Math.floor(image.minX) === Math.floor(image.startX) && image.touchesCurrent.x < image.touchesStart.x || Math.floor(image.maxX) === Math.floor(image.startX) && image.touchesCurrent.x > image.touchesStart.x)) {\n        image.isTouched = false;\n        return;\n      }\n      if (!swiper.isHorizontal() && (Math.floor(image.minY) === Math.floor(image.startY) && image.touchesCurrent.y < image.touchesStart.y || Math.floor(image.maxY) === Math.floor(image.startY) && image.touchesCurrent.y > image.touchesStart.y)) {\n        image.isTouched = false;\n        return;\n      }\n    }\n    if (e.cancelable) {\n      e.preventDefault();\n    }\n    e.stopPropagation();\n    image.isMoved = true;\n    const scaleRatio = (zoom.scale - currentScale) / (gesture.maxRatio - swiper.params.zoom.minRatio);\n    const {\n      originX,\n      originY\n    } = gesture;\n    image.currentX = image.touchesCurrent.x - image.touchesStart.x + image.startX + scaleRatio * (image.width - originX * 2);\n    image.currentY = image.touchesCurrent.y - image.touchesStart.y + image.startY + scaleRatio * (image.height - originY * 2);\n    if (image.currentX < image.minX) {\n      image.currentX = image.minX + 1 - (image.minX - image.currentX + 1) ** 0.8;\n    }\n    if (image.currentX > image.maxX) {\n      image.currentX = image.maxX - 1 + (image.currentX - image.maxX + 1) ** 0.8;\n    }\n    if (image.currentY < image.minY) {\n      image.currentY = image.minY + 1 - (image.minY - image.currentY + 1) ** 0.8;\n    }\n    if (image.currentY > image.maxY) {\n      image.currentY = image.maxY - 1 + (image.currentY - image.maxY + 1) ** 0.8;\n    }\n\n    // Velocity\n    if (!velocity.prevPositionX) velocity.prevPositionX = image.touchesCurrent.x;\n    if (!velocity.prevPositionY) velocity.prevPositionY = image.touchesCurrent.y;\n    if (!velocity.prevTime) velocity.prevTime = Date.now();\n    velocity.x = (image.touchesCurrent.x - velocity.prevPositionX) / (Date.now() - velocity.prevTime) / 2;\n    velocity.y = (image.touchesCurrent.y - velocity.prevPositionY) / (Date.now() - velocity.prevTime) / 2;\n    if (Math.abs(image.touchesCurrent.x - velocity.prevPositionX) < 2) velocity.x = 0;\n    if (Math.abs(image.touchesCurrent.y - velocity.prevPositionY) < 2) velocity.y = 0;\n    velocity.prevPositionX = image.touchesCurrent.x;\n    velocity.prevPositionY = image.touchesCurrent.y;\n    velocity.prevTime = Date.now();\n    gesture.imageWrapEl.style.transform = `translate3d(${image.currentX}px, ${image.currentY}px,0)`;\n  }\n  function onTouchEnd() {\n    const zoom = swiper.zoom;\n    if (!gesture.imageEl) return;\n    if (!image.isTouched || !image.isMoved) {\n      image.isTouched = false;\n      image.isMoved = false;\n      return;\n    }\n    image.isTouched = false;\n    image.isMoved = false;\n    let momentumDurationX = 300;\n    let momentumDurationY = 300;\n    const momentumDistanceX = velocity.x * momentumDurationX;\n    const newPositionX = image.currentX + momentumDistanceX;\n    const momentumDistanceY = velocity.y * momentumDurationY;\n    const newPositionY = image.currentY + momentumDistanceY;\n\n    // Fix duration\n    if (velocity.x !== 0) momentumDurationX = Math.abs((newPositionX - image.currentX) / velocity.x);\n    if (velocity.y !== 0) momentumDurationY = Math.abs((newPositionY - image.currentY) / velocity.y);\n    const momentumDuration = Math.max(momentumDurationX, momentumDurationY);\n    image.currentX = newPositionX;\n    image.currentY = newPositionY;\n    // Define if we need image drag\n    const scaledWidth = image.width * zoom.scale;\n    const scaledHeight = image.height * zoom.scale;\n    image.minX = Math.min(gesture.slideWidth / 2 - scaledWidth / 2, 0);\n    image.maxX = -image.minX;\n    image.minY = Math.min(gesture.slideHeight / 2 - scaledHeight / 2, 0);\n    image.maxY = -image.minY;\n    image.currentX = Math.max(Math.min(image.currentX, image.maxX), image.minX);\n    image.currentY = Math.max(Math.min(image.currentY, image.maxY), image.minY);\n    gesture.imageWrapEl.style.transitionDuration = `${momentumDuration}ms`;\n    gesture.imageWrapEl.style.transform = `translate3d(${image.currentX}px, ${image.currentY}px,0)`;\n  }\n  function onTransitionEnd() {\n    const zoom = swiper.zoom;\n    if (gesture.slideEl && swiper.activeIndex !== swiper.slides.indexOf(gesture.slideEl)) {\n      if (gesture.imageEl) {\n        gesture.imageEl.style.transform = 'translate3d(0,0,0) scale(1)';\n      }\n      if (gesture.imageWrapEl) {\n        gesture.imageWrapEl.style.transform = 'translate3d(0,0,0)';\n      }\n      gesture.slideEl.classList.remove(`${swiper.params.zoom.zoomedSlideClass}`);\n      zoom.scale = 1;\n      currentScale = 1;\n      gesture.slideEl = undefined;\n      gesture.imageEl = undefined;\n      gesture.imageWrapEl = undefined;\n      gesture.originX = 0;\n      gesture.originY = 0;\n    }\n  }\n  function zoomIn(e) {\n    const zoom = swiper.zoom;\n    const params = swiper.params.zoom;\n    if (!gesture.slideEl) {\n      if (e && e.target) {\n        gesture.slideEl = e.target.closest(`.${swiper.params.slideClass}, swiper-slide`);\n      }\n      if (!gesture.slideEl) {\n        if (swiper.params.virtual && swiper.params.virtual.enabled && swiper.virtual) {\n          gesture.slideEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(swiper.slidesEl, `.${swiper.params.slideActiveClass}`)[0];\n        } else {\n          gesture.slideEl = swiper.slides[swiper.activeIndex];\n        }\n      }\n      let imageEl = gesture.slideEl.querySelector(`.${params.containerClass}`);\n      if (imageEl) {\n        imageEl = imageEl.querySelectorAll('picture, img, svg, canvas, .swiper-zoom-target')[0];\n      }\n      gesture.imageEl = imageEl;\n      if (imageEl) {\n        gesture.imageWrapEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.a)(gesture.imageEl, `.${params.containerClass}`)[0];\n      } else {\n        gesture.imageWrapEl = undefined;\n      }\n    }\n    if (!gesture.imageEl || !gesture.imageWrapEl) return;\n    if (swiper.params.cssMode) {\n      swiper.wrapperEl.style.overflow = 'hidden';\n      swiper.wrapperEl.style.touchAction = 'none';\n    }\n    gesture.slideEl.classList.add(`${params.zoomedSlideClass}`);\n    let touchX;\n    let touchY;\n    let offsetX;\n    let offsetY;\n    let diffX;\n    let diffY;\n    let translateX;\n    let translateY;\n    let imageWidth;\n    let imageHeight;\n    let scaledWidth;\n    let scaledHeight;\n    let translateMinX;\n    let translateMinY;\n    let translateMaxX;\n    let translateMaxY;\n    let slideWidth;\n    let slideHeight;\n    if (typeof image.touchesStart.x === 'undefined' && e) {\n      touchX = e.pageX;\n      touchY = e.pageY;\n    } else {\n      touchX = image.touchesStart.x;\n      touchY = image.touchesStart.y;\n    }\n    const forceZoomRatio = typeof e === 'number' ? e : null;\n    if (currentScale === 1 && forceZoomRatio) {\n      touchX = undefined;\n      touchY = undefined;\n    }\n    zoom.scale = forceZoomRatio || gesture.imageWrapEl.getAttribute('data-swiper-zoom') || params.maxRatio;\n    currentScale = forceZoomRatio || gesture.imageWrapEl.getAttribute('data-swiper-zoom') || params.maxRatio;\n    if (e && !(currentScale === 1 && forceZoomRatio)) {\n      slideWidth = gesture.slideEl.offsetWidth;\n      slideHeight = gesture.slideEl.offsetHeight;\n      offsetX = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.b)(gesture.slideEl).left + window.scrollX;\n      offsetY = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.b)(gesture.slideEl).top + window.scrollY;\n      diffX = offsetX + slideWidth / 2 - touchX;\n      diffY = offsetY + slideHeight / 2 - touchY;\n      imageWidth = gesture.imageEl.offsetWidth;\n      imageHeight = gesture.imageEl.offsetHeight;\n      scaledWidth = imageWidth * zoom.scale;\n      scaledHeight = imageHeight * zoom.scale;\n      translateMinX = Math.min(slideWidth / 2 - scaledWidth / 2, 0);\n      translateMinY = Math.min(slideHeight / 2 - scaledHeight / 2, 0);\n      translateMaxX = -translateMinX;\n      translateMaxY = -translateMinY;\n      translateX = diffX * zoom.scale;\n      translateY = diffY * zoom.scale;\n      if (translateX < translateMinX) {\n        translateX = translateMinX;\n      }\n      if (translateX > translateMaxX) {\n        translateX = translateMaxX;\n      }\n      if (translateY < translateMinY) {\n        translateY = translateMinY;\n      }\n      if (translateY > translateMaxY) {\n        translateY = translateMaxY;\n      }\n    } else {\n      translateX = 0;\n      translateY = 0;\n    }\n    if (forceZoomRatio && zoom.scale === 1) {\n      gesture.originX = 0;\n      gesture.originY = 0;\n    }\n    gesture.imageWrapEl.style.transitionDuration = '300ms';\n    gesture.imageWrapEl.style.transform = `translate3d(${translateX}px, ${translateY}px,0)`;\n    gesture.imageEl.style.transitionDuration = '300ms';\n    gesture.imageEl.style.transform = `translate3d(0,0,0) scale(${zoom.scale})`;\n  }\n  function zoomOut() {\n    const zoom = swiper.zoom;\n    const params = swiper.params.zoom;\n    if (!gesture.slideEl) {\n      if (swiper.params.virtual && swiper.params.virtual.enabled && swiper.virtual) {\n        gesture.slideEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(swiper.slidesEl, `.${swiper.params.slideActiveClass}`)[0];\n      } else {\n        gesture.slideEl = swiper.slides[swiper.activeIndex];\n      }\n      let imageEl = gesture.slideEl.querySelector(`.${params.containerClass}`);\n      if (imageEl) {\n        imageEl = imageEl.querySelectorAll('picture, img, svg, canvas, .swiper-zoom-target')[0];\n      }\n      gesture.imageEl = imageEl;\n      if (imageEl) {\n        gesture.imageWrapEl = (0,_shared_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.a)(gesture.imageEl, `.${params.containerClass}`)[0];\n      } else {\n        gesture.imageWrapEl = undefined;\n      }\n    }\n    if (!gesture.imageEl || !gesture.imageWrapEl) return;\n    if (swiper.params.cssMode) {\n      swiper.wrapperEl.style.overflow = '';\n      swiper.wrapperEl.style.touchAction = '';\n    }\n    zoom.scale = 1;\n    currentScale = 1;\n    gesture.imageWrapEl.style.transitionDuration = '300ms';\n    gesture.imageWrapEl.style.transform = 'translate3d(0,0,0)';\n    gesture.imageEl.style.transitionDuration = '300ms';\n    gesture.imageEl.style.transform = 'translate3d(0,0,0) scale(1)';\n    gesture.slideEl.classList.remove(`${params.zoomedSlideClass}`);\n    gesture.slideEl = undefined;\n    gesture.originX = 0;\n    gesture.originY = 0;\n  }\n\n  // Toggle Zoom\n  function zoomToggle(e) {\n    const zoom = swiper.zoom;\n    if (zoom.scale && zoom.scale !== 1) {\n      // Zoom Out\n      zoomOut();\n    } else {\n      // Zoom In\n      zoomIn(e);\n    }\n  }\n  function getListeners() {\n    const passiveListener = swiper.params.passiveListeners ? {\n      passive: true,\n      capture: false\n    } : false;\n    const activeListenerWithCapture = swiper.params.passiveListeners ? {\n      passive: false,\n      capture: true\n    } : true;\n    return {\n      passiveListener,\n      activeListenerWithCapture\n    };\n  }\n\n  // Attach/Detach Events\n  function enable() {\n    const zoom = swiper.zoom;\n    if (zoom.enabled) return;\n    zoom.enabled = true;\n    const {\n      passiveListener,\n      activeListenerWithCapture\n    } = getListeners();\n\n    // Scale image\n    swiper.wrapperEl.addEventListener('pointerdown', onGestureStart, passiveListener);\n    swiper.wrapperEl.addEventListener('pointermove', onGestureChange, activeListenerWithCapture);\n    ['pointerup', 'pointercancel', 'pointerout'].forEach(eventName => {\n      swiper.wrapperEl.addEventListener(eventName, onGestureEnd, passiveListener);\n    });\n\n    // Move image\n    swiper.wrapperEl.addEventListener('pointermove', onTouchMove, activeListenerWithCapture);\n  }\n  function disable() {\n    const zoom = swiper.zoom;\n    if (!zoom.enabled) return;\n    zoom.enabled = false;\n    const {\n      passiveListener,\n      activeListenerWithCapture\n    } = getListeners();\n\n    // Scale image\n    swiper.wrapperEl.removeEventListener('pointerdown', onGestureStart, passiveListener);\n    swiper.wrapperEl.removeEventListener('pointermove', onGestureChange, activeListenerWithCapture);\n    ['pointerup', 'pointercancel', 'pointerout'].forEach(eventName => {\n      swiper.wrapperEl.removeEventListener(eventName, onGestureEnd, passiveListener);\n    });\n\n    // Move image\n    swiper.wrapperEl.removeEventListener('pointermove', onTouchMove, activeListenerWithCapture);\n  }\n  on('init', () => {\n    if (swiper.params.zoom.enabled) {\n      enable();\n    }\n  });\n  on('destroy', () => {\n    disable();\n  });\n  on('touchStart', (_s, e) => {\n    if (!swiper.zoom.enabled) return;\n    onTouchStart(e);\n  });\n  on('touchEnd', (_s, e) => {\n    if (!swiper.zoom.enabled) return;\n    onTouchEnd();\n  });\n  on('doubleTap', (_s, e) => {\n    if (!swiper.animating && swiper.params.zoom.enabled && swiper.zoom.enabled && swiper.params.zoom.toggle) {\n      zoomToggle(e);\n    }\n  });\n  on('transitionEnd', () => {\n    if (swiper.zoom.enabled && swiper.params.zoom.enabled) {\n      onTransitionEnd();\n    }\n  });\n  on('slideChange', () => {\n    if (swiper.zoom.enabled && swiper.params.zoom.enabled && swiper.params.cssMode) {\n      onTransitionEnd();\n    }\n  });\n  Object.assign(swiper.zoom, {\n    enable,\n    disable,\n    in: zoomIn,\n    out: zoomOut,\n    toggle: zoomToggle\n  });\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/modules/zoom.mjs?");

/***/ }),

/***/ "./node_modules/swiper/shared/classes-to-selector.mjs":
/*!************************************************************!*\
  !*** ./node_modules/swiper/shared/classes-to-selector.mjs ***!
  \************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   c: () => (/* binding */ classesToSelector)\n/* harmony export */ });\nfunction classesToSelector(classes) {\n  if (classes === void 0) {\n    classes = '';\n  }\n  return `.${classes.trim().replace(/([\\.:!+\\/])/g, '\\\\$1') // eslint-disable-line\n  .replace(/ /g, '.')}`;\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/shared/classes-to-selector.mjs?");

/***/ }),

/***/ "./node_modules/swiper/shared/create-element-if-not-defined.mjs":
/*!**********************************************************************!*\
  !*** ./node_modules/swiper/shared/create-element-if-not-defined.mjs ***!
  \**********************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   c: () => (/* binding */ createElementIfNotDefined)\n/* harmony export */ });\n/* harmony import */ var _utils_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./utils.mjs */ \"./node_modules/swiper/shared/utils.mjs\");\n\n\nfunction createElementIfNotDefined(swiper, originalParams, params, checkProps) {\n  if (swiper.params.createElements) {\n    Object.keys(checkProps).forEach(key => {\n      if (!params[key] && params.auto === true) {\n        let element = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.e)(swiper.el, `.${checkProps[key]}`)[0];\n        if (!element) {\n          element = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.c)('div', checkProps[key]);\n          element.className = checkProps[key];\n          swiper.el.append(element);\n        }\n        params[key] = element;\n        originalParams[key] = element;\n      }\n    });\n  }\n  return params;\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/shared/create-element-if-not-defined.mjs?");

/***/ }),

/***/ "./node_modules/swiper/shared/create-shadow.mjs":
/*!******************************************************!*\
  !*** ./node_modules/swiper/shared/create-shadow.mjs ***!
  \******************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   c: () => (/* binding */ createShadow)\n/* harmony export */ });\n/* harmony import */ var _utils_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./utils.mjs */ \"./node_modules/swiper/shared/utils.mjs\");\n\n\nfunction createShadow(suffix, slideEl, side) {\n  const shadowClass = `swiper-slide-shadow${side ? `-${side}` : ''}${suffix ? ` swiper-slide-shadow-${suffix}` : ''}`;\n  const shadowContainer = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.k)(slideEl);\n  let shadowEl = shadowContainer.querySelector(`.${shadowClass.split(' ').join('.')}`);\n  if (!shadowEl) {\n    shadowEl = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.c)('div', shadowClass.split(' '));\n    shadowContainer.append(shadowEl);\n  }\n  return shadowEl;\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/shared/create-shadow.mjs?");

/***/ }),

/***/ "./node_modules/swiper/shared/effect-init.mjs":
/*!****************************************************!*\
  !*** ./node_modules/swiper/shared/effect-init.mjs ***!
  \****************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   e: () => (/* binding */ effectInit)\n/* harmony export */ });\nfunction effectInit(params) {\n  const {\n    effect,\n    swiper,\n    on,\n    setTranslate,\n    setTransition,\n    overwriteParams,\n    perspective,\n    recreateShadows,\n    getEffectParams\n  } = params;\n  on('beforeInit', () => {\n    if (swiper.params.effect !== effect) return;\n    swiper.classNames.push(`${swiper.params.containerModifierClass}${effect}`);\n    if (perspective && perspective()) {\n      swiper.classNames.push(`${swiper.params.containerModifierClass}3d`);\n    }\n    const overwriteParamsResult = overwriteParams ? overwriteParams() : {};\n    Object.assign(swiper.params, overwriteParamsResult);\n    Object.assign(swiper.originalParams, overwriteParamsResult);\n  });\n  on('setTranslate', () => {\n    if (swiper.params.effect !== effect) return;\n    setTranslate();\n  });\n  on('setTransition', (_s, duration) => {\n    if (swiper.params.effect !== effect) return;\n    setTransition(duration);\n  });\n  on('transitionEnd', () => {\n    if (swiper.params.effect !== effect) return;\n    if (recreateShadows) {\n      if (!getEffectParams || !getEffectParams().slideShadows) return;\n      // remove shadows\n      swiper.slides.forEach(slideEl => {\n        slideEl.querySelectorAll('.swiper-slide-shadow-top, .swiper-slide-shadow-right, .swiper-slide-shadow-bottom, .swiper-slide-shadow-left').forEach(shadowEl => shadowEl.remove());\n      });\n      // create new one\n      recreateShadows();\n    }\n  });\n  let requireUpdateOnVirtual;\n  on('virtualUpdate', () => {\n    if (swiper.params.effect !== effect) return;\n    if (!swiper.slides.length) {\n      requireUpdateOnVirtual = true;\n    }\n    requestAnimationFrame(() => {\n      if (requireUpdateOnVirtual && swiper.slides && swiper.slides.length) {\n        setTranslate();\n        requireUpdateOnVirtual = false;\n      }\n    });\n  });\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/shared/effect-init.mjs?");

/***/ }),

/***/ "./node_modules/swiper/shared/effect-target.mjs":
/*!******************************************************!*\
  !*** ./node_modules/swiper/shared/effect-target.mjs ***!
  \******************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   e: () => (/* binding */ effectTarget)\n/* harmony export */ });\n/* harmony import */ var _utils_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./utils.mjs */ \"./node_modules/swiper/shared/utils.mjs\");\n\n\nfunction effectTarget(effectParams, slideEl) {\n  const transformEl = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.k)(slideEl);\n  if (transformEl !== slideEl) {\n    transformEl.style.backfaceVisibility = 'hidden';\n    transformEl.style['-webkit-backface-visibility'] = 'hidden';\n  }\n  return transformEl;\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/shared/effect-target.mjs?");

/***/ }),

/***/ "./node_modules/swiper/shared/effect-virtual-transition-end.mjs":
/*!**********************************************************************!*\
  !*** ./node_modules/swiper/shared/effect-virtual-transition-end.mjs ***!
  \**********************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   e: () => (/* binding */ effectVirtualTransitionEnd)\n/* harmony export */ });\n/* harmony import */ var _utils_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./utils.mjs */ \"./node_modules/swiper/shared/utils.mjs\");\n\n\nfunction effectVirtualTransitionEnd(_ref) {\n  let {\n    swiper,\n    duration,\n    transformElements,\n    allSlides\n  } = _ref;\n  const {\n    activeIndex\n  } = swiper;\n  const getSlide = el => {\n    if (!el.parentElement) {\n      // assume shadow root\n      const slide = swiper.slides.filter(slideEl => slideEl.shadowRoot && slideEl.shadowRoot === el.parentNode)[0];\n      return slide;\n    }\n    return el.parentElement;\n  };\n  if (swiper.params.virtualTranslate && duration !== 0) {\n    let eventTriggered = false;\n    let transitionEndTarget;\n    if (allSlides) {\n      transitionEndTarget = transformElements;\n    } else {\n      transitionEndTarget = transformElements.filter(transformEl => {\n        const el = transformEl.classList.contains('swiper-slide-transform') ? getSlide(transformEl) : transformEl;\n        return swiper.getSlideIndex(el) === activeIndex;\n      });\n    }\n    transitionEndTarget.forEach(el => {\n      (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_0__.i)(el, () => {\n        if (eventTriggered) return;\n        if (!swiper || swiper.destroyed) return;\n        eventTriggered = true;\n        swiper.animating = false;\n        const evt = new window.CustomEvent('transitionend', {\n          bubbles: true,\n          cancelable: true\n        });\n        swiper.wrapperEl.dispatchEvent(evt);\n      });\n    });\n  }\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/shared/effect-virtual-transition-end.mjs?");

/***/ }),

/***/ "./node_modules/swiper/shared/ssr-window.esm.mjs":
/*!*******************************************************!*\
  !*** ./node_modules/swiper/shared/ssr-window.esm.mjs ***!
  \*******************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   a: () => (/* binding */ getWindow),\n/* harmony export */   g: () => (/* binding */ getDocument)\n/* harmony export */ });\n/**\n * SSR Window 4.0.2\n * Better handling for window object in SSR environment\n * https://github.com/nolimits4web/ssr-window\n *\n * Copyright 2021, Vladimir Kharlampidi\n *\n * Licensed under MIT\n *\n * Released on: December 13, 2021\n */\n/* eslint-disable no-param-reassign */\nfunction isObject(obj) {\n  return obj !== null && typeof obj === 'object' && 'constructor' in obj && obj.constructor === Object;\n}\nfunction extend(target, src) {\n  if (target === void 0) {\n    target = {};\n  }\n  if (src === void 0) {\n    src = {};\n  }\n  Object.keys(src).forEach(key => {\n    if (typeof target[key] === 'undefined') target[key] = src[key];else if (isObject(src[key]) && isObject(target[key]) && Object.keys(src[key]).length > 0) {\n      extend(target[key], src[key]);\n    }\n  });\n}\nconst ssrDocument = {\n  body: {},\n  addEventListener() {},\n  removeEventListener() {},\n  activeElement: {\n    blur() {},\n    nodeName: ''\n  },\n  querySelector() {\n    return null;\n  },\n  querySelectorAll() {\n    return [];\n  },\n  getElementById() {\n    return null;\n  },\n  createEvent() {\n    return {\n      initEvent() {}\n    };\n  },\n  createElement() {\n    return {\n      children: [],\n      childNodes: [],\n      style: {},\n      setAttribute() {},\n      getElementsByTagName() {\n        return [];\n      }\n    };\n  },\n  createElementNS() {\n    return {};\n  },\n  importNode() {\n    return null;\n  },\n  location: {\n    hash: '',\n    host: '',\n    hostname: '',\n    href: '',\n    origin: '',\n    pathname: '',\n    protocol: '',\n    search: ''\n  }\n};\nfunction getDocument() {\n  const doc = typeof document !== 'undefined' ? document : {};\n  extend(doc, ssrDocument);\n  return doc;\n}\nconst ssrWindow = {\n  document: ssrDocument,\n  navigator: {\n    userAgent: ''\n  },\n  location: {\n    hash: '',\n    host: '',\n    hostname: '',\n    href: '',\n    origin: '',\n    pathname: '',\n    protocol: '',\n    search: ''\n  },\n  history: {\n    replaceState() {},\n    pushState() {},\n    go() {},\n    back() {}\n  },\n  CustomEvent: function CustomEvent() {\n    return this;\n  },\n  addEventListener() {},\n  removeEventListener() {},\n  getComputedStyle() {\n    return {\n      getPropertyValue() {\n        return '';\n      }\n    };\n  },\n  Image() {},\n  Date() {},\n  screen: {},\n  setTimeout() {},\n  clearTimeout() {},\n  matchMedia() {\n    return {};\n  },\n  requestAnimationFrame(callback) {\n    if (typeof setTimeout === 'undefined') {\n      callback();\n      return null;\n    }\n    return setTimeout(callback, 0);\n  },\n  cancelAnimationFrame(id) {\n    if (typeof setTimeout === 'undefined') {\n      return;\n    }\n    clearTimeout(id);\n  }\n};\nfunction getWindow() {\n  const win = typeof window !== 'undefined' ? window : {};\n  extend(win, ssrWindow);\n  return win;\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/shared/ssr-window.esm.mjs?");

/***/ }),

/***/ "./node_modules/swiper/shared/swiper-core.mjs":
/*!****************************************************!*\
  !*** ./node_modules/swiper/shared/swiper-core.mjs ***!
  \****************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   S: () => (/* binding */ Swiper),\n/* harmony export */   d: () => (/* binding */ defaults)\n/* harmony export */ });\n/* harmony import */ var _ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./ssr-window.esm.mjs */ \"./node_modules/swiper/shared/ssr-window.esm.mjs\");\n/* harmony import */ var _utils_mjs__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./utils.mjs */ \"./node_modules/swiper/shared/utils.mjs\");\n\n\n\nlet support;\nfunction calcSupport() {\n  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();\n  const document = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();\n  return {\n    smoothScroll: document.documentElement && document.documentElement.style && 'scrollBehavior' in document.documentElement.style,\n    touch: !!('ontouchstart' in window || window.DocumentTouch && document instanceof window.DocumentTouch)\n  };\n}\nfunction getSupport() {\n  if (!support) {\n    support = calcSupport();\n  }\n  return support;\n}\n\nlet deviceCached;\nfunction calcDevice(_temp) {\n  let {\n    userAgent\n  } = _temp === void 0 ? {} : _temp;\n  const support = getSupport();\n  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();\n  const platform = window.navigator.platform;\n  const ua = userAgent || window.navigator.userAgent;\n  const device = {\n    ios: false,\n    android: false\n  };\n  const screenWidth = window.screen.width;\n  const screenHeight = window.screen.height;\n  const android = ua.match(/(Android);?[\\s\\/]+([\\d.]+)?/); // eslint-disable-line\n  let ipad = ua.match(/(iPad).*OS\\s([\\d_]+)/);\n  const ipod = ua.match(/(iPod)(.*OS\\s([\\d_]+))?/);\n  const iphone = !ipad && ua.match(/(iPhone\\sOS|iOS)\\s([\\d_]+)/);\n  const windows = platform === 'Win32';\n  let macos = platform === 'MacIntel';\n\n  // iPadOs 13 fix\n  const iPadScreens = ['1024x1366', '1366x1024', '834x1194', '1194x834', '834x1112', '1112x834', '768x1024', '1024x768', '820x1180', '1180x820', '810x1080', '1080x810'];\n  if (!ipad && macos && support.touch && iPadScreens.indexOf(`${screenWidth}x${screenHeight}`) >= 0) {\n    ipad = ua.match(/(Version)\\/([\\d.]+)/);\n    if (!ipad) ipad = [0, 1, '13_0_0'];\n    macos = false;\n  }\n\n  // Android\n  if (android && !windows) {\n    device.os = 'android';\n    device.android = true;\n  }\n  if (ipad || iphone || ipod) {\n    device.os = 'ios';\n    device.ios = true;\n  }\n\n  // Export object\n  return device;\n}\nfunction getDevice(overrides) {\n  if (overrides === void 0) {\n    overrides = {};\n  }\n  if (!deviceCached) {\n    deviceCached = calcDevice(overrides);\n  }\n  return deviceCached;\n}\n\nlet browser;\nfunction calcBrowser() {\n  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();\n  let needPerspectiveFix = false;\n  function isSafari() {\n    const ua = window.navigator.userAgent.toLowerCase();\n    return ua.indexOf('safari') >= 0 && ua.indexOf('chrome') < 0 && ua.indexOf('android') < 0;\n  }\n  if (isSafari()) {\n    const ua = String(window.navigator.userAgent);\n    if (ua.includes('Version/')) {\n      const [major, minor] = ua.split('Version/')[1].split(' ')[0].split('.').map(num => Number(num));\n      needPerspectiveFix = major < 16 || major === 16 && minor < 2;\n    }\n  }\n  return {\n    isSafari: needPerspectiveFix || isSafari(),\n    needPerspectiveFix,\n    isWebView: /(iPhone|iPod|iPad).*AppleWebKit(?!.*Safari)/i.test(window.navigator.userAgent)\n  };\n}\nfunction getBrowser() {\n  if (!browser) {\n    browser = calcBrowser();\n  }\n  return browser;\n}\n\nfunction Resize(_ref) {\n  let {\n    swiper,\n    on,\n    emit\n  } = _ref;\n  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();\n  let observer = null;\n  let animationFrame = null;\n  const resizeHandler = () => {\n    if (!swiper || swiper.destroyed || !swiper.initialized) return;\n    emit('beforeResize');\n    emit('resize');\n  };\n  const createObserver = () => {\n    if (!swiper || swiper.destroyed || !swiper.initialized) return;\n    observer = new ResizeObserver(entries => {\n      animationFrame = window.requestAnimationFrame(() => {\n        const {\n          width,\n          height\n        } = swiper;\n        let newWidth = width;\n        let newHeight = height;\n        entries.forEach(_ref2 => {\n          let {\n            contentBoxSize,\n            contentRect,\n            target\n          } = _ref2;\n          if (target && target !== swiper.el) return;\n          newWidth = contentRect ? contentRect.width : (contentBoxSize[0] || contentBoxSize).inlineSize;\n          newHeight = contentRect ? contentRect.height : (contentBoxSize[0] || contentBoxSize).blockSize;\n        });\n        if (newWidth !== width || newHeight !== height) {\n          resizeHandler();\n        }\n      });\n    });\n    observer.observe(swiper.el);\n  };\n  const removeObserver = () => {\n    if (animationFrame) {\n      window.cancelAnimationFrame(animationFrame);\n    }\n    if (observer && observer.unobserve && swiper.el) {\n      observer.unobserve(swiper.el);\n      observer = null;\n    }\n  };\n  const orientationChangeHandler = () => {\n    if (!swiper || swiper.destroyed || !swiper.initialized) return;\n    emit('orientationchange');\n  };\n  on('init', () => {\n    if (swiper.params.resizeObserver && typeof window.ResizeObserver !== 'undefined') {\n      createObserver();\n      return;\n    }\n    window.addEventListener('resize', resizeHandler);\n    window.addEventListener('orientationchange', orientationChangeHandler);\n  });\n  on('destroy', () => {\n    removeObserver();\n    window.removeEventListener('resize', resizeHandler);\n    window.removeEventListener('orientationchange', orientationChangeHandler);\n  });\n}\n\nfunction Observer(_ref) {\n  let {\n    swiper,\n    extendParams,\n    on,\n    emit\n  } = _ref;\n  const observers = [];\n  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();\n  const attach = function (target, options) {\n    if (options === void 0) {\n      options = {};\n    }\n    const ObserverFunc = window.MutationObserver || window.WebkitMutationObserver;\n    const observer = new ObserverFunc(mutations => {\n      // The observerUpdate event should only be triggered\n      // once despite the number of mutations.  Additional\n      // triggers are redundant and are very costly\n      if (swiper.__preventObserver__) return;\n      if (mutations.length === 1) {\n        emit('observerUpdate', mutations[0]);\n        return;\n      }\n      const observerUpdate = function observerUpdate() {\n        emit('observerUpdate', mutations[0]);\n      };\n      if (window.requestAnimationFrame) {\n        window.requestAnimationFrame(observerUpdate);\n      } else {\n        window.setTimeout(observerUpdate, 0);\n      }\n    });\n    observer.observe(target, {\n      attributes: typeof options.attributes === 'undefined' ? true : options.attributes,\n      childList: typeof options.childList === 'undefined' ? true : options.childList,\n      characterData: typeof options.characterData === 'undefined' ? true : options.characterData\n    });\n    observers.push(observer);\n  };\n  const init = () => {\n    if (!swiper.params.observer) return;\n    if (swiper.params.observeParents) {\n      const containerParents = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.a)(swiper.hostEl);\n      for (let i = 0; i < containerParents.length; i += 1) {\n        attach(containerParents[i]);\n      }\n    }\n    // Observe container\n    attach(swiper.hostEl, {\n      childList: swiper.params.observeSlideChildren\n    });\n\n    // Observe wrapper\n    attach(swiper.wrapperEl, {\n      attributes: false\n    });\n  };\n  const destroy = () => {\n    observers.forEach(observer => {\n      observer.disconnect();\n    });\n    observers.splice(0, observers.length);\n  };\n  extendParams({\n    observer: false,\n    observeParents: false,\n    observeSlideChildren: false\n  });\n  on('init', init);\n  on('destroy', destroy);\n}\n\n/* eslint-disable no-underscore-dangle */\n\nvar eventsEmitter = {\n  on(events, handler, priority) {\n    const self = this;\n    if (!self.eventsListeners || self.destroyed) return self;\n    if (typeof handler !== 'function') return self;\n    const method = priority ? 'unshift' : 'push';\n    events.split(' ').forEach(event => {\n      if (!self.eventsListeners[event]) self.eventsListeners[event] = [];\n      self.eventsListeners[event][method](handler);\n    });\n    return self;\n  },\n  once(events, handler, priority) {\n    const self = this;\n    if (!self.eventsListeners || self.destroyed) return self;\n    if (typeof handler !== 'function') return self;\n    function onceHandler() {\n      self.off(events, onceHandler);\n      if (onceHandler.__emitterProxy) {\n        delete onceHandler.__emitterProxy;\n      }\n      for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {\n        args[_key] = arguments[_key];\n      }\n      handler.apply(self, args);\n    }\n    onceHandler.__emitterProxy = handler;\n    return self.on(events, onceHandler, priority);\n  },\n  onAny(handler, priority) {\n    const self = this;\n    if (!self.eventsListeners || self.destroyed) return self;\n    if (typeof handler !== 'function') return self;\n    const method = priority ? 'unshift' : 'push';\n    if (self.eventsAnyListeners.indexOf(handler) < 0) {\n      self.eventsAnyListeners[method](handler);\n    }\n    return self;\n  },\n  offAny(handler) {\n    const self = this;\n    if (!self.eventsListeners || self.destroyed) return self;\n    if (!self.eventsAnyListeners) return self;\n    const index = self.eventsAnyListeners.indexOf(handler);\n    if (index >= 0) {\n      self.eventsAnyListeners.splice(index, 1);\n    }\n    return self;\n  },\n  off(events, handler) {\n    const self = this;\n    if (!self.eventsListeners || self.destroyed) return self;\n    if (!self.eventsListeners) return self;\n    events.split(' ').forEach(event => {\n      if (typeof handler === 'undefined') {\n        self.eventsListeners[event] = [];\n      } else if (self.eventsListeners[event]) {\n        self.eventsListeners[event].forEach((eventHandler, index) => {\n          if (eventHandler === handler || eventHandler.__emitterProxy && eventHandler.__emitterProxy === handler) {\n            self.eventsListeners[event].splice(index, 1);\n          }\n        });\n      }\n    });\n    return self;\n  },\n  emit() {\n    const self = this;\n    if (!self.eventsListeners || self.destroyed) return self;\n    if (!self.eventsListeners) return self;\n    let events;\n    let data;\n    let context;\n    for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {\n      args[_key2] = arguments[_key2];\n    }\n    if (typeof args[0] === 'string' || Array.isArray(args[0])) {\n      events = args[0];\n      data = args.slice(1, args.length);\n      context = self;\n    } else {\n      events = args[0].events;\n      data = args[0].data;\n      context = args[0].context || self;\n    }\n    data.unshift(context);\n    const eventsArray = Array.isArray(events) ? events : events.split(' ');\n    eventsArray.forEach(event => {\n      if (self.eventsAnyListeners && self.eventsAnyListeners.length) {\n        self.eventsAnyListeners.forEach(eventHandler => {\n          eventHandler.apply(context, [event, ...data]);\n        });\n      }\n      if (self.eventsListeners && self.eventsListeners[event]) {\n        self.eventsListeners[event].forEach(eventHandler => {\n          eventHandler.apply(context, data);\n        });\n      }\n    });\n    return self;\n  }\n};\n\nfunction updateSize() {\n  const swiper = this;\n  let width;\n  let height;\n  const el = swiper.el;\n  if (typeof swiper.params.width !== 'undefined' && swiper.params.width !== null) {\n    width = swiper.params.width;\n  } else {\n    width = el.clientWidth;\n  }\n  if (typeof swiper.params.height !== 'undefined' && swiper.params.height !== null) {\n    height = swiper.params.height;\n  } else {\n    height = el.clientHeight;\n  }\n  if (width === 0 && swiper.isHorizontal() || height === 0 && swiper.isVertical()) {\n    return;\n  }\n\n  // Subtract paddings\n  width = width - parseInt((0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.l)(el, 'padding-left') || 0, 10) - parseInt((0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.l)(el, 'padding-right') || 0, 10);\n  height = height - parseInt((0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.l)(el, 'padding-top') || 0, 10) - parseInt((0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.l)(el, 'padding-bottom') || 0, 10);\n  if (Number.isNaN(width)) width = 0;\n  if (Number.isNaN(height)) height = 0;\n  Object.assign(swiper, {\n    width,\n    height,\n    size: swiper.isHorizontal() ? width : height\n  });\n}\n\nfunction updateSlides() {\n  const swiper = this;\n  function getDirectionLabel(property) {\n    if (swiper.isHorizontal()) {\n      return property;\n    }\n    // prettier-ignore\n    return {\n      'width': 'height',\n      'margin-top': 'margin-left',\n      'margin-bottom ': 'margin-right',\n      'margin-left': 'margin-top',\n      'margin-right': 'margin-bottom',\n      'padding-left': 'padding-top',\n      'padding-right': 'padding-bottom',\n      'marginRight': 'marginBottom'\n    }[property];\n  }\n  function getDirectionPropertyValue(node, label) {\n    return parseFloat(node.getPropertyValue(getDirectionLabel(label)) || 0);\n  }\n  const params = swiper.params;\n  const {\n    wrapperEl,\n    slidesEl,\n    size: swiperSize,\n    rtlTranslate: rtl,\n    wrongRTL\n  } = swiper;\n  const isVirtual = swiper.virtual && params.virtual.enabled;\n  const previousSlidesLength = isVirtual ? swiper.virtual.slides.length : swiper.slides.length;\n  const slides = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(slidesEl, `.${swiper.params.slideClass}, swiper-slide`);\n  const slidesLength = isVirtual ? swiper.virtual.slides.length : slides.length;\n  let snapGrid = [];\n  const slidesGrid = [];\n  const slidesSizesGrid = [];\n  let offsetBefore = params.slidesOffsetBefore;\n  if (typeof offsetBefore === 'function') {\n    offsetBefore = params.slidesOffsetBefore.call(swiper);\n  }\n  let offsetAfter = params.slidesOffsetAfter;\n  if (typeof offsetAfter === 'function') {\n    offsetAfter = params.slidesOffsetAfter.call(swiper);\n  }\n  const previousSnapGridLength = swiper.snapGrid.length;\n  const previousSlidesGridLength = swiper.slidesGrid.length;\n  let spaceBetween = params.spaceBetween;\n  let slidePosition = -offsetBefore;\n  let prevSlideSize = 0;\n  let index = 0;\n  if (typeof swiperSize === 'undefined') {\n    return;\n  }\n  if (typeof spaceBetween === 'string' && spaceBetween.indexOf('%') >= 0) {\n    spaceBetween = parseFloat(spaceBetween.replace('%', '')) / 100 * swiperSize;\n  } else if (typeof spaceBetween === 'string') {\n    spaceBetween = parseFloat(spaceBetween);\n  }\n  swiper.virtualSize = -spaceBetween;\n\n  // reset margins\n  slides.forEach(slideEl => {\n    if (rtl) {\n      slideEl.style.marginLeft = '';\n    } else {\n      slideEl.style.marginRight = '';\n    }\n    slideEl.style.marginBottom = '';\n    slideEl.style.marginTop = '';\n  });\n\n  // reset cssMode offsets\n  if (params.centeredSlides && params.cssMode) {\n    (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.s)(wrapperEl, '--swiper-centered-offset-before', '');\n    (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.s)(wrapperEl, '--swiper-centered-offset-after', '');\n  }\n  const gridEnabled = params.grid && params.grid.rows > 1 && swiper.grid;\n  if (gridEnabled) {\n    swiper.grid.initSlides(slidesLength);\n  }\n\n  // Calc slides\n  let slideSize;\n  const shouldResetSlideSize = params.slidesPerView === 'auto' && params.breakpoints && Object.keys(params.breakpoints).filter(key => {\n    return typeof params.breakpoints[key].slidesPerView !== 'undefined';\n  }).length > 0;\n  for (let i = 0; i < slidesLength; i += 1) {\n    slideSize = 0;\n    let slide;\n    if (slides[i]) slide = slides[i];\n    if (gridEnabled) {\n      swiper.grid.updateSlide(i, slide, slidesLength, getDirectionLabel);\n    }\n    if (slides[i] && (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.l)(slide, 'display') === 'none') continue; // eslint-disable-line\n\n    if (params.slidesPerView === 'auto') {\n      if (shouldResetSlideSize) {\n        slides[i].style[getDirectionLabel('width')] = ``;\n      }\n      const slideStyles = getComputedStyle(slide);\n      const currentTransform = slide.style.transform;\n      const currentWebKitTransform = slide.style.webkitTransform;\n      if (currentTransform) {\n        slide.style.transform = 'none';\n      }\n      if (currentWebKitTransform) {\n        slide.style.webkitTransform = 'none';\n      }\n      if (params.roundLengths) {\n        slideSize = swiper.isHorizontal() ? (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.f)(slide, 'width', true) : (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.f)(slide, 'height', true);\n      } else {\n        // eslint-disable-next-line\n        const width = getDirectionPropertyValue(slideStyles, 'width');\n        const paddingLeft = getDirectionPropertyValue(slideStyles, 'padding-left');\n        const paddingRight = getDirectionPropertyValue(slideStyles, 'padding-right');\n        const marginLeft = getDirectionPropertyValue(slideStyles, 'margin-left');\n        const marginRight = getDirectionPropertyValue(slideStyles, 'margin-right');\n        const boxSizing = slideStyles.getPropertyValue('box-sizing');\n        if (boxSizing && boxSizing === 'border-box') {\n          slideSize = width + marginLeft + marginRight;\n        } else {\n          const {\n            clientWidth,\n            offsetWidth\n          } = slide;\n          slideSize = width + paddingLeft + paddingRight + marginLeft + marginRight + (offsetWidth - clientWidth);\n        }\n      }\n      if (currentTransform) {\n        slide.style.transform = currentTransform;\n      }\n      if (currentWebKitTransform) {\n        slide.style.webkitTransform = currentWebKitTransform;\n      }\n      if (params.roundLengths) slideSize = Math.floor(slideSize);\n    } else {\n      slideSize = (swiperSize - (params.slidesPerView - 1) * spaceBetween) / params.slidesPerView;\n      if (params.roundLengths) slideSize = Math.floor(slideSize);\n      if (slides[i]) {\n        slides[i].style[getDirectionLabel('width')] = `${slideSize}px`;\n      }\n    }\n    if (slides[i]) {\n      slides[i].swiperSlideSize = slideSize;\n    }\n    slidesSizesGrid.push(slideSize);\n    if (params.centeredSlides) {\n      slidePosition = slidePosition + slideSize / 2 + prevSlideSize / 2 + spaceBetween;\n      if (prevSlideSize === 0 && i !== 0) slidePosition = slidePosition - swiperSize / 2 - spaceBetween;\n      if (i === 0) slidePosition = slidePosition - swiperSize / 2 - spaceBetween;\n      if (Math.abs(slidePosition) < 1 / 1000) slidePosition = 0;\n      if (params.roundLengths) slidePosition = Math.floor(slidePosition);\n      if (index % params.slidesPerGroup === 0) snapGrid.push(slidePosition);\n      slidesGrid.push(slidePosition);\n    } else {\n      if (params.roundLengths) slidePosition = Math.floor(slidePosition);\n      if ((index - Math.min(swiper.params.slidesPerGroupSkip, index)) % swiper.params.slidesPerGroup === 0) snapGrid.push(slidePosition);\n      slidesGrid.push(slidePosition);\n      slidePosition = slidePosition + slideSize + spaceBetween;\n    }\n    swiper.virtualSize += slideSize + spaceBetween;\n    prevSlideSize = slideSize;\n    index += 1;\n  }\n  swiper.virtualSize = Math.max(swiper.virtualSize, swiperSize) + offsetAfter;\n  if (rtl && wrongRTL && (params.effect === 'slide' || params.effect === 'coverflow')) {\n    wrapperEl.style.width = `${swiper.virtualSize + spaceBetween}px`;\n  }\n  if (params.setWrapperSize) {\n    wrapperEl.style[getDirectionLabel('width')] = `${swiper.virtualSize + spaceBetween}px`;\n  }\n  if (gridEnabled) {\n    swiper.grid.updateWrapperSize(slideSize, snapGrid, getDirectionLabel);\n  }\n\n  // Remove last grid elements depending on width\n  if (!params.centeredSlides) {\n    const newSlidesGrid = [];\n    for (let i = 0; i < snapGrid.length; i += 1) {\n      let slidesGridItem = snapGrid[i];\n      if (params.roundLengths) slidesGridItem = Math.floor(slidesGridItem);\n      if (snapGrid[i] <= swiper.virtualSize - swiperSize) {\n        newSlidesGrid.push(slidesGridItem);\n      }\n    }\n    snapGrid = newSlidesGrid;\n    if (Math.floor(swiper.virtualSize - swiperSize) - Math.floor(snapGrid[snapGrid.length - 1]) > 1) {\n      snapGrid.push(swiper.virtualSize - swiperSize);\n    }\n  }\n  if (isVirtual && params.loop) {\n    const size = slidesSizesGrid[0] + spaceBetween;\n    if (params.slidesPerGroup > 1) {\n      const groups = Math.ceil((swiper.virtual.slidesBefore + swiper.virtual.slidesAfter) / params.slidesPerGroup);\n      const groupSize = size * params.slidesPerGroup;\n      for (let i = 0; i < groups; i += 1) {\n        snapGrid.push(snapGrid[snapGrid.length - 1] + groupSize);\n      }\n    }\n    for (let i = 0; i < swiper.virtual.slidesBefore + swiper.virtual.slidesAfter; i += 1) {\n      if (params.slidesPerGroup === 1) {\n        snapGrid.push(snapGrid[snapGrid.length - 1] + size);\n      }\n      slidesGrid.push(slidesGrid[slidesGrid.length - 1] + size);\n      swiper.virtualSize += size;\n    }\n  }\n  if (snapGrid.length === 0) snapGrid = [0];\n  if (spaceBetween !== 0) {\n    const key = swiper.isHorizontal() && rtl ? 'marginLeft' : getDirectionLabel('marginRight');\n    slides.filter((_, slideIndex) => {\n      if (!params.cssMode || params.loop) return true;\n      if (slideIndex === slides.length - 1) {\n        return false;\n      }\n      return true;\n    }).forEach(slideEl => {\n      slideEl.style[key] = `${spaceBetween}px`;\n    });\n  }\n  if (params.centeredSlides && params.centeredSlidesBounds) {\n    let allSlidesSize = 0;\n    slidesSizesGrid.forEach(slideSizeValue => {\n      allSlidesSize += slideSizeValue + (spaceBetween || 0);\n    });\n    allSlidesSize -= spaceBetween;\n    const maxSnap = allSlidesSize - swiperSize;\n    snapGrid = snapGrid.map(snap => {\n      if (snap <= 0) return -offsetBefore;\n      if (snap > maxSnap) return maxSnap + offsetAfter;\n      return snap;\n    });\n  }\n  if (params.centerInsufficientSlides) {\n    let allSlidesSize = 0;\n    slidesSizesGrid.forEach(slideSizeValue => {\n      allSlidesSize += slideSizeValue + (spaceBetween || 0);\n    });\n    allSlidesSize -= spaceBetween;\n    if (allSlidesSize < swiperSize) {\n      const allSlidesOffset = (swiperSize - allSlidesSize) / 2;\n      snapGrid.forEach((snap, snapIndex) => {\n        snapGrid[snapIndex] = snap - allSlidesOffset;\n      });\n      slidesGrid.forEach((snap, snapIndex) => {\n        slidesGrid[snapIndex] = snap + allSlidesOffset;\n      });\n    }\n  }\n  Object.assign(swiper, {\n    slides,\n    snapGrid,\n    slidesGrid,\n    slidesSizesGrid\n  });\n  if (params.centeredSlides && params.cssMode && !params.centeredSlidesBounds) {\n    (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.s)(wrapperEl, '--swiper-centered-offset-before', `${-snapGrid[0]}px`);\n    (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.s)(wrapperEl, '--swiper-centered-offset-after', `${swiper.size / 2 - slidesSizesGrid[slidesSizesGrid.length - 1] / 2}px`);\n    const addToSnapGrid = -swiper.snapGrid[0];\n    const addToSlidesGrid = -swiper.slidesGrid[0];\n    swiper.snapGrid = swiper.snapGrid.map(v => v + addToSnapGrid);\n    swiper.slidesGrid = swiper.slidesGrid.map(v => v + addToSlidesGrid);\n  }\n  if (slidesLength !== previousSlidesLength) {\n    swiper.emit('slidesLengthChange');\n  }\n  if (snapGrid.length !== previousSnapGridLength) {\n    if (swiper.params.watchOverflow) swiper.checkOverflow();\n    swiper.emit('snapGridLengthChange');\n  }\n  if (slidesGrid.length !== previousSlidesGridLength) {\n    swiper.emit('slidesGridLengthChange');\n  }\n  if (params.watchSlidesProgress) {\n    swiper.updateSlidesOffset();\n  }\n  if (!isVirtual && !params.cssMode && (params.effect === 'slide' || params.effect === 'fade')) {\n    const backFaceHiddenClass = `${params.containerModifierClass}backface-hidden`;\n    const hasClassBackfaceClassAdded = swiper.el.classList.contains(backFaceHiddenClass);\n    if (slidesLength <= params.maxBackfaceHiddenSlides) {\n      if (!hasClassBackfaceClassAdded) swiper.el.classList.add(backFaceHiddenClass);\n    } else if (hasClassBackfaceClassAdded) {\n      swiper.el.classList.remove(backFaceHiddenClass);\n    }\n  }\n}\n\nfunction updateAutoHeight(speed) {\n  const swiper = this;\n  const activeSlides = [];\n  const isVirtual = swiper.virtual && swiper.params.virtual.enabled;\n  let newHeight = 0;\n  let i;\n  if (typeof speed === 'number') {\n    swiper.setTransition(speed);\n  } else if (speed === true) {\n    swiper.setTransition(swiper.params.speed);\n  }\n  const getSlideByIndex = index => {\n    if (isVirtual) {\n      return swiper.slides[swiper.getSlideIndexByData(index)];\n    }\n    return swiper.slides[index];\n  };\n  // Find slides currently in view\n  if (swiper.params.slidesPerView !== 'auto' && swiper.params.slidesPerView > 1) {\n    if (swiper.params.centeredSlides) {\n      (swiper.visibleSlides || []).forEach(slide => {\n        activeSlides.push(slide);\n      });\n    } else {\n      for (i = 0; i < Math.ceil(swiper.params.slidesPerView); i += 1) {\n        const index = swiper.activeIndex + i;\n        if (index > swiper.slides.length && !isVirtual) break;\n        activeSlides.push(getSlideByIndex(index));\n      }\n    }\n  } else {\n    activeSlides.push(getSlideByIndex(swiper.activeIndex));\n  }\n\n  // Find new height from highest slide in view\n  for (i = 0; i < activeSlides.length; i += 1) {\n    if (typeof activeSlides[i] !== 'undefined') {\n      const height = activeSlides[i].offsetHeight;\n      newHeight = height > newHeight ? height : newHeight;\n    }\n  }\n\n  // Update Height\n  if (newHeight || newHeight === 0) swiper.wrapperEl.style.height = `${newHeight}px`;\n}\n\nfunction updateSlidesOffset() {\n  const swiper = this;\n  const slides = swiper.slides;\n  // eslint-disable-next-line\n  const minusOffset = swiper.isElement ? swiper.isHorizontal() ? swiper.wrapperEl.offsetLeft : swiper.wrapperEl.offsetTop : 0;\n  for (let i = 0; i < slides.length; i += 1) {\n    slides[i].swiperSlideOffset = (swiper.isHorizontal() ? slides[i].offsetLeft : slides[i].offsetTop) - minusOffset - swiper.cssOverflowAdjustment();\n  }\n}\n\nfunction updateSlidesProgress(translate) {\n  if (translate === void 0) {\n    translate = this && this.translate || 0;\n  }\n  const swiper = this;\n  const params = swiper.params;\n  const {\n    slides,\n    rtlTranslate: rtl,\n    snapGrid\n  } = swiper;\n  if (slides.length === 0) return;\n  if (typeof slides[0].swiperSlideOffset === 'undefined') swiper.updateSlidesOffset();\n  let offsetCenter = -translate;\n  if (rtl) offsetCenter = translate;\n\n  // Visible Slides\n  slides.forEach(slideEl => {\n    slideEl.classList.remove(params.slideVisibleClass);\n  });\n  swiper.visibleSlidesIndexes = [];\n  swiper.visibleSlides = [];\n  let spaceBetween = params.spaceBetween;\n  if (typeof spaceBetween === 'string' && spaceBetween.indexOf('%') >= 0) {\n    spaceBetween = parseFloat(spaceBetween.replace('%', '')) / 100 * swiper.size;\n  } else if (typeof spaceBetween === 'string') {\n    spaceBetween = parseFloat(spaceBetween);\n  }\n  for (let i = 0; i < slides.length; i += 1) {\n    const slide = slides[i];\n    let slideOffset = slide.swiperSlideOffset;\n    if (params.cssMode && params.centeredSlides) {\n      slideOffset -= slides[0].swiperSlideOffset;\n    }\n    const slideProgress = (offsetCenter + (params.centeredSlides ? swiper.minTranslate() : 0) - slideOffset) / (slide.swiperSlideSize + spaceBetween);\n    const originalSlideProgress = (offsetCenter - snapGrid[0] + (params.centeredSlides ? swiper.minTranslate() : 0) - slideOffset) / (slide.swiperSlideSize + spaceBetween);\n    const slideBefore = -(offsetCenter - slideOffset);\n    const slideAfter = slideBefore + swiper.slidesSizesGrid[i];\n    const isVisible = slideBefore >= 0 && slideBefore < swiper.size - 1 || slideAfter > 1 && slideAfter <= swiper.size || slideBefore <= 0 && slideAfter >= swiper.size;\n    if (isVisible) {\n      swiper.visibleSlides.push(slide);\n      swiper.visibleSlidesIndexes.push(i);\n      slides[i].classList.add(params.slideVisibleClass);\n    }\n    slide.progress = rtl ? -slideProgress : slideProgress;\n    slide.originalProgress = rtl ? -originalSlideProgress : originalSlideProgress;\n  }\n}\n\nfunction updateProgress(translate) {\n  const swiper = this;\n  if (typeof translate === 'undefined') {\n    const multiplier = swiper.rtlTranslate ? -1 : 1;\n    // eslint-disable-next-line\n    translate = swiper && swiper.translate && swiper.translate * multiplier || 0;\n  }\n  const params = swiper.params;\n  const translatesDiff = swiper.maxTranslate() - swiper.minTranslate();\n  let {\n    progress,\n    isBeginning,\n    isEnd,\n    progressLoop\n  } = swiper;\n  const wasBeginning = isBeginning;\n  const wasEnd = isEnd;\n  if (translatesDiff === 0) {\n    progress = 0;\n    isBeginning = true;\n    isEnd = true;\n  } else {\n    progress = (translate - swiper.minTranslate()) / translatesDiff;\n    const isBeginningRounded = Math.abs(translate - swiper.minTranslate()) < 1;\n    const isEndRounded = Math.abs(translate - swiper.maxTranslate()) < 1;\n    isBeginning = isBeginningRounded || progress <= 0;\n    isEnd = isEndRounded || progress >= 1;\n    if (isBeginningRounded) progress = 0;\n    if (isEndRounded) progress = 1;\n  }\n  if (params.loop) {\n    const firstSlideIndex = swiper.getSlideIndexByData(0);\n    const lastSlideIndex = swiper.getSlideIndexByData(swiper.slides.length - 1);\n    const firstSlideTranslate = swiper.slidesGrid[firstSlideIndex];\n    const lastSlideTranslate = swiper.slidesGrid[lastSlideIndex];\n    const translateMax = swiper.slidesGrid[swiper.slidesGrid.length - 1];\n    const translateAbs = Math.abs(translate);\n    if (translateAbs >= firstSlideTranslate) {\n      progressLoop = (translateAbs - firstSlideTranslate) / translateMax;\n    } else {\n      progressLoop = (translateAbs + translateMax - lastSlideTranslate) / translateMax;\n    }\n    if (progressLoop > 1) progressLoop -= 1;\n  }\n  Object.assign(swiper, {\n    progress,\n    progressLoop,\n    isBeginning,\n    isEnd\n  });\n  if (params.watchSlidesProgress || params.centeredSlides && params.autoHeight) swiper.updateSlidesProgress(translate);\n  if (isBeginning && !wasBeginning) {\n    swiper.emit('reachBeginning toEdge');\n  }\n  if (isEnd && !wasEnd) {\n    swiper.emit('reachEnd toEdge');\n  }\n  if (wasBeginning && !isBeginning || wasEnd && !isEnd) {\n    swiper.emit('fromEdge');\n  }\n  swiper.emit('progress', progress);\n}\n\nfunction updateSlidesClasses() {\n  const swiper = this;\n  const {\n    slides,\n    params,\n    slidesEl,\n    activeIndex\n  } = swiper;\n  const isVirtual = swiper.virtual && params.virtual.enabled;\n  const getFilteredSlide = selector => {\n    return (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(slidesEl, `.${params.slideClass}${selector}, swiper-slide${selector}`)[0];\n  };\n  slides.forEach(slideEl => {\n    slideEl.classList.remove(params.slideActiveClass, params.slideNextClass, params.slidePrevClass);\n  });\n  let activeSlide;\n  if (isVirtual) {\n    if (params.loop) {\n      let slideIndex = activeIndex - swiper.virtual.slidesBefore;\n      if (slideIndex < 0) slideIndex = swiper.virtual.slides.length + slideIndex;\n      if (slideIndex >= swiper.virtual.slides.length) slideIndex -= swiper.virtual.slides.length;\n      activeSlide = getFilteredSlide(`[data-swiper-slide-index=\"${slideIndex}\"]`);\n    } else {\n      activeSlide = getFilteredSlide(`[data-swiper-slide-index=\"${activeIndex}\"]`);\n    }\n  } else {\n    activeSlide = slides[activeIndex];\n  }\n  if (activeSlide) {\n    // Active classes\n    activeSlide.classList.add(params.slideActiveClass);\n\n    // Next Slide\n    let nextSlide = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.m)(activeSlide, `.${params.slideClass}, swiper-slide`)[0];\n    if (params.loop && !nextSlide) {\n      nextSlide = slides[0];\n    }\n    if (nextSlide) {\n      nextSlide.classList.add(params.slideNextClass);\n    }\n    // Prev Slide\n    let prevSlide = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.o)(activeSlide, `.${params.slideClass}, swiper-slide`)[0];\n    if (params.loop && !prevSlide === 0) {\n      prevSlide = slides[slides.length - 1];\n    }\n    if (prevSlide) {\n      prevSlide.classList.add(params.slidePrevClass);\n    }\n  }\n  swiper.emitSlidesClasses();\n}\n\nconst processLazyPreloader = (swiper, imageEl) => {\n  if (!swiper || swiper.destroyed || !swiper.params) return;\n  const slideSelector = () => swiper.isElement ? `swiper-slide` : `.${swiper.params.slideClass}`;\n  const slideEl = imageEl.closest(slideSelector());\n  if (slideEl) {\n    const lazyEl = slideEl.querySelector(`.${swiper.params.lazyPreloaderClass}`);\n    if (lazyEl) lazyEl.remove();\n  }\n};\nconst unlazy = (swiper, index) => {\n  if (!swiper.slides[index]) return;\n  const imageEl = swiper.slides[index].querySelector('[loading=\"lazy\"]');\n  if (imageEl) imageEl.removeAttribute('loading');\n};\nconst preload = swiper => {\n  if (!swiper || swiper.destroyed || !swiper.params) return;\n  let amount = swiper.params.lazyPreloadPrevNext;\n  const len = swiper.slides.length;\n  if (!len || !amount || amount < 0) return;\n  amount = Math.min(amount, len);\n  const slidesPerView = swiper.params.slidesPerView === 'auto' ? swiper.slidesPerViewDynamic() : Math.ceil(swiper.params.slidesPerView);\n  const activeIndex = swiper.activeIndex;\n  if (swiper.params.grid && swiper.params.grid.rows > 1) {\n    const activeColumn = activeIndex;\n    const preloadColumns = [activeColumn - amount];\n    preloadColumns.push(...Array.from({\n      length: amount\n    }).map((_, i) => {\n      return activeColumn + slidesPerView + i;\n    }));\n    swiper.slides.forEach((slideEl, i) => {\n      if (preloadColumns.includes(slideEl.column)) unlazy(swiper, i);\n    });\n    return;\n  }\n  const slideIndexLastInView = activeIndex + slidesPerView - 1;\n  if (swiper.params.rewind || swiper.params.loop) {\n    for (let i = activeIndex - amount; i <= slideIndexLastInView + amount; i += 1) {\n      const realIndex = (i % len + len) % len;\n      if (realIndex < activeIndex || realIndex > slideIndexLastInView) unlazy(swiper, realIndex);\n    }\n  } else {\n    for (let i = Math.max(activeIndex - amount, 0); i <= Math.min(slideIndexLastInView + amount, len - 1); i += 1) {\n      if (i !== activeIndex && (i > slideIndexLastInView || i < activeIndex)) {\n        unlazy(swiper, i);\n      }\n    }\n  }\n};\n\nfunction getActiveIndexByTranslate(swiper) {\n  const {\n    slidesGrid,\n    params\n  } = swiper;\n  const translate = swiper.rtlTranslate ? swiper.translate : -swiper.translate;\n  let activeIndex;\n  for (let i = 0; i < slidesGrid.length; i += 1) {\n    if (typeof slidesGrid[i + 1] !== 'undefined') {\n      if (translate >= slidesGrid[i] && translate < slidesGrid[i + 1] - (slidesGrid[i + 1] - slidesGrid[i]) / 2) {\n        activeIndex = i;\n      } else if (translate >= slidesGrid[i] && translate < slidesGrid[i + 1]) {\n        activeIndex = i + 1;\n      }\n    } else if (translate >= slidesGrid[i]) {\n      activeIndex = i;\n    }\n  }\n  // Normalize slideIndex\n  if (params.normalizeSlideIndex) {\n    if (activeIndex < 0 || typeof activeIndex === 'undefined') activeIndex = 0;\n  }\n  return activeIndex;\n}\nfunction updateActiveIndex(newActiveIndex) {\n  const swiper = this;\n  const translate = swiper.rtlTranslate ? swiper.translate : -swiper.translate;\n  const {\n    snapGrid,\n    params,\n    activeIndex: previousIndex,\n    realIndex: previousRealIndex,\n    snapIndex: previousSnapIndex\n  } = swiper;\n  let activeIndex = newActiveIndex;\n  let snapIndex;\n  const getVirtualRealIndex = aIndex => {\n    let realIndex = aIndex - swiper.virtual.slidesBefore;\n    if (realIndex < 0) {\n      realIndex = swiper.virtual.slides.length + realIndex;\n    }\n    if (realIndex >= swiper.virtual.slides.length) {\n      realIndex -= swiper.virtual.slides.length;\n    }\n    return realIndex;\n  };\n  if (typeof activeIndex === 'undefined') {\n    activeIndex = getActiveIndexByTranslate(swiper);\n  }\n  if (snapGrid.indexOf(translate) >= 0) {\n    snapIndex = snapGrid.indexOf(translate);\n  } else {\n    const skip = Math.min(params.slidesPerGroupSkip, activeIndex);\n    snapIndex = skip + Math.floor((activeIndex - skip) / params.slidesPerGroup);\n  }\n  if (snapIndex >= snapGrid.length) snapIndex = snapGrid.length - 1;\n  if (activeIndex === previousIndex) {\n    if (snapIndex !== previousSnapIndex) {\n      swiper.snapIndex = snapIndex;\n      swiper.emit('snapIndexChange');\n    }\n    if (swiper.params.loop && swiper.virtual && swiper.params.virtual.enabled) {\n      swiper.realIndex = getVirtualRealIndex(activeIndex);\n    }\n    return;\n  }\n  // Get real index\n  let realIndex;\n  if (swiper.virtual && params.virtual.enabled && params.loop) {\n    realIndex = getVirtualRealIndex(activeIndex);\n  } else if (swiper.slides[activeIndex]) {\n    realIndex = parseInt(swiper.slides[activeIndex].getAttribute('data-swiper-slide-index') || activeIndex, 10);\n  } else {\n    realIndex = activeIndex;\n  }\n  Object.assign(swiper, {\n    previousSnapIndex,\n    snapIndex,\n    previousRealIndex,\n    realIndex,\n    previousIndex,\n    activeIndex\n  });\n  if (swiper.initialized) {\n    preload(swiper);\n  }\n  swiper.emit('activeIndexChange');\n  swiper.emit('snapIndexChange');\n  if (previousRealIndex !== realIndex) {\n    swiper.emit('realIndexChange');\n  }\n  if (swiper.initialized || swiper.params.runCallbacksOnInit) {\n    swiper.emit('slideChange');\n  }\n}\n\nfunction updateClickedSlide(e) {\n  const swiper = this;\n  const params = swiper.params;\n  const slide = e.closest(`.${params.slideClass}, swiper-slide`);\n  let slideFound = false;\n  let slideIndex;\n  if (slide) {\n    for (let i = 0; i < swiper.slides.length; i += 1) {\n      if (swiper.slides[i] === slide) {\n        slideFound = true;\n        slideIndex = i;\n        break;\n      }\n    }\n  }\n  if (slide && slideFound) {\n    swiper.clickedSlide = slide;\n    if (swiper.virtual && swiper.params.virtual.enabled) {\n      swiper.clickedIndex = parseInt(slide.getAttribute('data-swiper-slide-index'), 10);\n    } else {\n      swiper.clickedIndex = slideIndex;\n    }\n  } else {\n    swiper.clickedSlide = undefined;\n    swiper.clickedIndex = undefined;\n    return;\n  }\n  if (params.slideToClickedSlide && swiper.clickedIndex !== undefined && swiper.clickedIndex !== swiper.activeIndex) {\n    swiper.slideToClickedSlide();\n  }\n}\n\nvar update = {\n  updateSize,\n  updateSlides,\n  updateAutoHeight,\n  updateSlidesOffset,\n  updateSlidesProgress,\n  updateProgress,\n  updateSlidesClasses,\n  updateActiveIndex,\n  updateClickedSlide\n};\n\nfunction getSwiperTranslate(axis) {\n  if (axis === void 0) {\n    axis = this.isHorizontal() ? 'x' : 'y';\n  }\n  const swiper = this;\n  const {\n    params,\n    rtlTranslate: rtl,\n    translate,\n    wrapperEl\n  } = swiper;\n  if (params.virtualTranslate) {\n    return rtl ? -translate : translate;\n  }\n  if (params.cssMode) {\n    return translate;\n  }\n  let currentTranslate = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.h)(wrapperEl, axis);\n  currentTranslate += swiper.cssOverflowAdjustment();\n  if (rtl) currentTranslate = -currentTranslate;\n  return currentTranslate || 0;\n}\n\nfunction setTranslate(translate, byController) {\n  const swiper = this;\n  const {\n    rtlTranslate: rtl,\n    params,\n    wrapperEl,\n    progress\n  } = swiper;\n  let x = 0;\n  let y = 0;\n  const z = 0;\n  if (swiper.isHorizontal()) {\n    x = rtl ? -translate : translate;\n  } else {\n    y = translate;\n  }\n  if (params.roundLengths) {\n    x = Math.floor(x);\n    y = Math.floor(y);\n  }\n  swiper.previousTranslate = swiper.translate;\n  swiper.translate = swiper.isHorizontal() ? x : y;\n  if (params.cssMode) {\n    wrapperEl[swiper.isHorizontal() ? 'scrollLeft' : 'scrollTop'] = swiper.isHorizontal() ? -x : -y;\n  } else if (!params.virtualTranslate) {\n    if (swiper.isHorizontal()) {\n      x -= swiper.cssOverflowAdjustment();\n    } else {\n      y -= swiper.cssOverflowAdjustment();\n    }\n    wrapperEl.style.transform = `translate3d(${x}px, ${y}px, ${z}px)`;\n  }\n\n  // Check if we need to update progress\n  let newProgress;\n  const translatesDiff = swiper.maxTranslate() - swiper.minTranslate();\n  if (translatesDiff === 0) {\n    newProgress = 0;\n  } else {\n    newProgress = (translate - swiper.minTranslate()) / translatesDiff;\n  }\n  if (newProgress !== progress) {\n    swiper.updateProgress(translate);\n  }\n  swiper.emit('setTranslate', swiper.translate, byController);\n}\n\nfunction minTranslate() {\n  return -this.snapGrid[0];\n}\n\nfunction maxTranslate() {\n  return -this.snapGrid[this.snapGrid.length - 1];\n}\n\nfunction translateTo(translate, speed, runCallbacks, translateBounds, internal) {\n  if (translate === void 0) {\n    translate = 0;\n  }\n  if (speed === void 0) {\n    speed = this.params.speed;\n  }\n  if (runCallbacks === void 0) {\n    runCallbacks = true;\n  }\n  if (translateBounds === void 0) {\n    translateBounds = true;\n  }\n  const swiper = this;\n  const {\n    params,\n    wrapperEl\n  } = swiper;\n  if (swiper.animating && params.preventInteractionOnTransition) {\n    return false;\n  }\n  const minTranslate = swiper.minTranslate();\n  const maxTranslate = swiper.maxTranslate();\n  let newTranslate;\n  if (translateBounds && translate > minTranslate) newTranslate = minTranslate;else if (translateBounds && translate < maxTranslate) newTranslate = maxTranslate;else newTranslate = translate;\n\n  // Update progress\n  swiper.updateProgress(newTranslate);\n  if (params.cssMode) {\n    const isH = swiper.isHorizontal();\n    if (speed === 0) {\n      wrapperEl[isH ? 'scrollLeft' : 'scrollTop'] = -newTranslate;\n    } else {\n      if (!swiper.support.smoothScroll) {\n        (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.p)({\n          swiper,\n          targetPosition: -newTranslate,\n          side: isH ? 'left' : 'top'\n        });\n        return true;\n      }\n      wrapperEl.scrollTo({\n        [isH ? 'left' : 'top']: -newTranslate,\n        behavior: 'smooth'\n      });\n    }\n    return true;\n  }\n  if (speed === 0) {\n    swiper.setTransition(0);\n    swiper.setTranslate(newTranslate);\n    if (runCallbacks) {\n      swiper.emit('beforeTransitionStart', speed, internal);\n      swiper.emit('transitionEnd');\n    }\n  } else {\n    swiper.setTransition(speed);\n    swiper.setTranslate(newTranslate);\n    if (runCallbacks) {\n      swiper.emit('beforeTransitionStart', speed, internal);\n      swiper.emit('transitionStart');\n    }\n    if (!swiper.animating) {\n      swiper.animating = true;\n      if (!swiper.onTranslateToWrapperTransitionEnd) {\n        swiper.onTranslateToWrapperTransitionEnd = function transitionEnd(e) {\n          if (!swiper || swiper.destroyed) return;\n          if (e.target !== this) return;\n          swiper.wrapperEl.removeEventListener('transitionend', swiper.onTranslateToWrapperTransitionEnd);\n          swiper.onTranslateToWrapperTransitionEnd = null;\n          delete swiper.onTranslateToWrapperTransitionEnd;\n          if (runCallbacks) {\n            swiper.emit('transitionEnd');\n          }\n        };\n      }\n      swiper.wrapperEl.addEventListener('transitionend', swiper.onTranslateToWrapperTransitionEnd);\n    }\n  }\n  return true;\n}\n\nvar translate = {\n  getTranslate: getSwiperTranslate,\n  setTranslate,\n  minTranslate,\n  maxTranslate,\n  translateTo\n};\n\nfunction setTransition(duration, byController) {\n  const swiper = this;\n  if (!swiper.params.cssMode) {\n    swiper.wrapperEl.style.transitionDuration = `${duration}ms`;\n  }\n  swiper.emit('setTransition', duration, byController);\n}\n\nfunction transitionEmit(_ref) {\n  let {\n    swiper,\n    runCallbacks,\n    direction,\n    step\n  } = _ref;\n  const {\n    activeIndex,\n    previousIndex\n  } = swiper;\n  let dir = direction;\n  if (!dir) {\n    if (activeIndex > previousIndex) dir = 'next';else if (activeIndex < previousIndex) dir = 'prev';else dir = 'reset';\n  }\n  swiper.emit(`transition${step}`);\n  if (runCallbacks && activeIndex !== previousIndex) {\n    if (dir === 'reset') {\n      swiper.emit(`slideResetTransition${step}`);\n      return;\n    }\n    swiper.emit(`slideChangeTransition${step}`);\n    if (dir === 'next') {\n      swiper.emit(`slideNextTransition${step}`);\n    } else {\n      swiper.emit(`slidePrevTransition${step}`);\n    }\n  }\n}\n\nfunction transitionStart(runCallbacks, direction) {\n  if (runCallbacks === void 0) {\n    runCallbacks = true;\n  }\n  const swiper = this;\n  const {\n    params\n  } = swiper;\n  if (params.cssMode) return;\n  if (params.autoHeight) {\n    swiper.updateAutoHeight();\n  }\n  transitionEmit({\n    swiper,\n    runCallbacks,\n    direction,\n    step: 'Start'\n  });\n}\n\nfunction transitionEnd(runCallbacks, direction) {\n  if (runCallbacks === void 0) {\n    runCallbacks = true;\n  }\n  const swiper = this;\n  const {\n    params\n  } = swiper;\n  swiper.animating = false;\n  if (params.cssMode) return;\n  swiper.setTransition(0);\n  transitionEmit({\n    swiper,\n    runCallbacks,\n    direction,\n    step: 'End'\n  });\n}\n\nvar transition = {\n  setTransition,\n  transitionStart,\n  transitionEnd\n};\n\nfunction slideTo(index, speed, runCallbacks, internal, initial) {\n  if (index === void 0) {\n    index = 0;\n  }\n  if (speed === void 0) {\n    speed = this.params.speed;\n  }\n  if (runCallbacks === void 0) {\n    runCallbacks = true;\n  }\n  if (typeof index === 'string') {\n    index = parseInt(index, 10);\n  }\n  const swiper = this;\n  let slideIndex = index;\n  if (slideIndex < 0) slideIndex = 0;\n  const {\n    params,\n    snapGrid,\n    slidesGrid,\n    previousIndex,\n    activeIndex,\n    rtlTranslate: rtl,\n    wrapperEl,\n    enabled\n  } = swiper;\n  if (swiper.animating && params.preventInteractionOnTransition || !enabled && !internal && !initial) {\n    return false;\n  }\n  const skip = Math.min(swiper.params.slidesPerGroupSkip, slideIndex);\n  let snapIndex = skip + Math.floor((slideIndex - skip) / swiper.params.slidesPerGroup);\n  if (snapIndex >= snapGrid.length) snapIndex = snapGrid.length - 1;\n  const translate = -snapGrid[snapIndex];\n  // Normalize slideIndex\n  if (params.normalizeSlideIndex) {\n    for (let i = 0; i < slidesGrid.length; i += 1) {\n      const normalizedTranslate = -Math.floor(translate * 100);\n      const normalizedGrid = Math.floor(slidesGrid[i] * 100);\n      const normalizedGridNext = Math.floor(slidesGrid[i + 1] * 100);\n      if (typeof slidesGrid[i + 1] !== 'undefined') {\n        if (normalizedTranslate >= normalizedGrid && normalizedTranslate < normalizedGridNext - (normalizedGridNext - normalizedGrid) / 2) {\n          slideIndex = i;\n        } else if (normalizedTranslate >= normalizedGrid && normalizedTranslate < normalizedGridNext) {\n          slideIndex = i + 1;\n        }\n      } else if (normalizedTranslate >= normalizedGrid) {\n        slideIndex = i;\n      }\n    }\n  }\n  // Directions locks\n  if (swiper.initialized && slideIndex !== activeIndex) {\n    if (!swiper.allowSlideNext && (rtl ? translate > swiper.translate && translate > swiper.minTranslate() : translate < swiper.translate && translate < swiper.minTranslate())) {\n      return false;\n    }\n    if (!swiper.allowSlidePrev && translate > swiper.translate && translate > swiper.maxTranslate()) {\n      if ((activeIndex || 0) !== slideIndex) {\n        return false;\n      }\n    }\n  }\n  if (slideIndex !== (previousIndex || 0) && runCallbacks) {\n    swiper.emit('beforeSlideChangeStart');\n  }\n\n  // Update progress\n  swiper.updateProgress(translate);\n  let direction;\n  if (slideIndex > activeIndex) direction = 'next';else if (slideIndex < activeIndex) direction = 'prev';else direction = 'reset';\n\n  // Update Index\n  if (rtl && -translate === swiper.translate || !rtl && translate === swiper.translate) {\n    swiper.updateActiveIndex(slideIndex);\n    // Update Height\n    if (params.autoHeight) {\n      swiper.updateAutoHeight();\n    }\n    swiper.updateSlidesClasses();\n    if (params.effect !== 'slide') {\n      swiper.setTranslate(translate);\n    }\n    if (direction !== 'reset') {\n      swiper.transitionStart(runCallbacks, direction);\n      swiper.transitionEnd(runCallbacks, direction);\n    }\n    return false;\n  }\n  if (params.cssMode) {\n    const isH = swiper.isHorizontal();\n    const t = rtl ? translate : -translate;\n    if (speed === 0) {\n      const isVirtual = swiper.virtual && swiper.params.virtual.enabled;\n      if (isVirtual) {\n        swiper.wrapperEl.style.scrollSnapType = 'none';\n        swiper._immediateVirtual = true;\n      }\n      if (isVirtual && !swiper._cssModeVirtualInitialSet && swiper.params.initialSlide > 0) {\n        swiper._cssModeVirtualInitialSet = true;\n        requestAnimationFrame(() => {\n          wrapperEl[isH ? 'scrollLeft' : 'scrollTop'] = t;\n        });\n      } else {\n        wrapperEl[isH ? 'scrollLeft' : 'scrollTop'] = t;\n      }\n      if (isVirtual) {\n        requestAnimationFrame(() => {\n          swiper.wrapperEl.style.scrollSnapType = '';\n          swiper._immediateVirtual = false;\n        });\n      }\n    } else {\n      if (!swiper.support.smoothScroll) {\n        (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.p)({\n          swiper,\n          targetPosition: t,\n          side: isH ? 'left' : 'top'\n        });\n        return true;\n      }\n      wrapperEl.scrollTo({\n        [isH ? 'left' : 'top']: t,\n        behavior: 'smooth'\n      });\n    }\n    return true;\n  }\n  swiper.setTransition(speed);\n  swiper.setTranslate(translate);\n  swiper.updateActiveIndex(slideIndex);\n  swiper.updateSlidesClasses();\n  swiper.emit('beforeTransitionStart', speed, internal);\n  swiper.transitionStart(runCallbacks, direction);\n  if (speed === 0) {\n    swiper.transitionEnd(runCallbacks, direction);\n  } else if (!swiper.animating) {\n    swiper.animating = true;\n    if (!swiper.onSlideToWrapperTransitionEnd) {\n      swiper.onSlideToWrapperTransitionEnd = function transitionEnd(e) {\n        if (!swiper || swiper.destroyed) return;\n        if (e.target !== this) return;\n        swiper.wrapperEl.removeEventListener('transitionend', swiper.onSlideToWrapperTransitionEnd);\n        swiper.onSlideToWrapperTransitionEnd = null;\n        delete swiper.onSlideToWrapperTransitionEnd;\n        swiper.transitionEnd(runCallbacks, direction);\n      };\n    }\n    swiper.wrapperEl.addEventListener('transitionend', swiper.onSlideToWrapperTransitionEnd);\n  }\n  return true;\n}\n\nfunction slideToLoop(index, speed, runCallbacks, internal) {\n  if (index === void 0) {\n    index = 0;\n  }\n  if (speed === void 0) {\n    speed = this.params.speed;\n  }\n  if (runCallbacks === void 0) {\n    runCallbacks = true;\n  }\n  if (typeof index === 'string') {\n    const indexAsNumber = parseInt(index, 10);\n    index = indexAsNumber;\n  }\n  const swiper = this;\n  let newIndex = index;\n  if (swiper.params.loop) {\n    if (swiper.virtual && swiper.params.virtual.enabled) {\n      // eslint-disable-next-line\n      newIndex = newIndex + swiper.virtual.slidesBefore;\n    } else {\n      newIndex = swiper.getSlideIndexByData(newIndex);\n    }\n  }\n  return swiper.slideTo(newIndex, speed, runCallbacks, internal);\n}\n\n/* eslint no-unused-vars: \"off\" */\nfunction slideNext(speed, runCallbacks, internal) {\n  if (speed === void 0) {\n    speed = this.params.speed;\n  }\n  if (runCallbacks === void 0) {\n    runCallbacks = true;\n  }\n  const swiper = this;\n  const {\n    enabled,\n    params,\n    animating\n  } = swiper;\n  if (!enabled) return swiper;\n  let perGroup = params.slidesPerGroup;\n  if (params.slidesPerView === 'auto' && params.slidesPerGroup === 1 && params.slidesPerGroupAuto) {\n    perGroup = Math.max(swiper.slidesPerViewDynamic('current', true), 1);\n  }\n  const increment = swiper.activeIndex < params.slidesPerGroupSkip ? 1 : perGroup;\n  const isVirtual = swiper.virtual && params.virtual.enabled;\n  if (params.loop) {\n    if (animating && !isVirtual && params.loopPreventsSliding) return false;\n    swiper.loopFix({\n      direction: 'next'\n    });\n    // eslint-disable-next-line\n    swiper._clientLeft = swiper.wrapperEl.clientLeft;\n  }\n  if (params.rewind && swiper.isEnd) {\n    return swiper.slideTo(0, speed, runCallbacks, internal);\n  }\n  return swiper.slideTo(swiper.activeIndex + increment, speed, runCallbacks, internal);\n}\n\n/* eslint no-unused-vars: \"off\" */\nfunction slidePrev(speed, runCallbacks, internal) {\n  if (speed === void 0) {\n    speed = this.params.speed;\n  }\n  if (runCallbacks === void 0) {\n    runCallbacks = true;\n  }\n  const swiper = this;\n  const {\n    params,\n    snapGrid,\n    slidesGrid,\n    rtlTranslate,\n    enabled,\n    animating\n  } = swiper;\n  if (!enabled) return swiper;\n  const isVirtual = swiper.virtual && params.virtual.enabled;\n  if (params.loop) {\n    if (animating && !isVirtual && params.loopPreventsSliding) return false;\n    swiper.loopFix({\n      direction: 'prev'\n    });\n    // eslint-disable-next-line\n    swiper._clientLeft = swiper.wrapperEl.clientLeft;\n  }\n  const translate = rtlTranslate ? swiper.translate : -swiper.translate;\n  function normalize(val) {\n    if (val < 0) return -Math.floor(Math.abs(val));\n    return Math.floor(val);\n  }\n  const normalizedTranslate = normalize(translate);\n  const normalizedSnapGrid = snapGrid.map(val => normalize(val));\n  let prevSnap = snapGrid[normalizedSnapGrid.indexOf(normalizedTranslate) - 1];\n  if (typeof prevSnap === 'undefined' && params.cssMode) {\n    let prevSnapIndex;\n    snapGrid.forEach((snap, snapIndex) => {\n      if (normalizedTranslate >= snap) {\n        // prevSnap = snap;\n        prevSnapIndex = snapIndex;\n      }\n    });\n    if (typeof prevSnapIndex !== 'undefined') {\n      prevSnap = snapGrid[prevSnapIndex > 0 ? prevSnapIndex - 1 : prevSnapIndex];\n    }\n  }\n  let prevIndex = 0;\n  if (typeof prevSnap !== 'undefined') {\n    prevIndex = slidesGrid.indexOf(prevSnap);\n    if (prevIndex < 0) prevIndex = swiper.activeIndex - 1;\n    if (params.slidesPerView === 'auto' && params.slidesPerGroup === 1 && params.slidesPerGroupAuto) {\n      prevIndex = prevIndex - swiper.slidesPerViewDynamic('previous', true) + 1;\n      prevIndex = Math.max(prevIndex, 0);\n    }\n  }\n  if (params.rewind && swiper.isBeginning) {\n    const lastIndex = swiper.params.virtual && swiper.params.virtual.enabled && swiper.virtual ? swiper.virtual.slides.length - 1 : swiper.slides.length - 1;\n    return swiper.slideTo(lastIndex, speed, runCallbacks, internal);\n  }\n  return swiper.slideTo(prevIndex, speed, runCallbacks, internal);\n}\n\n/* eslint no-unused-vars: \"off\" */\nfunction slideReset(speed, runCallbacks, internal) {\n  if (speed === void 0) {\n    speed = this.params.speed;\n  }\n  if (runCallbacks === void 0) {\n    runCallbacks = true;\n  }\n  const swiper = this;\n  return swiper.slideTo(swiper.activeIndex, speed, runCallbacks, internal);\n}\n\n/* eslint no-unused-vars: \"off\" */\nfunction slideToClosest(speed, runCallbacks, internal, threshold) {\n  if (speed === void 0) {\n    speed = this.params.speed;\n  }\n  if (runCallbacks === void 0) {\n    runCallbacks = true;\n  }\n  if (threshold === void 0) {\n    threshold = 0.5;\n  }\n  const swiper = this;\n  let index = swiper.activeIndex;\n  const skip = Math.min(swiper.params.slidesPerGroupSkip, index);\n  const snapIndex = skip + Math.floor((index - skip) / swiper.params.slidesPerGroup);\n  const translate = swiper.rtlTranslate ? swiper.translate : -swiper.translate;\n  if (translate >= swiper.snapGrid[snapIndex]) {\n    // The current translate is on or after the current snap index, so the choice\n    // is between the current index and the one after it.\n    const currentSnap = swiper.snapGrid[snapIndex];\n    const nextSnap = swiper.snapGrid[snapIndex + 1];\n    if (translate - currentSnap > (nextSnap - currentSnap) * threshold) {\n      index += swiper.params.slidesPerGroup;\n    }\n  } else {\n    // The current translate is before the current snap index, so the choice\n    // is between the current index and the one before it.\n    const prevSnap = swiper.snapGrid[snapIndex - 1];\n    const currentSnap = swiper.snapGrid[snapIndex];\n    if (translate - prevSnap <= (currentSnap - prevSnap) * threshold) {\n      index -= swiper.params.slidesPerGroup;\n    }\n  }\n  index = Math.max(index, 0);\n  index = Math.min(index, swiper.slidesGrid.length - 1);\n  return swiper.slideTo(index, speed, runCallbacks, internal);\n}\n\nfunction slideToClickedSlide() {\n  const swiper = this;\n  const {\n    params,\n    slidesEl\n  } = swiper;\n  const slidesPerView = params.slidesPerView === 'auto' ? swiper.slidesPerViewDynamic() : params.slidesPerView;\n  let slideToIndex = swiper.clickedIndex;\n  let realIndex;\n  const slideSelector = swiper.isElement ? `swiper-slide` : `.${params.slideClass}`;\n  if (params.loop) {\n    if (swiper.animating) return;\n    realIndex = parseInt(swiper.clickedSlide.getAttribute('data-swiper-slide-index'), 10);\n    if (params.centeredSlides) {\n      if (slideToIndex < swiper.loopedSlides - slidesPerView / 2 || slideToIndex > swiper.slides.length - swiper.loopedSlides + slidesPerView / 2) {\n        swiper.loopFix();\n        slideToIndex = swiper.getSlideIndex((0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(slidesEl, `${slideSelector}[data-swiper-slide-index=\"${realIndex}\"]`)[0]);\n        (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.n)(() => {\n          swiper.slideTo(slideToIndex);\n        });\n      } else {\n        swiper.slideTo(slideToIndex);\n      }\n    } else if (slideToIndex > swiper.slides.length - slidesPerView) {\n      swiper.loopFix();\n      slideToIndex = swiper.getSlideIndex((0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(slidesEl, `${slideSelector}[data-swiper-slide-index=\"${realIndex}\"]`)[0]);\n      (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.n)(() => {\n        swiper.slideTo(slideToIndex);\n      });\n    } else {\n      swiper.slideTo(slideToIndex);\n    }\n  } else {\n    swiper.slideTo(slideToIndex);\n  }\n}\n\nvar slide = {\n  slideTo,\n  slideToLoop,\n  slideNext,\n  slidePrev,\n  slideReset,\n  slideToClosest,\n  slideToClickedSlide\n};\n\nfunction loopCreate(slideRealIndex) {\n  const swiper = this;\n  const {\n    params,\n    slidesEl\n  } = swiper;\n  if (!params.loop || swiper.virtual && swiper.params.virtual.enabled) return;\n  const slides = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(slidesEl, `.${params.slideClass}, swiper-slide`);\n  slides.forEach((el, index) => {\n    el.setAttribute('data-swiper-slide-index', index);\n  });\n  swiper.loopFix({\n    slideRealIndex,\n    direction: params.centeredSlides ? undefined : 'next'\n  });\n}\n\nfunction loopFix(_temp) {\n  let {\n    slideRealIndex,\n    slideTo = true,\n    direction,\n    setTranslate,\n    activeSlideIndex,\n    byController,\n    byMousewheel\n  } = _temp === void 0 ? {} : _temp;\n  const swiper = this;\n  if (!swiper.params.loop) return;\n  swiper.emit('beforeLoopFix');\n  const {\n    slides,\n    allowSlidePrev,\n    allowSlideNext,\n    slidesEl,\n    params\n  } = swiper;\n  swiper.allowSlidePrev = true;\n  swiper.allowSlideNext = true;\n  if (swiper.virtual && params.virtual.enabled) {\n    if (slideTo) {\n      if (!params.centeredSlides && swiper.snapIndex === 0) {\n        swiper.slideTo(swiper.virtual.slides.length, 0, false, true);\n      } else if (params.centeredSlides && swiper.snapIndex < params.slidesPerView) {\n        swiper.slideTo(swiper.virtual.slides.length + swiper.snapIndex, 0, false, true);\n      } else if (swiper.snapIndex === swiper.snapGrid.length - 1) {\n        swiper.slideTo(swiper.virtual.slidesBefore, 0, false, true);\n      }\n    }\n    swiper.allowSlidePrev = allowSlidePrev;\n    swiper.allowSlideNext = allowSlideNext;\n    swiper.emit('loopFix');\n    return;\n  }\n  const slidesPerView = params.slidesPerView === 'auto' ? swiper.slidesPerViewDynamic() : Math.ceil(parseFloat(params.slidesPerView, 10));\n  let loopedSlides = params.loopedSlides || slidesPerView;\n  if (loopedSlides % params.slidesPerGroup !== 0) {\n    loopedSlides += params.slidesPerGroup - loopedSlides % params.slidesPerGroup;\n  }\n  swiper.loopedSlides = loopedSlides;\n  const prependSlidesIndexes = [];\n  const appendSlidesIndexes = [];\n  let activeIndex = swiper.activeIndex;\n  if (typeof activeSlideIndex === 'undefined') {\n    activeSlideIndex = swiper.getSlideIndex(swiper.slides.filter(el => el.classList.contains(params.slideActiveClass))[0]);\n  } else {\n    activeIndex = activeSlideIndex;\n  }\n  const isNext = direction === 'next' || !direction;\n  const isPrev = direction === 'prev' || !direction;\n  let slidesPrepended = 0;\n  let slidesAppended = 0;\n  // prepend last slides before start\n  if (activeSlideIndex < loopedSlides) {\n    slidesPrepended = Math.max(loopedSlides - activeSlideIndex, params.slidesPerGroup);\n    for (let i = 0; i < loopedSlides - activeSlideIndex; i += 1) {\n      const index = i - Math.floor(i / slides.length) * slides.length;\n      prependSlidesIndexes.push(slides.length - index - 1);\n    }\n  } else if (activeSlideIndex /* + slidesPerView */ > swiper.slides.length - loopedSlides * 2) {\n    slidesAppended = Math.max(activeSlideIndex - (swiper.slides.length - loopedSlides * 2), params.slidesPerGroup);\n    for (let i = 0; i < slidesAppended; i += 1) {\n      const index = i - Math.floor(i / slides.length) * slides.length;\n      appendSlidesIndexes.push(index);\n    }\n  }\n  if (isPrev) {\n    prependSlidesIndexes.forEach(index => {\n      swiper.slides[index].swiperLoopMoveDOM = true;\n      slidesEl.prepend(swiper.slides[index]);\n      swiper.slides[index].swiperLoopMoveDOM = false;\n    });\n  }\n  if (isNext) {\n    appendSlidesIndexes.forEach(index => {\n      swiper.slides[index].swiperLoopMoveDOM = true;\n      slidesEl.append(swiper.slides[index]);\n      swiper.slides[index].swiperLoopMoveDOM = false;\n    });\n  }\n  swiper.recalcSlides();\n  if (params.slidesPerView === 'auto') {\n    swiper.updateSlides();\n  }\n  if (params.watchSlidesProgress) {\n    swiper.updateSlidesOffset();\n  }\n  if (slideTo) {\n    if (prependSlidesIndexes.length > 0 && isPrev) {\n      if (typeof slideRealIndex === 'undefined') {\n        const currentSlideTranslate = swiper.slidesGrid[activeIndex];\n        const newSlideTranslate = swiper.slidesGrid[activeIndex + slidesPrepended];\n        const diff = newSlideTranslate - currentSlideTranslate;\n        if (byMousewheel) {\n          swiper.setTranslate(swiper.translate - diff);\n        } else {\n          swiper.slideTo(activeIndex + slidesPrepended, 0, false, true);\n          if (setTranslate) {\n            swiper.touches[swiper.isHorizontal() ? 'startX' : 'startY'] += diff;\n            swiper.touchEventsData.currentTranslate = swiper.translate;\n          }\n        }\n      } else {\n        if (setTranslate) {\n          swiper.slideToLoop(slideRealIndex, 0, false, true);\n          swiper.touchEventsData.currentTranslate = swiper.translate;\n        }\n      }\n    } else if (appendSlidesIndexes.length > 0 && isNext) {\n      if (typeof slideRealIndex === 'undefined') {\n        const currentSlideTranslate = swiper.slidesGrid[activeIndex];\n        const newSlideTranslate = swiper.slidesGrid[activeIndex - slidesAppended];\n        const diff = newSlideTranslate - currentSlideTranslate;\n        if (byMousewheel) {\n          swiper.setTranslate(swiper.translate - diff);\n        } else {\n          swiper.slideTo(activeIndex - slidesAppended, 0, false, true);\n          if (setTranslate) {\n            swiper.touches[swiper.isHorizontal() ? 'startX' : 'startY'] += diff;\n            swiper.touchEventsData.currentTranslate = swiper.translate;\n          }\n        }\n      } else {\n        swiper.slideToLoop(slideRealIndex, 0, false, true);\n      }\n    }\n  }\n  swiper.allowSlidePrev = allowSlidePrev;\n  swiper.allowSlideNext = allowSlideNext;\n  if (swiper.controller && swiper.controller.control && !byController) {\n    const loopParams = {\n      slideRealIndex,\n      slideTo: false,\n      direction,\n      setTranslate,\n      activeSlideIndex,\n      byController: true\n    };\n    if (Array.isArray(swiper.controller.control)) {\n      swiper.controller.control.forEach(c => {\n        if (!c.destroyed && c.params.loop) c.loopFix(loopParams);\n      });\n    } else if (swiper.controller.control instanceof swiper.constructor && swiper.controller.control.params.loop) {\n      swiper.controller.control.loopFix(loopParams);\n    }\n  }\n  swiper.emit('loopFix');\n}\n\nfunction loopDestroy() {\n  const swiper = this;\n  const {\n    params,\n    slidesEl\n  } = swiper;\n  if (!params.loop || swiper.virtual && swiper.params.virtual.enabled) return;\n  swiper.recalcSlides();\n  const newSlidesOrder = [];\n  swiper.slides.forEach(slideEl => {\n    const index = typeof slideEl.swiperSlideIndex === 'undefined' ? slideEl.getAttribute('data-swiper-slide-index') * 1 : slideEl.swiperSlideIndex;\n    newSlidesOrder[index] = slideEl;\n  });\n  swiper.slides.forEach(slideEl => {\n    slideEl.removeAttribute('data-swiper-slide-index');\n  });\n  newSlidesOrder.forEach(slideEl => {\n    slidesEl.append(slideEl);\n  });\n  swiper.recalcSlides();\n  swiper.slideTo(swiper.realIndex, 0);\n}\n\nvar loop = {\n  loopCreate,\n  loopFix,\n  loopDestroy\n};\n\nfunction setGrabCursor(moving) {\n  const swiper = this;\n  if (!swiper.params.simulateTouch || swiper.params.watchOverflow && swiper.isLocked || swiper.params.cssMode) return;\n  const el = swiper.params.touchEventsTarget === 'container' ? swiper.el : swiper.wrapperEl;\n  if (swiper.isElement) {\n    swiper.__preventObserver__ = true;\n  }\n  el.style.cursor = 'move';\n  el.style.cursor = moving ? 'grabbing' : 'grab';\n  if (swiper.isElement) {\n    requestAnimationFrame(() => {\n      swiper.__preventObserver__ = false;\n    });\n  }\n}\n\nfunction unsetGrabCursor() {\n  const swiper = this;\n  if (swiper.params.watchOverflow && swiper.isLocked || swiper.params.cssMode) {\n    return;\n  }\n  if (swiper.isElement) {\n    swiper.__preventObserver__ = true;\n  }\n  swiper[swiper.params.touchEventsTarget === 'container' ? 'el' : 'wrapperEl'].style.cursor = '';\n  if (swiper.isElement) {\n    requestAnimationFrame(() => {\n      swiper.__preventObserver__ = false;\n    });\n  }\n}\n\nvar grabCursor = {\n  setGrabCursor,\n  unsetGrabCursor\n};\n\n// Modified from https://stackoverflow.com/questions/54520554/custom-element-getrootnode-closest-function-crossing-multiple-parent-shadowd\nfunction closestElement(selector, base) {\n  if (base === void 0) {\n    base = this;\n  }\n  function __closestFrom(el) {\n    if (!el || el === (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)() || el === (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)()) return null;\n    if (el.assignedSlot) el = el.assignedSlot;\n    const found = el.closest(selector);\n    if (!found && !el.getRootNode) {\n      return null;\n    }\n    return found || __closestFrom(el.getRootNode().host);\n  }\n  return __closestFrom(base);\n}\nfunction onTouchStart(event) {\n  const swiper = this;\n  const document = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();\n  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();\n  const data = swiper.touchEventsData;\n  data.evCache.push(event);\n  const {\n    params,\n    touches,\n    enabled\n  } = swiper;\n  if (!enabled) return;\n  if (!params.simulateTouch && event.pointerType === 'mouse') return;\n  if (swiper.animating && params.preventInteractionOnTransition) {\n    return;\n  }\n  if (!swiper.animating && params.cssMode && params.loop) {\n    swiper.loopFix();\n  }\n  let e = event;\n  if (e.originalEvent) e = e.originalEvent;\n  let targetEl = e.target;\n  if (params.touchEventsTarget === 'wrapper') {\n    if (!swiper.wrapperEl.contains(targetEl)) return;\n  }\n  if ('which' in e && e.which === 3) return;\n  if ('button' in e && e.button > 0) return;\n  if (data.isTouched && data.isMoved) return;\n\n  // change target el for shadow root component\n  const swipingClassHasValue = !!params.noSwipingClass && params.noSwipingClass !== '';\n  // eslint-disable-next-line\n  const eventPath = event.composedPath ? event.composedPath() : event.path;\n  if (swipingClassHasValue && e.target && e.target.shadowRoot && eventPath) {\n    targetEl = eventPath[0];\n  }\n  const noSwipingSelector = params.noSwipingSelector ? params.noSwipingSelector : `.${params.noSwipingClass}`;\n  const isTargetShadow = !!(e.target && e.target.shadowRoot);\n\n  // use closestElement for shadow root element to get the actual closest for nested shadow root element\n  if (params.noSwiping && (isTargetShadow ? closestElement(noSwipingSelector, targetEl) : targetEl.closest(noSwipingSelector))) {\n    swiper.allowClick = true;\n    return;\n  }\n  if (params.swipeHandler) {\n    if (!targetEl.closest(params.swipeHandler)) return;\n  }\n  touches.currentX = e.pageX;\n  touches.currentY = e.pageY;\n  const startX = touches.currentX;\n  const startY = touches.currentY;\n\n  // Do NOT start if iOS edge swipe is detected. Otherwise iOS app cannot swipe-to-go-back anymore\n\n  const edgeSwipeDetection = params.edgeSwipeDetection || params.iOSEdgeSwipeDetection;\n  const edgeSwipeThreshold = params.edgeSwipeThreshold || params.iOSEdgeSwipeThreshold;\n  if (edgeSwipeDetection && (startX <= edgeSwipeThreshold || startX >= window.innerWidth - edgeSwipeThreshold)) {\n    if (edgeSwipeDetection === 'prevent') {\n      event.preventDefault();\n    } else {\n      return;\n    }\n  }\n  Object.assign(data, {\n    isTouched: true,\n    isMoved: false,\n    allowTouchCallbacks: true,\n    isScrolling: undefined,\n    startMoving: undefined\n  });\n  touches.startX = startX;\n  touches.startY = startY;\n  data.touchStartTime = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.d)();\n  swiper.allowClick = true;\n  swiper.updateSize();\n  swiper.swipeDirection = undefined;\n  if (params.threshold > 0) data.allowThresholdMove = false;\n  let preventDefault = true;\n  if (targetEl.matches(data.focusableElements)) {\n    preventDefault = false;\n    if (targetEl.nodeName === 'SELECT') {\n      data.isTouched = false;\n    }\n  }\n  if (document.activeElement && document.activeElement.matches(data.focusableElements) && document.activeElement !== targetEl) {\n    document.activeElement.blur();\n  }\n  const shouldPreventDefault = preventDefault && swiper.allowTouchMove && params.touchStartPreventDefault;\n  if ((params.touchStartForcePreventDefault || shouldPreventDefault) && !targetEl.isContentEditable) {\n    e.preventDefault();\n  }\n  if (params.freeMode && params.freeMode.enabled && swiper.freeMode && swiper.animating && !params.cssMode) {\n    swiper.freeMode.onTouchStart();\n  }\n  swiper.emit('touchStart', e);\n}\n\nfunction onTouchMove(event) {\n  const document = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();\n  const swiper = this;\n  const data = swiper.touchEventsData;\n  const {\n    params,\n    touches,\n    rtlTranslate: rtl,\n    enabled\n  } = swiper;\n  if (!enabled) return;\n  if (!params.simulateTouch && event.pointerType === 'mouse') return;\n  let e = event;\n  if (e.originalEvent) e = e.originalEvent;\n  if (!data.isTouched) {\n    if (data.startMoving && data.isScrolling) {\n      swiper.emit('touchMoveOpposite', e);\n    }\n    return;\n  }\n  const pointerIndex = data.evCache.findIndex(cachedEv => cachedEv.pointerId === e.pointerId);\n  if (pointerIndex >= 0) data.evCache[pointerIndex] = e;\n  const targetTouch = data.evCache.length > 1 ? data.evCache[0] : e;\n  const pageX = targetTouch.pageX;\n  const pageY = targetTouch.pageY;\n  if (e.preventedByNestedSwiper) {\n    touches.startX = pageX;\n    touches.startY = pageY;\n    return;\n  }\n  if (!swiper.allowTouchMove) {\n    if (!e.target.matches(data.focusableElements)) {\n      swiper.allowClick = false;\n    }\n    if (data.isTouched) {\n      Object.assign(touches, {\n        startX: pageX,\n        startY: pageY,\n        prevX: swiper.touches.currentX,\n        prevY: swiper.touches.currentY,\n        currentX: pageX,\n        currentY: pageY\n      });\n      data.touchStartTime = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.d)();\n    }\n    return;\n  }\n  if (params.touchReleaseOnEdges && !params.loop) {\n    if (swiper.isVertical()) {\n      // Vertical\n      if (pageY < touches.startY && swiper.translate <= swiper.maxTranslate() || pageY > touches.startY && swiper.translate >= swiper.minTranslate()) {\n        data.isTouched = false;\n        data.isMoved = false;\n        return;\n      }\n    } else if (pageX < touches.startX && swiper.translate <= swiper.maxTranslate() || pageX > touches.startX && swiper.translate >= swiper.minTranslate()) {\n      return;\n    }\n  }\n  if (document.activeElement) {\n    if (e.target === document.activeElement && e.target.matches(data.focusableElements)) {\n      data.isMoved = true;\n      swiper.allowClick = false;\n      return;\n    }\n  }\n  if (data.allowTouchCallbacks) {\n    swiper.emit('touchMove', e);\n  }\n  if (e.targetTouches && e.targetTouches.length > 1) return;\n  touches.currentX = pageX;\n  touches.currentY = pageY;\n  const diffX = touches.currentX - touches.startX;\n  const diffY = touches.currentY - touches.startY;\n  if (swiper.params.threshold && Math.sqrt(diffX ** 2 + diffY ** 2) < swiper.params.threshold) return;\n  if (typeof data.isScrolling === 'undefined') {\n    let touchAngle;\n    if (swiper.isHorizontal() && touches.currentY === touches.startY || swiper.isVertical() && touches.currentX === touches.startX) {\n      data.isScrolling = false;\n    } else {\n      // eslint-disable-next-line\n      if (diffX * diffX + diffY * diffY >= 25) {\n        touchAngle = Math.atan2(Math.abs(diffY), Math.abs(diffX)) * 180 / Math.PI;\n        data.isScrolling = swiper.isHorizontal() ? touchAngle > params.touchAngle : 90 - touchAngle > params.touchAngle;\n      }\n    }\n  }\n  if (data.isScrolling) {\n    swiper.emit('touchMoveOpposite', e);\n  }\n  if (typeof data.startMoving === 'undefined') {\n    if (touches.currentX !== touches.startX || touches.currentY !== touches.startY) {\n      data.startMoving = true;\n    }\n  }\n  if (data.isScrolling || swiper.zoom && swiper.params.zoom && swiper.params.zoom.enabled && data.evCache.length > 1) {\n    data.isTouched = false;\n    return;\n  }\n  if (!data.startMoving) {\n    return;\n  }\n  swiper.allowClick = false;\n  if (!params.cssMode && e.cancelable) {\n    e.preventDefault();\n  }\n  if (params.touchMoveStopPropagation && !params.nested) {\n    e.stopPropagation();\n  }\n  let diff = swiper.isHorizontal() ? diffX : diffY;\n  let touchesDiff = swiper.isHorizontal() ? touches.currentX - touches.previousX : touches.currentY - touches.previousY;\n  if (params.oneWayMovement) {\n    diff = Math.abs(diff) * (rtl ? 1 : -1);\n    touchesDiff = Math.abs(touchesDiff) * (rtl ? 1 : -1);\n  }\n  touches.diff = diff;\n  diff *= params.touchRatio;\n  if (rtl) {\n    diff = -diff;\n    touchesDiff = -touchesDiff;\n  }\n  const prevTouchesDirection = swiper.touchesDirection;\n  swiper.swipeDirection = diff > 0 ? 'prev' : 'next';\n  swiper.touchesDirection = touchesDiff > 0 ? 'prev' : 'next';\n  const isLoop = swiper.params.loop && !params.cssMode;\n  if (!data.isMoved) {\n    if (isLoop) {\n      swiper.loopFix({\n        direction: swiper.swipeDirection\n      });\n    }\n    data.startTranslate = swiper.getTranslate();\n    swiper.setTransition(0);\n    if (swiper.animating) {\n      const evt = new window.CustomEvent('transitionend', {\n        bubbles: true,\n        cancelable: true\n      });\n      swiper.wrapperEl.dispatchEvent(evt);\n    }\n    data.allowMomentumBounce = false;\n    // Grab Cursor\n    if (params.grabCursor && (swiper.allowSlideNext === true || swiper.allowSlidePrev === true)) {\n      swiper.setGrabCursor(true);\n    }\n    swiper.emit('sliderFirstMove', e);\n  }\n  let loopFixed;\n  if (data.isMoved && prevTouchesDirection !== swiper.touchesDirection && isLoop && Math.abs(diff) >= 1) {\n    // need another loop fix\n    swiper.loopFix({\n      direction: swiper.swipeDirection,\n      setTranslate: true\n    });\n    loopFixed = true;\n  }\n  swiper.emit('sliderMove', e);\n  data.isMoved = true;\n  data.currentTranslate = diff + data.startTranslate;\n  let disableParentSwiper = true;\n  let resistanceRatio = params.resistanceRatio;\n  if (params.touchReleaseOnEdges) {\n    resistanceRatio = 0;\n  }\n  if (diff > 0) {\n    if (isLoop && !loopFixed && data.currentTranslate > (params.centeredSlides ? swiper.minTranslate() - swiper.size / 2 : swiper.minTranslate())) {\n      swiper.loopFix({\n        direction: 'prev',\n        setTranslate: true,\n        activeSlideIndex: 0\n      });\n    }\n    if (data.currentTranslate > swiper.minTranslate()) {\n      disableParentSwiper = false;\n      if (params.resistance) {\n        data.currentTranslate = swiper.minTranslate() - 1 + (-swiper.minTranslate() + data.startTranslate + diff) ** resistanceRatio;\n      }\n    }\n  } else if (diff < 0) {\n    if (isLoop && !loopFixed && data.currentTranslate < (params.centeredSlides ? swiper.maxTranslate() + swiper.size / 2 : swiper.maxTranslate())) {\n      swiper.loopFix({\n        direction: 'next',\n        setTranslate: true,\n        activeSlideIndex: swiper.slides.length - (params.slidesPerView === 'auto' ? swiper.slidesPerViewDynamic() : Math.ceil(parseFloat(params.slidesPerView, 10)))\n      });\n    }\n    if (data.currentTranslate < swiper.maxTranslate()) {\n      disableParentSwiper = false;\n      if (params.resistance) {\n        data.currentTranslate = swiper.maxTranslate() + 1 - (swiper.maxTranslate() - data.startTranslate - diff) ** resistanceRatio;\n      }\n    }\n  }\n  if (disableParentSwiper) {\n    e.preventedByNestedSwiper = true;\n  }\n\n  // Directions locks\n  if (!swiper.allowSlideNext && swiper.swipeDirection === 'next' && data.currentTranslate < data.startTranslate) {\n    data.currentTranslate = data.startTranslate;\n  }\n  if (!swiper.allowSlidePrev && swiper.swipeDirection === 'prev' && data.currentTranslate > data.startTranslate) {\n    data.currentTranslate = data.startTranslate;\n  }\n  if (!swiper.allowSlidePrev && !swiper.allowSlideNext) {\n    data.currentTranslate = data.startTranslate;\n  }\n\n  // Threshold\n  if (params.threshold > 0) {\n    if (Math.abs(diff) > params.threshold || data.allowThresholdMove) {\n      if (!data.allowThresholdMove) {\n        data.allowThresholdMove = true;\n        touches.startX = touches.currentX;\n        touches.startY = touches.currentY;\n        data.currentTranslate = data.startTranslate;\n        touches.diff = swiper.isHorizontal() ? touches.currentX - touches.startX : touches.currentY - touches.startY;\n        return;\n      }\n    } else {\n      data.currentTranslate = data.startTranslate;\n      return;\n    }\n  }\n  if (!params.followFinger || params.cssMode) return;\n\n  // Update active index in free mode\n  if (params.freeMode && params.freeMode.enabled && swiper.freeMode || params.watchSlidesProgress) {\n    swiper.updateActiveIndex();\n    swiper.updateSlidesClasses();\n  }\n  if (params.freeMode && params.freeMode.enabled && swiper.freeMode) {\n    swiper.freeMode.onTouchMove();\n  }\n  // Update progress\n  swiper.updateProgress(data.currentTranslate);\n  // Update translate\n  swiper.setTranslate(data.currentTranslate);\n}\n\nfunction onTouchEnd(event) {\n  const swiper = this;\n  const data = swiper.touchEventsData;\n  const pointerIndex = data.evCache.findIndex(cachedEv => cachedEv.pointerId === event.pointerId);\n  if (pointerIndex >= 0) {\n    data.evCache.splice(pointerIndex, 1);\n  }\n  if (['pointercancel', 'pointerout', 'pointerleave'].includes(event.type)) {\n    const proceed = event.type === 'pointercancel' && (swiper.browser.isSafari || swiper.browser.isWebView);\n    if (!proceed) {\n      return;\n    }\n  }\n  const {\n    params,\n    touches,\n    rtlTranslate: rtl,\n    slidesGrid,\n    enabled\n  } = swiper;\n  if (!enabled) return;\n  if (!params.simulateTouch && event.pointerType === 'mouse') return;\n  let e = event;\n  if (e.originalEvent) e = e.originalEvent;\n  if (data.allowTouchCallbacks) {\n    swiper.emit('touchEnd', e);\n  }\n  data.allowTouchCallbacks = false;\n  if (!data.isTouched) {\n    if (data.isMoved && params.grabCursor) {\n      swiper.setGrabCursor(false);\n    }\n    data.isMoved = false;\n    data.startMoving = false;\n    return;\n  }\n  // Return Grab Cursor\n  if (params.grabCursor && data.isMoved && data.isTouched && (swiper.allowSlideNext === true || swiper.allowSlidePrev === true)) {\n    swiper.setGrabCursor(false);\n  }\n\n  // Time diff\n  const touchEndTime = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.d)();\n  const timeDiff = touchEndTime - data.touchStartTime;\n\n  // Tap, doubleTap, Click\n  if (swiper.allowClick) {\n    const pathTree = e.path || e.composedPath && e.composedPath();\n    swiper.updateClickedSlide(pathTree && pathTree[0] || e.target);\n    swiper.emit('tap click', e);\n    if (timeDiff < 300 && touchEndTime - data.lastClickTime < 300) {\n      swiper.emit('doubleTap doubleClick', e);\n    }\n  }\n  data.lastClickTime = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.d)();\n  (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.n)(() => {\n    if (!swiper.destroyed) swiper.allowClick = true;\n  });\n  if (!data.isTouched || !data.isMoved || !swiper.swipeDirection || touches.diff === 0 || data.currentTranslate === data.startTranslate) {\n    data.isTouched = false;\n    data.isMoved = false;\n    data.startMoving = false;\n    return;\n  }\n  data.isTouched = false;\n  data.isMoved = false;\n  data.startMoving = false;\n  let currentPos;\n  if (params.followFinger) {\n    currentPos = rtl ? swiper.translate : -swiper.translate;\n  } else {\n    currentPos = -data.currentTranslate;\n  }\n  if (params.cssMode) {\n    return;\n  }\n  if (params.freeMode && params.freeMode.enabled) {\n    swiper.freeMode.onTouchEnd({\n      currentPos\n    });\n    return;\n  }\n\n  // Find current slide\n  let stopIndex = 0;\n  let groupSize = swiper.slidesSizesGrid[0];\n  for (let i = 0; i < slidesGrid.length; i += i < params.slidesPerGroupSkip ? 1 : params.slidesPerGroup) {\n    const increment = i < params.slidesPerGroupSkip - 1 ? 1 : params.slidesPerGroup;\n    if (typeof slidesGrid[i + increment] !== 'undefined') {\n      if (currentPos >= slidesGrid[i] && currentPos < slidesGrid[i + increment]) {\n        stopIndex = i;\n        groupSize = slidesGrid[i + increment] - slidesGrid[i];\n      }\n    } else if (currentPos >= slidesGrid[i]) {\n      stopIndex = i;\n      groupSize = slidesGrid[slidesGrid.length - 1] - slidesGrid[slidesGrid.length - 2];\n    }\n  }\n  let rewindFirstIndex = null;\n  let rewindLastIndex = null;\n  if (params.rewind) {\n    if (swiper.isBeginning) {\n      rewindLastIndex = params.virtual && params.virtual.enabled && swiper.virtual ? swiper.virtual.slides.length - 1 : swiper.slides.length - 1;\n    } else if (swiper.isEnd) {\n      rewindFirstIndex = 0;\n    }\n  }\n  // Find current slide size\n  const ratio = (currentPos - slidesGrid[stopIndex]) / groupSize;\n  const increment = stopIndex < params.slidesPerGroupSkip - 1 ? 1 : params.slidesPerGroup;\n  if (timeDiff > params.longSwipesMs) {\n    // Long touches\n    if (!params.longSwipes) {\n      swiper.slideTo(swiper.activeIndex);\n      return;\n    }\n    if (swiper.swipeDirection === 'next') {\n      if (ratio >= params.longSwipesRatio) swiper.slideTo(params.rewind && swiper.isEnd ? rewindFirstIndex : stopIndex + increment);else swiper.slideTo(stopIndex);\n    }\n    if (swiper.swipeDirection === 'prev') {\n      if (ratio > 1 - params.longSwipesRatio) {\n        swiper.slideTo(stopIndex + increment);\n      } else if (rewindLastIndex !== null && ratio < 0 && Math.abs(ratio) > params.longSwipesRatio) {\n        swiper.slideTo(rewindLastIndex);\n      } else {\n        swiper.slideTo(stopIndex);\n      }\n    }\n  } else {\n    // Short swipes\n    if (!params.shortSwipes) {\n      swiper.slideTo(swiper.activeIndex);\n      return;\n    }\n    const isNavButtonTarget = swiper.navigation && (e.target === swiper.navigation.nextEl || e.target === swiper.navigation.prevEl);\n    if (!isNavButtonTarget) {\n      if (swiper.swipeDirection === 'next') {\n        swiper.slideTo(rewindFirstIndex !== null ? rewindFirstIndex : stopIndex + increment);\n      }\n      if (swiper.swipeDirection === 'prev') {\n        swiper.slideTo(rewindLastIndex !== null ? rewindLastIndex : stopIndex);\n      }\n    } else if (e.target === swiper.navigation.nextEl) {\n      swiper.slideTo(stopIndex + increment);\n    } else {\n      swiper.slideTo(stopIndex);\n    }\n  }\n}\n\nfunction onResize() {\n  const swiper = this;\n  const {\n    params,\n    el\n  } = swiper;\n  if (el && el.offsetWidth === 0) return;\n\n  // Breakpoints\n  if (params.breakpoints) {\n    swiper.setBreakpoint();\n  }\n\n  // Save locks\n  const {\n    allowSlideNext,\n    allowSlidePrev,\n    snapGrid\n  } = swiper;\n  const isVirtual = swiper.virtual && swiper.params.virtual.enabled;\n\n  // Disable locks on resize\n  swiper.allowSlideNext = true;\n  swiper.allowSlidePrev = true;\n  swiper.updateSize();\n  swiper.updateSlides();\n  swiper.updateSlidesClasses();\n  const isVirtualLoop = isVirtual && params.loop;\n  if ((params.slidesPerView === 'auto' || params.slidesPerView > 1) && swiper.isEnd && !swiper.isBeginning && !swiper.params.centeredSlides && !isVirtualLoop) {\n    swiper.slideTo(swiper.slides.length - 1, 0, false, true);\n  } else {\n    if (swiper.params.loop && !isVirtual) {\n      swiper.slideToLoop(swiper.realIndex, 0, false, true);\n    } else {\n      swiper.slideTo(swiper.activeIndex, 0, false, true);\n    }\n  }\n  if (swiper.autoplay && swiper.autoplay.running && swiper.autoplay.paused) {\n    clearTimeout(swiper.autoplay.resizeTimeout);\n    swiper.autoplay.resizeTimeout = setTimeout(() => {\n      if (swiper.autoplay && swiper.autoplay.running && swiper.autoplay.paused) {\n        swiper.autoplay.resume();\n      }\n    }, 500);\n  }\n  // Return locks after resize\n  swiper.allowSlidePrev = allowSlidePrev;\n  swiper.allowSlideNext = allowSlideNext;\n  if (swiper.params.watchOverflow && snapGrid !== swiper.snapGrid) {\n    swiper.checkOverflow();\n  }\n}\n\nfunction onClick(e) {\n  const swiper = this;\n  if (!swiper.enabled) return;\n  if (!swiper.allowClick) {\n    if (swiper.params.preventClicks) e.preventDefault();\n    if (swiper.params.preventClicksPropagation && swiper.animating) {\n      e.stopPropagation();\n      e.stopImmediatePropagation();\n    }\n  }\n}\n\nfunction onScroll() {\n  const swiper = this;\n  const {\n    wrapperEl,\n    rtlTranslate,\n    enabled\n  } = swiper;\n  if (!enabled) return;\n  swiper.previousTranslate = swiper.translate;\n  if (swiper.isHorizontal()) {\n    swiper.translate = -wrapperEl.scrollLeft;\n  } else {\n    swiper.translate = -wrapperEl.scrollTop;\n  }\n  // eslint-disable-next-line\n  if (swiper.translate === 0) swiper.translate = 0;\n  swiper.updateActiveIndex();\n  swiper.updateSlidesClasses();\n  let newProgress;\n  const translatesDiff = swiper.maxTranslate() - swiper.minTranslate();\n  if (translatesDiff === 0) {\n    newProgress = 0;\n  } else {\n    newProgress = (swiper.translate - swiper.minTranslate()) / translatesDiff;\n  }\n  if (newProgress !== swiper.progress) {\n    swiper.updateProgress(rtlTranslate ? -swiper.translate : swiper.translate);\n  }\n  swiper.emit('setTranslate', swiper.translate, false);\n}\n\nfunction onLoad(e) {\n  const swiper = this;\n  processLazyPreloader(swiper, e.target);\n  if (swiper.params.cssMode || swiper.params.slidesPerView !== 'auto' && !swiper.params.autoHeight) {\n    return;\n  }\n  swiper.update();\n}\n\nlet dummyEventAttached = false;\nfunction dummyEventListener() {}\nconst events = (swiper, method) => {\n  const document = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();\n  const {\n    params,\n    el,\n    wrapperEl,\n    device\n  } = swiper;\n  const capture = !!params.nested;\n  const domMethod = method === 'on' ? 'addEventListener' : 'removeEventListener';\n  const swiperMethod = method;\n\n  // Touch Events\n  el[domMethod]('pointerdown', swiper.onTouchStart, {\n    passive: false\n  });\n  document[domMethod]('pointermove', swiper.onTouchMove, {\n    passive: false,\n    capture\n  });\n  document[domMethod]('pointerup', swiper.onTouchEnd, {\n    passive: true\n  });\n  document[domMethod]('pointercancel', swiper.onTouchEnd, {\n    passive: true\n  });\n  document[domMethod]('pointerout', swiper.onTouchEnd, {\n    passive: true\n  });\n  document[domMethod]('pointerleave', swiper.onTouchEnd, {\n    passive: true\n  });\n\n  // Prevent Links Clicks\n  if (params.preventClicks || params.preventClicksPropagation) {\n    el[domMethod]('click', swiper.onClick, true);\n  }\n  if (params.cssMode) {\n    wrapperEl[domMethod]('scroll', swiper.onScroll);\n  }\n\n  // Resize handler\n  if (params.updateOnWindowResize) {\n    swiper[swiperMethod](device.ios || device.android ? 'resize orientationchange observerUpdate' : 'resize observerUpdate', onResize, true);\n  } else {\n    swiper[swiperMethod]('observerUpdate', onResize, true);\n  }\n\n  // Images loader\n  el[domMethod]('load', swiper.onLoad, {\n    capture: true\n  });\n};\nfunction attachEvents() {\n  const swiper = this;\n  const document = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();\n  const {\n    params\n  } = swiper;\n  swiper.onTouchStart = onTouchStart.bind(swiper);\n  swiper.onTouchMove = onTouchMove.bind(swiper);\n  swiper.onTouchEnd = onTouchEnd.bind(swiper);\n  if (params.cssMode) {\n    swiper.onScroll = onScroll.bind(swiper);\n  }\n  swiper.onClick = onClick.bind(swiper);\n  swiper.onLoad = onLoad.bind(swiper);\n  if (!dummyEventAttached) {\n    document.addEventListener('touchstart', dummyEventListener);\n    dummyEventAttached = true;\n  }\n  events(swiper, 'on');\n}\nfunction detachEvents() {\n  const swiper = this;\n  events(swiper, 'off');\n}\nvar events$1 = {\n  attachEvents,\n  detachEvents\n};\n\nconst isGridEnabled = (swiper, params) => {\n  return swiper.grid && params.grid && params.grid.rows > 1;\n};\nfunction setBreakpoint() {\n  const swiper = this;\n  const {\n    realIndex,\n    initialized,\n    params,\n    el\n  } = swiper;\n  const breakpoints = params.breakpoints;\n  if (!breakpoints || breakpoints && Object.keys(breakpoints).length === 0) return;\n\n  // Get breakpoint for window width and update parameters\n  const breakpoint = swiper.getBreakpoint(breakpoints, swiper.params.breakpointsBase, swiper.el);\n  if (!breakpoint || swiper.currentBreakpoint === breakpoint) return;\n  const breakpointOnlyParams = breakpoint in breakpoints ? breakpoints[breakpoint] : undefined;\n  const breakpointParams = breakpointOnlyParams || swiper.originalParams;\n  const wasMultiRow = isGridEnabled(swiper, params);\n  const isMultiRow = isGridEnabled(swiper, breakpointParams);\n  const wasEnabled = params.enabled;\n  if (wasMultiRow && !isMultiRow) {\n    el.classList.remove(`${params.containerModifierClass}grid`, `${params.containerModifierClass}grid-column`);\n    swiper.emitContainerClasses();\n  } else if (!wasMultiRow && isMultiRow) {\n    el.classList.add(`${params.containerModifierClass}grid`);\n    if (breakpointParams.grid.fill && breakpointParams.grid.fill === 'column' || !breakpointParams.grid.fill && params.grid.fill === 'column') {\n      el.classList.add(`${params.containerModifierClass}grid-column`);\n    }\n    swiper.emitContainerClasses();\n  }\n\n  // Toggle navigation, pagination, scrollbar\n  ['navigation', 'pagination', 'scrollbar'].forEach(prop => {\n    if (typeof breakpointParams[prop] === 'undefined') return;\n    const wasModuleEnabled = params[prop] && params[prop].enabled;\n    const isModuleEnabled = breakpointParams[prop] && breakpointParams[prop].enabled;\n    if (wasModuleEnabled && !isModuleEnabled) {\n      swiper[prop].disable();\n    }\n    if (!wasModuleEnabled && isModuleEnabled) {\n      swiper[prop].enable();\n    }\n  });\n  const directionChanged = breakpointParams.direction && breakpointParams.direction !== params.direction;\n  const needsReLoop = params.loop && (breakpointParams.slidesPerView !== params.slidesPerView || directionChanged);\n  if (directionChanged && initialized) {\n    swiper.changeDirection();\n  }\n  (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.q)(swiper.params, breakpointParams);\n  const isEnabled = swiper.params.enabled;\n  Object.assign(swiper, {\n    allowTouchMove: swiper.params.allowTouchMove,\n    allowSlideNext: swiper.params.allowSlideNext,\n    allowSlidePrev: swiper.params.allowSlidePrev\n  });\n  if (wasEnabled && !isEnabled) {\n    swiper.disable();\n  } else if (!wasEnabled && isEnabled) {\n    swiper.enable();\n  }\n  swiper.currentBreakpoint = breakpoint;\n  swiper.emit('_beforeBreakpoint', breakpointParams);\n  if (needsReLoop && initialized) {\n    swiper.loopDestroy();\n    swiper.loopCreate(realIndex);\n    swiper.updateSlides();\n  }\n  swiper.emit('breakpoint', breakpointParams);\n}\n\nfunction getBreakpoint(breakpoints, base, containerEl) {\n  if (base === void 0) {\n    base = 'window';\n  }\n  if (!breakpoints || base === 'container' && !containerEl) return undefined;\n  let breakpoint = false;\n  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();\n  const currentHeight = base === 'window' ? window.innerHeight : containerEl.clientHeight;\n  const points = Object.keys(breakpoints).map(point => {\n    if (typeof point === 'string' && point.indexOf('@') === 0) {\n      const minRatio = parseFloat(point.substr(1));\n      const value = currentHeight * minRatio;\n      return {\n        value,\n        point\n      };\n    }\n    return {\n      value: point,\n      point\n    };\n  });\n  points.sort((a, b) => parseInt(a.value, 10) - parseInt(b.value, 10));\n  for (let i = 0; i < points.length; i += 1) {\n    const {\n      point,\n      value\n    } = points[i];\n    if (base === 'window') {\n      if (window.matchMedia(`(min-width: ${value}px)`).matches) {\n        breakpoint = point;\n      }\n    } else if (value <= containerEl.clientWidth) {\n      breakpoint = point;\n    }\n  }\n  return breakpoint || 'max';\n}\n\nvar breakpoints = {\n  setBreakpoint,\n  getBreakpoint\n};\n\nfunction prepareClasses(entries, prefix) {\n  const resultClasses = [];\n  entries.forEach(item => {\n    if (typeof item === 'object') {\n      Object.keys(item).forEach(classNames => {\n        if (item[classNames]) {\n          resultClasses.push(prefix + classNames);\n        }\n      });\n    } else if (typeof item === 'string') {\n      resultClasses.push(prefix + item);\n    }\n  });\n  return resultClasses;\n}\nfunction addClasses() {\n  const swiper = this;\n  const {\n    classNames,\n    params,\n    rtl,\n    el,\n    device\n  } = swiper;\n  // prettier-ignore\n  const suffixes = prepareClasses(['initialized', params.direction, {\n    'free-mode': swiper.params.freeMode && params.freeMode.enabled\n  }, {\n    'autoheight': params.autoHeight\n  }, {\n    'rtl': rtl\n  }, {\n    'grid': params.grid && params.grid.rows > 1\n  }, {\n    'grid-column': params.grid && params.grid.rows > 1 && params.grid.fill === 'column'\n  }, {\n    'android': device.android\n  }, {\n    'ios': device.ios\n  }, {\n    'css-mode': params.cssMode\n  }, {\n    'centered': params.cssMode && params.centeredSlides\n  }, {\n    'watch-progress': params.watchSlidesProgress\n  }], params.containerModifierClass);\n  classNames.push(...suffixes);\n  el.classList.add(...classNames);\n  swiper.emitContainerClasses();\n}\n\nfunction removeClasses() {\n  const swiper = this;\n  const {\n    el,\n    classNames\n  } = swiper;\n  el.classList.remove(...classNames);\n  swiper.emitContainerClasses();\n}\n\nvar classes = {\n  addClasses,\n  removeClasses\n};\n\nfunction checkOverflow() {\n  const swiper = this;\n  const {\n    isLocked: wasLocked,\n    params\n  } = swiper;\n  const {\n    slidesOffsetBefore\n  } = params;\n  if (slidesOffsetBefore) {\n    const lastSlideIndex = swiper.slides.length - 1;\n    const lastSlideRightEdge = swiper.slidesGrid[lastSlideIndex] + swiper.slidesSizesGrid[lastSlideIndex] + slidesOffsetBefore * 2;\n    swiper.isLocked = swiper.size > lastSlideRightEdge;\n  } else {\n    swiper.isLocked = swiper.snapGrid.length === 1;\n  }\n  if (params.allowSlideNext === true) {\n    swiper.allowSlideNext = !swiper.isLocked;\n  }\n  if (params.allowSlidePrev === true) {\n    swiper.allowSlidePrev = !swiper.isLocked;\n  }\n  if (wasLocked && wasLocked !== swiper.isLocked) {\n    swiper.isEnd = false;\n  }\n  if (wasLocked !== swiper.isLocked) {\n    swiper.emit(swiper.isLocked ? 'lock' : 'unlock');\n  }\n}\nvar checkOverflow$1 = {\n  checkOverflow\n};\n\nvar defaults = {\n  init: true,\n  direction: 'horizontal',\n  oneWayMovement: false,\n  touchEventsTarget: 'wrapper',\n  initialSlide: 0,\n  speed: 300,\n  cssMode: false,\n  updateOnWindowResize: true,\n  resizeObserver: true,\n  nested: false,\n  createElements: false,\n  enabled: true,\n  focusableElements: 'input, select, option, textarea, button, video, label',\n  // Overrides\n  width: null,\n  height: null,\n  //\n  preventInteractionOnTransition: false,\n  // ssr\n  userAgent: null,\n  url: null,\n  // To support iOS's swipe-to-go-back gesture (when being used in-app).\n  edgeSwipeDetection: false,\n  edgeSwipeThreshold: 20,\n  // Autoheight\n  autoHeight: false,\n  // Set wrapper width\n  setWrapperSize: false,\n  // Virtual Translate\n  virtualTranslate: false,\n  // Effects\n  effect: 'slide',\n  // 'slide' or 'fade' or 'cube' or 'coverflow' or 'flip'\n\n  // Breakpoints\n  breakpoints: undefined,\n  breakpointsBase: 'window',\n  // Slides grid\n  spaceBetween: 0,\n  slidesPerView: 1,\n  slidesPerGroup: 1,\n  slidesPerGroupSkip: 0,\n  slidesPerGroupAuto: false,\n  centeredSlides: false,\n  centeredSlidesBounds: false,\n  slidesOffsetBefore: 0,\n  // in px\n  slidesOffsetAfter: 0,\n  // in px\n  normalizeSlideIndex: true,\n  centerInsufficientSlides: false,\n  // Disable swiper and hide navigation when container not overflow\n  watchOverflow: true,\n  // Round length\n  roundLengths: false,\n  // Touches\n  touchRatio: 1,\n  touchAngle: 45,\n  simulateTouch: true,\n  shortSwipes: true,\n  longSwipes: true,\n  longSwipesRatio: 0.5,\n  longSwipesMs: 300,\n  followFinger: true,\n  allowTouchMove: true,\n  threshold: 5,\n  touchMoveStopPropagation: false,\n  touchStartPreventDefault: true,\n  touchStartForcePreventDefault: false,\n  touchReleaseOnEdges: false,\n  // Unique Navigation Elements\n  uniqueNavElements: true,\n  // Resistance\n  resistance: true,\n  resistanceRatio: 0.85,\n  // Progress\n  watchSlidesProgress: false,\n  // Cursor\n  grabCursor: false,\n  // Clicks\n  preventClicks: true,\n  preventClicksPropagation: true,\n  slideToClickedSlide: false,\n  // loop\n  loop: false,\n  loopedSlides: null,\n  loopPreventsSliding: true,\n  // rewind\n  rewind: false,\n  // Swiping/no swiping\n  allowSlidePrev: true,\n  allowSlideNext: true,\n  swipeHandler: null,\n  // '.swipe-handler',\n  noSwiping: true,\n  noSwipingClass: 'swiper-no-swiping',\n  noSwipingSelector: null,\n  // Passive Listeners\n  passiveListeners: true,\n  maxBackfaceHiddenSlides: 10,\n  // NS\n  containerModifierClass: 'swiper-',\n  // NEW\n  slideClass: 'swiper-slide',\n  slideActiveClass: 'swiper-slide-active',\n  slideVisibleClass: 'swiper-slide-visible',\n  slideNextClass: 'swiper-slide-next',\n  slidePrevClass: 'swiper-slide-prev',\n  wrapperClass: 'swiper-wrapper',\n  lazyPreloaderClass: 'swiper-lazy-preloader',\n  lazyPreloadPrevNext: 0,\n  // Callbacks\n  runCallbacksOnInit: true,\n  // Internals\n  _emitClasses: false\n};\n\nfunction moduleExtendParams(params, allModulesParams) {\n  return function extendParams(obj) {\n    if (obj === void 0) {\n      obj = {};\n    }\n    const moduleParamName = Object.keys(obj)[0];\n    const moduleParams = obj[moduleParamName];\n    if (typeof moduleParams !== 'object' || moduleParams === null) {\n      (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.q)(allModulesParams, obj);\n      return;\n    }\n    if (['navigation', 'pagination', 'scrollbar'].indexOf(moduleParamName) >= 0 && params[moduleParamName] === true) {\n      params[moduleParamName] = {\n        auto: true\n      };\n    }\n    if (!(moduleParamName in params && 'enabled' in moduleParams)) {\n      (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.q)(allModulesParams, obj);\n      return;\n    }\n    if (params[moduleParamName] === true) {\n      params[moduleParamName] = {\n        enabled: true\n      };\n    }\n    if (typeof params[moduleParamName] === 'object' && !('enabled' in params[moduleParamName])) {\n      params[moduleParamName].enabled = true;\n    }\n    if (!params[moduleParamName]) params[moduleParamName] = {\n      enabled: false\n    };\n    (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.q)(allModulesParams, obj);\n  };\n}\n\n/* eslint no-param-reassign: \"off\" */\nconst prototypes = {\n  eventsEmitter,\n  update,\n  translate,\n  transition,\n  slide,\n  loop,\n  grabCursor,\n  events: events$1,\n  breakpoints,\n  checkOverflow: checkOverflow$1,\n  classes\n};\nconst extendedDefaults = {};\nclass Swiper {\n  constructor() {\n    let el;\n    let params;\n    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {\n      args[_key] = arguments[_key];\n    }\n    if (args.length === 1 && args[0].constructor && Object.prototype.toString.call(args[0]).slice(8, -1) === 'Object') {\n      params = args[0];\n    } else {\n      [el, params] = args;\n    }\n    if (!params) params = {};\n    params = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.q)({}, params);\n    if (el && !params.el) params.el = el;\n    const document = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();\n    if (params.el && typeof params.el === 'string' && document.querySelectorAll(params.el).length > 1) {\n      const swipers = [];\n      document.querySelectorAll(params.el).forEach(containerEl => {\n        const newParams = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.q)({}, params, {\n          el: containerEl\n        });\n        swipers.push(new Swiper(newParams));\n      });\n      // eslint-disable-next-line no-constructor-return\n      return swipers;\n    }\n\n    // Swiper Instance\n    const swiper = this;\n    swiper.__swiper__ = true;\n    swiper.support = getSupport();\n    swiper.device = getDevice({\n      userAgent: params.userAgent\n    });\n    swiper.browser = getBrowser();\n    swiper.eventsListeners = {};\n    swiper.eventsAnyListeners = [];\n    swiper.modules = [...swiper.__modules__];\n    if (params.modules && Array.isArray(params.modules)) {\n      swiper.modules.push(...params.modules);\n    }\n    const allModulesParams = {};\n    swiper.modules.forEach(mod => {\n      mod({\n        params,\n        swiper,\n        extendParams: moduleExtendParams(params, allModulesParams),\n        on: swiper.on.bind(swiper),\n        once: swiper.once.bind(swiper),\n        off: swiper.off.bind(swiper),\n        emit: swiper.emit.bind(swiper)\n      });\n    });\n\n    // Extend defaults with modules params\n    const swiperParams = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.q)({}, defaults, allModulesParams);\n\n    // Extend defaults with passed params\n    swiper.params = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.q)({}, swiperParams, extendedDefaults, params);\n    swiper.originalParams = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.q)({}, swiper.params);\n    swiper.passedParams = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.q)({}, params);\n\n    // add event listeners\n    if (swiper.params && swiper.params.on) {\n      Object.keys(swiper.params.on).forEach(eventName => {\n        swiper.on(eventName, swiper.params.on[eventName]);\n      });\n    }\n    if (swiper.params && swiper.params.onAny) {\n      swiper.onAny(swiper.params.onAny);\n    }\n\n    // Extend Swiper\n    Object.assign(swiper, {\n      enabled: swiper.params.enabled,\n      el,\n      // Classes\n      classNames: [],\n      // Slides\n      slides: [],\n      slidesGrid: [],\n      snapGrid: [],\n      slidesSizesGrid: [],\n      // isDirection\n      isHorizontal() {\n        return swiper.params.direction === 'horizontal';\n      },\n      isVertical() {\n        return swiper.params.direction === 'vertical';\n      },\n      // Indexes\n      activeIndex: 0,\n      realIndex: 0,\n      //\n      isBeginning: true,\n      isEnd: false,\n      // Props\n      translate: 0,\n      previousTranslate: 0,\n      progress: 0,\n      velocity: 0,\n      animating: false,\n      cssOverflowAdjustment() {\n        // Returns 0 unless `translate` is > 2**23\n        // Should be subtracted from css values to prevent overflow\n        return Math.trunc(this.translate / 2 ** 23) * 2 ** 23;\n      },\n      // Locks\n      allowSlideNext: swiper.params.allowSlideNext,\n      allowSlidePrev: swiper.params.allowSlidePrev,\n      // Touch Events\n      touchEventsData: {\n        isTouched: undefined,\n        isMoved: undefined,\n        allowTouchCallbacks: undefined,\n        touchStartTime: undefined,\n        isScrolling: undefined,\n        currentTranslate: undefined,\n        startTranslate: undefined,\n        allowThresholdMove: undefined,\n        // Form elements to match\n        focusableElements: swiper.params.focusableElements,\n        // Last click time\n        lastClickTime: 0,\n        clickTimeout: undefined,\n        // Velocities\n        velocities: [],\n        allowMomentumBounce: undefined,\n        startMoving: undefined,\n        evCache: []\n      },\n      // Clicks\n      allowClick: true,\n      // Touches\n      allowTouchMove: swiper.params.allowTouchMove,\n      touches: {\n        startX: 0,\n        startY: 0,\n        currentX: 0,\n        currentY: 0,\n        diff: 0\n      },\n      // Images\n      imagesToLoad: [],\n      imagesLoaded: 0\n    });\n    swiper.emit('_swiper');\n\n    // Init\n    if (swiper.params.init) {\n      swiper.init();\n    }\n\n    // Return app instance\n    // eslint-disable-next-line no-constructor-return\n    return swiper;\n  }\n  getSlideIndex(slideEl) {\n    const {\n      slidesEl,\n      params\n    } = this;\n    const slides = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(slidesEl, `.${params.slideClass}, swiper-slide`);\n    const firstSlideIndex = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.g)(slides[0]);\n    return (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.g)(slideEl) - firstSlideIndex;\n  }\n  getSlideIndexByData(index) {\n    return this.getSlideIndex(this.slides.filter(slideEl => slideEl.getAttribute('data-swiper-slide-index') * 1 === index)[0]);\n  }\n  recalcSlides() {\n    const swiper = this;\n    const {\n      slidesEl,\n      params\n    } = swiper;\n    swiper.slides = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(slidesEl, `.${params.slideClass}, swiper-slide`);\n  }\n  enable() {\n    const swiper = this;\n    if (swiper.enabled) return;\n    swiper.enabled = true;\n    if (swiper.params.grabCursor) {\n      swiper.setGrabCursor();\n    }\n    swiper.emit('enable');\n  }\n  disable() {\n    const swiper = this;\n    if (!swiper.enabled) return;\n    swiper.enabled = false;\n    if (swiper.params.grabCursor) {\n      swiper.unsetGrabCursor();\n    }\n    swiper.emit('disable');\n  }\n  setProgress(progress, speed) {\n    const swiper = this;\n    progress = Math.min(Math.max(progress, 0), 1);\n    const min = swiper.minTranslate();\n    const max = swiper.maxTranslate();\n    const current = (max - min) * progress + min;\n    swiper.translateTo(current, typeof speed === 'undefined' ? 0 : speed);\n    swiper.updateActiveIndex();\n    swiper.updateSlidesClasses();\n  }\n  emitContainerClasses() {\n    const swiper = this;\n    if (!swiper.params._emitClasses || !swiper.el) return;\n    const cls = swiper.el.className.split(' ').filter(className => {\n      return className.indexOf('swiper') === 0 || className.indexOf(swiper.params.containerModifierClass) === 0;\n    });\n    swiper.emit('_containerClasses', cls.join(' '));\n  }\n  getSlideClasses(slideEl) {\n    const swiper = this;\n    if (swiper.destroyed) return '';\n    return slideEl.className.split(' ').filter(className => {\n      return className.indexOf('swiper-slide') === 0 || className.indexOf(swiper.params.slideClass) === 0;\n    }).join(' ');\n  }\n  emitSlidesClasses() {\n    const swiper = this;\n    if (!swiper.params._emitClasses || !swiper.el) return;\n    const updates = [];\n    swiper.slides.forEach(slideEl => {\n      const classNames = swiper.getSlideClasses(slideEl);\n      updates.push({\n        slideEl,\n        classNames\n      });\n      swiper.emit('_slideClass', slideEl, classNames);\n    });\n    swiper.emit('_slideClasses', updates);\n  }\n  slidesPerViewDynamic(view, exact) {\n    if (view === void 0) {\n      view = 'current';\n    }\n    if (exact === void 0) {\n      exact = false;\n    }\n    const swiper = this;\n    const {\n      params,\n      slides,\n      slidesGrid,\n      slidesSizesGrid,\n      size: swiperSize,\n      activeIndex\n    } = swiper;\n    let spv = 1;\n    if (params.centeredSlides) {\n      let slideSize = slides[activeIndex] ? slides[activeIndex].swiperSlideSize : 0;\n      let breakLoop;\n      for (let i = activeIndex + 1; i < slides.length; i += 1) {\n        if (slides[i] && !breakLoop) {\n          slideSize += slides[i].swiperSlideSize;\n          spv += 1;\n          if (slideSize > swiperSize) breakLoop = true;\n        }\n      }\n      for (let i = activeIndex - 1; i >= 0; i -= 1) {\n        if (slides[i] && !breakLoop) {\n          slideSize += slides[i].swiperSlideSize;\n          spv += 1;\n          if (slideSize > swiperSize) breakLoop = true;\n        }\n      }\n    } else {\n      // eslint-disable-next-line\n      if (view === 'current') {\n        for (let i = activeIndex + 1; i < slides.length; i += 1) {\n          const slideInView = exact ? slidesGrid[i] + slidesSizesGrid[i] - slidesGrid[activeIndex] < swiperSize : slidesGrid[i] - slidesGrid[activeIndex] < swiperSize;\n          if (slideInView) {\n            spv += 1;\n          }\n        }\n      } else {\n        // previous\n        for (let i = activeIndex - 1; i >= 0; i -= 1) {\n          const slideInView = slidesGrid[activeIndex] - slidesGrid[i] < swiperSize;\n          if (slideInView) {\n            spv += 1;\n          }\n        }\n      }\n    }\n    return spv;\n  }\n  update() {\n    const swiper = this;\n    if (!swiper || swiper.destroyed) return;\n    const {\n      snapGrid,\n      params\n    } = swiper;\n    // Breakpoints\n    if (params.breakpoints) {\n      swiper.setBreakpoint();\n    }\n    [...swiper.el.querySelectorAll('[loading=\"lazy\"]')].forEach(imageEl => {\n      if (imageEl.complete) {\n        processLazyPreloader(swiper, imageEl);\n      }\n    });\n    swiper.updateSize();\n    swiper.updateSlides();\n    swiper.updateProgress();\n    swiper.updateSlidesClasses();\n    function setTranslate() {\n      const translateValue = swiper.rtlTranslate ? swiper.translate * -1 : swiper.translate;\n      const newTranslate = Math.min(Math.max(translateValue, swiper.maxTranslate()), swiper.minTranslate());\n      swiper.setTranslate(newTranslate);\n      swiper.updateActiveIndex();\n      swiper.updateSlidesClasses();\n    }\n    let translated;\n    if (params.freeMode && params.freeMode.enabled && !params.cssMode) {\n      setTranslate();\n      if (params.autoHeight) {\n        swiper.updateAutoHeight();\n      }\n    } else {\n      if ((params.slidesPerView === 'auto' || params.slidesPerView > 1) && swiper.isEnd && !params.centeredSlides) {\n        const slides = swiper.virtual && params.virtual.enabled ? swiper.virtual.slides : swiper.slides;\n        translated = swiper.slideTo(slides.length - 1, 0, false, true);\n      } else {\n        translated = swiper.slideTo(swiper.activeIndex, 0, false, true);\n      }\n      if (!translated) {\n        setTranslate();\n      }\n    }\n    if (params.watchOverflow && snapGrid !== swiper.snapGrid) {\n      swiper.checkOverflow();\n    }\n    swiper.emit('update');\n  }\n  changeDirection(newDirection, needUpdate) {\n    if (needUpdate === void 0) {\n      needUpdate = true;\n    }\n    const swiper = this;\n    const currentDirection = swiper.params.direction;\n    if (!newDirection) {\n      // eslint-disable-next-line\n      newDirection = currentDirection === 'horizontal' ? 'vertical' : 'horizontal';\n    }\n    if (newDirection === currentDirection || newDirection !== 'horizontal' && newDirection !== 'vertical') {\n      return swiper;\n    }\n    swiper.el.classList.remove(`${swiper.params.containerModifierClass}${currentDirection}`);\n    swiper.el.classList.add(`${swiper.params.containerModifierClass}${newDirection}`);\n    swiper.emitContainerClasses();\n    swiper.params.direction = newDirection;\n    swiper.slides.forEach(slideEl => {\n      if (newDirection === 'vertical') {\n        slideEl.style.width = '';\n      } else {\n        slideEl.style.height = '';\n      }\n    });\n    swiper.emit('changeDirection');\n    if (needUpdate) swiper.update();\n    return swiper;\n  }\n  changeLanguageDirection(direction) {\n    const swiper = this;\n    if (swiper.rtl && direction === 'rtl' || !swiper.rtl && direction === 'ltr') return;\n    swiper.rtl = direction === 'rtl';\n    swiper.rtlTranslate = swiper.params.direction === 'horizontal' && swiper.rtl;\n    if (swiper.rtl) {\n      swiper.el.classList.add(`${swiper.params.containerModifierClass}rtl`);\n      swiper.el.dir = 'rtl';\n    } else {\n      swiper.el.classList.remove(`${swiper.params.containerModifierClass}rtl`);\n      swiper.el.dir = 'ltr';\n    }\n    swiper.update();\n  }\n  mount(element) {\n    const swiper = this;\n    if (swiper.mounted) return true;\n\n    // Find el\n    let el = element || swiper.params.el;\n    if (typeof el === 'string') {\n      el = document.querySelector(el);\n    }\n    if (!el) {\n      return false;\n    }\n    el.swiper = swiper;\n    if (el.parentNode && el.parentNode.host && el.parentNode.host.nodeName === 'SWIPER-CONTAINER') {\n      swiper.isElement = true;\n    }\n    const getWrapperSelector = () => {\n      return `.${(swiper.params.wrapperClass || '').trim().split(' ').join('.')}`;\n    };\n    const getWrapper = () => {\n      if (el && el.shadowRoot && el.shadowRoot.querySelector) {\n        const res = el.shadowRoot.querySelector(getWrapperSelector());\n        // Children needs to return slot items\n        return res;\n      }\n      return (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(el, getWrapperSelector())[0];\n    };\n    // Find Wrapper\n    let wrapperEl = getWrapper();\n    if (!wrapperEl && swiper.params.createElements) {\n      wrapperEl = (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.c)('div', swiper.params.wrapperClass);\n      el.append(wrapperEl);\n      (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.e)(el, `.${swiper.params.slideClass}`).forEach(slideEl => {\n        wrapperEl.append(slideEl);\n      });\n    }\n    Object.assign(swiper, {\n      el,\n      wrapperEl,\n      slidesEl: swiper.isElement && !el.parentNode.host.slideSlots ? el.parentNode.host : wrapperEl,\n      hostEl: swiper.isElement ? el.parentNode.host : el,\n      mounted: true,\n      // RTL\n      rtl: el.dir.toLowerCase() === 'rtl' || (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.l)(el, 'direction') === 'rtl',\n      rtlTranslate: swiper.params.direction === 'horizontal' && (el.dir.toLowerCase() === 'rtl' || (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.l)(el, 'direction') === 'rtl'),\n      wrongRTL: (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.l)(wrapperEl, 'display') === '-webkit-box'\n    });\n    return true;\n  }\n  init(el) {\n    const swiper = this;\n    if (swiper.initialized) return swiper;\n    const mounted = swiper.mount(el);\n    if (mounted === false) return swiper;\n    swiper.emit('beforeInit');\n\n    // Set breakpoint\n    if (swiper.params.breakpoints) {\n      swiper.setBreakpoint();\n    }\n\n    // Add Classes\n    swiper.addClasses();\n\n    // Update size\n    swiper.updateSize();\n\n    // Update slides\n    swiper.updateSlides();\n    if (swiper.params.watchOverflow) {\n      swiper.checkOverflow();\n    }\n\n    // Set Grab Cursor\n    if (swiper.params.grabCursor && swiper.enabled) {\n      swiper.setGrabCursor();\n    }\n\n    // Slide To Initial Slide\n    if (swiper.params.loop && swiper.virtual && swiper.params.virtual.enabled) {\n      swiper.slideTo(swiper.params.initialSlide + swiper.virtual.slidesBefore, 0, swiper.params.runCallbacksOnInit, false, true);\n    } else {\n      swiper.slideTo(swiper.params.initialSlide, 0, swiper.params.runCallbacksOnInit, false, true);\n    }\n\n    // Create loop\n    if (swiper.params.loop) {\n      swiper.loopCreate();\n    }\n\n    // Attach events\n    swiper.attachEvents();\n    [...swiper.el.querySelectorAll('[loading=\"lazy\"]')].forEach(imageEl => {\n      if (imageEl.complete) {\n        processLazyPreloader(swiper, imageEl);\n      } else {\n        imageEl.addEventListener('load', e => {\n          processLazyPreloader(swiper, e.target);\n        });\n      }\n    });\n    preload(swiper);\n\n    // Init Flag\n    swiper.initialized = true;\n    preload(swiper);\n\n    // Emit\n    swiper.emit('init');\n    swiper.emit('afterInit');\n    return swiper;\n  }\n  destroy(deleteInstance, cleanStyles) {\n    if (deleteInstance === void 0) {\n      deleteInstance = true;\n    }\n    if (cleanStyles === void 0) {\n      cleanStyles = true;\n    }\n    const swiper = this;\n    const {\n      params,\n      el,\n      wrapperEl,\n      slides\n    } = swiper;\n    if (typeof swiper.params === 'undefined' || swiper.destroyed) {\n      return null;\n    }\n    swiper.emit('beforeDestroy');\n\n    // Init Flag\n    swiper.initialized = false;\n\n    // Detach events\n    swiper.detachEvents();\n\n    // Destroy loop\n    if (params.loop) {\n      swiper.loopDestroy();\n    }\n\n    // Cleanup styles\n    if (cleanStyles) {\n      swiper.removeClasses();\n      el.removeAttribute('style');\n      wrapperEl.removeAttribute('style');\n      if (slides && slides.length) {\n        slides.forEach(slideEl => {\n          slideEl.classList.remove(params.slideVisibleClass, params.slideActiveClass, params.slideNextClass, params.slidePrevClass);\n          slideEl.removeAttribute('style');\n          slideEl.removeAttribute('data-swiper-slide-index');\n        });\n      }\n    }\n    swiper.emit('destroy');\n\n    // Detach emitter events\n    Object.keys(swiper.eventsListeners).forEach(eventName => {\n      swiper.off(eventName);\n    });\n    if (deleteInstance !== false) {\n      swiper.el.swiper = null;\n      (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.r)(swiper);\n    }\n    swiper.destroyed = true;\n    return null;\n  }\n  static extendDefaults(newDefaults) {\n    (0,_utils_mjs__WEBPACK_IMPORTED_MODULE_1__.q)(extendedDefaults, newDefaults);\n  }\n  static get extendedDefaults() {\n    return extendedDefaults;\n  }\n  static get defaults() {\n    return defaults;\n  }\n  static installModule(mod) {\n    if (!Swiper.prototype.__modules__) Swiper.prototype.__modules__ = [];\n    const modules = Swiper.prototype.__modules__;\n    if (typeof mod === 'function' && modules.indexOf(mod) < 0) {\n      modules.push(mod);\n    }\n  }\n  static use(module) {\n    if (Array.isArray(module)) {\n      module.forEach(m => Swiper.installModule(m));\n      return Swiper;\n    }\n    Swiper.installModule(module);\n    return Swiper;\n  }\n}\nObject.keys(prototypes).forEach(prototypeGroup => {\n  Object.keys(prototypes[prototypeGroup]).forEach(protoMethod => {\n    Swiper.prototype[protoMethod] = prototypes[prototypeGroup][protoMethod];\n  });\n});\nSwiper.use([Resize, Observer]);\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/shared/swiper-core.mjs?");

/***/ }),

/***/ "./node_modules/swiper/shared/utils.mjs":
/*!**********************************************!*\
  !*** ./node_modules/swiper/shared/utils.mjs ***!
  \**********************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   a: () => (/* binding */ elementParents),\n/* harmony export */   b: () => (/* binding */ elementOffset),\n/* harmony export */   c: () => (/* binding */ createElement),\n/* harmony export */   d: () => (/* binding */ now),\n/* harmony export */   e: () => (/* binding */ elementChildren),\n/* harmony export */   f: () => (/* binding */ elementOuterSize),\n/* harmony export */   g: () => (/* binding */ elementIndex),\n/* harmony export */   h: () => (/* binding */ getTranslate),\n/* harmony export */   i: () => (/* binding */ elementTransitionEnd),\n/* harmony export */   j: () => (/* binding */ isObject),\n/* harmony export */   k: () => (/* binding */ getSlideTransformEl),\n/* harmony export */   l: () => (/* binding */ elementStyle),\n/* harmony export */   m: () => (/* binding */ elementNextAll),\n/* harmony export */   n: () => (/* binding */ nextTick),\n/* harmony export */   o: () => (/* binding */ elementPrevAll),\n/* harmony export */   p: () => (/* binding */ animateCSSModeScroll),\n/* harmony export */   q: () => (/* binding */ extend),\n/* harmony export */   r: () => (/* binding */ deleteProps),\n/* harmony export */   s: () => (/* binding */ setCSSProperty)\n/* harmony export */ });\n/* harmony import */ var _ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./ssr-window.esm.mjs */ \"./node_modules/swiper/shared/ssr-window.esm.mjs\");\n\n\nfunction deleteProps(obj) {\n  const object = obj;\n  Object.keys(object).forEach(key => {\n    try {\n      object[key] = null;\n    } catch (e) {\n      // no getter for object\n    }\n    try {\n      delete object[key];\n    } catch (e) {\n      // something got wrong\n    }\n  });\n}\nfunction nextTick(callback, delay) {\n  if (delay === void 0) {\n    delay = 0;\n  }\n  return setTimeout(callback, delay);\n}\nfunction now() {\n  return Date.now();\n}\nfunction getComputedStyle(el) {\n  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();\n  let style;\n  if (window.getComputedStyle) {\n    style = window.getComputedStyle(el, null);\n  }\n  if (!style && el.currentStyle) {\n    style = el.currentStyle;\n  }\n  if (!style) {\n    style = el.style;\n  }\n  return style;\n}\nfunction getTranslate(el, axis) {\n  if (axis === void 0) {\n    axis = 'x';\n  }\n  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();\n  let matrix;\n  let curTransform;\n  let transformMatrix;\n  const curStyle = getComputedStyle(el);\n  if (window.WebKitCSSMatrix) {\n    curTransform = curStyle.transform || curStyle.webkitTransform;\n    if (curTransform.split(',').length > 6) {\n      curTransform = curTransform.split(', ').map(a => a.replace(',', '.')).join(', ');\n    }\n    // Some old versions of Webkit choke when 'none' is passed; pass\n    // empty string instead in this case\n    transformMatrix = new window.WebKitCSSMatrix(curTransform === 'none' ? '' : curTransform);\n  } else {\n    transformMatrix = curStyle.MozTransform || curStyle.OTransform || curStyle.MsTransform || curStyle.msTransform || curStyle.transform || curStyle.getPropertyValue('transform').replace('translate(', 'matrix(1, 0, 0, 1,');\n    matrix = transformMatrix.toString().split(',');\n  }\n  if (axis === 'x') {\n    // Latest Chrome and webkits Fix\n    if (window.WebKitCSSMatrix) curTransform = transformMatrix.m41;\n    // Crazy IE10 Matrix\n    else if (matrix.length === 16) curTransform = parseFloat(matrix[12]);\n    // Normal Browsers\n    else curTransform = parseFloat(matrix[4]);\n  }\n  if (axis === 'y') {\n    // Latest Chrome and webkits Fix\n    if (window.WebKitCSSMatrix) curTransform = transformMatrix.m42;\n    // Crazy IE10 Matrix\n    else if (matrix.length === 16) curTransform = parseFloat(matrix[13]);\n    // Normal Browsers\n    else curTransform = parseFloat(matrix[5]);\n  }\n  return curTransform || 0;\n}\nfunction isObject(o) {\n  return typeof o === 'object' && o !== null && o.constructor && Object.prototype.toString.call(o).slice(8, -1) === 'Object';\n}\nfunction isNode(node) {\n  // eslint-disable-next-line\n  if (typeof window !== 'undefined' && typeof window.HTMLElement !== 'undefined') {\n    return node instanceof HTMLElement;\n  }\n  return node && (node.nodeType === 1 || node.nodeType === 11);\n}\nfunction extend() {\n  const to = Object(arguments.length <= 0 ? undefined : arguments[0]);\n  const noExtend = ['__proto__', 'constructor', 'prototype'];\n  for (let i = 1; i < arguments.length; i += 1) {\n    const nextSource = i < 0 || arguments.length <= i ? undefined : arguments[i];\n    if (nextSource !== undefined && nextSource !== null && !isNode(nextSource)) {\n      const keysArray = Object.keys(Object(nextSource)).filter(key => noExtend.indexOf(key) < 0);\n      for (let nextIndex = 0, len = keysArray.length; nextIndex < len; nextIndex += 1) {\n        const nextKey = keysArray[nextIndex];\n        const desc = Object.getOwnPropertyDescriptor(nextSource, nextKey);\n        if (desc !== undefined && desc.enumerable) {\n          if (isObject(to[nextKey]) && isObject(nextSource[nextKey])) {\n            if (nextSource[nextKey].__swiper__) {\n              to[nextKey] = nextSource[nextKey];\n            } else {\n              extend(to[nextKey], nextSource[nextKey]);\n            }\n          } else if (!isObject(to[nextKey]) && isObject(nextSource[nextKey])) {\n            to[nextKey] = {};\n            if (nextSource[nextKey].__swiper__) {\n              to[nextKey] = nextSource[nextKey];\n            } else {\n              extend(to[nextKey], nextSource[nextKey]);\n            }\n          } else {\n            to[nextKey] = nextSource[nextKey];\n          }\n        }\n      }\n    }\n  }\n  return to;\n}\nfunction setCSSProperty(el, varName, varValue) {\n  el.style.setProperty(varName, varValue);\n}\nfunction animateCSSModeScroll(_ref) {\n  let {\n    swiper,\n    targetPosition,\n    side\n  } = _ref;\n  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();\n  const startPosition = -swiper.translate;\n  let startTime = null;\n  let time;\n  const duration = swiper.params.speed;\n  swiper.wrapperEl.style.scrollSnapType = 'none';\n  window.cancelAnimationFrame(swiper.cssModeFrameID);\n  const dir = targetPosition > startPosition ? 'next' : 'prev';\n  const isOutOfBound = (current, target) => {\n    return dir === 'next' && current >= target || dir === 'prev' && current <= target;\n  };\n  const animate = () => {\n    time = new Date().getTime();\n    if (startTime === null) {\n      startTime = time;\n    }\n    const progress = Math.max(Math.min((time - startTime) / duration, 1), 0);\n    const easeProgress = 0.5 - Math.cos(progress * Math.PI) / 2;\n    let currentPosition = startPosition + easeProgress * (targetPosition - startPosition);\n    if (isOutOfBound(currentPosition, targetPosition)) {\n      currentPosition = targetPosition;\n    }\n    swiper.wrapperEl.scrollTo({\n      [side]: currentPosition\n    });\n    if (isOutOfBound(currentPosition, targetPosition)) {\n      swiper.wrapperEl.style.overflow = 'hidden';\n      swiper.wrapperEl.style.scrollSnapType = '';\n      setTimeout(() => {\n        swiper.wrapperEl.style.overflow = '';\n        swiper.wrapperEl.scrollTo({\n          [side]: currentPosition\n        });\n      });\n      window.cancelAnimationFrame(swiper.cssModeFrameID);\n      return;\n    }\n    swiper.cssModeFrameID = window.requestAnimationFrame(animate);\n  };\n  animate();\n}\nfunction getSlideTransformEl(slideEl) {\n  return slideEl.querySelector('.swiper-slide-transform') || slideEl.shadowRoot && slideEl.shadowRoot.querySelector('.swiper-slide-transform') || slideEl;\n}\nfunction elementChildren(element, selector) {\n  if (selector === void 0) {\n    selector = '';\n  }\n  return [...element.children].filter(el => el.matches(selector));\n}\nfunction createElement(tag, classes) {\n  if (classes === void 0) {\n    classes = [];\n  }\n  const el = document.createElement(tag);\n  el.classList.add(...(Array.isArray(classes) ? classes : [classes]));\n  return el;\n}\nfunction elementOffset(el) {\n  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();\n  const document = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.g)();\n  const box = el.getBoundingClientRect();\n  const body = document.body;\n  const clientTop = el.clientTop || body.clientTop || 0;\n  const clientLeft = el.clientLeft || body.clientLeft || 0;\n  const scrollTop = el === window ? window.scrollY : el.scrollTop;\n  const scrollLeft = el === window ? window.scrollX : el.scrollLeft;\n  return {\n    top: box.top + scrollTop - clientTop,\n    left: box.left + scrollLeft - clientLeft\n  };\n}\nfunction elementPrevAll(el, selector) {\n  const prevEls = [];\n  while (el.previousElementSibling) {\n    const prev = el.previousElementSibling; // eslint-disable-line\n    if (selector) {\n      if (prev.matches(selector)) prevEls.push(prev);\n    } else prevEls.push(prev);\n    el = prev;\n  }\n  return prevEls;\n}\nfunction elementNextAll(el, selector) {\n  const nextEls = [];\n  while (el.nextElementSibling) {\n    const next = el.nextElementSibling; // eslint-disable-line\n    if (selector) {\n      if (next.matches(selector)) nextEls.push(next);\n    } else nextEls.push(next);\n    el = next;\n  }\n  return nextEls;\n}\nfunction elementStyle(el, prop) {\n  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();\n  return window.getComputedStyle(el, null).getPropertyValue(prop);\n}\nfunction elementIndex(el) {\n  let child = el;\n  let i;\n  if (child) {\n    i = 0;\n    // eslint-disable-next-line\n    while ((child = child.previousSibling) !== null) {\n      if (child.nodeType === 1) i += 1;\n    }\n    return i;\n  }\n  return undefined;\n}\nfunction elementParents(el, selector) {\n  const parents = []; // eslint-disable-line\n  let parent = el.parentElement; // eslint-disable-line\n  while (parent) {\n    if (selector) {\n      if (parent.matches(selector)) parents.push(parent);\n    } else {\n      parents.push(parent);\n    }\n    parent = parent.parentElement;\n  }\n  return parents;\n}\nfunction elementTransitionEnd(el, callback) {\n  function fireCallBack(e) {\n    if (e.target !== el) return;\n    callback.call(el, e);\n    el.removeEventListener('transitionend', fireCallBack);\n  }\n  if (callback) {\n    el.addEventListener('transitionend', fireCallBack);\n  }\n}\nfunction elementOuterSize(el, size, includeMargins) {\n  const window = (0,_ssr_window_esm_mjs__WEBPACK_IMPORTED_MODULE_0__.a)();\n  if (includeMargins) {\n    return el[size === 'width' ? 'offsetWidth' : 'offsetHeight'] + parseFloat(window.getComputedStyle(el, null).getPropertyValue(size === 'width' ? 'margin-right' : 'margin-top')) + parseFloat(window.getComputedStyle(el, null).getPropertyValue(size === 'width' ? 'margin-left' : 'margin-bottom'));\n  }\n  return el.offsetWidth;\n}\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/shared/utils.mjs?");

/***/ }),

/***/ "./node_modules/swiper/swiper.mjs":
/*!****************************************!*\
  !*** ./node_modules/swiper/swiper.mjs ***!
  \****************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   Swiper: () => (/* reexport safe */ _shared_swiper_core_mjs__WEBPACK_IMPORTED_MODULE_0__.S),\n/* harmony export */   \"default\": () => (/* reexport safe */ _shared_swiper_core_mjs__WEBPACK_IMPORTED_MODULE_0__.S)\n/* harmony export */ });\n/* harmony import */ var _shared_swiper_core_mjs__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./shared/swiper-core.mjs */ \"./node_modules/swiper/shared/swiper-core.mjs\");\n/**\n * Swiper 10.1.0\n * Most modern mobile touch slider and framework with hardware accelerated transitions\n * https://swiperjs.com\n *\n * Copyright 2014-2023 Vladimir Kharlampidi\n *\n * Released under the MIT License\n *\n * Released on: August 1, 2023\n */\n\n\n\n\n//# sourceURL=webpack://persona-patman/./node_modules/swiper/swiper.mjs?");

/***/ })

}]);